instructions:
  - group: Jump
    mnemonic: DJNZ d
    opcodes:
      - opcode: 0x10
      - opcode: 0xDD 0x10
      - opcode: 0xFD 0x10
    steps:
      - 
      - Address = PC; PC = PC + 1; request(action.memory_read);
      - 
      - B = B - 1; if B == 0; instruction_complete(); else; PC = PC + signed(Data); WZ = PC; endif;
      - 
      - 
      - 
      - 
      - 
      - 
