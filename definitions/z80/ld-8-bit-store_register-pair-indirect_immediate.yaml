instructions:
  - group: LD
    mnemonic: LD (HL), n
    opcodes:
      - opcode: 0x36
    steps:
      - [Address = PC, PC = PC + 1, MemoryRead]
      - []  # Data will be set with n.
      - []
      - [Address = HL, MemoryWrite]
      - []
      - []
    next_opcode: overlapped