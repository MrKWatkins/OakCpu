instructions:
  - group: LD
    mnemonic: LD (HL), n
    opcodes:
      - opcode: 0x36
    steps:
      - Address = PC; PC = PC + 1; memory_read();
      - # Data will be set with n.
      - 
      - Address = HL; memory_write();
      - 
      - 
    next_opcode: overlapped