instructions:
  - group: LD
    mnemonic: LD (nn), HL
    opcodes:
      - opcode: 0x22
    steps:
      - Address = PC; PC = PC + 1; memory_read();
      - Z = Data;
      - 
      - Address = PC; PC = PC + 1; memory_read();
      - W = Data;
      - 
      - Address = WZ; Data = L; WZ = WZ + 1; memory_write();
      - 
      - 
      - Address = WZ; Data = H; memory_write();
      - 
      - 
