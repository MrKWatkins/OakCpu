instructions:
  - group: AND
    mnemonic: AND R0
    opcodes:
      - opcode: 0xA0
        R0: B
      - opcode: 0xA1
        R0: C
      - opcode: 0xA2
        R0: D
      - opcode: 0xA3
        R0: E
      - opcode: 0xA4
        R0: H
      - opcode: 0xA5
        R0: L
      - opcode: 0xA7
        R0: A
    steps:
      - A = A & R0; flags();
    flags:
      PV: even_parity(A)
      X: copy_from(A)
      H: 1
      Y: copy_from(A)
      Z: is_zero(A)
      S: copy_from(A)
    next_opcode: overlapped

  - group: AND
    mnemonic: AND (HL)
    opcodes:
      - opcode: 0xA6
    steps:
      - Address = HL; memory_read();
      - 
      - 
      - A = A & Data; flags();
    flags:
      PV: even_parity(A)
      X: copy_from(A)
      H: 1
      Y: copy_from(A)
      Z: is_zero(A)
      S: copy_from(A)
    next_opcode: overlapped