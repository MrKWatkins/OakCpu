instructions:
  - group: XOR
    mnemonic: XOR R0
    opcodes:
      - opcode: 0xA8
        R0: B
      - opcode: 0xA9
        R0: C
      - opcode: 0xAA
        R0: D
      - opcode: 0xAB
        R0: E
      - opcode: 0xAC
        R0: H
      - opcode: 0xAD
        R0: L
      - opcode: 0xAF
        R0: A
    steps:
      - A = A ^ R0; flags();
    flags:
      PV: even_parity(A)
      X: copy_from(A)
      H: 0
      Y: copy_from(A)
      Z: is_zero(A)
      S: copy_from(A)
    next_opcode: overlapped

  - group: XOR
    mnemonic: XOR (HL)
    opcodes:
      - opcode: 0xAE
    steps:
      - Address = HL; memory_read();
      - 
      - 
      - A = A ^ Data; flags();
    flags:
      PV: even_parity(A)
      X: copy_from(A)
      H: 0
      Y: copy_from(A)
      Z: is_zero(A)
      S: copy_from(A)
    next_opcode: overlapped