instructions:
  - group: 8-Bit Load
    mnemonic: LD (HL), n
    opcodes:
      - opcode: 0x36
    steps:
      - Address = PC; PC = PC + 1; request(action.memory_read);
      - # Data will be set with n.
      - 
      - Address = HL; request(action.memory_write);
      - 
      - 
  
      - 
  - group: 8-Bit Load
    mnemonic: LD (RP0 + d), n
    opcodes:
      - opcode: 0xDD 0x36
        RP0: IX
      - opcode: 0xFD 0x36
        RP0: IY
    steps:
      - Address = PC; PC = PC + 1; request(action.memory_read);
      - Latch = Data;
      - 
      - Address = PC; PC = PC + 1; request(action.memory_read);
      - 
      - 
      - 
      - 
      - WZ = RP0 + signed(Latch); Address = WZ; request(action.memory_write);
      - 
      - 
      - 
