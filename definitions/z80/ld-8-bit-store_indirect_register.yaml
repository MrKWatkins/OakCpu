instructions:
  - group: LD
    mnemonic: LD (nn), A
    opcodes:
      - opcode: 0x32
    steps:
      - Address = PC; PC = PC + 1; memory_read();
      - Z = Data;
      - 
      - Address = PC; PC = PC + 1; memory_read();
      - W = Data;
      - 
      - Address = WZ; Data = A; Z = Z + 1; W = A; memory_write();
      - 
      - 
