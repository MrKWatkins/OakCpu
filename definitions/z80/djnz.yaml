instructions:
  - group: DJNZ
    mnemonic: DJNZ d
    opcodes:
      - opcode: 0x10
    steps:
      - 
      - Address = PC; PC = PC + 1; memory_read();
      - B = B - 1; finish_instruction(B == 0);
      - PC = PC + signed(Data); WZ = PC;
      - 
      - 
      - 
      - 
      - 
