instructions:
  - group: LD
    mnemonic: LD HL, (nn)
    opcodes:
      - opcode: 0x2A
    steps:
      - Address = PC; PC = PC + 1; memory_read();
      - Z = Data;
      - 
      - Address = PC; PC = PC + 1; memory_read();
      - W = Data;
      - 
      - Address = WZ; WZ = WZ + 1; memory_read();
      - L = Data;
      - 
      - Address = WZ; memory_read();
      - H = Data;
      - 
