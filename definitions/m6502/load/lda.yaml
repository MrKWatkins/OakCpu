instructions:
  - group: Load
    mnemonic: "LDA #n"
    opcodes:
      - opcode: 0xA9
    steps:
      - Address = PC; PC = PC + 1; request(action.memory_read);
      - A = Data; flags();
    flags:
      Z: is_zero(A)
      N: is_negative(A)
