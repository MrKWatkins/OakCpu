instructions:
  - group: Store
    mnemonic: STA zp
    next_opcode: overlapped
    opcodes:
      - opcode: 0x85
    steps:
      - Address = PC; PC = PC + 1; request(action.memory_read);
      - Address = Data; Data = A; request(action.memory_write);
      -

  - group: Store
    mnemonic: STA zp,X
    next_opcode: overlapped
    opcodes:
      - opcode: 0x95
    steps:
      - Address = PC; PC = PC + 1; request(action.memory_read);
      - AD = Data; Address = AD; request(action.memory_read);
      - Address = (AD + X) & 0x00FF; Data = A; request(action.memory_write);
      -

  - group: Store
    mnemonic: STA abs
    next_opcode: overlapped
    opcodes:
      - opcode: 0x8D
    steps:
      - Address = PC; PC = PC + 1; request(action.memory_read);
      - Address = PC; PC = PC + 1; AD = Data; request(action.memory_read);
      - Address = (Data << 8) | AD; Data = A; request(action.memory_write);
      -

  - group: Store
    mnemonic: STA abs,X
    next_opcode: overlapped
    opcodes:
      - opcode: 0x9D
    steps:
      - Address = PC; PC = PC + 1; request(action.memory_read);
      - Address = PC; PC = PC + 1; AD = Data; request(action.memory_read);
      - AD = (Data << 8) | AD; Address = (AD & 0xFF00) | ((AD + X) & 0x00FF); request(action.memory_read);
      - Address = AD + X; Data = A; request(action.memory_write);
      -

  - group: Store
    mnemonic: STA abs,Y
    next_opcode: overlapped
    opcodes:
      - opcode: 0x99
    steps:
      - Address = PC; PC = PC + 1; request(action.memory_read);
      - Address = PC; PC = PC + 1; AD = Data; request(action.memory_read);
      - AD = (Data << 8) | AD; Address = (AD & 0xFF00) | ((AD + Y) & 0x00FF); request(action.memory_read);
      - Address = AD + Y; Data = A; request(action.memory_write);
      -

  - group: Store
    mnemonic: "STA (zp,X)"
    next_opcode: overlapped
    opcodes:
      - opcode: 0x81
    steps:
      - Address = PC; PC = PC + 1; request(action.memory_read);
      - AD = Data; Address = AD; request(action.memory_read);
      - AD = (AD + X) & 0x00FF; Address = AD; request(action.memory_read);
      - Address = (AD + 1) & 0x00FF; AD = Data; request(action.memory_read);
      - Address = (Data << 8) | AD; Data = A; request(action.memory_write);
      -

  - group: Store
    mnemonic: "STA (zp),Y"
    next_opcode: overlapped
    opcodes:
      - opcode: 0x91
    steps:
      - Address = PC; PC = PC + 1; request(action.memory_read);
      - AD = Data; Address = AD; request(action.memory_read);
      - Address = (AD + 1) & 0x00FF; AD = Data; request(action.memory_read);
      - AD = AD | (Data << 8); Address = (AD & 0xFF00) | ((AD + Y) & 0x00FF); request(action.memory_read);
      - Address = AD + Y; Data = A; request(action.memory_write);
      -
