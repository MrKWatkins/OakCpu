//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file may cause incorrect behaviour and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
using System;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;

namespace MrKWatkins.OakCpu.M6502;

public sealed unsafe partial class M6502InstructionEmulator
{
    private const ushort OpcodeReadStep0 = 0;

    private static int Error(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        throw new NotSupportedException("Opcode not supported");
    }

    // Opcode read
    private static int Opcode_read(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.OpcodeRead, emulator.address, emulator.data);
        ushort nextInstruction1 = 65535;
        nextInstruction1 = Unsafe.Add(ref MemoryMarshal.GetArrayDataReference(emulator.opcodeStepTable), emulator.data);
        if (nextInstruction1 != 65535)
        {
            return 2 + emulator.ExecuteDecodedInstruction(nextInstruction1, onActionRequired);
        }

        return 2;
    }

    // branch not taken finish
    private static int branch_not_taken_finish(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        return emulator.CompleteInstruction(false, 1);
    }

    // branch taken finish
    private static int branch_taken_finish(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.PC = emulator.ad;
        return emulator.CompleteInstruction(false, 1);
    }

    // branch page cross
    private static int branch_page_cross(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        ushort nextInstruction4 = 65535;
        emulator.address = (ushort)(emulator.PC & 0xFF00 | emulator.ad & 0b11111111);
        nextInstruction4 = 2;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction4 != 65535)
        {
            return 1 + emulator.ExecuteDecodedInstruction(nextInstruction4, onActionRequired);
        }

        return 1;
    }

    // ora finish
    private static int ora_finish(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.A |= emulator.data;
        emulator.P &= 0x5D;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        return emulator.CompleteInstruction(true, 1);
    }

    // and finish
    private static int and_finish(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.A &= emulator.data;
        emulator.P &= 0x5D;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        return emulator.CompleteInstruction(true, 1);
    }

    // eor finish
    private static int eor_finish(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.A ^= emulator.data;
        emulator.P &= 0x5D;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        return emulator.CompleteInstruction(true, 1);
    }

    // ldx finish
    private static int ldx_finish(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.X = emulator.data;
        emulator.P &= 0x5D;
        if (emulator.X == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.X & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        return emulator.CompleteInstruction(true, 1);
    }

    // lda finish
    private static int lda_finish(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.A = emulator.data;
        emulator.P &= 0x5D;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        return emulator.CompleteInstruction(true, 1);
    }

    // ldy finish
    private static int ldy_finish(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.Y = emulator.data;
        emulator.P &= 0x5D;
        if (emulator.Y == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.Y & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        return emulator.CompleteInstruction(true, 1);
    }

    // cmp finish
    private static int cmp_finish(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.P &= 0x5C;
        if (emulator.A >= emulator.data)
        {
            emulator.P |= 0x01;
        }

        var result = emulator.A - emulator.data;
        if (result == 0)
        {
            emulator.P |= 0x02;
        }

        if (((result & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        return emulator.CompleteInstruction(true, 1);
    }

    // adc finish
    private static int adc_finish(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        var carry = emulator.P & 0b00000001;
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left + right + carry;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) + (right & 0b00001111) + carry;
            var high = (left & 0b11110000) + (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (low > 0x09)
            {
                high += 0x10;
                low += 0x06;
            }

            if (((high & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((((high ^ left) & (high ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if (high > 0x90)
            {
                high += 0x60;
            }

            if (high > 0xFF)
            {
                emulator.P |= 0x01;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((binary_result ^ left) & (binary_result ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) != 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        return emulator.CompleteInstruction(true, 1);
    }

    // sbc finish
    private static int sbc_finish(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        var borrow = 0x01 - (emulator.P & 0b00000001);
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left - right - borrow;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) - (right & 0b00001111) - borrow;
            var high = (left & 0b11110000) - (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((low & 0b00010000) != 0x00)
            {
                low -= 0x06;
                high -= 0x10;
            }

            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (((binary_result & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((high & 0x0100) != 0x00)
            {
                high -= 0x60;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        return emulator.CompleteInstruction(true, 1);
    }

    // lax finish
    private static int lax_finish(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.A = emulator.data;
        emulator.X = emulator.data;
        emulator.P &= 0x5D;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        return emulator.CompleteInstruction(true, 1);
    }

    // Halt cycle
    private static int Halt_cycle(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.OpcodeRead, emulator.address, emulator.data);
        ushort nextInstruction16 = 65535;
        nextInstruction16 = Unsafe.Add(ref MemoryMarshal.GetArrayDataReference(emulator.opcodeStepTable), emulator.data);
        if (nextInstruction16 != 65535)
        {
            return 2 + emulator.ExecuteDecodedInstruction(nextInstruction16, onActionRequired);
        }

        emulator.nextSequenceStep = 14;
        return 2;
    }

    // Interrupt Mode 0
    private static int Interrupt_Mode_0(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        return 1;
    }

    // BRK
    private static int BRK(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(0x0100 | emulator.S);
        emulator.data = (byte)(emulator.PC >> 0x08);
        emulator.S -= 0x01;
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.address = (ushort)(0x0100 | emulator.S);
        emulator.data = (byte)(emulator.PC & 0b11111111);
        emulator.S -= 0x01;
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.address = (ushort)(0x0100 | emulator.S);
        emulator.data = (byte)(emulator.P | 0b00110000);
        emulator.P |= 0x04;
        emulator.S -= 0x01;
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.address = 0xFFFE;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = 0xFFFF;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.PC = (ushort)(emulator.data << 0x08 | emulator.ad);
        return 6;
    }

    // ORA (zp,X)
    private static int ORA_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad + emulator.X & 0b11111111);
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A |= emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 5;
    }

    // SLO (zp,X)
    private static int SLO_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad + emulator.X & 0b11111111);
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var value = emulator.data;
        emulator.data = (byte)(value << 0x01 & 0b11111111);
        emulator.A |= emulator.data;
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 7;
    }

    // ORA zp
    private static int ORA_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A |= emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 2;
    }

    // ASL zp
    private static int ASL_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var value = emulator.data;
        emulator.data = (byte)(value << 0x01 & 0b11111111);
        emulator.P &= 0x7C;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.data & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 4;
    }

    // SLO zp
    private static int SLO_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var value = emulator.data;
        emulator.data = (byte)(value << 0x01 & 0b11111111);
        emulator.A |= emulator.data;
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 4;
    }

    // PHP
    private static int PHP(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(0x0100 | emulator.S);
        emulator.data = (byte)(emulator.P | 0b00110000);
        emulator.S -= 0x01;
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 2;
    }

    // ORA #n
    private static int ORA_n(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A |= emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 1;
    }

    // ASL A
    private static int ASL_A(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        var value = emulator.A;
        emulator.A = (byte)(value << 0x01 & 0b11111111);
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        return 0;
    }

    // ANC #n
    private static int ANC_n(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A &= emulator.data;
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x81;
        }

        return 1;
    }

    // ORA abs
    private static int ORA_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A |= emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 3;
    }

    // ASL abs
    private static int ASL_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var value = emulator.data;
        emulator.data = (byte)(value << 0x01 & 0b11111111);
        emulator.P &= 0x7C;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.data & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // SLO abs
    private static int SLO_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var value = emulator.data;
        emulator.data = (byte)(value << 0x01 & 0b11111111);
        emulator.A |= emulator.data;
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // BPL
    private static int BPL(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        ushort nextInstruction76 = 65535;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        if (((emulator.P & 0b10000000) == 0b10000000 /* flag.N */))
        {
            nextInstruction76 = 1;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction76 != 65535)
        {
            return 1 + emulator.ExecuteDecodedInstruction(nextInstruction76, onActionRequired);
        }

        ushort nextInstruction77 = 65535;
        emulator.ad = (ushort)(emulator.PC + (sbyte)emulator.data);
        emulator.address = emulator.PC;
        if ((emulator.ad & 0xFF00) != (emulator.PC & 0xFF00))
        {
            nextInstruction77 = 3;
        }
        else
        {
            nextInstruction77 = 2;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction77 != 65535)
        {
            return 2 + emulator.ExecuteDecodedInstruction(nextInstruction77, onActionRequired);
        }

        return 2;
    }

    // ORA (zp),Y
    private static int ORA_zp_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction81 = 65535;
        emulator.ad = (ushort)(emulator.ad | emulator.data << 0x08);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.Y > 0xFF))
        {
            nextInstruction81 = 4;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction81 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction81, onActionRequired);
        }

        ushort nextInstruction82 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        nextInstruction82 = 4;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction82 != 65535)
        {
            return 5 + emulator.ExecuteDecodedInstruction(nextInstruction82, onActionRequired);
        }

        return 5;
    }

    // SLO (zp),Y
    private static int SLO_zp_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad | emulator.data << 0x08);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var value = emulator.data;
        emulator.data = (byte)(value << 0x01 & 0b11111111);
        emulator.A |= emulator.data;
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 7;
    }

    // ORA zp,X
    private static int ORA_zp_X_15(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A |= emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 3;
    }

    // ASL zp,X
    private static int ASL_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var value = emulator.data;
        emulator.data = (byte)(value << 0x01 & 0b11111111);
        emulator.P &= 0x7C;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.data & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // SLO zp,X
    private static int SLO_zp_X_17(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var value = emulator.data;
        emulator.data = (byte)(value << 0x01 & 0b11111111);
        emulator.A |= emulator.data;
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // CLC
    private static int CLC(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.P &= 0xFE;
        return 0;
    }

    // ORA abs,Y
    private static int ORA_abs_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction110 = 65535;
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.Y > 0xFF))
        {
            nextInstruction110 = 4;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction110 != 65535)
        {
            return 3 + emulator.ExecuteDecodedInstruction(nextInstruction110, onActionRequired);
        }

        ushort nextInstruction111 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        nextInstruction111 = 4;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction111 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction111, onActionRequired);
        }

        return 4;
    }

    // SLO abs,Y
    private static int SLO_abs_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var value = emulator.data;
        emulator.data = (byte)(value << 0x01 & 0b11111111);
        emulator.A |= emulator.data;
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 6;
    }

    // ORA abs,X
    private static int ORA_abs_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction121 = 65535;
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.X & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.X > 0xFF))
        {
            nextInstruction121 = 4;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction121 != 65535)
        {
            return 3 + emulator.ExecuteDecodedInstruction(nextInstruction121, onActionRequired);
        }

        ushort nextInstruction122 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.X);
        nextInstruction122 = 4;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction122 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction122, onActionRequired);
        }

        return 4;
    }

    // ASL abs,X
    private static int ASL_abs_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var value = emulator.data;
        emulator.data = (byte)(value << 0x01 & 0b11111111);
        emulator.P &= 0x7C;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.data & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 6;
    }

    // SLO abs,X
    private static int SLO_abs_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var value = emulator.data;
        emulator.data = (byte)(value << 0x01 & 0b11111111);
        emulator.A |= emulator.data;
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 6;
    }

    // JSR abs
    private static int JSR_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = (ushort)(0x0100 | emulator.S);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(0x0100 | emulator.S);
        emulator.data = (byte)(emulator.PC >> 0x08);
        emulator.S -= 0x01;
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.address = (ushort)(0x0100 | emulator.S);
        emulator.data = (byte)(emulator.PC & 0b11111111);
        emulator.S -= 0x01;
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.PC = (ushort)(emulator.data << 0x08 | emulator.ad);
        return 5;
    }

    // AND (zp,X)
    private static int AND_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad + emulator.X & 0b11111111);
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A &= emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 5;
    }

    // RLA (zp,X)
    private static int RLA_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad + emulator.X & 0b11111111);
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var value = emulator.data;
        emulator.data = (byte)((value << 0x01 | carry) & 0b11111111);
        emulator.A &= emulator.data;
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 7;
    }

    // BIT zp
    private static int BIT_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var result = emulator.A & emulator.data;

        // Update flags.
        int flags = emulator.P & 0b00011101; // Copy flag.C, flag.I, flag.D and B from P.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 1; // Set Z if is_zero(result) is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.data & 0b01000000) != 0x00)) << 6; // Set V if (data & 0x40) != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.data & 0b10000000) != 0x00))) << 7; // Set N if is_negative(data) is true.
        emulator.P = (byte)flags;
        return 2;
    }

    // AND zp
    private static int AND_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A &= emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 2;
    }

    // ROL zp
    private static int ROL_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var value = emulator.data;
        emulator.data = (byte)((value << 0x01 | carry) & 0b11111111);
        emulator.P &= 0x7C;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.data & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 4;
    }

    // RLA zp
    private static int RLA_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var value = emulator.data;
        emulator.data = (byte)((value << 0x01 | carry) & 0b11111111);
        emulator.A &= emulator.data;
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 4;
    }

    // PLP
    private static int PLP(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(0x0100 | emulator.S);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.S += 0x01;
        emulator.address = (ushort)(0x0100 | emulator.S);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.P = (byte)(emulator.data & 0b11101111 | 0b00100000);
        return 3;
    }

    // AND #n
    private static int AND_n(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A &= emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 1;
    }

    // ROL A
    private static int ROL_A(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        var carry = emulator.P & 0b00000001;
        var value = emulator.A;
        emulator.A = (byte)((value << 0x01 | carry) & 0b11111111);
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        return 0;
    }

    // ANC #n
    private static int ANC_n_2B(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A &= emulator.data;
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x81;
        }

        return 1;
    }

    // BIT abs
    private static int BIT_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var result = emulator.A & emulator.data;

        // Update flags.
        int flags = emulator.P & 0b00011101; // Copy flag.C, flag.I, flag.D and B from P.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 1; // Set Z if is_zero(result) is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.data & 0b01000000) != 0x00)) << 6; // Set V if (data & 0x40) != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.data & 0b10000000) != 0x00))) << 7; // Set N if is_negative(data) is true.
        emulator.P = (byte)flags;
        return 3;
    }

    // AND abs
    private static int AND_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A &= emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 3;
    }

    // ROL abs
    private static int ROL_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var value = emulator.data;
        emulator.data = (byte)((value << 0x01 | carry) & 0b11111111);
        emulator.P &= 0x7C;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.data & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // RLA abs
    private static int RLA_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var value = emulator.data;
        emulator.data = (byte)((value << 0x01 | carry) & 0b11111111);
        emulator.A &= emulator.data;
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // BMI
    private static int BMI(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        ushort nextInstruction202 = 65535;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        if (!((emulator.P & 0b10000000) == 0b10000000 /* flag.N */))
        {
            nextInstruction202 = 1;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction202 != 65535)
        {
            return 1 + emulator.ExecuteDecodedInstruction(nextInstruction202, onActionRequired);
        }

        ushort nextInstruction203 = 65535;
        emulator.ad = (ushort)(emulator.PC + (sbyte)emulator.data);
        emulator.address = emulator.PC;
        if ((emulator.ad & 0xFF00) != (emulator.PC & 0xFF00))
        {
            nextInstruction203 = 3;
        }
        else
        {
            nextInstruction203 = 2;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction203 != 65535)
        {
            return 2 + emulator.ExecuteDecodedInstruction(nextInstruction203, onActionRequired);
        }

        return 2;
    }

    // AND (zp),Y
    private static int AND_zp_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction207 = 65535;
        emulator.ad = (ushort)(emulator.ad | emulator.data << 0x08);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.Y > 0xFF))
        {
            nextInstruction207 = 5;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction207 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction207, onActionRequired);
        }

        ushort nextInstruction208 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        nextInstruction208 = 5;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction208 != 65535)
        {
            return 5 + emulator.ExecuteDecodedInstruction(nextInstruction208, onActionRequired);
        }

        return 5;
    }

    // RLA (zp),Y
    private static int RLA_zp_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad | emulator.data << 0x08);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var value = emulator.data;
        emulator.data = (byte)((value << 0x01 | carry) & 0b11111111);
        emulator.A &= emulator.data;
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 7;
    }

    // AND zp,X
    private static int AND_zp_X_35(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A &= emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 3;
    }

    // ROL zp,X
    private static int ROL_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var value = emulator.data;
        emulator.data = (byte)((value << 0x01 | carry) & 0b11111111);
        emulator.P &= 0x7C;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.data & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // RLA zp,X
    private static int RLA_zp_X_37(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var value = emulator.data;
        emulator.data = (byte)((value << 0x01 | carry) & 0b11111111);
        emulator.A &= emulator.data;
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // SEC
    private static int SEC(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.P |= 0x01;
        return 0;
    }

    // AND abs,Y
    private static int AND_abs_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction236 = 65535;
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.Y > 0xFF))
        {
            nextInstruction236 = 5;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction236 != 65535)
        {
            return 3 + emulator.ExecuteDecodedInstruction(nextInstruction236, onActionRequired);
        }

        ushort nextInstruction237 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        nextInstruction237 = 5;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction237 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction237, onActionRequired);
        }

        return 4;
    }

    // RLA abs,Y
    private static int RLA_abs_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var value = emulator.data;
        emulator.data = (byte)((value << 0x01 | carry) & 0b11111111);
        emulator.A &= emulator.data;
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 6;
    }

    // AND abs,X
    private static int AND_abs_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction247 = 65535;
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.X & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.X > 0xFF))
        {
            nextInstruction247 = 5;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction247 != 65535)
        {
            return 3 + emulator.ExecuteDecodedInstruction(nextInstruction247, onActionRequired);
        }

        ushort nextInstruction248 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.X);
        nextInstruction248 = 5;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction248 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction248, onActionRequired);
        }

        return 4;
    }

    // ROL abs,X
    private static int ROL_abs_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var value = emulator.data;
        emulator.data = (byte)((value << 0x01 | carry) & 0b11111111);
        emulator.P &= 0x7C;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.data & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 6;
    }

    // RLA abs,X
    private static int RLA_abs_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var value = emulator.data;
        emulator.data = (byte)((value << 0x01 | carry) & 0b11111111);
        emulator.A &= emulator.data;
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((value & 0b10000000) != 0x00))
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 6;
    }

    // RTI
    private static int RTI(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(0x0100 | emulator.S);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.S += 0x01;
        emulator.address = (ushort)(0x0100 | emulator.S);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.S += 0x01;
        emulator.P = (byte)(emulator.data & 0b11101111 | 0b00100000);
        emulator.address = (ushort)(0x0100 | emulator.S);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.S += 0x01;
        emulator.ad = (ushort)emulator.data;
        emulator.address = (ushort)(0x0100 | emulator.S);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.PC = (ushort)(emulator.data << 0x08 | emulator.ad);
        return 5;
    }

    // EOR (zp,X)
    private static int EOR_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad + emulator.X & 0b11111111);
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A ^= emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 5;
    }

    // SRE (zp,X)
    private static int SRE_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad + emulator.X & 0b11111111);
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var value = emulator.data;
        emulator.data = (byte)(value >> 0x01);
        emulator.A ^= emulator.data;
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if ((value & 0b00000001) != 0x00)
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 7;
    }

    // EOR zp
    private static int EOR_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A ^= emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 2;
    }

    // LSR zp
    private static int LSR_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var value = emulator.data;
        emulator.data = (byte)(value >> 0x01);
        emulator.P &= 0x7C;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if ((value & 0b00000001) != 0x00)
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 4;
    }

    // SRE zp
    private static int SRE_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var value = emulator.data;
        emulator.data = (byte)(value >> 0x01);
        emulator.A ^= emulator.data;
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if ((value & 0b00000001) != 0x00)
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 4;
    }

    // PHA
    private static int PHA(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(0x0100 | emulator.S);
        emulator.data = emulator.A;
        emulator.S -= 0x01;
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 2;
    }

    // EOR #n
    private static int EOR_n(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A ^= emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 1;
    }

    // LSR A
    private static int LSR_A(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        var value = emulator.A;
        emulator.A = (byte)(value >> 0x01);
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if ((value & 0b00000001) != 0x00)
        {
            emulator.P |= 0x01;
        }

        return 0;
    }

    // ALR #n
    private static int ALR_n(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var value = emulator.A & emulator.data;
        emulator.A = (byte)(value >> 0x01);
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if ((value & 0b00000001) != 0x00)
        {
            emulator.P |= 0x01;
        }

        return 1;
    }

    // JMP abs
    private static int JMP_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.PC = (ushort)(emulator.data << 0x08 | emulator.ad);
        return 2;
    }

    // EOR abs
    private static int EOR_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A ^= emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 3;
    }

    // LSR abs
    private static int LSR_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var value = emulator.data;
        emulator.data = (byte)(value >> 0x01);
        emulator.P &= 0x7C;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if ((value & 0b00000001) != 0x00)
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // SRE abs
    private static int SRE_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var value = emulator.data;
        emulator.data = (byte)(value >> 0x01);
        emulator.A ^= emulator.data;
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if ((value & 0b00000001) != 0x00)
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // BVC
    private static int BVC(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        ushort nextInstruction323 = 65535;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        if (((emulator.P & 0b01000000) == 0b01000000 /* flag.V */))
        {
            nextInstruction323 = 1;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction323 != 65535)
        {
            return 1 + emulator.ExecuteDecodedInstruction(nextInstruction323, onActionRequired);
        }

        ushort nextInstruction324 = 65535;
        emulator.ad = (ushort)(emulator.PC + (sbyte)emulator.data);
        emulator.address = emulator.PC;
        if ((emulator.ad & 0xFF00) != (emulator.PC & 0xFF00))
        {
            nextInstruction324 = 3;
        }
        else
        {
            nextInstruction324 = 2;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction324 != 65535)
        {
            return 2 + emulator.ExecuteDecodedInstruction(nextInstruction324, onActionRequired);
        }

        return 2;
    }

    // EOR (zp),Y
    private static int EOR_zp_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction328 = 65535;
        emulator.ad = (ushort)(emulator.ad | emulator.data << 0x08);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.Y > 0xFF))
        {
            nextInstruction328 = 6;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction328 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction328, onActionRequired);
        }

        ushort nextInstruction329 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        nextInstruction329 = 6;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction329 != 65535)
        {
            return 5 + emulator.ExecuteDecodedInstruction(nextInstruction329, onActionRequired);
        }

        return 5;
    }

    // SRE (zp),Y
    private static int SRE_zp_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad | emulator.data << 0x08);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var value = emulator.data;
        emulator.data = (byte)(value >> 0x01);
        emulator.A ^= emulator.data;
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if ((value & 0b00000001) != 0x00)
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 7;
    }

    // EOR zp,X
    private static int EOR_zp_X_55(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A ^= emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 3;
    }

    // LSR zp,X
    private static int LSR_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var value = emulator.data;
        emulator.data = (byte)(value >> 0x01);
        emulator.P &= 0x7C;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if ((value & 0b00000001) != 0x00)
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // SRE zp,X
    private static int SRE_zp_X_57(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var value = emulator.data;
        emulator.data = (byte)(value >> 0x01);
        emulator.A ^= emulator.data;
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if ((value & 0b00000001) != 0x00)
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // CLI
    private static int CLI(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.P &= 0xFB;
        return 0;
    }

    // EOR abs,Y
    private static int EOR_abs_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction357 = 65535;
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.Y > 0xFF))
        {
            nextInstruction357 = 6;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction357 != 65535)
        {
            return 3 + emulator.ExecuteDecodedInstruction(nextInstruction357, onActionRequired);
        }

        ushort nextInstruction358 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        nextInstruction358 = 6;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction358 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction358, onActionRequired);
        }

        return 4;
    }

    // SRE abs,Y
    private static int SRE_abs_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var value = emulator.data;
        emulator.data = (byte)(value >> 0x01);
        emulator.A ^= emulator.data;
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if ((value & 0b00000001) != 0x00)
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 6;
    }

    // EOR abs,X
    private static int EOR_abs_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction368 = 65535;
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.X & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.X > 0xFF))
        {
            nextInstruction368 = 6;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction368 != 65535)
        {
            return 3 + emulator.ExecuteDecodedInstruction(nextInstruction368, onActionRequired);
        }

        ushort nextInstruction369 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.X);
        nextInstruction369 = 6;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction369 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction369, onActionRequired);
        }

        return 4;
    }

    // LSR abs,X
    private static int LSR_abs_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var value = emulator.data;
        emulator.data = (byte)(value >> 0x01);
        emulator.P &= 0x7C;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if ((value & 0b00000001) != 0x00)
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 6;
    }

    // SRE abs,X
    private static int SRE_abs_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var value = emulator.data;
        emulator.data = (byte)(value >> 0x01);
        emulator.A ^= emulator.data;
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if ((value & 0b00000001) != 0x00)
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 6;
    }

    // RTS
    private static int RTS(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(0x0100 | emulator.S);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.S += 0x01;
        emulator.address = (ushort)(0x0100 | emulator.S);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.S += 0x01;
        emulator.ad = (ushort)emulator.data;
        emulator.address = (ushort)(0x0100 | emulator.S);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.ad = emulator.address;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.PC = (ushort)(emulator.ad + 0x01);
        return 5;
    }

    // ADC (zp,X)
    private static int ADC_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad + emulator.X & 0b11111111);
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left + right + carry;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) + (right & 0b00001111) + carry;
            var high = (left & 0b11110000) + (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (low > 0x09)
            {
                high += 0x10;
                low += 0x06;
            }

            if (((high & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((((high ^ left) & (high ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if (high > 0x90)
            {
                high += 0x60;
            }

            if (high > 0xFF)
            {
                emulator.P |= 0x01;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((binary_result ^ left) & (binary_result ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) != 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        return 5;
    }

    // RRA (zp,X)
    private static int RRA_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad + emulator.X & 0b11111111);
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var value = emulator.data;
        emulator.data = (byte)((value >> 0x01 | carry << 0x07) & 0b11111111);
        carry = value & 0b00000001;
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left + right + carry;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) + (right & 0b00001111) + carry;
            var high = (left & 0b11110000) + (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (low > 0x09)
            {
                high += 0x10;
                low += 0x06;
            }

            if (((high & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((((high ^ left) & (high ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if (high > 0x90)
            {
                high += 0x60;
            }

            if (high > 0xFF)
            {
                emulator.P |= 0x01;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((binary_result ^ left) & (binary_result ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) != 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 7;
    }

    // ADC zp
    private static int ADC_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left + right + carry;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) + (right & 0b00001111) + carry;
            var high = (left & 0b11110000) + (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (low > 0x09)
            {
                high += 0x10;
                low += 0x06;
            }

            if (((high & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((((high ^ left) & (high ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if (high > 0x90)
            {
                high += 0x60;
            }

            if (high > 0xFF)
            {
                emulator.P |= 0x01;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((binary_result ^ left) & (binary_result ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) != 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        return 2;
    }

    // ROR zp
    private static int ROR_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var value = emulator.data;
        emulator.data = (byte)((value >> 0x01 | carry << 0x07) & 0b11111111);
        emulator.P &= 0x7C;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.data & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if ((value & 0b00000001) != 0x00)
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 4;
    }

    // RRA zp
    private static int RRA_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var value = emulator.data;
        emulator.data = (byte)((value >> 0x01 | carry << 0x07) & 0b11111111);
        carry = value & 0b00000001;
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left + right + carry;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) + (right & 0b00001111) + carry;
            var high = (left & 0b11110000) + (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (low > 0x09)
            {
                high += 0x10;
                low += 0x06;
            }

            if (((high & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((((high ^ left) & (high ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if (high > 0x90)
            {
                high += 0x60;
            }

            if (high > 0xFF)
            {
                emulator.P |= 0x01;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((binary_result ^ left) & (binary_result ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) != 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 4;
    }

    // PLA
    private static int PLA(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(0x0100 | emulator.S);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.S += 0x01;
        emulator.address = (ushort)(0x0100 | emulator.S);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A = emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 3;
    }

    // ADC #n
    private static int ADC_n(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left + right + carry;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) + (right & 0b00001111) + carry;
            var high = (left & 0b11110000) + (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (low > 0x09)
            {
                high += 0x10;
                low += 0x06;
            }

            if (((high & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((((high ^ left) & (high ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if (high > 0x90)
            {
                high += 0x60;
            }

            if (high > 0xFF)
            {
                emulator.P |= 0x01;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((binary_result ^ left) & (binary_result ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) != 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        return 1;
    }

    // ROR A
    private static int ROR_A(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        var carry = emulator.P & 0b00000001;
        var value = emulator.A;
        emulator.A = (byte)((value >> 0x01 | carry << 0x07) & 0b11111111);
        emulator.P &= 0x7C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if ((value & 0b00000001) != 0x00)
        {
            emulator.P |= 0x01;
        }

        return 0;
    }

    // ARR #n
    private static int ARR_n(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var value = emulator.A & emulator.data;
        emulator.A = (byte)((value >> 0x01 | (emulator.P & 0b00000001) << 0x07) & 0b11111111);
        emulator.P &= 0x3C;
        if (emulator.A == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.A & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if (((emulator.A >> 0x06 ^ emulator.A >> 0x05) & 0b00000001) != 0x00)
        {
            emulator.P |= 0x40;
        }

        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            if ((value & 0b00001111) + (value & 0b00000001) > 0x05)
            {
                emulator.A = (byte)(emulator.A & 0b11110000 | emulator.A + 0x06 & 0b00001111);
            }

            if ((value & 0b11110000) + (value & 0b00010000) > 0x50)
            {
                emulator.A = (byte)(emulator.A + 0x60 & 0b11111111);
                emulator.P |= 0x01;
            }
        }
        else
        {
            if ((emulator.A & 0b01000000) != 0x00)
            {
                emulator.P |= 0x01;
            }
        }

        return 1;
    }

    // JMP (abs)
    private static int JMP_abs_6C(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad | emulator.data << 0x08);
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.PC = (ushort)(emulator.data << 0x08 | emulator.ad);
        return 4;
    }

    // ADC abs
    private static int ADC_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left + right + carry;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) + (right & 0b00001111) + carry;
            var high = (left & 0b11110000) + (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (low > 0x09)
            {
                high += 0x10;
                low += 0x06;
            }

            if (((high & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((((high ^ left) & (high ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if (high > 0x90)
            {
                high += 0x60;
            }

            if (high > 0xFF)
            {
                emulator.P |= 0x01;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((binary_result ^ left) & (binary_result ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) != 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        return 3;
    }

    // ROR abs
    private static int ROR_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var value = emulator.data;
        emulator.data = (byte)((value >> 0x01 | carry << 0x07) & 0b11111111);
        emulator.P &= 0x7C;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.data & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if ((value & 0b00000001) != 0x00)
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // RRA abs
    private static int RRA_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var value = emulator.data;
        emulator.data = (byte)((value >> 0x01 | carry << 0x07) & 0b11111111);
        carry = value & 0b00000001;
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left + right + carry;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) + (right & 0b00001111) + carry;
            var high = (left & 0b11110000) + (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (low > 0x09)
            {
                high += 0x10;
                low += 0x06;
            }

            if (((high & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((((high ^ left) & (high ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if (high > 0x90)
            {
                high += 0x60;
            }

            if (high > 0xFF)
            {
                emulator.P |= 0x01;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((binary_result ^ left) & (binary_result ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) != 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // BVS
    private static int BVS(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        ushort nextInstruction447 = 65535;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        if (!((emulator.P & 0b01000000) == 0b01000000 /* flag.V */))
        {
            nextInstruction447 = 1;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction447 != 65535)
        {
            return 1 + emulator.ExecuteDecodedInstruction(nextInstruction447, onActionRequired);
        }

        ushort nextInstruction448 = 65535;
        emulator.ad = (ushort)(emulator.PC + (sbyte)emulator.data);
        emulator.address = emulator.PC;
        if ((emulator.ad & 0xFF00) != (emulator.PC & 0xFF00))
        {
            nextInstruction448 = 3;
        }
        else
        {
            nextInstruction448 = 2;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction448 != 65535)
        {
            return 2 + emulator.ExecuteDecodedInstruction(nextInstruction448, onActionRequired);
        }

        return 2;
    }

    // ADC (zp),Y
    private static int ADC_zp_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction452 = 65535;
        emulator.ad = (ushort)(emulator.ad | emulator.data << 0x08);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.Y > 0xFF))
        {
            nextInstruction452 = 11;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction452 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction452, onActionRequired);
        }

        ushort nextInstruction453 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        nextInstruction453 = 11;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction453 != 65535)
        {
            return 5 + emulator.ExecuteDecodedInstruction(nextInstruction453, onActionRequired);
        }

        return 5;
    }

    // RRA (zp),Y
    private static int RRA_zp_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad | emulator.data << 0x08);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var value = emulator.data;
        emulator.data = (byte)((value >> 0x01 | carry << 0x07) & 0b11111111);
        carry = value & 0b00000001;
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left + right + carry;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) + (right & 0b00001111) + carry;
            var high = (left & 0b11110000) + (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (low > 0x09)
            {
                high += 0x10;
                low += 0x06;
            }

            if (((high & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((((high ^ left) & (high ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if (high > 0x90)
            {
                high += 0x60;
            }

            if (high > 0xFF)
            {
                emulator.P |= 0x01;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((binary_result ^ left) & (binary_result ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) != 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 7;
    }

    // ADC zp,X
    private static int ADC_zp_X_75(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left + right + carry;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) + (right & 0b00001111) + carry;
            var high = (left & 0b11110000) + (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (low > 0x09)
            {
                high += 0x10;
                low += 0x06;
            }

            if (((high & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((((high ^ left) & (high ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if (high > 0x90)
            {
                high += 0x60;
            }

            if (high > 0xFF)
            {
                emulator.P |= 0x01;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((binary_result ^ left) & (binary_result ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) != 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        return 3;
    }

    // ROR zp,X
    private static int ROR_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var value = emulator.data;
        emulator.data = (byte)((value >> 0x01 | carry << 0x07) & 0b11111111);
        emulator.P &= 0x7C;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.data & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if ((value & 0b00000001) != 0x00)
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // RRA zp,X
    private static int RRA_zp_X_77(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var value = emulator.data;
        emulator.data = (byte)((value >> 0x01 | carry << 0x07) & 0b11111111);
        carry = value & 0b00000001;
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left + right + carry;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) + (right & 0b00001111) + carry;
            var high = (left & 0b11110000) + (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (low > 0x09)
            {
                high += 0x10;
                low += 0x06;
            }

            if (((high & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((((high ^ left) & (high ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if (high > 0x90)
            {
                high += 0x60;
            }

            if (high > 0xFF)
            {
                emulator.P |= 0x01;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((binary_result ^ left) & (binary_result ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) != 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // SEI
    private static int SEI(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.P |= 0x04;
        return 0;
    }

    // ADC abs,Y
    private static int ADC_abs_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction481 = 65535;
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.Y > 0xFF))
        {
            nextInstruction481 = 11;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction481 != 65535)
        {
            return 3 + emulator.ExecuteDecodedInstruction(nextInstruction481, onActionRequired);
        }

        ushort nextInstruction482 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        nextInstruction482 = 11;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction482 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction482, onActionRequired);
        }

        return 4;
    }

    // RRA abs,Y
    private static int RRA_abs_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var value = emulator.data;
        emulator.data = (byte)((value >> 0x01 | carry << 0x07) & 0b11111111);
        carry = value & 0b00000001;
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left + right + carry;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) + (right & 0b00001111) + carry;
            var high = (left & 0b11110000) + (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (low > 0x09)
            {
                high += 0x10;
                low += 0x06;
            }

            if (((high & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((((high ^ left) & (high ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if (high > 0x90)
            {
                high += 0x60;
            }

            if (high > 0xFF)
            {
                emulator.P |= 0x01;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((binary_result ^ left) & (binary_result ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) != 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 6;
    }

    // ADC abs,X
    private static int ADC_abs_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction492 = 65535;
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.X & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.X > 0xFF))
        {
            nextInstruction492 = 11;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction492 != 65535)
        {
            return 3 + emulator.ExecuteDecodedInstruction(nextInstruction492, onActionRequired);
        }

        ushort nextInstruction493 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.X);
        nextInstruction493 = 11;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction493 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction493, onActionRequired);
        }

        return 4;
    }

    // ROR abs,X
    private static int ROR_abs_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var value = emulator.data;
        emulator.data = (byte)((value >> 0x01 | carry << 0x07) & 0b11111111);
        emulator.P &= 0x7C;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.data & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        if ((value & 0b00000001) != 0x00)
        {
            emulator.P |= 0x01;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 6;
    }

    // RRA abs,X
    private static int RRA_abs_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        var carry = emulator.P & 0b00000001;
        var value = emulator.data;
        emulator.data = (byte)((value >> 0x01 | carry << 0x07) & 0b11111111);
        carry = value & 0b00000001;
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left + right + carry;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) + (right & 0b00001111) + carry;
            var high = (left & 0b11110000) + (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (low > 0x09)
            {
                high += 0x10;
                low += 0x06;
            }

            if (((high & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((((high ^ left) & (high ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if (high > 0x90)
            {
                high += 0x60;
            }

            if (high > 0xFF)
            {
                emulator.P |= 0x01;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((binary_result ^ left) & (binary_result ^ right) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) != 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 6;
    }

    // STA (zp,X)
    private static int STA_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad + emulator.X & 0b11111111);
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.data = emulator.A;
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // SAX (zp,X)
    private static int SAX_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad + emulator.X & 0b11111111);
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.data = (byte)(emulator.A & emulator.X);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // STY zp
    private static int STY_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        emulator.data = emulator.Y;
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 2;
    }

    // STA zp
    private static int STA_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        emulator.data = emulator.A;
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 2;
    }

    // STX zp
    private static int STX_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        emulator.data = emulator.X;
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 2;
    }

    // SAX zp
    private static int SAX_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        emulator.data = (byte)(emulator.A & emulator.X);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 2;
    }

    // DEY
    private static int DEY(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.Y -= 0x01;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.Y == 0)) << 1; // Set Z if is_zero(Y) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.Y & 0b10000000) != 0x00))) << 7; // Set N if is_negative(Y) is true.
        emulator.P = (byte)flags;
        return 0;
    }

    // TXA
    private static int TXA(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.A = emulator.X;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 0;
    }

    // STY abs
    private static int STY_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.data = emulator.Y;
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 3;
    }

    // STA abs
    private static int STA_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.data = emulator.A;
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 3;
    }

    // STX abs
    private static int STX_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.data = emulator.X;
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 3;
    }

    // SAX abs
    private static int SAX_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.data = (byte)(emulator.A & emulator.X);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 3;
    }

    // BCC
    private static int BCC(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        ushort nextInstruction550 = 65535;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        if (((emulator.P & 0b00000001) == 0b00000001 /* flag.C */))
        {
            nextInstruction550 = 1;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction550 != 65535)
        {
            return 1 + emulator.ExecuteDecodedInstruction(nextInstruction550, onActionRequired);
        }

        ushort nextInstruction551 = 65535;
        emulator.ad = (ushort)(emulator.PC + (sbyte)emulator.data);
        emulator.address = emulator.PC;
        if ((emulator.ad & 0xFF00) != (emulator.PC & 0xFF00))
        {
            nextInstruction551 = 3;
        }
        else
        {
            nextInstruction551 = 2;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction551 != 65535)
        {
            return 2 + emulator.ExecuteDecodedInstruction(nextInstruction551, onActionRequired);
        }

        return 2;
    }

    // STA (zp),Y
    private static int STA_zp_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad | emulator.data << 0x08);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        emulator.data = emulator.A;
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // STY zp,X
    private static int STY_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X & 0b11111111);
        emulator.data = emulator.Y;
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 3;
    }

    // STA zp,X
    private static int STA_zp_X_95(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X & 0b11111111);
        emulator.data = emulator.A;
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 3;
    }

    // STX zp,Y
    private static int STX_zp_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.Y & 0b11111111);
        emulator.data = emulator.X;
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 3;
    }

    // SAX zp,Y
    private static int SAX_zp_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.Y & 0b11111111);
        emulator.data = (byte)(emulator.A & emulator.X);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 3;
    }

    // TYA
    private static int TYA(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.A = emulator.Y;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 0;
    }

    // STA abs,Y
    private static int STA_abs_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        emulator.data = emulator.A;
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 4;
    }

    // TXS
    private static int TXS(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.S = emulator.X;
        return 0;
    }

    // STA abs,X
    private static int STA_abs_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X);
        emulator.data = emulator.A;
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 4;
    }

    // LDY #n
    private static int LDY_n(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.Y = emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.Y == 0)) << 1; // Set Z if is_zero(Y) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.Y & 0b10000000) != 0x00))) << 7; // Set N if is_negative(Y) is true.
        emulator.P = (byte)flags;
        return 1;
    }

    // LDA (zp,X)
    private static int LDA_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad + emulator.X & 0b11111111);
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A = emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 5;
    }

    // LDX #n
    private static int LDX_n(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.X = emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.X == 0)) << 1; // Set Z if is_zero(X) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.X & 0b10000000) != 0x00))) << 7; // Set N if is_negative(X) is true.
        emulator.P = (byte)flags;
        return 1;
    }

    // LAX (zp,X)
    private static int LAX_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad + emulator.X & 0b11111111);
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A = emulator.data;
        emulator.X = emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 5;
    }

    // LDY zp
    private static int LDY_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.Y = emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.Y == 0)) << 1; // Set Z if is_zero(Y) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.Y & 0b10000000) != 0x00))) << 7; // Set N if is_negative(Y) is true.
        emulator.P = (byte)flags;
        return 2;
    }

    // LDA zp
    private static int LDA_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A = emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 2;
    }

    // LDX zp
    private static int LDX_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.X = emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.X == 0)) << 1; // Set Z if is_zero(X) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.X & 0b10000000) != 0x00))) << 7; // Set N if is_negative(X) is true.
        emulator.P = (byte)flags;
        return 2;
    }

    // LAX zp
    private static int LAX_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A = emulator.data;
        emulator.X = emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 2;
    }

    // TAY
    private static int TAY(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.Y = emulator.A;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.Y == 0)) << 1; // Set Z if is_zero(Y) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.Y & 0b10000000) != 0x00))) << 7; // Set N if is_negative(Y) is true.
        emulator.P = (byte)flags;
        return 0;
    }

    // LDA #n
    private static int LDA_n(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A = emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 1;
    }

    // TAX
    private static int TAX(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.X = emulator.A;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.X == 0)) << 1; // Set Z if is_zero(X) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.X & 0b10000000) != 0x00))) << 7; // Set N if is_negative(X) is true.
        emulator.P = (byte)flags;
        return 0;
    }

    // LAX #n
    private static int LAX_n(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var value = emulator.data & (emulator.A | 0b11101110);
        emulator.A = (byte)value;
        emulator.X = (byte)value;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 1;
    }

    // LDY abs
    private static int LDY_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.Y = emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.Y == 0)) << 1; // Set Z if is_zero(Y) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.Y & 0b10000000) != 0x00))) << 7; // Set N if is_negative(Y) is true.
        emulator.P = (byte)flags;
        return 3;
    }

    // LDA abs
    private static int LDA_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A = emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 3;
    }

    // LDX abs
    private static int LDX_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.X = emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.X == 0)) << 1; // Set Z if is_zero(X) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.X & 0b10000000) != 0x00))) << 7; // Set N if is_negative(X) is true.
        emulator.P = (byte)flags;
        return 3;
    }

    // LAX abs
    private static int LAX_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A = emulator.data;
        emulator.X = emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 3;
    }

    // BCS
    private static int BCS(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        ushort nextInstruction636 = 65535;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        if (!((emulator.P & 0b00000001) == 0b00000001 /* flag.C */))
        {
            nextInstruction636 = 1;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction636 != 65535)
        {
            return 1 + emulator.ExecuteDecodedInstruction(nextInstruction636, onActionRequired);
        }

        ushort nextInstruction637 = 65535;
        emulator.ad = (ushort)(emulator.PC + (sbyte)emulator.data);
        emulator.address = emulator.PC;
        if ((emulator.ad & 0xFF00) != (emulator.PC & 0xFF00))
        {
            nextInstruction637 = 3;
        }
        else
        {
            nextInstruction637 = 2;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction637 != 65535)
        {
            return 2 + emulator.ExecuteDecodedInstruction(nextInstruction637, onActionRequired);
        }

        return 2;
    }

    // LDA (zp),Y
    private static int LDA_zp_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction641 = 65535;
        emulator.ad = (ushort)(emulator.ad | emulator.data << 0x08);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.Y > 0xFF))
        {
            nextInstruction641 = 8;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction641 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction641, onActionRequired);
        }

        ushort nextInstruction642 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        nextInstruction642 = 8;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction642 != 65535)
        {
            return 5 + emulator.ExecuteDecodedInstruction(nextInstruction642, onActionRequired);
        }

        return 5;
    }

    // LAX (zp),Y
    private static int LAX_zp_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction646 = 65535;
        emulator.ad = (ushort)(emulator.ad | emulator.data << 0x08);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.Y > 0xFF))
        {
            nextInstruction646 = 13;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction646 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction646, onActionRequired);
        }

        ushort nextInstruction647 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        nextInstruction647 = 13;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction647 != 65535)
        {
            return 5 + emulator.ExecuteDecodedInstruction(nextInstruction647, onActionRequired);
        }

        return 5;
    }

    // LDY zp,X
    private static int LDY_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.Y = emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.Y == 0)) << 1; // Set Z if is_zero(Y) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.Y & 0b10000000) != 0x00))) << 7; // Set N if is_negative(Y) is true.
        emulator.P = (byte)flags;
        return 3;
    }

    // LDA zp,X
    private static int LDA_zp_X_B5(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A = emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 3;
    }

    // LDX zp,Y
    private static int LDX_zp_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.Y & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.X = emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.X == 0)) << 1; // Set Z if is_zero(X) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.X & 0b10000000) != 0x00))) << 7; // Set N if is_negative(X) is true.
        emulator.P = (byte)flags;
        return 3;
    }

    // LAX zp,Y
    private static int LAX_zp_Y_B7(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.Y & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.A = emulator.data;
        emulator.X = emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 1; // Set Z if is_zero(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.A & 0b10000000) != 0x00))) << 7; // Set N if is_negative(A) is true.
        emulator.P = (byte)flags;
        return 3;
    }

    // CLV
    private static int CLV(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.P &= 0xBF;
        return 0;
    }

    // LDA abs,Y
    private static int LDA_abs_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction667 = 65535;
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.Y > 0xFF))
        {
            nextInstruction667 = 8;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction667 != 65535)
        {
            return 3 + emulator.ExecuteDecodedInstruction(nextInstruction667, onActionRequired);
        }

        ushort nextInstruction668 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        nextInstruction668 = 8;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction668 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction668, onActionRequired);
        }

        return 4;
    }

    // TSX
    private static int TSX(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.X = emulator.S;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.X == 0)) << 1; // Set Z if is_zero(X) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.X & 0b10000000) != 0x00))) << 7; // Set N if is_negative(X) is true.
        emulator.P = (byte)flags;
        return 0;
    }

    // LDY abs,X
    private static int LDY_abs_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction672 = 65535;
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.X & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.X > 0xFF))
        {
            nextInstruction672 = 9;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction672 != 65535)
        {
            return 3 + emulator.ExecuteDecodedInstruction(nextInstruction672, onActionRequired);
        }

        ushort nextInstruction673 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.X);
        nextInstruction673 = 9;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction673 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction673, onActionRequired);
        }

        return 4;
    }

    // LDA abs,X
    private static int LDA_abs_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction676 = 65535;
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.X & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.X > 0xFF))
        {
            nextInstruction676 = 8;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction676 != 65535)
        {
            return 3 + emulator.ExecuteDecodedInstruction(nextInstruction676, onActionRequired);
        }

        ushort nextInstruction677 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.X);
        nextInstruction677 = 8;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction677 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction677, onActionRequired);
        }

        return 4;
    }

    // LDX abs,Y
    private static int LDX_abs_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction680 = 65535;
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.Y > 0xFF))
        {
            nextInstruction680 = 7;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction680 != 65535)
        {
            return 3 + emulator.ExecuteDecodedInstruction(nextInstruction680, onActionRequired);
        }

        ushort nextInstruction681 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        nextInstruction681 = 7;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction681 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction681, onActionRequired);
        }

        return 4;
    }

    // LAX abs,Y
    private static int LAX_abs_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction684 = 65535;
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.Y > 0xFF))
        {
            nextInstruction684 = 13;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction684 != 65535)
        {
            return 3 + emulator.ExecuteDecodedInstruction(nextInstruction684, onActionRequired);
        }

        ushort nextInstruction685 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        nextInstruction685 = 13;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction685 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction685, onActionRequired);
        }

        return 4;
    }

    // CPY #n
    private static int CPY_n(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var result = emulator.Y - emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011100; // Copy flag.I, flag.D, flag.B and V from P.
        flags |= Unsafe.BitCast<bool, byte>(emulator.Y >= emulator.data); // Set C if Y >= data is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 1; // Set Z if is_zero(result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((result & 0b10000000) != 0x00))) << 7; // Set N if is_negative(result) is true.
        emulator.P = (byte)flags;
        return 1;
    }

    // CMP (zp,X)
    private static int CMP_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad + emulator.X & 0b11111111);
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var result = emulator.A - emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011100; // Copy flag.I, flag.D, flag.B and V from P.
        flags |= Unsafe.BitCast<bool, byte>(emulator.A >= emulator.data); // Set C if A >= data is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 1; // Set Z if is_zero(result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((result & 0b10000000) != 0x00))) << 7; // Set N if is_negative(result) is true.
        emulator.P = (byte)flags;
        return 5;
    }

    // DCP (zp,X)
    private static int DCP_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad + emulator.X & 0b11111111);
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.data = (byte)(emulator.data - 0x01 & 0b11111111);
        emulator.P &= 0x5C;
        if (emulator.A >= emulator.data)
        {
            emulator.P |= 0x01;
        }

        var result = emulator.A - emulator.data;
        if (result == 0)
        {
            emulator.P |= 0x02;
        }

        if (((result & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 7;
    }

    // CPY zp
    private static int CPY_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var result = emulator.Y - emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011100; // Copy flag.I, flag.D, flag.B and V from P.
        flags |= Unsafe.BitCast<bool, byte>(emulator.Y >= emulator.data); // Set C if Y >= data is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 1; // Set Z if is_zero(result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((result & 0b10000000) != 0x00))) << 7; // Set N if is_negative(result) is true.
        emulator.P = (byte)flags;
        return 2;
    }

    // CMP zp
    private static int CMP_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var result = emulator.A - emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011100; // Copy flag.I, flag.D, flag.B and V from P.
        flags |= Unsafe.BitCast<bool, byte>(emulator.A >= emulator.data); // Set C if A >= data is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 1; // Set Z if is_zero(result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((result & 0b10000000) != 0x00))) << 7; // Set N if is_negative(result) is true.
        emulator.P = (byte)flags;
        return 2;
    }

    // DEC zp
    private static int DEC_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.data = (byte)(emulator.data - 0x01 & 0b11111111);
        emulator.P &= 0x7D;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.data & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 4;
    }

    // DCP zp
    private static int DCP_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.data = (byte)(emulator.data - 0x01 & 0b11111111);
        emulator.P &= 0x5C;
        if (emulator.A >= emulator.data)
        {
            emulator.P |= 0x01;
        }

        var result = emulator.A - emulator.data;
        if (result == 0)
        {
            emulator.P |= 0x02;
        }

        if (((result & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 4;
    }

    // INY
    private static int INY(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.Y += 0x01;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.Y == 0)) << 1; // Set Z if is_zero(Y) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.Y & 0b10000000) != 0x00))) << 7; // Set N if is_negative(Y) is true.
        emulator.P = (byte)flags;
        return 0;
    }

    // CMP #n
    private static int CMP_n(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var result = emulator.A - emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011100; // Copy flag.I, flag.D, flag.B and V from P.
        flags |= Unsafe.BitCast<bool, byte>(emulator.A >= emulator.data); // Set C if A >= data is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 1; // Set Z if is_zero(result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((result & 0b10000000) != 0x00))) << 7; // Set N if is_negative(result) is true.
        emulator.P = (byte)flags;
        return 1;
    }

    // DEX
    private static int DEX(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.X -= 0x01;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.X == 0)) << 1; // Set Z if is_zero(X) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.X & 0b10000000) != 0x00))) << 7; // Set N if is_negative(X) is true.
        emulator.P = (byte)flags;
        return 0;
    }

    // SBX #n
    private static int SBX_n(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var value = emulator.A & emulator.X;
        emulator.X = (byte)(value - emulator.data & 0b11111111);
        emulator.P &= 0x7C;
        if (value >= emulator.data)
        {
            emulator.P |= 0x01;
        }

        if (emulator.X == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.X & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        return 1;
    }

    // CPY abs
    private static int CPY_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var result = emulator.Y - emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011100; // Copy flag.I, flag.D, flag.B and V from P.
        flags |= Unsafe.BitCast<bool, byte>(emulator.Y >= emulator.data); // Set C if Y >= data is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 1; // Set Z if is_zero(result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((result & 0b10000000) != 0x00))) << 7; // Set N if is_negative(result) is true.
        emulator.P = (byte)flags;
        return 3;
    }

    // CMP abs
    private static int CMP_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var result = emulator.A - emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011100; // Copy flag.I, flag.D, flag.B and V from P.
        flags |= Unsafe.BitCast<bool, byte>(emulator.A >= emulator.data); // Set C if A >= data is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 1; // Set Z if is_zero(result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((result & 0b10000000) != 0x00))) << 7; // Set N if is_negative(result) is true.
        emulator.P = (byte)flags;
        return 3;
    }

    // DEC abs
    private static int DEC_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.data = (byte)(emulator.data - 0x01 & 0b11111111);
        emulator.P &= 0x7D;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.data & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // DCP abs
    private static int DCP_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.data = (byte)(emulator.data - 0x01 & 0b11111111);
        emulator.P &= 0x5C;
        if (emulator.A >= emulator.data)
        {
            emulator.P |= 0x01;
        }

        var result = emulator.A - emulator.data;
        if (result == 0)
        {
            emulator.P |= 0x02;
        }

        if (((result & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // BNE
    private static int BNE(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        ushort nextInstruction744 = 65535;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        if (((emulator.P & 0b00000010) == 0b00000010 /* flag.Z */))
        {
            nextInstruction744 = 1;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction744 != 65535)
        {
            return 1 + emulator.ExecuteDecodedInstruction(nextInstruction744, onActionRequired);
        }

        ushort nextInstruction745 = 65535;
        emulator.ad = (ushort)(emulator.PC + (sbyte)emulator.data);
        emulator.address = emulator.PC;
        if ((emulator.ad & 0xFF00) != (emulator.PC & 0xFF00))
        {
            nextInstruction745 = 3;
        }
        else
        {
            nextInstruction745 = 2;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction745 != 65535)
        {
            return 2 + emulator.ExecuteDecodedInstruction(nextInstruction745, onActionRequired);
        }

        return 2;
    }

    // CMP (zp),Y
    private static int CMP_zp_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction749 = 65535;
        emulator.ad = (ushort)(emulator.ad | emulator.data << 0x08);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.Y > 0xFF))
        {
            nextInstruction749 = 10;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction749 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction749, onActionRequired);
        }

        ushort nextInstruction750 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        nextInstruction750 = 10;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction750 != 65535)
        {
            return 5 + emulator.ExecuteDecodedInstruction(nextInstruction750, onActionRequired);
        }

        return 5;
    }

    // DCP (zp),Y
    private static int DCP_zp_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad | emulator.data << 0x08);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.data = (byte)(emulator.data - 0x01 & 0b11111111);
        emulator.P &= 0x5C;
        if (emulator.A >= emulator.data)
        {
            emulator.P |= 0x01;
        }

        var result = emulator.A - emulator.data;
        if (result == 0)
        {
            emulator.P |= 0x02;
        }

        if (((result & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 7;
    }

    // CMP zp,X
    private static int CMP_zp_X_D5(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var result = emulator.A - emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011100; // Copy flag.I, flag.D, flag.B and V from P.
        flags |= Unsafe.BitCast<bool, byte>(emulator.A >= emulator.data); // Set C if A >= data is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 1; // Set Z if is_zero(result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((result & 0b10000000) != 0x00))) << 7; // Set N if is_negative(result) is true.
        emulator.P = (byte)flags;
        return 3;
    }

    // DEC zp,X
    private static int DEC_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.data = (byte)(emulator.data - 0x01 & 0b11111111);
        emulator.P &= 0x7D;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.data & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // DCP zp,X
    private static int DCP_zp_X_D7(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.data = (byte)(emulator.data - 0x01 & 0b11111111);
        emulator.P &= 0x5C;
        if (emulator.A >= emulator.data)
        {
            emulator.P |= 0x01;
        }

        var result = emulator.A - emulator.data;
        if (result == 0)
        {
            emulator.P |= 0x02;
        }

        if (((result & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // CLD
    private static int CLD(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.P &= 0xF7;
        return 0;
    }

    // CMP abs,Y
    private static int CMP_abs_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction778 = 65535;
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.Y > 0xFF))
        {
            nextInstruction778 = 10;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction778 != 65535)
        {
            return 3 + emulator.ExecuteDecodedInstruction(nextInstruction778, onActionRequired);
        }

        ushort nextInstruction779 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        nextInstruction779 = 10;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction779 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction779, onActionRequired);
        }

        return 4;
    }

    // DCP abs,Y
    private static int DCP_abs_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.data = (byte)(emulator.data - 0x01 & 0b11111111);
        emulator.P &= 0x5C;
        if (emulator.A >= emulator.data)
        {
            emulator.P |= 0x01;
        }

        var result = emulator.A - emulator.data;
        if (result == 0)
        {
            emulator.P |= 0x02;
        }

        if (((result & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 6;
    }

    // CMP abs,X
    private static int CMP_abs_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction789 = 65535;
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.X & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.X > 0xFF))
        {
            nextInstruction789 = 10;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction789 != 65535)
        {
            return 3 + emulator.ExecuteDecodedInstruction(nextInstruction789, onActionRequired);
        }

        ushort nextInstruction790 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.X);
        nextInstruction790 = 10;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction790 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction790, onActionRequired);
        }

        return 4;
    }

    // DEC abs,X
    private static int DEC_abs_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.data = (byte)(emulator.data - 0x01 & 0b11111111);
        emulator.P &= 0x7D;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.data & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 6;
    }

    // DCP abs,X
    private static int DCP_abs_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.data = (byte)(emulator.data - 0x01 & 0b11111111);
        emulator.P &= 0x5C;
        if (emulator.A >= emulator.data)
        {
            emulator.P |= 0x01;
        }

        var result = emulator.A - emulator.data;
        if (result == 0)
        {
            emulator.P |= 0x02;
        }

        if (((result & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 6;
    }

    // CPX #n
    private static int CPX_n(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var result = emulator.X - emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011100; // Copy flag.I, flag.D, flag.B and V from P.
        flags |= Unsafe.BitCast<bool, byte>(emulator.X >= emulator.data); // Set C if X >= data is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 1; // Set Z if is_zero(result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((result & 0b10000000) != 0x00))) << 7; // Set N if is_negative(result) is true.
        emulator.P = (byte)flags;
        return 1;
    }

    // SBC (zp,X)
    private static int SBC_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad + emulator.X & 0b11111111);
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var borrow = 0x01 - (emulator.P & 0b00000001);
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left - right - borrow;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) - (right & 0b00001111) - borrow;
            var high = (left & 0b11110000) - (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((low & 0b00010000) != 0x00)
            {
                low -= 0x06;
                high -= 0x10;
            }

            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (((binary_result & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((high & 0x0100) != 0x00)
            {
                high -= 0x60;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        return 5;
    }

    // ISC (zp,X)
    private static int ISC_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad + emulator.X & 0b11111111);
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.data = (byte)(emulator.data + 0x01 & 0b11111111);
        var borrow = 0x01 - (emulator.P & 0b00000001);
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left - right - borrow;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) - (right & 0b00001111) - borrow;
            var high = (left & 0b11110000) - (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((low & 0b00010000) != 0x00)
            {
                low -= 0x06;
                high -= 0x10;
            }

            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (((binary_result & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((high & 0x0100) != 0x00)
            {
                high -= 0x60;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 7;
    }

    // CPX zp
    private static int CPX_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var result = emulator.X - emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011100; // Copy flag.I, flag.D, flag.B and V from P.
        flags |= Unsafe.BitCast<bool, byte>(emulator.X >= emulator.data); // Set C if X >= data is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 1; // Set Z if is_zero(result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((result & 0b10000000) != 0x00))) << 7; // Set N if is_negative(result) is true.
        emulator.P = (byte)flags;
        return 2;
    }

    // SBC zp
    private static int SBC_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var borrow = 0x01 - (emulator.P & 0b00000001);
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left - right - borrow;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) - (right & 0b00001111) - borrow;
            var high = (left & 0b11110000) - (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((low & 0b00010000) != 0x00)
            {
                low -= 0x06;
                high -= 0x10;
            }

            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (((binary_result & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((high & 0x0100) != 0x00)
            {
                high -= 0x60;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        return 2;
    }

    // INC zp
    private static int INC_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.data = (byte)(emulator.data + 0x01 & 0b11111111);
        emulator.P &= 0x7D;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.data & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 4;
    }

    // ISC zp
    private static int ISC_zp(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.data = (byte)(emulator.data + 0x01 & 0b11111111);
        var borrow = 0x01 - (emulator.P & 0b00000001);
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left - right - borrow;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) - (right & 0b00001111) - borrow;
            var high = (left & 0b11110000) - (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((low & 0b00010000) != 0x00)
            {
                low -= 0x06;
                high -= 0x10;
            }

            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (((binary_result & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((high & 0x0100) != 0x00)
            {
                high -= 0x60;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 4;
    }

    // INX
    private static int INX(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.X += 0x01;

        // Update flags.
        int flags = emulator.P & 0b01011101; // Copy flag.C, flag.I, flag.D, flag.B and V from P.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.X == 0)) << 1; // Set Z if is_zero(X) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((emulator.X & 0b10000000) != 0x00))) << 7; // Set N if is_negative(X) is true.
        emulator.P = (byte)flags;
        return 0;
    }

    // SBC #n
    private static int SBC_n(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var borrow = 0x01 - (emulator.P & 0b00000001);
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left - right - borrow;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) - (right & 0b00001111) - borrow;
            var high = (left & 0b11110000) - (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((low & 0b00010000) != 0x00)
            {
                low -= 0x06;
                high -= 0x10;
            }

            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (((binary_result & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((high & 0x0100) != 0x00)
            {
                high -= 0x60;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        return 1;
    }

    // NOP
    private static int NOP(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        return 0;
    }

    // USBC #n
    private static int USBC_n(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var borrow = 0x01 - (emulator.P & 0b00000001);
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left - right - borrow;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) - (right & 0b00001111) - borrow;
            var high = (left & 0b11110000) - (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((low & 0b00010000) != 0x00)
            {
                low -= 0x06;
                high -= 0x10;
            }

            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (((binary_result & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((high & 0x0100) != 0x00)
            {
                high -= 0x60;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        return 1;
    }

    // CPX abs
    private static int CPX_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var result = emulator.X - emulator.data;

        // Update flags.
        int flags = emulator.P & 0b01011100; // Copy flag.I, flag.D, flag.B and V from P.
        flags |= Unsafe.BitCast<bool, byte>(emulator.X >= emulator.data); // Set C if X >= data is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 1; // Set Z if is_zero(result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(((result & 0b10000000) != 0x00))) << 7; // Set N if is_negative(result) is true.
        emulator.P = (byte)flags;
        return 3;
    }

    // SBC abs
    private static int SBC_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var borrow = 0x01 - (emulator.P & 0b00000001);
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left - right - borrow;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) - (right & 0b00001111) - borrow;
            var high = (left & 0b11110000) - (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((low & 0b00010000) != 0x00)
            {
                low -= 0x06;
                high -= 0x10;
            }

            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (((binary_result & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((high & 0x0100) != 0x00)
            {
                high -= 0x60;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        return 3;
    }

    // INC abs
    private static int INC_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.data = (byte)(emulator.data + 0x01 & 0b11111111);
        emulator.P &= 0x7D;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.data & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // ISC abs
    private static int ISC_abs(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.data << 0x08 | emulator.ad);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.data = (byte)(emulator.data + 0x01 & 0b11111111);
        var borrow = 0x01 - (emulator.P & 0b00000001);
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left - right - borrow;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) - (right & 0b00001111) - borrow;
            var high = (left & 0b11110000) - (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((low & 0b00010000) != 0x00)
            {
                low -= 0x06;
                high -= 0x10;
            }

            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (((binary_result & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((high & 0x0100) != 0x00)
            {
                high -= 0x60;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // BEQ
    private static int BEQ(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        ushort nextInstruction863 = 65535;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        if (!((emulator.P & 0b00000010) == 0b00000010 /* flag.Z */))
        {
            nextInstruction863 = 1;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction863 != 65535)
        {
            return 1 + emulator.ExecuteDecodedInstruction(nextInstruction863, onActionRequired);
        }

        ushort nextInstruction864 = 65535;
        emulator.ad = (ushort)(emulator.PC + (sbyte)emulator.data);
        emulator.address = emulator.PC;
        if ((emulator.ad & 0xFF00) != (emulator.PC & 0xFF00))
        {
            nextInstruction864 = 3;
        }
        else
        {
            nextInstruction864 = 2;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction864 != 65535)
        {
            return 2 + emulator.ExecuteDecodedInstruction(nextInstruction864, onActionRequired);
        }

        return 2;
    }

    // SBC (zp),Y
    private static int SBC_zp_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction868 = 65535;
        emulator.ad = (ushort)(emulator.ad | emulator.data << 0x08);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.Y > 0xFF))
        {
            nextInstruction868 = 12;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction868 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction868, onActionRequired);
        }

        ushort nextInstruction869 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        nextInstruction869 = 12;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction869 != 65535)
        {
            return 5 + emulator.ExecuteDecodedInstruction(nextInstruction869, onActionRequired);
        }

        return 5;
    }

    // ISC (zp),Y
    private static int ISC_zp_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + 0x01 & 0b11111111);
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.ad | emulator.data << 0x08);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.data = (byte)(emulator.data + 0x01 & 0b11111111);
        var borrow = 0x01 - (emulator.P & 0b00000001);
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left - right - borrow;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) - (right & 0b00001111) - borrow;
            var high = (left & 0b11110000) - (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((low & 0b00010000) != 0x00)
            {
                low -= 0x06;
                high -= 0x10;
            }

            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (((binary_result & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((high & 0x0100) != 0x00)
            {
                high -= 0x60;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 7;
    }

    // SBC zp,X
    private static int SBC_zp_X_F5(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        var borrow = 0x01 - (emulator.P & 0b00000001);
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left - right - borrow;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) - (right & 0b00001111) - borrow;
            var high = (left & 0b11110000) - (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((low & 0b00010000) != 0x00)
            {
                low -= 0x06;
                high -= 0x10;
            }

            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (((binary_result & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((high & 0x0100) != 0x00)
            {
                high -= 0x60;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        return 3;
    }

    // INC zp,X
    private static int INC_zp_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.data = (byte)(emulator.data + 0x01 & 0b11111111);
        emulator.P &= 0x7D;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.data & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // ISC zp,X
    private static int ISC_zp_X_F7(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)emulator.data;
        emulator.address = emulator.ad;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.data = (byte)(emulator.data + 0x01 & 0b11111111);
        var borrow = 0x01 - (emulator.P & 0b00000001);
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left - right - borrow;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) - (right & 0b00001111) - borrow;
            var high = (left & 0b11110000) - (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((low & 0b00010000) != 0x00)
            {
                low -= 0x06;
                high -= 0x10;
            }

            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (((binary_result & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((high & 0x0100) != 0x00)
            {
                high -= 0x60;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 5;
    }

    // SED
    private static int SED(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.P |= 0x08;
        return 0;
    }

    // SBC abs,Y
    private static int SBC_abs_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction897 = 65535;
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.Y > 0xFF))
        {
            nextInstruction897 = 12;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction897 != 65535)
        {
            return 3 + emulator.ExecuteDecodedInstruction(nextInstruction897, onActionRequired);
        }

        ushort nextInstruction898 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        nextInstruction898 = 12;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction898 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction898, onActionRequired);
        }

        return 4;
    }

    // ISC abs,Y
    private static int ISC_abs_Y(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.Y & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.Y);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.data = (byte)(emulator.data + 0x01 & 0b11111111);
        var borrow = 0x01 - (emulator.P & 0b00000001);
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left - right - borrow;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) - (right & 0b00001111) - borrow;
            var high = (left & 0b11110000) - (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((low & 0b00010000) != 0x00)
            {
                low -= 0x06;
                high -= 0x10;
            }

            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (((binary_result & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((high & 0x0100) != 0x00)
            {
                high -= 0x60;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 6;
    }

    // SBC abs,X
    private static int SBC_abs_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        ushort nextInstruction908 = 65535;
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.X & 0b11111111);
        if (!((emulator.ad & 0b11111111) + emulator.X > 0xFF))
        {
            nextInstruction908 = 12;
        }

        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction908 != 65535)
        {
            return 3 + emulator.ExecuteDecodedInstruction(nextInstruction908, onActionRequired);
        }

        ushort nextInstruction909 = 65535;
        emulator.address = (ushort)(emulator.ad + emulator.X);
        nextInstruction909 = 12;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        if (nextInstruction909 != 65535)
        {
            return 4 + emulator.ExecuteDecodedInstruction(nextInstruction909, onActionRequired);
        }

        return 4;
    }

    // INC abs,X
    private static int INC_abs_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.data = (byte)(emulator.data + 0x01 & 0b11111111);
        emulator.P &= 0x7D;
        if (emulator.data == 0)
        {
            emulator.P |= 0x02;
        }

        if (((emulator.data & 0b10000000) != 0x00))
        {
            emulator.P |= 0x80;
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 6;
    }

    // ISC abs,X
    private static int ISC_abs_X(M6502InstructionEmulator emulator, Action<ActionRequired, ushort, byte> onActionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
        emulator.ad = (ushort)emulator.data;
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.ad = (ushort)(emulator.data << 0x08 | emulator.ad);
        emulator.address = (ushort)(emulator.ad & 0xFF00 | emulator.ad + emulator.X & 0b11111111);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        emulator.address = (ushort)(emulator.ad + emulator.X);
        onActionRequired(ActionRequired.MemoryRead, emulator.address, emulator.data);
        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        emulator.data = (byte)(emulator.data + 0x01 & 0b11111111);
        var borrow = 0x01 - (emulator.P & 0b00000001);
        var left = emulator.A;
        var right = emulator.data;
        var binary_result = left - right - borrow;
        if (((emulator.P & 0b00001000) == 0b00001000 /* flag.D */))
        {
            var low = (left & 0b00001111) - (right & 0b00001111) - borrow;
            var high = (left & 0b11110000) - (right & 0b11110000);
            emulator.P &= 0x1C;
            if ((low & 0b00010000) != 0x00)
            {
                low -= 0x06;
                high -= 0x10;
            }

            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if ((binary_result & 0b11111111) == 0x00)
            {
                emulator.P |= 0x02;
            }

            if (((binary_result & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }

            if ((high & 0x0100) != 0x00)
            {
                high -= 0x60;
            }

            emulator.A = (byte)(low & 0b00001111 | high & 0b11110000);
        }
        else
        {
            emulator.A = (byte)binary_result;
            emulator.P &= 0x1C;
            if ((((left ^ right) & (binary_result ^ left) & 0b10000000) != 0x00))
            {
                emulator.P |= 0x40;
            }

            if ((binary_result & 0x0100) == 0x00)
            {
                emulator.P |= 0x01;
            }

            if (emulator.A == 0)
            {
                emulator.P |= 0x02;
            }

            if (((emulator.A & 0b10000000) != 0x00))
            {
                emulator.P |= 0x80;
            }
        }

        onActionRequired(ActionRequired.MemoryWrite, emulator.address, emulator.data);
        return 6;
    }
}
