//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file may cause incorrect behaviour and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;

namespace MrKWatkins.OakCpu.Z80;

public sealed unsafe partial class Z80Emulator
{
    public ActionRequired Step()
    {
        var step = Unsafe.Add(ref MemoryMarshal.GetArrayDataReference(Steps), currentStep);
        currentStep = step.NextStep;
        var actionRequired = step.ActionRequired;
        if (step.Handler != default)
        {
            step.Handler(this, ref actionRequired);
        }

        return actionRequired;
    }

    private static void Error(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        throw new NotSupportedException("Opcode not supported");
    }

    // Opcode read [0]
    [MethodImpl(MethodImplOptions.AggressiveInlining)]
    private static void Step_0(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
    }

    // Opcode read [1]
    private static void Step_1(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.PC += 0x01;
    }

    // Opcode read [2]
    private static void Step_2(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.IR;
        emulator.R = (byte)(emulator.R & 0x80 | emulator.R + 0x01 & 0x7F);
    }

    // Opcode read [3]
    private static void Step_3(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.currentStep = Unsafe.Add(ref MemoryMarshal.GetArrayDataReference(emulator.opcodeStepTable), emulator.data);
    }

    // Read opcode after prefix 0xCB [0]
    private static void Step_4(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTablePrefixCB;
        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // Read opcode after prefix 0xDD [0]
    private static void Step_5(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTablePrefixDD;
        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // Read opcode after prefix 0xED [0]
    private static void Step_6(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTablePrefixED;
        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // Read opcode after prefix 0xFD [0]
    private static void Step_7(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTablePrefixFD;
        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // Halt cycle [0]
    private static void Step_8(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
    }

    // Halt cycle [2]
    private static void Step_10(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.IR;
        emulator.R = (byte)(emulator.R & 0x80 | emulator.R + 0x01 & 0x7F);
    }

    // Halt cycle [3]
    private static void Step_11(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }
    }

    // Interrupt Mode 0 [0]
    private static void Step_12(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.iff1 = false;
        emulator.iff2 = false;
    }

    // Interrupt Mode 0 [3]
    private static void Step_15(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.IR;
        emulator.R = (byte)(emulator.R & 0x80 | emulator.R + 0x01 & 0x7F);
    }

    // Interrupt Mode 0 [4]
    private static void Step_16(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.currentStep = Unsafe.Add(ref MemoryMarshal.GetArrayDataReference(emulator.opcodeStepTable), emulator.data);
    }

    // Interrupt Mode 1 [0]
    private static void Step_17(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.iff1 = false;
        emulator.iff2 = false;
    }

    // Interrupt Mode 1 [3]
    private static void Step_20(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.IR;
        emulator.R = (byte)(emulator.R & 0x80 | emulator.R + 0x01 & 0x7F);
    }

    // Interrupt Mode 1 [6]
    private static void Step_23(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // Interrupt Mode 1 [9]
    private static void Step_26(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = 0x38;
        emulator.WZ = 0x38;
    }

    // Interrupt Mode 2 [0]
    private static void Step_29(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.iff1 = false;
        emulator.iff2 = false;
    }

    // Interrupt Mode 2 [2]
    private static void Step_31(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.latch = emulator.data;
    }

    // Interrupt Mode 2 [3]
    private static void Step_32(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.IR;
        emulator.R = (byte)(emulator.R & 0x80 | emulator.R + 0x01 & 0x7F);
    }

    // Interrupt Mode 2 [6]
    private static void Step_35(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // Interrupt Mode 2 [9]
    private static void Step_38(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.Z = emulator.latch;
        emulator.W = emulator.I;
    }

    // Interrupt Mode 2 [12]
    private static void Step_41(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.WZ += 0x01;
    }

    // Interrupt Mode 2 [13]
    private static void Step_42(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.latch = emulator.data;
    }

    // Interrupt Mode 2 [15]
    private static void Step_44(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
    }

    // Interrupt Mode 2 [16]
    private static void Step_45(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
        emulator.Z = emulator.latch;
        emulator.PC = emulator.WZ;
    }

    // 0x00: NOP [0]
    private static void Step_47(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x01: LD BC, nn [0]
    private static void Step_48(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x01: LD BC, nn [1]
    private static void Step_49(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.C = emulator.data;
    }

    // 0x01: LD BC, nn [3]
    private static void Step_51(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x01: LD BC, nn [4]
    private static void Step_52(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B = emulator.data;
    }

    // 0x01: LD BC, nn [5]
    private static void Step_53(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x02: LD (BC), A [0]
    private static void Step_54(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.BC;
        emulator.data = emulator.A;
        emulator.Z = (byte)(emulator.C + 0x01);
        emulator.W = emulator.A;
    }

    // 0x02: LD (BC), A [2]
    private static void Step_56(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x03: INC BC [0]
    private static void Step_57(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.BC += 0x01;
    }

    // 0x03: INC BC [1]
    private static void Step_58(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x04: INC B [0]
    private static void Step_59(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B += 0x01;

        // Update flags.
        // Reset N.
        int flags = emulator.B & 0b00101000; // Copy X and Y from B.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0x80)) << 2; // Set PV if B == 0x80 is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.B & 0x0F) == 0x00)) << 4; // Set H if (B & 0x0F) == 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0)) << 6; // Set Z if is_zero(B) is true.
        flags |= emulator.B & 0x80; // Set S if is_negative(B) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x05: DEC B [0]
    private static void Step_60(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B -= 0x01;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.B & 0b00101000; // Copy X and Y from B.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0x7F)) << 2; // Set PV if B == 0x7F is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.B & 0x0F) == 0x0F)) << 4; // Set H if (B & 0x0F) == 0x0F is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0)) << 6; // Set Z if is_zero(B) is true.
        flags |= emulator.B & 0x80; // Set S if is_negative(B) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x06: LD B, n [0]
    private static void Step_61(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x06: LD B, n [1]
    private static void Step_62(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B = emulator.data;
    }

    // 0x06: LD B, n [2]
    private static void Step_63(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x07: RLCA [0]
    private static void Step_64(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.A;
        emulator.A = (byte)(emulator.A << 0x01 | emulator.A >> 0x07);

        // Update flags.
        // Reset N and H.
        int flags = emulator.A & 0b00101000; // Copy X and Y from A.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x08: EX AF; AF' [0]
    private static void Step_65(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.Shadow_AF;
        emulator.Shadow_AF = emulator.AF;
        emulator.AF = temp;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x09: ADD HL, BC [0]
    private static void Step_66(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.HL;
        var right = emulator.BC;
        var result = left + right;
        emulator.HL = (ushort)result;
        emulator.WZ = (ushort)(left + 0x01);

        // Update flags.
        // Reset N.
        int flags = emulator.H & 0b00101000; // Copy X and Y from H.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        flags |= Unsafe.BitCast<bool, byte>((result & 65536) == 65536); // Set C if (result & 65536) == 65536 is true.
        flags |= ((left ^ result ^ right) & 0x1000) >> 8; // Set H if did_16_bit_addition_half_carry(left, right, result) is true.
        emulator.F = (byte)flags;
    }

    // 0x09: ADD HL, BC [6]
    private static void Step_72(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x0A: LD A, (BC) [0]
    private static void Step_73(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.BC;
    }

    // 0x0A: LD A, (BC) [1]
    private static void Step_74(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A = emulator.data;
        emulator.WZ = (ushort)(emulator.BC + 0x01);
    }

    // 0x0A: LD A, (BC) [2]
    private static void Step_75(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x0B: DEC BC [0]
    private static void Step_76(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.BC -= 0x01;
    }

    // 0x0B: DEC BC [1]
    private static void Step_77(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x0C: INC C [0]
    private static void Step_78(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.C += 0x01;

        // Update flags.
        // Reset N.
        int flags = emulator.C & 0b00101000; // Copy X and Y from C.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.C == 0x80)) << 2; // Set PV if C == 0x80 is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.C & 0x0F) == 0x00)) << 4; // Set H if (C & 0x0F) == 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.C == 0)) << 6; // Set Z if is_zero(C) is true.
        flags |= emulator.C & 0x80; // Set S if is_negative(C) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x0D: DEC C [0]
    private static void Step_79(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.C -= 0x01;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.C & 0b00101000; // Copy X and Y from C.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.C == 0x7F)) << 2; // Set PV if C == 0x7F is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.C & 0x0F) == 0x0F)) << 4; // Set H if (C & 0x0F) == 0x0F is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.C == 0)) << 6; // Set Z if is_zero(C) is true.
        flags |= emulator.C & 0x80; // Set S if is_negative(C) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x0E: LD C, n [0]
    private static void Step_80(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x0E: LD C, n [1]
    private static void Step_81(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.C = emulator.data;
    }

    // 0x0E: LD C, n [2]
    private static void Step_82(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x0F: RRCA [0]
    private static void Step_83(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.A;
        emulator.A = (byte)(emulator.A >> 0x01 | emulator.A << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.A & 0b00101000; // Copy X and Y from A.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x10: DJNZ d [1]
    private static void Step_85(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x10: DJNZ d [3]
    private static void Step_87(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B -= 0x01;
        if (emulator.B == 0x00)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
        else
        {
            emulator.PC = (ushort)(emulator.PC + (sbyte)emulator.data);
            emulator.WZ = emulator.PC;
        }
    }

    // 0x10: DJNZ d [8]
    private static void Step_92(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x11: LD DE, nn [0]
    private static void Step_93(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x11: LD DE, nn [1]
    private static void Step_94(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.E = emulator.data;
    }

    // 0x11: LD DE, nn [3]
    private static void Step_96(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x11: LD DE, nn [4]
    private static void Step_97(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.D = emulator.data;
    }

    // 0x11: LD DE, nn [5]
    private static void Step_98(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x12: LD (DE), A [0]
    private static void Step_99(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.DE;
        emulator.data = emulator.A;
        emulator.Z = (byte)(emulator.E + 0x01);
        emulator.W = emulator.A;
    }

    // 0x12: LD (DE), A [2]
    private static void Step_101(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x13: INC DE [0]
    private static void Step_102(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.DE += 0x01;
    }

    // 0x13: INC DE [1]
    private static void Step_103(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x14: INC D [0]
    private static void Step_104(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.D += 0x01;

        // Update flags.
        // Reset N.
        int flags = emulator.D & 0b00101000; // Copy X and Y from D.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.D == 0x80)) << 2; // Set PV if D == 0x80 is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.D & 0x0F) == 0x00)) << 4; // Set H if (D & 0x0F) == 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.D == 0)) << 6; // Set Z if is_zero(D) is true.
        flags |= emulator.D & 0x80; // Set S if is_negative(D) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x15: DEC D [0]
    private static void Step_105(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.D -= 0x01;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.D & 0b00101000; // Copy X and Y from D.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.D == 0x7F)) << 2; // Set PV if D == 0x7F is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.D & 0x0F) == 0x0F)) << 4; // Set H if (D & 0x0F) == 0x0F is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.D == 0)) << 6; // Set Z if is_zero(D) is true.
        flags |= emulator.D & 0x80; // Set S if is_negative(D) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x16: LD D, n [0]
    private static void Step_106(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x16: LD D, n [1]
    private static void Step_107(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.D = emulator.data;
    }

    // 0x16: LD D, n [2]
    private static void Step_108(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x17: RLA [0]
    private static void Step_109(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.A;
        emulator.A = (byte)(emulator.A << 0x01 | (emulator.F & 0b00000001 /* flag.C */));

        // Update flags.
        // Reset N and H.
        int flags = emulator.A & 0b00101000; // Copy X and Y from A.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x18: JR d [0]
    private static void Step_110(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x18: JR d [3]
    private static void Step_113(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.PC = (ushort)(emulator.PC + (sbyte)emulator.data);
        emulator.WZ = emulator.PC;
    }

    // 0x18: JR d [7]
    private static void Step_117(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x19: ADD HL, DE [0]
    private static void Step_118(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.HL;
        var right = emulator.DE;
        var result = left + right;
        emulator.HL = (ushort)result;
        emulator.WZ = (ushort)(left + 0x01);

        // Update flags.
        // Reset N.
        int flags = emulator.H & 0b00101000; // Copy X and Y from H.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        flags |= Unsafe.BitCast<bool, byte>((result & 65536) == 65536); // Set C if (result & 65536) == 65536 is true.
        flags |= ((left ^ result ^ right) & 0x1000) >> 8; // Set H if did_16_bit_addition_half_carry(left, right, result) is true.
        emulator.F = (byte)flags;
    }

    // 0x19: ADD HL, DE [6]
    private static void Step_124(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x1A: LD A, (DE) [0]
    private static void Step_125(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.DE;
    }

    // 0x1A: LD A, (DE) [1]
    private static void Step_126(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A = emulator.data;
        emulator.WZ = (ushort)(emulator.DE + 0x01);
    }

    // 0x1A: LD A, (DE) [2]
    private static void Step_127(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x1B: DEC DE [0]
    private static void Step_128(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.DE -= 0x01;
    }

    // 0x1B: DEC DE [1]
    private static void Step_129(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x1C: INC E [0]
    private static void Step_130(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.E += 0x01;

        // Update flags.
        // Reset N.
        int flags = emulator.E & 0b00101000; // Copy X and Y from E.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.E == 0x80)) << 2; // Set PV if E == 0x80 is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.E & 0x0F) == 0x00)) << 4; // Set H if (E & 0x0F) == 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.E == 0)) << 6; // Set Z if is_zero(E) is true.
        flags |= emulator.E & 0x80; // Set S if is_negative(E) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x1D: DEC E [0]
    private static void Step_131(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.E -= 0x01;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.E & 0b00101000; // Copy X and Y from E.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.E == 0x7F)) << 2; // Set PV if E == 0x7F is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.E & 0x0F) == 0x0F)) << 4; // Set H if (E & 0x0F) == 0x0F is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.E == 0)) << 6; // Set Z if is_zero(E) is true.
        flags |= emulator.E & 0x80; // Set S if is_negative(E) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x1E: LD E, n [0]
    private static void Step_132(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x1E: LD E, n [1]
    private static void Step_133(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.E = emulator.data;
    }

    // 0x1E: LD E, n [2]
    private static void Step_134(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x1F: RRA [0]
    private static void Step_135(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.A;
        emulator.A = (byte)(emulator.A >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = emulator.A & 0b00101000; // Copy X and Y from A.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x01) == 0x01); // Set C if (temp & 0x01) == 0x01 is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x20: JR NZ, d [0]
    private static void Step_136(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x20: JR NZ, d [2]
    private static void Step_138(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b01000000) == 0b01000000 /* !condition.NZ */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0x20: JR NZ, d [3]
    private static void Step_139(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.PC = (ushort)(emulator.PC + (sbyte)emulator.data);
        emulator.WZ = emulator.PC;
    }

    // 0x20: JR NZ, d [7]
    private static void Step_143(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x21: LD HL, nn [0]
    private static void Step_144(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x21: LD HL, nn [1]
    private static void Step_145(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.L = emulator.data;
    }

    // 0x21: LD HL, nn [3]
    private static void Step_147(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x21: LD HL, nn [4]
    private static void Step_148(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.H = emulator.data;
    }

    // 0x21: LD HL, nn [5]
    private static void Step_149(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x22: LD (nn), HL [0]
    private static void Step_150(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x22: LD (nn), HL [1]
    private static void Step_151(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0x22: LD (nn), HL [3]
    private static void Step_153(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x22: LD (nn), HL [4]
    private static void Step_154(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0x22: LD (nn), HL [6]
    private static void Step_156(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.data = emulator.L;
        emulator.WZ += 0x01;
    }

    // 0x22: LD (nn), HL [9]
    private static void Step_159(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.data = emulator.H;
    }

    // 0x22: LD (nn), HL [11]
    private static void Step_161(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x23: INC HL [0]
    private static void Step_162(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.HL += 0x01;
    }

    // 0x23: INC HL [1]
    private static void Step_163(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x24: INC H [0]
    private static void Step_164(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.H += 0x01;

        // Update flags.
        // Reset N.
        int flags = emulator.H & 0b00101000; // Copy X and Y from H.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.H == 0x80)) << 2; // Set PV if H == 0x80 is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.H & 0x0F) == 0x00)) << 4; // Set H if (H & 0x0F) == 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.H == 0)) << 6; // Set Z if is_zero(H) is true.
        flags |= emulator.H & 0x80; // Set S if is_negative(H) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x25: DEC H [0]
    private static void Step_165(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.H -= 0x01;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.H & 0b00101000; // Copy X and Y from H.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.H == 0x7F)) << 2; // Set PV if H == 0x7F is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.H & 0x0F) == 0x0F)) << 4; // Set H if (H & 0x0F) == 0x0F is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.H == 0)) << 6; // Set Z if is_zero(H) is true.
        flags |= emulator.H & 0x80; // Set S if is_negative(H) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x26: LD H, n [0]
    private static void Step_166(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x26: LD H, n [1]
    private static void Step_167(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.H = emulator.data;
    }

    // 0x26: LD H, n [2]
    private static void Step_168(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x27: DAA [0]
    private static void Step_169(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var low_a = emulator.A & 0x0F;
        var modifier = 0x00;
        if (((emulator.F & 0b00010000) == 0b00010000 /* flag.H */) || low_a > 0x09)
        {
            modifier += 0x06;
        }

        var carry = 0x00;
        if (((emulator.F & 0b00000001) == 0b00000001 /* flag.C */) || emulator.A > 0x99)
        {
            modifier += 0x60;
            carry = 0xFF;
        }

        if (((emulator.F & 0b00000010) == 0b00000010 /* flag.N */))
        {
            emulator.A = (byte)(emulator.A - modifier);
        }
        else
        {
            emulator.A = (byte)(emulator.A + modifier);
        }

        var half_carry = 0x00;
        if (((emulator.F & 0b00000010) == 0b00000010 /* flag.N */))
        {
            if (((emulator.F & 0b00010000) == 0b00010000 /* flag.H */) && low_a < 0x06)
            {
                half_carry = 0xFF;
            }
        }
        else
        {
            if (low_a >= 0x0A)
            {
                half_carry = 0xFF;
            }
        }

        // Update flags.
        int flags = carry & 0b00000001; // Copy C from carry.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= half_carry & 0b00010000; // Copy H from half_carry.
        flags |= emulator.F & 0b00000010; // Copy N from F.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x28: JR Z, d [0]
    private static void Step_170(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x28: JR Z, d [2]
    private static void Step_172(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b01000000) == 0b00000000 /* !condition.Z */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0x28: JR Z, d [3]
    private static void Step_173(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.PC = (ushort)(emulator.PC + (sbyte)emulator.data);
        emulator.WZ = emulator.PC;
    }

    // 0x28: JR Z, d [7]
    private static void Step_177(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x29: ADD HL, HL [0]
    private static void Step_178(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.HL;
        var right = emulator.HL;
        var result = left + right;
        emulator.HL = (ushort)result;
        emulator.WZ = (ushort)(left + 0x01);

        // Update flags.
        // Reset N.
        int flags = emulator.H & 0b00101000; // Copy X and Y from H.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        flags |= Unsafe.BitCast<bool, byte>((result & 65536) == 65536); // Set C if (result & 65536) == 65536 is true.
        flags |= ((left ^ result ^ right) & 0x1000) >> 8; // Set H if did_16_bit_addition_half_carry(left, right, result) is true.
        emulator.F = (byte)flags;
    }

    // 0x29: ADD HL, HL [6]
    private static void Step_184(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x2A: LD HL, (nn) [0]
    private static void Step_185(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x2A: LD HL, (nn) [1]
    private static void Step_186(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0x2A: LD HL, (nn) [3]
    private static void Step_188(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x2A: LD HL, (nn) [4]
    private static void Step_189(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0x2A: LD HL, (nn) [6]
    private static void Step_191(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.WZ += 0x01;
    }

    // 0x2A: LD HL, (nn) [7]
    private static void Step_192(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.L = emulator.data;
    }

    // 0x2A: LD HL, (nn) [9]
    private static void Step_194(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
    }

    // 0x2A: LD HL, (nn) [10]
    private static void Step_195(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.H = emulator.data;
    }

    // 0x2A: LD HL, (nn) [11]
    private static void Step_196(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x2B: DEC HL [0]
    private static void Step_197(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.HL -= 0x01;
    }

    // 0x2B: DEC HL [1]
    private static void Step_198(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x2C: INC L [0]
    private static void Step_199(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.L += 0x01;

        // Update flags.
        // Reset N.
        int flags = emulator.L & 0b00101000; // Copy X and Y from L.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.L == 0x80)) << 2; // Set PV if L == 0x80 is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.L & 0x0F) == 0x00)) << 4; // Set H if (L & 0x0F) == 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.L == 0)) << 6; // Set Z if is_zero(L) is true.
        flags |= emulator.L & 0x80; // Set S if is_negative(L) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x2D: DEC L [0]
    private static void Step_200(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.L -= 0x01;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.L & 0b00101000; // Copy X and Y from L.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.L == 0x7F)) << 2; // Set PV if L == 0x7F is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.L & 0x0F) == 0x0F)) << 4; // Set H if (L & 0x0F) == 0x0F is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.L == 0)) << 6; // Set Z if is_zero(L) is true.
        flags |= emulator.L & 0x80; // Set S if is_negative(L) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x2E: LD L, n [0]
    private static void Step_201(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x2E: LD L, n [1]
    private static void Step_202(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.L = emulator.data;
    }

    // 0x2E: LD L, n [2]
    private static void Step_203(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x2F: CPL [0]
    private static void Step_204(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A = (byte)~emulator.A;

        // Update flags.
        int flags = 0b00010010; // Set N and H.
        flags |= emulator.A & 0b00101000; // Copy X and Y from A.
        flags |= emulator.F & 0b11000101; // Copy C, PV, Z and S from F.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x30: JR NC, d [0]
    private static void Step_205(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x30: JR NC, d [2]
    private static void Step_207(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000001) == 0b00000001 /* !condition.NC */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0x30: JR NC, d [3]
    private static void Step_208(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.PC = (ushort)(emulator.PC + (sbyte)emulator.data);
        emulator.WZ = emulator.PC;
    }

    // 0x30: JR NC, d [7]
    private static void Step_212(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x31: LD SP, nn [0]
    private static void Step_213(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x31: LD SP, nn [1]
    private static void Step_214(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SPL = emulator.data;
    }

    // 0x31: LD SP, nn [3]
    private static void Step_216(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x31: LD SP, nn [4]
    private static void Step_217(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SPH = emulator.data;
    }

    // 0x31: LD SP, nn [5]
    private static void Step_218(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x32: LD (nn), A [0]
    private static void Step_219(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x32: LD (nn), A [1]
    private static void Step_220(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0x32: LD (nn), A [3]
    private static void Step_222(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x32: LD (nn), A [4]
    private static void Step_223(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0x32: LD (nn), A [6]
    private static void Step_225(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.data = emulator.A;
        emulator.Z += 0x01;
        emulator.W = emulator.A;
    }

    // 0x32: LD (nn), A [8]
    private static void Step_227(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x33: INC SP [0]
    private static void Step_228(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP += 0x01;
    }

    // 0x33: INC SP [1]
    private static void Step_229(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x34: INC (HL) [0]
    private static void Step_230(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
    }

    // 0x34: INC (HL) [2]
    private static void Step_232(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data += 0x01;

        // Update flags.
        // Reset N.
        int flags = emulator.data & 0b00101000; // Copy X and Y from data.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0x80)) << 2; // Set PV if data == 0x80 is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.data & 0x0F) == 0x00)) << 4; // Set H if (data & 0x0F) == 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        flags |= emulator.data & 0x80; // Set S if is_negative(data) is true.
        emulator.F = (byte)flags;
    }

    // 0x34: INC (HL) [6]
    private static void Step_236(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x35: DEC (HL) [0]
    private static void Step_237(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
    }

    // 0x35: DEC (HL) [2]
    private static void Step_239(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data -= 0x01;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.data & 0b00101000; // Copy X and Y from data.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0x7F)) << 2; // Set PV if data == 0x7F is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.data & 0x0F) == 0x0F)) << 4; // Set H if (data & 0x0F) == 0x0F is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        flags |= emulator.data & 0x80; // Set S if is_negative(data) is true.
        emulator.F = (byte)flags;
    }

    // 0x35: DEC (HL) [6]
    private static void Step_243(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x36: LD (HL), n [0]
    private static void Step_244(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x36: LD (HL), n [3]
    private static void Step_247(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
    }

    // 0x36: LD (HL), n [5]
    private static void Step_249(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x37: SCF [0]
    private static void Step_250(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var xy = emulator.Q ^ emulator.F | emulator.A;

        // Update flags.
        int flags = 0b00000001; // Set C. Reset N and H.
        flags |= xy & 0b00101000; // Copy X and Y from xy.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x38: JR C, d [0]
    private static void Step_251(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x38: JR C, d [2]
    private static void Step_253(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000001) == 0b00000000 /* !condition.C */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0x38: JR C, d [3]
    private static void Step_254(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.PC = (ushort)(emulator.PC + (sbyte)emulator.data);
        emulator.WZ = emulator.PC;
    }

    // 0x38: JR C, d [7]
    private static void Step_258(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x39: ADD HL, SP [0]
    private static void Step_259(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.HL;
        var right = emulator.SP;
        var result = left + right;
        emulator.HL = (ushort)result;
        emulator.WZ = (ushort)(left + 0x01);

        // Update flags.
        // Reset N.
        int flags = emulator.H & 0b00101000; // Copy X and Y from H.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        flags |= Unsafe.BitCast<bool, byte>((result & 65536) == 65536); // Set C if (result & 65536) == 65536 is true.
        flags |= ((left ^ result ^ right) & 0x1000) >> 8; // Set H if did_16_bit_addition_half_carry(left, right, result) is true.
        emulator.F = (byte)flags;
    }

    // 0x39: ADD HL, SP [6]
    private static void Step_265(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x3A: LD A, (nn) [0]
    private static void Step_266(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x3A: LD A, (nn) [1]
    private static void Step_267(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0x3A: LD A, (nn) [3]
    private static void Step_269(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x3A: LD A, (nn) [4]
    private static void Step_270(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0x3A: LD A, (nn) [6]
    private static void Step_272(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.WZ += 0x01;
    }

    // 0x3A: LD A, (nn) [7]
    private static void Step_273(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A = emulator.data;
    }

    // 0x3A: LD A, (nn) [8]
    private static void Step_274(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x3B: DEC SP [0]
    private static void Step_275(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
    }

    // 0x3B: DEC SP [1]
    private static void Step_276(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x3C: INC A [0]
    private static void Step_277(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A += 0x01;

        // Update flags.
        // Reset N.
        int flags = emulator.A & 0b00101000; // Copy X and Y from A.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0x80)) << 2; // Set PV if A == 0x80 is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.A & 0x0F) == 0x00)) << 4; // Set H if (A & 0x0F) == 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        flags |= emulator.A & 0x80; // Set S if is_negative(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x3D: DEC A [0]
    private static void Step_278(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A -= 0x01;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.A & 0b00101000; // Copy X and Y from A.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0x7F)) << 2; // Set PV if A == 0x7F is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.A & 0x0F) == 0x0F)) << 4; // Set H if (A & 0x0F) == 0x0F is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        flags |= emulator.A & 0x80; // Set S if is_negative(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x3E: LD A, n [0]
    private static void Step_279(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0x3E: LD A, n [1]
    private static void Step_280(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A = emulator.data;
    }

    // 0x3E: LD A, n [2]
    private static void Step_281(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x3F: CCF [0]
    private static void Step_282(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var xy = emulator.Q ^ emulator.F | emulator.A;

        // Update flags.
        // Reset N.
        int flags = xy & 0b00101000; // Copy X and Y from xy.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        flags |= Unsafe.BitCast<bool, byte>((emulator.F & 0b00000001 /* flag.C */) == 0x00); // Set C if C == 0x00 is true.
        flags |= ((emulator.F & 0b00000001 /* flag.C */)) << 4; // Set H if C is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x40: LD B, B [0]
    private static void Step_283(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x41: LD B, C [0]
    private static void Step_284(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B = emulator.C;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x42: LD B, D [0]
    private static void Step_285(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B = emulator.D;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x43: LD B, E [0]
    private static void Step_286(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B = emulator.E;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x44: LD B, H [0]
    private static void Step_287(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B = emulator.H;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x45: LD B, L [0]
    private static void Step_288(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B = emulator.L;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x46: LD B, (HL) [0]
    private static void Step_289(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
    }

    // 0x46: LD B, (HL) [1]
    private static void Step_290(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B = emulator.data;
    }

    // 0x46: LD B, (HL) [2]
    private static void Step_291(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x47: LD B, A [0]
    private static void Step_292(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B = emulator.A;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x48: LD C, B [0]
    private static void Step_293(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.C = emulator.B;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x49: LD C, C [0]
    private static void Step_294(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x4A: LD C, D [0]
    private static void Step_295(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.C = emulator.D;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x4B: LD C, E [0]
    private static void Step_296(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.C = emulator.E;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x4C: LD C, H [0]
    private static void Step_297(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.C = emulator.H;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x4D: LD C, L [0]
    private static void Step_298(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.C = emulator.L;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x4E: LD C, (HL) [0]
    private static void Step_299(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
    }

    // 0x4E: LD C, (HL) [1]
    private static void Step_300(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.C = emulator.data;
    }

    // 0x4E: LD C, (HL) [2]
    private static void Step_301(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x4F: LD C, A [0]
    private static void Step_302(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.C = emulator.A;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x50: LD D, B [0]
    private static void Step_303(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.D = emulator.B;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x51: LD D, C [0]
    private static void Step_304(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.D = emulator.C;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x52: LD D, D [0]
    private static void Step_305(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x53: LD D, E [0]
    private static void Step_306(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.D = emulator.E;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x54: LD D, H [0]
    private static void Step_307(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.D = emulator.H;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x55: LD D, L [0]
    private static void Step_308(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.D = emulator.L;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x56: LD D, (HL) [0]
    private static void Step_309(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
    }

    // 0x56: LD D, (HL) [1]
    private static void Step_310(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.D = emulator.data;
    }

    // 0x56: LD D, (HL) [2]
    private static void Step_311(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x57: LD D, A [0]
    private static void Step_312(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.D = emulator.A;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x58: LD E, B [0]
    private static void Step_313(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.E = emulator.B;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x59: LD E, C [0]
    private static void Step_314(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.E = emulator.C;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x5A: LD E, D [0]
    private static void Step_315(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.E = emulator.D;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x5B: LD E, E [0]
    private static void Step_316(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x5C: LD E, H [0]
    private static void Step_317(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.E = emulator.H;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x5D: LD E, L [0]
    private static void Step_318(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.E = emulator.L;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x5E: LD E, (HL) [0]
    private static void Step_319(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
    }

    // 0x5E: LD E, (HL) [1]
    private static void Step_320(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.E = emulator.data;
    }

    // 0x5E: LD E, (HL) [2]
    private static void Step_321(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x5F: LD E, A [0]
    private static void Step_322(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.E = emulator.A;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x60: LD H, B [0]
    private static void Step_323(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.H = emulator.B;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x61: LD H, C [0]
    private static void Step_324(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.H = emulator.C;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x62: LD H, D [0]
    private static void Step_325(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.H = emulator.D;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x63: LD H, E [0]
    private static void Step_326(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.H = emulator.E;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x64: LD H, H [0]
    private static void Step_327(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x65: LD H, L [0]
    private static void Step_328(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.H = emulator.L;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x66: LD H, (HL) [0]
    private static void Step_329(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
    }

    // 0x66: LD H, (HL) [1]
    private static void Step_330(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.H = emulator.data;
    }

    // 0x66: LD H, (HL) [2]
    private static void Step_331(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x67: LD H, A [0]
    private static void Step_332(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.H = emulator.A;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x68: LD L, B [0]
    private static void Step_333(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.L = emulator.B;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x69: LD L, C [0]
    private static void Step_334(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.L = emulator.C;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x6A: LD L, D [0]
    private static void Step_335(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.L = emulator.D;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x6B: LD L, E [0]
    private static void Step_336(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.L = emulator.E;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x6C: LD L, H [0]
    private static void Step_337(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.L = emulator.H;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x6D: LD L, L [0]
    private static void Step_338(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x6E: LD L, (HL) [0]
    private static void Step_339(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
    }

    // 0x6E: LD L, (HL) [1]
    private static void Step_340(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.L = emulator.data;
    }

    // 0x6E: LD L, (HL) [2]
    private static void Step_341(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x6F: LD L, A [0]
    private static void Step_342(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.L = emulator.A;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x70: LD (HL), B [0]
    private static void Step_343(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
        emulator.data = emulator.B;
    }

    // 0x70: LD (HL), B [2]
    private static void Step_345(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x71: LD (HL), C [0]
    private static void Step_346(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
        emulator.data = emulator.C;
    }

    // 0x71: LD (HL), C [2]
    private static void Step_348(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x72: LD (HL), D [0]
    private static void Step_349(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
        emulator.data = emulator.D;
    }

    // 0x72: LD (HL), D [2]
    private static void Step_351(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x73: LD (HL), E [0]
    private static void Step_352(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
        emulator.data = emulator.E;
    }

    // 0x73: LD (HL), E [2]
    private static void Step_354(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x74: LD (HL), H [0]
    private static void Step_355(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
        emulator.data = emulator.H;
    }

    // 0x74: LD (HL), H [2]
    private static void Step_357(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x75: LD (HL), L [0]
    private static void Step_358(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
        emulator.data = emulator.L;
    }

    // 0x75: LD (HL), L [2]
    private static void Step_360(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x76: HALT [0]
    private static void Step_361(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        emulator.halted = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Move to halted cycle.
        emulator.currentStep = 9;
        Step_8(emulator, ref actionRequired);
    }

    // 0x77: LD (HL), A [0]
    private static void Step_362(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
        emulator.data = emulator.A;
    }

    // 0x77: LD (HL), A [2]
    private static void Step_364(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x78: LD A, B [0]
    private static void Step_365(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A = emulator.B;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x79: LD A, C [0]
    private static void Step_366(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A = emulator.C;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x7A: LD A, D [0]
    private static void Step_367(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A = emulator.D;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x7B: LD A, E [0]
    private static void Step_368(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A = emulator.E;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x7C: LD A, H [0]
    private static void Step_369(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A = emulator.H;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x7D: LD A, L [0]
    private static void Step_370(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A = emulator.L;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x7E: LD A, (HL) [0]
    private static void Step_371(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
    }

    // 0x7E: LD A, (HL) [1]
    private static void Step_372(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A = emulator.data;
    }

    // 0x7E: LD A, (HL) [2]
    private static void Step_373(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x7F: LD A, A [0]
    private static void Step_374(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x80: ADD A, B [0]
    private static void Step_375(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.B;
        var result = left + right;
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x81: ADD A, C [0]
    private static void Step_376(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.C;
        var result = left + right;
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x82: ADD A, D [0]
    private static void Step_377(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.D;
        var result = left + right;
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x83: ADD A, E [0]
    private static void Step_378(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.E;
        var result = left + right;
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x84: ADD A, H [0]
    private static void Step_379(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.H;
        var result = left + right;
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x85: ADD A, L [0]
    private static void Step_380(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.L;
        var result = left + right;
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x86: ADD A, (HL) [0]
    private static void Step_381(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
    }

    // 0x86: ADD A, (HL) [2]
    private static void Step_383(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left + right;
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x87: ADD A, A [0]
    private static void Step_384(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.A;
        var result = left + right;
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x88: ADC A, B [0]
    private static void Step_385(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.B;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x89: ADC A, C [0]
    private static void Step_386(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.C;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x8A: ADC A, D [0]
    private static void Step_387(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.D;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x8B: ADC A, E [0]
    private static void Step_388(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.E;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x8C: ADC A, H [0]
    private static void Step_389(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.H;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x8D: ADC A, L [0]
    private static void Step_390(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.L;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x8E: ADC A, (HL) [0]
    private static void Step_391(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
    }

    // 0x8E: ADC A, (HL) [1]
    private static void Step_392(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0x8E: ADC A, (HL) [2]
    private static void Step_393(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x8F: ADC A, A [0]
    private static void Step_394(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.A;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x90: SUB B [0]
    private static void Step_395(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.B;
        var result = left - right;
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x91: SUB C [0]
    private static void Step_396(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.C;
        var result = left - right;
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x92: SUB D [0]
    private static void Step_397(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.D;
        var result = left - right;
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x93: SUB E [0]
    private static void Step_398(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.E;
        var result = left - right;
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x94: SUB H [0]
    private static void Step_399(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.H;
        var result = left - right;
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x95: SUB L [0]
    private static void Step_400(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.L;
        var result = left - right;
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x96: SUB (HL) [0]
    private static void Step_401(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
    }

    // 0x96: SUB (HL) [1]
    private static void Step_402(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left - right;
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0x96: SUB (HL) [2]
    private static void Step_403(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x97: SUB A [0]
    private static void Step_404(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.A;
        var result = left - right;
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x98: SBC B [0]
    private static void Step_405(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.B;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x99: SBC C [0]
    private static void Step_406(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.C;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x9A: SBC D [0]
    private static void Step_407(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.D;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x9B: SBC E [0]
    private static void Step_408(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.E;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x9C: SBC H [0]
    private static void Step_409(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.H;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x9D: SBC L [0]
    private static void Step_410(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.L;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0x9E: SBC (HL) [0]
    private static void Step_411(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
    }

    // 0x9E: SBC (HL) [1]
    private static void Step_412(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0x9E: SBC (HL) [2]
    private static void Step_413(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0x9F: SBC A [0]
    private static void Step_414(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.A;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xA0: AND B [0]
    private static void Step_415(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A &= emulator.B;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset C and N.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xA1: AND C [0]
    private static void Step_416(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A &= emulator.C;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset C and N.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xA2: AND D [0]
    private static void Step_417(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A &= emulator.D;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset C and N.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xA3: AND E [0]
    private static void Step_418(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A &= emulator.E;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset C and N.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xA4: AND H [0]
    private static void Step_419(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A &= emulator.H;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset C and N.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xA5: AND L [0]
    private static void Step_420(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A &= emulator.L;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset C and N.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xA6: AND (HL) [0]
    private static void Step_421(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
    }

    // 0xA6: AND (HL) [1]
    private static void Step_422(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A &= emulator.data;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset C and N.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xA6: AND (HL) [2]
    private static void Step_423(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xA7: AND A [0]
    private static void Step_424(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A &= emulator.A;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset C and N.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xA8: XOR B [0]
    private static void Step_425(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A ^= emulator.B;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xA9: XOR C [0]
    private static void Step_426(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A ^= emulator.C;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xAA: XOR D [0]
    private static void Step_427(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A ^= emulator.D;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xAB: XOR E [0]
    private static void Step_428(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A ^= emulator.E;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xAC: XOR H [0]
    private static void Step_429(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A ^= emulator.H;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xAD: XOR L [0]
    private static void Step_430(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A ^= emulator.L;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xAE: XOR (HL) [0]
    private static void Step_431(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
    }

    // 0xAE: XOR (HL) [1]
    private static void Step_432(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A ^= emulator.data;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xAE: XOR (HL) [2]
    private static void Step_433(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xAF: XOR A [0]
    private static void Step_434(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A ^= emulator.A;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xB0: OR B [0]
    private static void Step_435(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A |= emulator.B;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xB1: OR C [0]
    private static void Step_436(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A |= emulator.C;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xB2: OR D [0]
    private static void Step_437(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A |= emulator.D;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xB3: OR E [0]
    private static void Step_438(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A |= emulator.E;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xB4: OR H [0]
    private static void Step_439(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A |= emulator.H;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xB5: OR L [0]
    private static void Step_440(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A |= emulator.L;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xB6: OR (HL) [0]
    private static void Step_441(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
    }

    // 0xB6: OR (HL) [1]
    private static void Step_442(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A |= emulator.data;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xB6: OR (HL) [2]
    private static void Step_443(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xB7: OR A [0]
    private static void Step_444(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A |= emulator.A;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xB8: CP B [0]
    private static void Step_445(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.B;
        var result = left - right;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= right & 0b00101000; // Copy X and Y from right.
        flags |= result & 0b10000000; // Copy S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 6; // Set Z if is_zero(result) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xB9: CP C [0]
    private static void Step_446(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.C;
        var result = left - right;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= right & 0b00101000; // Copy X and Y from right.
        flags |= result & 0b10000000; // Copy S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 6; // Set Z if is_zero(result) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xBA: CP D [0]
    private static void Step_447(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.D;
        var result = left - right;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= right & 0b00101000; // Copy X and Y from right.
        flags |= result & 0b10000000; // Copy S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 6; // Set Z if is_zero(result) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xBB: CP E [0]
    private static void Step_448(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.E;
        var result = left - right;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= right & 0b00101000; // Copy X and Y from right.
        flags |= result & 0b10000000; // Copy S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 6; // Set Z if is_zero(result) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xBC: CP H [0]
    private static void Step_449(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.H;
        var result = left - right;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= right & 0b00101000; // Copy X and Y from right.
        flags |= result & 0b10000000; // Copy S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 6; // Set Z if is_zero(result) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xBD: CP L [0]
    private static void Step_450(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.L;
        var result = left - right;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= right & 0b00101000; // Copy X and Y from right.
        flags |= result & 0b10000000; // Copy S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 6; // Set Z if is_zero(result) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xBE: CP (HL) [0]
    private static void Step_451(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
    }

    // 0xBE: CP (HL) [1]
    private static void Step_452(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left - right;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= right & 0b00101000; // Copy X and Y from right.
        flags |= result & 0b10000000; // Copy S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 6; // Set Z if is_zero(result) is true.
        emulator.F = (byte)flags;
    }

    // 0xBE: CP (HL) [2]
    private static void Step_453(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xBF: CP A [0]
    private static void Step_454(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.A;
        var result = left - right;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= right & 0b00101000; // Copy X and Y from right.
        flags |= result & 0b10000000; // Copy S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 6; // Set Z if is_zero(result) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xC0: RET NZ [0]
    private static void Step_455(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b01000000) == 0b01000000 /* !condition.NZ */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xC0: RET NZ [1]
    private static void Step_456(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xC0: RET NZ [2]
    private static void Step_457(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xC0: RET NZ [4]
    private static void Step_459(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xC0: RET NZ [5]
    private static void Step_460(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
        emulator.PC = emulator.WZ;
    }

    // 0xC0: RET NZ [6]
    private static void Step_461(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xC1: POP BC [0]
    private static void Step_462(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xC1: POP BC [1]
    private static void Step_463(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.C = emulator.data;
    }

    // 0xC1: POP BC [3]
    private static void Step_465(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xC1: POP BC [4]
    private static void Step_466(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B = emulator.data;
    }

    // 0xC1: POP BC [5]
    private static void Step_467(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xC2: JP NZ [0]
    private static void Step_468(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xC2: JP NZ [1]
    private static void Step_469(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xC2: JP NZ [3]
    private static void Step_471(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xC2: JP NZ [4]
    private static void Step_472(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xC2: JP NZ [5]
    private static void Step_473(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b01000000) == 0b00000000 /* condition.NZ */)
        {
            emulator.PC = emulator.WZ;
        }

        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xC3: JP [0]
    private static void Step_474(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xC3: JP [1]
    private static void Step_475(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xC3: JP [3]
    private static void Step_477(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xC3: JP [4]
    private static void Step_478(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
        emulator.PC = emulator.WZ;
    }

    // 0xC3: JP [5]
    private static void Step_479(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xC4: CALL NZ, nn [0]
    private static void Step_480(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xC4: CALL NZ, nn [1]
    private static void Step_481(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xC4: CALL NZ, nn [3]
    private static void Step_483(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xC4: CALL NZ, nn [4]
    private static void Step_484(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xC4: CALL NZ, nn [5]
    private static void Step_485(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b01000000) == 0b01000000 /* !condition.NZ */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xC4: CALL NZ, nn [7]
    private static void Step_487(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xC4: CALL NZ, nn [10]
    private static void Step_490(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = emulator.WZ;
    }

    // 0xC4: CALL NZ, nn [12]
    private static void Step_492(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xC5: PUSH BC [1]
    private static void Step_494(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.B;
    }

    // 0xC5: PUSH BC [4]
    private static void Step_497(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.C;
    }

    // 0xC5: PUSH BC [6]
    private static void Step_499(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xC6: ADD A, n [0]
    private static void Step_500(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xC6: ADD A, n [2]
    private static void Step_502(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left + right;
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xC7: RST 0x00 [1]
    private static void Step_504(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xC7: RST 0x00 [4]
    private static void Step_507(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = 0x00;
        emulator.WZ = 0x00;
    }

    // 0xC7: RST 0x00 [6]
    private static void Step_509(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xC8: RET Z [0]
    private static void Step_510(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b01000000) == 0b00000000 /* !condition.Z */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xC8: RET Z [1]
    private static void Step_511(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xC8: RET Z [2]
    private static void Step_512(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xC8: RET Z [4]
    private static void Step_514(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xC8: RET Z [5]
    private static void Step_515(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
        emulator.PC = emulator.WZ;
    }

    // 0xC8: RET Z [6]
    private static void Step_516(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xC9: RET [0]
    private static void Step_517(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xC9: RET [1]
    private static void Step_518(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xC9: RET [3]
    private static void Step_520(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xC9: RET [4]
    private static void Step_521(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
        emulator.PC = emulator.WZ;
    }

    // 0xC9: RET [5]
    private static void Step_522(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCA: JP Z [0]
    private static void Step_523(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xCA: JP Z [1]
    private static void Step_524(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xCA: JP Z [3]
    private static void Step_526(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xCA: JP Z [4]
    private static void Step_527(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xCA: JP Z [5]
    private static void Step_528(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b01000000) == 0b01000000 /* condition.Z */)
        {
            emulator.PC = emulator.WZ;
        }

        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCC: CALL Z, nn [0]
    private static void Step_529(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xCC: CALL Z, nn [1]
    private static void Step_530(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xCC: CALL Z, nn [3]
    private static void Step_532(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xCC: CALL Z, nn [4]
    private static void Step_533(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xCC: CALL Z, nn [5]
    private static void Step_534(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b01000000) == 0b00000000 /* !condition.Z */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xCC: CALL Z, nn [7]
    private static void Step_536(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xCC: CALL Z, nn [10]
    private static void Step_539(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = emulator.WZ;
    }

    // 0xCC: CALL Z, nn [12]
    private static void Step_541(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCD: CALL nn [0]
    private static void Step_542(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xCD: CALL nn [1]
    private static void Step_543(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xCD: CALL nn [3]
    private static void Step_545(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xCD: CALL nn [4]
    private static void Step_546(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xCD: CALL nn [7]
    private static void Step_549(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xCD: CALL nn [10]
    private static void Step_552(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = emulator.WZ;
    }

    // 0xCD: CALL nn [12]
    private static void Step_554(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCE: ADC A, n [0]
    private static void Step_555(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xCE: ADC A, n [2]
    private static void Step_557(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCF: RST 0x08 [1]
    private static void Step_559(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xCF: RST 0x08 [4]
    private static void Step_562(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = 0x08;
        emulator.WZ = 0x08;
    }

    // 0xCF: RST 0x08 [6]
    private static void Step_564(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xD0: RET NC [0]
    private static void Step_565(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000001) == 0b00000001 /* !condition.NC */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xD0: RET NC [1]
    private static void Step_566(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xD0: RET NC [2]
    private static void Step_567(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xD0: RET NC [4]
    private static void Step_569(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xD0: RET NC [5]
    private static void Step_570(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
        emulator.PC = emulator.WZ;
    }

    // 0xD0: RET NC [6]
    private static void Step_571(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xD1: POP DE [0]
    private static void Step_572(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xD1: POP DE [1]
    private static void Step_573(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.E = emulator.data;
    }

    // 0xD1: POP DE [3]
    private static void Step_575(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xD1: POP DE [4]
    private static void Step_576(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.D = emulator.data;
    }

    // 0xD1: POP DE [5]
    private static void Step_577(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xD2: JP NC [0]
    private static void Step_578(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xD2: JP NC [1]
    private static void Step_579(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xD2: JP NC [3]
    private static void Step_581(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xD2: JP NC [4]
    private static void Step_582(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xD2: JP NC [5]
    private static void Step_583(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000001) == 0b00000000 /* condition.NC */)
        {
            emulator.PC = emulator.WZ;
        }

        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xD3: OUT (n), A [0]
    private static void Step_584(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xD3: OUT (n), A [1]
    private static void Step_585(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
        emulator.W = emulator.A;
    }

    // 0xD3: OUT (n), A [3]
    private static void Step_587(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.data = emulator.A;
    }

    // 0xD3: OUT (n), A [5]
    private static void Step_589(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z += 0x01;
    }

    // 0xD3: OUT (n), A [6]
    private static void Step_590(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xD4: CALL NC, nn [0]
    private static void Step_591(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xD4: CALL NC, nn [1]
    private static void Step_592(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xD4: CALL NC, nn [3]
    private static void Step_594(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xD4: CALL NC, nn [4]
    private static void Step_595(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xD4: CALL NC, nn [5]
    private static void Step_596(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000001) == 0b00000001 /* !condition.NC */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xD4: CALL NC, nn [7]
    private static void Step_598(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xD4: CALL NC, nn [10]
    private static void Step_601(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = emulator.WZ;
    }

    // 0xD4: CALL NC, nn [12]
    private static void Step_603(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xD5: PUSH DE [1]
    private static void Step_605(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.D;
    }

    // 0xD5: PUSH DE [4]
    private static void Step_608(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.E;
    }

    // 0xD5: PUSH DE [6]
    private static void Step_610(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xD6: SUB n [0]
    private static void Step_611(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xD6: SUB n [1]
    private static void Step_612(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left - right;
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xD6: SUB n [2]
    private static void Step_613(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xD7: RST 0x10 [1]
    private static void Step_615(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xD7: RST 0x10 [4]
    private static void Step_618(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = 0x10;
        emulator.WZ = 0x10;
    }

    // 0xD7: RST 0x10 [6]
    private static void Step_620(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xD8: RET C [0]
    private static void Step_621(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000001) == 0b00000000 /* !condition.C */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xD8: RET C [1]
    private static void Step_622(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xD8: RET C [2]
    private static void Step_623(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xD8: RET C [4]
    private static void Step_625(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xD8: RET C [5]
    private static void Step_626(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
        emulator.PC = emulator.WZ;
    }

    // 0xD8: RET C [6]
    private static void Step_627(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xD9: EXX [0]
    private static void Step_628(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.Shadow_BC;
        emulator.Shadow_BC = emulator.BC;
        emulator.BC = temp;
        temp = emulator.Shadow_DE;
        emulator.Shadow_DE = emulator.DE;
        emulator.DE = temp;
        temp = emulator.Shadow_HL;
        emulator.Shadow_HL = emulator.HL;
        emulator.HL = temp;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDA: JP C [0]
    private static void Step_629(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDA: JP C [1]
    private static void Step_630(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDA: JP C [3]
    private static void Step_632(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDA: JP C [4]
    private static void Step_633(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDA: JP C [5]
    private static void Step_634(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000001) == 0b00000001 /* condition.C */)
        {
            emulator.PC = emulator.WZ;
        }

        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDB: IN A, (n) [0]
    private static void Step_635(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDB: IN A, (n) [1]
    private static void Step_636(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
        emulator.W = emulator.A;
    }

    // 0xDB: IN A, (n) [3]
    private static void Step_638(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.data = emulator.A;
    }

    // 0xDB: IN A, (n) [5]
    private static void Step_640(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A = emulator.data;
        emulator.WZ += 0x01;
    }

    // 0xDB: IN A, (n) [6]
    private static void Step_641(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDC: CALL C, nn [0]
    private static void Step_642(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDC: CALL C, nn [1]
    private static void Step_643(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDC: CALL C, nn [3]
    private static void Step_645(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDC: CALL C, nn [4]
    private static void Step_646(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDC: CALL C, nn [5]
    private static void Step_647(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000001) == 0b00000000 /* !condition.C */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xDC: CALL C, nn [7]
    private static void Step_649(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xDC: CALL C, nn [10]
    private static void Step_652(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = emulator.WZ;
    }

    // 0xDC: CALL C, nn [12]
    private static void Step_654(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDE: SBC n [0]
    private static void Step_655(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDE: SBC n [1]
    private static void Step_656(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xDE: SBC n [2]
    private static void Step_657(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDF: RST 0x18 [1]
    private static void Step_659(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xDF: RST 0x18 [4]
    private static void Step_662(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = 0x18;
        emulator.WZ = 0x18;
    }

    // 0xDF: RST 0x18 [6]
    private static void Step_664(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xE0: RET PO [0]
    private static void Step_665(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000100) == 0b00000100 /* !condition.PO */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xE0: RET PO [1]
    private static void Step_666(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xE0: RET PO [2]
    private static void Step_667(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xE0: RET PO [4]
    private static void Step_669(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xE0: RET PO [5]
    private static void Step_670(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
        emulator.PC = emulator.WZ;
    }

    // 0xE0: RET PO [6]
    private static void Step_671(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xE1: POP HL [0]
    private static void Step_672(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xE1: POP HL [1]
    private static void Step_673(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.L = emulator.data;
    }

    // 0xE1: POP HL [3]
    private static void Step_675(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xE1: POP HL [4]
    private static void Step_676(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.H = emulator.data;
    }

    // 0xE1: POP HL [5]
    private static void Step_677(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xE2: JP PO [0]
    private static void Step_678(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xE2: JP PO [1]
    private static void Step_679(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xE2: JP PO [3]
    private static void Step_681(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xE2: JP PO [4]
    private static void Step_682(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xE2: JP PO [5]
    private static void Step_683(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000100) == 0b00000000 /* condition.PO */)
        {
            emulator.PC = emulator.WZ;
        }

        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xE3: EX (SP); HL [0]
    private static void Step_684(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
    }

    // 0xE3: EX (SP); HL [1]
    private static void Step_685(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xE3: EX (SP); HL [3]
    private static void Step_687(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = (ushort)(emulator.SP + 0x01);
    }

    // 0xE3: EX (SP); HL [4]
    private static void Step_688(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xE3: EX (SP); HL [7]
    private static void Step_691(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = emulator.H;
    }

    // 0xE3: EX (SP); HL [10]
    private static void Step_694(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.data = emulator.L;
    }

    // 0xE3: EX (SP); HL [14]
    private static void Step_698(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.HL = emulator.WZ;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xE4: CALL PO, nn [0]
    private static void Step_699(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xE4: CALL PO, nn [1]
    private static void Step_700(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xE4: CALL PO, nn [3]
    private static void Step_702(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xE4: CALL PO, nn [4]
    private static void Step_703(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xE4: CALL PO, nn [5]
    private static void Step_704(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000100) == 0b00000100 /* !condition.PO */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xE4: CALL PO, nn [7]
    private static void Step_706(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xE4: CALL PO, nn [10]
    private static void Step_709(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = emulator.WZ;
    }

    // 0xE4: CALL PO, nn [12]
    private static void Step_711(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xE5: PUSH HL [1]
    private static void Step_713(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.H;
    }

    // 0xE5: PUSH HL [4]
    private static void Step_716(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.L;
    }

    // 0xE5: PUSH HL [6]
    private static void Step_718(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xE6: AND n [0]
    private static void Step_719(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xE6: AND n [1]
    private static void Step_720(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A &= emulator.data;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset C and N.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xE6: AND n [2]
    private static void Step_721(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xE7: RST 0x20 [1]
    private static void Step_723(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xE7: RST 0x20 [4]
    private static void Step_726(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = 0x20;
        emulator.WZ = 0x20;
    }

    // 0xE7: RST 0x20 [6]
    private static void Step_728(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xE8: RET PE [0]
    private static void Step_729(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000100) == 0b00000000 /* !condition.PE */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xE8: RET PE [1]
    private static void Step_730(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xE8: RET PE [2]
    private static void Step_731(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xE8: RET PE [4]
    private static void Step_733(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xE8: RET PE [5]
    private static void Step_734(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
        emulator.PC = emulator.WZ;
    }

    // 0xE8: RET PE [6]
    private static void Step_735(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xE9: JP HL [0]
    private static void Step_736(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.PC = emulator.HL;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xEA: JP PE [0]
    private static void Step_737(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xEA: JP PE [1]
    private static void Step_738(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xEA: JP PE [3]
    private static void Step_740(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xEA: JP PE [4]
    private static void Step_741(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xEA: JP PE [5]
    private static void Step_742(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000100) == 0b00000100 /* condition.PE */)
        {
            emulator.PC = emulator.WZ;
        }

        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xEB: EX DE; HL [0]
    private static void Step_743(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.DE;
        emulator.DE = emulator.HL;
        emulator.HL = temp;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xEC: CALL PE, nn [0]
    private static void Step_744(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xEC: CALL PE, nn [1]
    private static void Step_745(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xEC: CALL PE, nn [3]
    private static void Step_747(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xEC: CALL PE, nn [4]
    private static void Step_748(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xEC: CALL PE, nn [5]
    private static void Step_749(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000100) == 0b00000000 /* !condition.PE */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xEC: CALL PE, nn [7]
    private static void Step_751(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xEC: CALL PE, nn [10]
    private static void Step_754(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = emulator.WZ;
    }

    // 0xEC: CALL PE, nn [12]
    private static void Step_756(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xEE: XOR n [0]
    private static void Step_757(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xEE: XOR n [1]
    private static void Step_758(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A ^= emulator.data;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xEE: XOR n [2]
    private static void Step_759(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xEF: RST 0x28 [1]
    private static void Step_761(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xEF: RST 0x28 [4]
    private static void Step_764(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = 0x28;
        emulator.WZ = 0x28;
    }

    // 0xEF: RST 0x28 [6]
    private static void Step_766(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xF0: RET P [0]
    private static void Step_767(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b10000000) == 0b10000000 /* !condition.P */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xF0: RET P [1]
    private static void Step_768(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xF0: RET P [2]
    private static void Step_769(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xF0: RET P [4]
    private static void Step_771(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xF0: RET P [5]
    private static void Step_772(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
        emulator.PC = emulator.WZ;
    }

    // 0xF0: RET P [6]
    private static void Step_773(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xF1: POP AF [0]
    private static void Step_774(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xF1: POP AF [1]
    private static void Step_775(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.F = emulator.data;
    }

    // 0xF1: POP AF [3]
    private static void Step_777(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xF1: POP AF [4]
    private static void Step_778(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A = emulator.data;
    }

    // 0xF1: POP AF [5]
    private static void Step_779(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xF2: JP P [0]
    private static void Step_780(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xF2: JP P [1]
    private static void Step_781(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xF2: JP P [3]
    private static void Step_783(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xF2: JP P [4]
    private static void Step_784(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xF2: JP P [5]
    private static void Step_785(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b10000000) == 0b00000000 /* condition.P */)
        {
            emulator.PC = emulator.WZ;
        }

        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xF3: DI [0]
    private static void Step_786(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.iff1 = false;
        emulator.iff2 = false;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xF4: CALL P, nn [0]
    private static void Step_787(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xF4: CALL P, nn [1]
    private static void Step_788(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xF4: CALL P, nn [3]
    private static void Step_790(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xF4: CALL P, nn [4]
    private static void Step_791(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xF4: CALL P, nn [5]
    private static void Step_792(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b10000000) == 0b10000000 /* !condition.P */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xF4: CALL P, nn [7]
    private static void Step_794(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xF4: CALL P, nn [10]
    private static void Step_797(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = emulator.WZ;
    }

    // 0xF4: CALL P, nn [12]
    private static void Step_799(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xF5: PUSH AF [1]
    private static void Step_801(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.A;
    }

    // 0xF5: PUSH AF [4]
    private static void Step_804(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.F;
    }

    // 0xF5: PUSH AF [6]
    private static void Step_806(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xF6: OR n [0]
    private static void Step_807(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xF6: OR n [1]
    private static void Step_808(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A |= emulator.data;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xF6: OR n [2]
    private static void Step_809(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xF7: RST 0x30 [1]
    private static void Step_811(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xF7: RST 0x30 [4]
    private static void Step_814(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = 0x30;
        emulator.WZ = 0x30;
    }

    // 0xF7: RST 0x30 [6]
    private static void Step_816(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xF8: RET M [0]
    private static void Step_817(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b10000000) == 0b00000000 /* !condition.M */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xF8: RET M [1]
    private static void Step_818(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xF8: RET M [2]
    private static void Step_819(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xF8: RET M [4]
    private static void Step_821(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xF8: RET M [5]
    private static void Step_822(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
        emulator.PC = emulator.WZ;
    }

    // 0xF8: RET M [6]
    private static void Step_823(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xF9: LD SP, HL [0]
    private static void Step_824(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP = emulator.HL;
    }

    // 0xF9: LD SP, HL [1]
    private static void Step_825(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFA: JP M [0]
    private static void Step_826(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFA: JP M [1]
    private static void Step_827(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xFA: JP M [3]
    private static void Step_829(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFA: JP M [4]
    private static void Step_830(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xFA: JP M [5]
    private static void Step_831(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b10000000) == 0b10000000 /* condition.M */)
        {
            emulator.PC = emulator.WZ;
        }

        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFB: EI [0]
    private static void Step_832(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.iff1 = true;
        emulator.iff2 = true;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        emulator.address = emulator.PC;
        emulator.currentStep = 0x01;
    }

    // 0xFC: CALL M, nn [0]
    private static void Step_833(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFC: CALL M, nn [1]
    private static void Step_834(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xFC: CALL M, nn [3]
    private static void Step_836(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFC: CALL M, nn [4]
    private static void Step_837(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xFC: CALL M, nn [5]
    private static void Step_838(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b10000000) == 0b00000000 /* !condition.M */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xFC: CALL M, nn [7]
    private static void Step_840(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xFC: CALL M, nn [10]
    private static void Step_843(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = emulator.WZ;
    }

    // 0xFC: CALL M, nn [12]
    private static void Step_845(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFE: CP n [0]
    private static void Step_846(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFE: CP n [1]
    private static void Step_847(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left - right;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= right & 0b00101000; // Copy X and Y from right.
        flags |= result & 0b10000000; // Copy S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 6; // Set Z if is_zero(result) is true.
        emulator.F = (byte)flags;
    }

    // 0xFE: CP n [2]
    private static void Step_848(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFF: RST 0x38 [1]
    private static void Step_850(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xFF: RST 0x38 [4]
    private static void Step_853(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = 0x38;
        emulator.WZ = 0x38;
    }

    // 0xFF: RST 0x38 [6]
    private static void Step_855(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0x00: RLC B [0]
    private static void Step_856(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.B;
        emulator.B = (byte)(emulator.B << 0x01 | emulator.B >> 0x07);

        // Update flags.
        // Reset N and H.
        int flags = emulator.B & 0b10101000; // Copy X, Y and S from B.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.B) & 0x01)) << 2; // Set PV if even_parity(B) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0)) << 6; // Set Z if is_zero(B) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x01: RLC C [0]
    private static void Step_857(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.C;
        emulator.C = (byte)(emulator.C << 0x01 | emulator.C >> 0x07);

        // Update flags.
        // Reset N and H.
        int flags = emulator.C & 0b10101000; // Copy X, Y and S from C.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.C) & 0x01)) << 2; // Set PV if even_parity(C) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.C == 0)) << 6; // Set Z if is_zero(C) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x02: RLC D [0]
    private static void Step_858(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.D;
        emulator.D = (byte)(emulator.D << 0x01 | emulator.D >> 0x07);

        // Update flags.
        // Reset N and H.
        int flags = emulator.D & 0b10101000; // Copy X, Y and S from D.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.D) & 0x01)) << 2; // Set PV if even_parity(D) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.D == 0)) << 6; // Set Z if is_zero(D) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x03: RLC E [0]
    private static void Step_859(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.E;
        emulator.E = (byte)(emulator.E << 0x01 | emulator.E >> 0x07);

        // Update flags.
        // Reset N and H.
        int flags = emulator.E & 0b10101000; // Copy X, Y and S from E.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.E) & 0x01)) << 2; // Set PV if even_parity(E) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.E == 0)) << 6; // Set Z if is_zero(E) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x04: RLC H [0]
    private static void Step_860(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.H;
        emulator.H = (byte)(emulator.H << 0x01 | emulator.H >> 0x07);

        // Update flags.
        // Reset N and H.
        int flags = emulator.H & 0b10101000; // Copy X, Y and S from H.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.H) & 0x01)) << 2; // Set PV if even_parity(H) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.H == 0)) << 6; // Set Z if is_zero(H) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x05: RLC L [0]
    private static void Step_861(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.L;
        emulator.L = (byte)(emulator.L << 0x01 | emulator.L >> 0x07);

        // Update flags.
        // Reset N and H.
        int flags = emulator.L & 0b10101000; // Copy X, Y and S from L.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.L) & 0x01)) << 2; // Set PV if even_parity(L) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.L == 0)) << 6; // Set Z if is_zero(L) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x06: RLC (HL) [0]
    private static void Step_862(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0x06: RLC (HL) [3]
    private static void Step_865(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | emulator.data >> 0x07);

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // 0xCB 0x06: RLC (HL) [6]
    private static void Step_868(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0x07: RLC A [0]
    private static void Step_869(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.A;
        emulator.A = (byte)(emulator.A << 0x01 | emulator.A >> 0x07);

        // Update flags.
        // Reset N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x08: RRC B [0]
    private static void Step_870(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.B;
        emulator.B = (byte)(emulator.B >> 0x01 | emulator.B << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.B & 0b10101000; // Copy X, Y and S from B.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.B) & 0x01)) << 2; // Set PV if even_parity(B) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0)) << 6; // Set Z if is_zero(B) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x09: RRC C [0]
    private static void Step_871(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.C;
        emulator.C = (byte)(emulator.C >> 0x01 | emulator.C << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.C & 0b10101000; // Copy X, Y and S from C.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.C) & 0x01)) << 2; // Set PV if even_parity(C) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.C == 0)) << 6; // Set Z if is_zero(C) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x0A: RRC D [0]
    private static void Step_872(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.D;
        emulator.D = (byte)(emulator.D >> 0x01 | emulator.D << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.D & 0b10101000; // Copy X, Y and S from D.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.D) & 0x01)) << 2; // Set PV if even_parity(D) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.D == 0)) << 6; // Set Z if is_zero(D) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x0B: RRC E [0]
    private static void Step_873(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.E;
        emulator.E = (byte)(emulator.E >> 0x01 | emulator.E << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.E & 0b10101000; // Copy X, Y and S from E.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.E) & 0x01)) << 2; // Set PV if even_parity(E) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.E == 0)) << 6; // Set Z if is_zero(E) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x0C: RRC H [0]
    private static void Step_874(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.H;
        emulator.H = (byte)(emulator.H >> 0x01 | emulator.H << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.H & 0b10101000; // Copy X, Y and S from H.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.H) & 0x01)) << 2; // Set PV if even_parity(H) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.H == 0)) << 6; // Set Z if is_zero(H) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x0D: RRC L [0]
    private static void Step_875(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.L;
        emulator.L = (byte)(emulator.L >> 0x01 | emulator.L << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.L & 0b10101000; // Copy X, Y and S from L.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.L) & 0x01)) << 2; // Set PV if even_parity(L) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.L == 0)) << 6; // Set Z if is_zero(L) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x0E: RRC (HL) [0]
    private static void Step_876(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0x0E: RRC (HL) [3]
    private static void Step_879(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // 0xCB 0x0E: RRC (HL) [6]
    private static void Step_882(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0x0F: RRC A [0]
    private static void Step_883(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.A;
        emulator.A = (byte)(emulator.A >> 0x01 | emulator.A << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x10: RL B [0]
    private static void Step_884(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.B;
        emulator.B = (byte)(emulator.B << 0x01 | (emulator.F & 0b00000001 /* flag.C */));

        // Update flags.
        // Reset N and H.
        int flags = emulator.B & 0b10101000; // Copy X, Y and S from B.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.B) & 0x01)) << 2; // Set PV if even_parity(B) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0)) << 6; // Set Z if is_zero(B) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x11: RL C [0]
    private static void Step_885(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.C;
        emulator.C = (byte)(emulator.C << 0x01 | (emulator.F & 0b00000001 /* flag.C */));

        // Update flags.
        // Reset N and H.
        int flags = emulator.C & 0b10101000; // Copy X, Y and S from C.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.C) & 0x01)) << 2; // Set PV if even_parity(C) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.C == 0)) << 6; // Set Z if is_zero(C) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x12: RL D [0]
    private static void Step_886(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.D;
        emulator.D = (byte)(emulator.D << 0x01 | (emulator.F & 0b00000001 /* flag.C */));

        // Update flags.
        // Reset N and H.
        int flags = emulator.D & 0b10101000; // Copy X, Y and S from D.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.D) & 0x01)) << 2; // Set PV if even_parity(D) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.D == 0)) << 6; // Set Z if is_zero(D) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x13: RL E [0]
    private static void Step_887(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.E;
        emulator.E = (byte)(emulator.E << 0x01 | (emulator.F & 0b00000001 /* flag.C */));

        // Update flags.
        // Reset N and H.
        int flags = emulator.E & 0b10101000; // Copy X, Y and S from E.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.E) & 0x01)) << 2; // Set PV if even_parity(E) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.E == 0)) << 6; // Set Z if is_zero(E) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x14: RL H [0]
    private static void Step_888(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.H;
        emulator.H = (byte)(emulator.H << 0x01 | (emulator.F & 0b00000001 /* flag.C */));

        // Update flags.
        // Reset N and H.
        int flags = emulator.H & 0b10101000; // Copy X, Y and S from H.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.H) & 0x01)) << 2; // Set PV if even_parity(H) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.H == 0)) << 6; // Set Z if is_zero(H) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x15: RL L [0]
    private static void Step_889(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.L;
        emulator.L = (byte)(emulator.L << 0x01 | (emulator.F & 0b00000001 /* flag.C */));

        // Update flags.
        // Reset N and H.
        int flags = emulator.L & 0b10101000; // Copy X, Y and S from L.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.L) & 0x01)) << 2; // Set PV if even_parity(L) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.L == 0)) << 6; // Set Z if is_zero(L) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x16: RL (HL) [0]
    private static void Step_890(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0x16: RL (HL) [3]
    private static void Step_893(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | (emulator.F & 0b00000001 /* flag.C */));

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // 0xCB 0x16: RL (HL) [6]
    private static void Step_896(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0x17: RL A [0]
    private static void Step_897(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.A;
        emulator.A = (byte)(emulator.A << 0x01 | (emulator.F & 0b00000001 /* flag.C */));

        // Update flags.
        // Reset N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x18: RR B [0]
    private static void Step_898(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.B;
        emulator.B = (byte)(emulator.B >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.B & 0b10101000; // Copy X, Y and S from B.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.B) & 0x01)) << 2; // Set PV if even_parity(B) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0)) << 6; // Set Z if is_zero(B) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x19: RR C [0]
    private static void Step_899(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.C;
        emulator.C = (byte)(emulator.C >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.C & 0b10101000; // Copy X, Y and S from C.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.C) & 0x01)) << 2; // Set PV if even_parity(C) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.C == 0)) << 6; // Set Z if is_zero(C) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x1A: RR D [0]
    private static void Step_900(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.D;
        emulator.D = (byte)(emulator.D >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.D & 0b10101000; // Copy X, Y and S from D.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.D) & 0x01)) << 2; // Set PV if even_parity(D) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.D == 0)) << 6; // Set Z if is_zero(D) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x1B: RR E [0]
    private static void Step_901(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.E;
        emulator.E = (byte)(emulator.E >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.E & 0b10101000; // Copy X, Y and S from E.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.E) & 0x01)) << 2; // Set PV if even_parity(E) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.E == 0)) << 6; // Set Z if is_zero(E) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x1C: RR H [0]
    private static void Step_902(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.H;
        emulator.H = (byte)(emulator.H >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.H & 0b10101000; // Copy X, Y and S from H.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.H) & 0x01)) << 2; // Set PV if even_parity(H) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.H == 0)) << 6; // Set Z if is_zero(H) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x1D: RR L [0]
    private static void Step_903(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.L;
        emulator.L = (byte)(emulator.L >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.L & 0b10101000; // Copy X, Y and S from L.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.L) & 0x01)) << 2; // Set PV if even_parity(L) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.L == 0)) << 6; // Set Z if is_zero(L) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x1E: RR (HL) [0]
    private static void Step_904(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0x1E: RR (HL) [3]
    private static void Step_907(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // 0xCB 0x1E: RR (HL) [6]
    private static void Step_910(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0x1F: RR A [0]
    private static void Step_911(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.A;
        emulator.A = (byte)(emulator.A >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x20: SLA B [0]
    private static void Step_912(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.B;
        emulator.B <<= 0x01;

        // Update flags.
        // Reset N and H.
        int flags = emulator.B & 0b10101000; // Copy X, Y and S from B.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.B) & 0x01)) << 2; // Set PV if even_parity(B) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0)) << 6; // Set Z if is_zero(B) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x21: SLA C [0]
    private static void Step_913(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.C;
        emulator.C <<= 0x01;

        // Update flags.
        // Reset N and H.
        int flags = emulator.C & 0b10101000; // Copy X, Y and S from C.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.C) & 0x01)) << 2; // Set PV if even_parity(C) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.C == 0)) << 6; // Set Z if is_zero(C) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x22: SLA D [0]
    private static void Step_914(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.D;
        emulator.D <<= 0x01;

        // Update flags.
        // Reset N and H.
        int flags = emulator.D & 0b10101000; // Copy X, Y and S from D.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.D) & 0x01)) << 2; // Set PV if even_parity(D) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.D == 0)) << 6; // Set Z if is_zero(D) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x23: SLA E [0]
    private static void Step_915(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.E;
        emulator.E <<= 0x01;

        // Update flags.
        // Reset N and H.
        int flags = emulator.E & 0b10101000; // Copy X, Y and S from E.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.E) & 0x01)) << 2; // Set PV if even_parity(E) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.E == 0)) << 6; // Set Z if is_zero(E) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x24: SLA H [0]
    private static void Step_916(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.H;
        emulator.H <<= 0x01;

        // Update flags.
        // Reset N and H.
        int flags = emulator.H & 0b10101000; // Copy X, Y and S from H.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.H) & 0x01)) << 2; // Set PV if even_parity(H) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.H == 0)) << 6; // Set Z if is_zero(H) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x25: SLA L [0]
    private static void Step_917(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.L;
        emulator.L <<= 0x01;

        // Update flags.
        // Reset N and H.
        int flags = emulator.L & 0b10101000; // Copy X, Y and S from L.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.L) & 0x01)) << 2; // Set PV if even_parity(L) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.L == 0)) << 6; // Set Z if is_zero(L) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x26: SLA (HL) [0]
    private static void Step_918(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0x26: SLA (HL) [3]
    private static void Step_921(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data <<= 0x01;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // 0xCB 0x26: SLA (HL) [6]
    private static void Step_924(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0x27: SLA A [0]
    private static void Step_925(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.A;
        emulator.A <<= 0x01;

        // Update flags.
        // Reset N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x28: SRA B [0]
    private static void Step_926(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.B;
        emulator.B = (byte)(emulator.B >> 0x01 | emulator.B & 0x80);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.B & 0b10101000; // Copy X, Y and S from B.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.B) & 0x01)) << 2; // Set PV if even_parity(B) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0)) << 6; // Set Z if is_zero(B) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x29: SRA C [0]
    private static void Step_927(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.C;
        emulator.C = (byte)(emulator.C >> 0x01 | emulator.C & 0x80);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.C & 0b10101000; // Copy X, Y and S from C.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.C) & 0x01)) << 2; // Set PV if even_parity(C) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.C == 0)) << 6; // Set Z if is_zero(C) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x2A: SRA D [0]
    private static void Step_928(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.D;
        emulator.D = (byte)(emulator.D >> 0x01 | emulator.D & 0x80);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.D & 0b10101000; // Copy X, Y and S from D.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.D) & 0x01)) << 2; // Set PV if even_parity(D) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.D == 0)) << 6; // Set Z if is_zero(D) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x2B: SRA E [0]
    private static void Step_929(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.E;
        emulator.E = (byte)(emulator.E >> 0x01 | emulator.E & 0x80);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.E & 0b10101000; // Copy X, Y and S from E.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.E) & 0x01)) << 2; // Set PV if even_parity(E) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.E == 0)) << 6; // Set Z if is_zero(E) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x2C: SRA H [0]
    private static void Step_930(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.H;
        emulator.H = (byte)(emulator.H >> 0x01 | emulator.H & 0x80);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.H & 0b10101000; // Copy X, Y and S from H.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.H) & 0x01)) << 2; // Set PV if even_parity(H) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.H == 0)) << 6; // Set Z if is_zero(H) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x2D: SRA L [0]
    private static void Step_931(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.L;
        emulator.L = (byte)(emulator.L >> 0x01 | emulator.L & 0x80);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.L & 0b10101000; // Copy X, Y and S from L.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.L) & 0x01)) << 2; // Set PV if even_parity(L) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.L == 0)) << 6; // Set Z if is_zero(L) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x2E: SRA (HL) [0]
    private static void Step_932(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0x2E: SRA (HL) [3]
    private static void Step_935(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data & 0x80);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // 0xCB 0x2E: SRA (HL) [6]
    private static void Step_938(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0x2F: SRA A [0]
    private static void Step_939(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.A;
        emulator.A = (byte)(emulator.A >> 0x01 | emulator.A & 0x80);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x30: SLL B [0]
    private static void Step_940(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.B;
        emulator.B = (byte)(emulator.B << 0x01 | 0x01);

        // Update flags.
        // Reset N and H.
        int flags = emulator.B & 0b10101000; // Copy X, Y and S from B.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.B) & 0x01)) << 2; // Set PV if even_parity(B) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0)) << 6; // Set Z if is_zero(B) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x31: SLL C [0]
    private static void Step_941(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.C;
        emulator.C = (byte)(emulator.C << 0x01 | 0x01);

        // Update flags.
        // Reset N and H.
        int flags = emulator.C & 0b10101000; // Copy X, Y and S from C.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.C) & 0x01)) << 2; // Set PV if even_parity(C) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.C == 0)) << 6; // Set Z if is_zero(C) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x32: SLL D [0]
    private static void Step_942(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.D;
        emulator.D = (byte)(emulator.D << 0x01 | 0x01);

        // Update flags.
        // Reset N and H.
        int flags = emulator.D & 0b10101000; // Copy X, Y and S from D.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.D) & 0x01)) << 2; // Set PV if even_parity(D) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.D == 0)) << 6; // Set Z if is_zero(D) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x33: SLL E [0]
    private static void Step_943(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.E;
        emulator.E = (byte)(emulator.E << 0x01 | 0x01);

        // Update flags.
        // Reset N and H.
        int flags = emulator.E & 0b10101000; // Copy X, Y and S from E.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.E) & 0x01)) << 2; // Set PV if even_parity(E) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.E == 0)) << 6; // Set Z if is_zero(E) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x34: SLL H [0]
    private static void Step_944(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.H;
        emulator.H = (byte)(emulator.H << 0x01 | 0x01);

        // Update flags.
        // Reset N and H.
        int flags = emulator.H & 0b10101000; // Copy X, Y and S from H.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.H) & 0x01)) << 2; // Set PV if even_parity(H) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.H == 0)) << 6; // Set Z if is_zero(H) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x35: SLL L [0]
    private static void Step_945(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.L;
        emulator.L = (byte)(emulator.L << 0x01 | 0x01);

        // Update flags.
        // Reset N and H.
        int flags = emulator.L & 0b10101000; // Copy X, Y and S from L.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.L) & 0x01)) << 2; // Set PV if even_parity(L) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.L == 0)) << 6; // Set Z if is_zero(L) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x36: SLL (HL) [0]
    private static void Step_946(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0x36: SLL (HL) [3]
    private static void Step_949(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | 0x01);

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // 0xCB 0x36: SLL (HL) [6]
    private static void Step_952(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0x37: SLL A [0]
    private static void Step_953(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.A;
        emulator.A = (byte)(emulator.A << 0x01 | 0x01);

        // Update flags.
        // Reset N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x38: SRL B [0]
    private static void Step_954(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.B;
        emulator.B = (byte)(emulator.B >> 0x01 & 0x7F);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.B & 0b10101000; // Copy X, Y and S from B.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.B) & 0x01)) << 2; // Set PV if even_parity(B) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0)) << 6; // Set Z if is_zero(B) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x39: SRL C [0]
    private static void Step_955(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.C;
        emulator.C = (byte)(emulator.C >> 0x01 & 0x7F);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.C & 0b10101000; // Copy X, Y and S from C.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.C) & 0x01)) << 2; // Set PV if even_parity(C) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.C == 0)) << 6; // Set Z if is_zero(C) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x3A: SRL D [0]
    private static void Step_956(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.D;
        emulator.D = (byte)(emulator.D >> 0x01 & 0x7F);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.D & 0b10101000; // Copy X, Y and S from D.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.D) & 0x01)) << 2; // Set PV if even_parity(D) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.D == 0)) << 6; // Set Z if is_zero(D) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x3B: SRL E [0]
    private static void Step_957(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.E;
        emulator.E = (byte)(emulator.E >> 0x01 & 0x7F);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.E & 0b10101000; // Copy X, Y and S from E.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.E) & 0x01)) << 2; // Set PV if even_parity(E) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.E == 0)) << 6; // Set Z if is_zero(E) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x3C: SRL H [0]
    private static void Step_958(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.H;
        emulator.H = (byte)(emulator.H >> 0x01 & 0x7F);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.H & 0b10101000; // Copy X, Y and S from H.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.H) & 0x01)) << 2; // Set PV if even_parity(H) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.H == 0)) << 6; // Set Z if is_zero(H) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x3D: SRL L [0]
    private static void Step_959(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.L;
        emulator.L = (byte)(emulator.L >> 0x01 & 0x7F);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.L & 0b10101000; // Copy X, Y and S from L.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.L) & 0x01)) << 2; // Set PV if even_parity(L) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.L == 0)) << 6; // Set Z if is_zero(L) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x3E: SRL (HL) [0]
    private static void Step_960(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0x3E: SRL (HL) [3]
    private static void Step_963(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 & 0x7F);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // 0xCB 0x3E: SRL (HL) [6]
    private static void Step_966(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0x3F: SRL A [0]
    private static void Step_967(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.A;
        emulator.A = (byte)(emulator.A >> 0x01 & 0x7F);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x40: BIT 0x00, B [0]
    private static void Step_968(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.B & 0x01;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.B & 0b00101000; // Copy X and Y from B.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x41: BIT 0x00, C [0]
    private static void Step_969(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.C & 0x01;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.C & 0b00101000; // Copy X and Y from C.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x42: BIT 0x00, D [0]
    private static void Step_970(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.D & 0x01;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.D & 0b00101000; // Copy X and Y from D.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x43: BIT 0x00, E [0]
    private static void Step_971(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.E & 0x01;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.E & 0b00101000; // Copy X and Y from E.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x44: BIT 0x00, H [0]
    private static void Step_972(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.H & 0x01;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.H & 0b00101000; // Copy X and Y from H.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x45: BIT 0x00, L [0]
    private static void Step_973(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.L & 0x01;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.L & 0b00101000; // Copy X and Y from L.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x46: BIT 0x00, (HL) [0]
    private static void Step_974(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0x46: BIT 0x00, (HL) [3]
    private static void Step_977(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0x47: BIT 0x00, A [0]
    private static void Step_978(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.A & 0x01;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.A & 0b00101000; // Copy X and Y from A.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x48: BIT 0x01, B [0]
    private static void Step_979(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.B & 0x01 << 0x01;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.B & 0b00101000; // Copy X and Y from B.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x49: BIT 0x01, C [0]
    private static void Step_980(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.C & 0x01 << 0x01;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.C & 0b00101000; // Copy X and Y from C.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x4A: BIT 0x01, D [0]
    private static void Step_981(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.D & 0x01 << 0x01;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.D & 0b00101000; // Copy X and Y from D.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x4B: BIT 0x01, E [0]
    private static void Step_982(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.E & 0x01 << 0x01;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.E & 0b00101000; // Copy X and Y from E.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x4C: BIT 0x01, H [0]
    private static void Step_983(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.H & 0x01 << 0x01;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.H & 0b00101000; // Copy X and Y from H.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x4D: BIT 0x01, L [0]
    private static void Step_984(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.L & 0x01 << 0x01;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.L & 0b00101000; // Copy X and Y from L.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x4E: BIT 0x01, (HL) [0]
    private static void Step_985(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0x4E: BIT 0x01, (HL) [3]
    private static void Step_988(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01 << 0x01;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0x4F: BIT 0x01, A [0]
    private static void Step_989(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.A & 0x01 << 0x01;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.A & 0b00101000; // Copy X and Y from A.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x50: BIT 0x02, B [0]
    private static void Step_990(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.B & 0x01 << 0x02;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.B & 0b00101000; // Copy X and Y from B.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x51: BIT 0x02, C [0]
    private static void Step_991(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.C & 0x01 << 0x02;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.C & 0b00101000; // Copy X and Y from C.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x52: BIT 0x02, D [0]
    private static void Step_992(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.D & 0x01 << 0x02;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.D & 0b00101000; // Copy X and Y from D.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x53: BIT 0x02, E [0]
    private static void Step_993(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.E & 0x01 << 0x02;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.E & 0b00101000; // Copy X and Y from E.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x54: BIT 0x02, H [0]
    private static void Step_994(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.H & 0x01 << 0x02;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.H & 0b00101000; // Copy X and Y from H.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x55: BIT 0x02, L [0]
    private static void Step_995(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.L & 0x01 << 0x02;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.L & 0b00101000; // Copy X and Y from L.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x56: BIT 0x02, (HL) [0]
    private static void Step_996(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0x56: BIT 0x02, (HL) [3]
    private static void Step_999(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01 << 0x02;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0x57: BIT 0x02, A [0]
    private static void Step_1000(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.A & 0x01 << 0x02;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.A & 0b00101000; // Copy X and Y from A.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x58: BIT 0x03, B [0]
    private static void Step_1001(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.B & 0x01 << 0x03;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.B & 0b00101000; // Copy X and Y from B.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x59: BIT 0x03, C [0]
    private static void Step_1002(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.C & 0x01 << 0x03;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.C & 0b00101000; // Copy X and Y from C.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x5A: BIT 0x03, D [0]
    private static void Step_1003(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.D & 0x01 << 0x03;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.D & 0b00101000; // Copy X and Y from D.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x5B: BIT 0x03, E [0]
    private static void Step_1004(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.E & 0x01 << 0x03;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.E & 0b00101000; // Copy X and Y from E.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x5C: BIT 0x03, H [0]
    private static void Step_1005(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.H & 0x01 << 0x03;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.H & 0b00101000; // Copy X and Y from H.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x5D: BIT 0x03, L [0]
    private static void Step_1006(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.L & 0x01 << 0x03;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.L & 0b00101000; // Copy X and Y from L.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x5E: BIT 0x03, (HL) [0]
    private static void Step_1007(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0x5E: BIT 0x03, (HL) [3]
    private static void Step_1010(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01 << 0x03;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0x5F: BIT 0x03, A [0]
    private static void Step_1011(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.A & 0x01 << 0x03;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.A & 0b00101000; // Copy X and Y from A.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x60: BIT 0x04, B [0]
    private static void Step_1012(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.B & 0x01 << 0x04;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.B & 0b00101000; // Copy X and Y from B.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x61: BIT 0x04, C [0]
    private static void Step_1013(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.C & 0x01 << 0x04;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.C & 0b00101000; // Copy X and Y from C.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x62: BIT 0x04, D [0]
    private static void Step_1014(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.D & 0x01 << 0x04;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.D & 0b00101000; // Copy X and Y from D.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x63: BIT 0x04, E [0]
    private static void Step_1015(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.E & 0x01 << 0x04;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.E & 0b00101000; // Copy X and Y from E.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x64: BIT 0x04, H [0]
    private static void Step_1016(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.H & 0x01 << 0x04;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.H & 0b00101000; // Copy X and Y from H.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x65: BIT 0x04, L [0]
    private static void Step_1017(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.L & 0x01 << 0x04;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.L & 0b00101000; // Copy X and Y from L.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x66: BIT 0x04, (HL) [0]
    private static void Step_1018(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0x66: BIT 0x04, (HL) [3]
    private static void Step_1021(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01 << 0x04;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0x67: BIT 0x04, A [0]
    private static void Step_1022(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.A & 0x01 << 0x04;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.A & 0b00101000; // Copy X and Y from A.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x68: BIT 0x05, B [0]
    private static void Step_1023(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.B & 0x01 << 0x05;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.B & 0b00101000; // Copy X and Y from B.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x69: BIT 0x05, C [0]
    private static void Step_1024(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.C & 0x01 << 0x05;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.C & 0b00101000; // Copy X and Y from C.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x6A: BIT 0x05, D [0]
    private static void Step_1025(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.D & 0x01 << 0x05;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.D & 0b00101000; // Copy X and Y from D.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x6B: BIT 0x05, E [0]
    private static void Step_1026(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.E & 0x01 << 0x05;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.E & 0b00101000; // Copy X and Y from E.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x6C: BIT 0x05, H [0]
    private static void Step_1027(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.H & 0x01 << 0x05;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.H & 0b00101000; // Copy X and Y from H.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x6D: BIT 0x05, L [0]
    private static void Step_1028(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.L & 0x01 << 0x05;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.L & 0b00101000; // Copy X and Y from L.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x6E: BIT 0x05, (HL) [0]
    private static void Step_1029(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0x6E: BIT 0x05, (HL) [3]
    private static void Step_1032(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01 << 0x05;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0x6F: BIT 0x05, A [0]
    private static void Step_1033(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.A & 0x01 << 0x05;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.A & 0b00101000; // Copy X and Y from A.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x70: BIT 0x06, B [0]
    private static void Step_1034(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.B & 0x01 << 0x06;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.B & 0b00101000; // Copy X and Y from B.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x71: BIT 0x06, C [0]
    private static void Step_1035(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.C & 0x01 << 0x06;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.C & 0b00101000; // Copy X and Y from C.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x72: BIT 0x06, D [0]
    private static void Step_1036(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.D & 0x01 << 0x06;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.D & 0b00101000; // Copy X and Y from D.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x73: BIT 0x06, E [0]
    private static void Step_1037(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.E & 0x01 << 0x06;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.E & 0b00101000; // Copy X and Y from E.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x74: BIT 0x06, H [0]
    private static void Step_1038(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.H & 0x01 << 0x06;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.H & 0b00101000; // Copy X and Y from H.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x75: BIT 0x06, L [0]
    private static void Step_1039(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.L & 0x01 << 0x06;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.L & 0b00101000; // Copy X and Y from L.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x76: BIT 0x06, (HL) [0]
    private static void Step_1040(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0x76: BIT 0x06, (HL) [3]
    private static void Step_1043(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01 << 0x06;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0x77: BIT 0x06, A [0]
    private static void Step_1044(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.A & 0x01 << 0x06;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.A & 0b00101000; // Copy X and Y from A.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x78: BIT 0x07, B [0]
    private static void Step_1045(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.B & 0x01 << 0x07;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.B & 0b00101000; // Copy X and Y from B.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x79: BIT 0x07, C [0]
    private static void Step_1046(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.C & 0x01 << 0x07;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.C & 0b00101000; // Copy X and Y from C.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x7A: BIT 0x07, D [0]
    private static void Step_1047(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.D & 0x01 << 0x07;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.D & 0b00101000; // Copy X and Y from D.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x7B: BIT 0x07, E [0]
    private static void Step_1048(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.E & 0x01 << 0x07;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.E & 0b00101000; // Copy X and Y from E.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x7C: BIT 0x07, H [0]
    private static void Step_1049(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.H & 0x01 << 0x07;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.H & 0b00101000; // Copy X and Y from H.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x7D: BIT 0x07, L [0]
    private static void Step_1050(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.L & 0x01 << 0x07;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.L & 0b00101000; // Copy X and Y from L.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x7E: BIT 0x07, (HL) [0]
    private static void Step_1051(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0x7E: BIT 0x07, (HL) [3]
    private static void Step_1054(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01 << 0x07;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0x7F: BIT 0x07, A [0]
    private static void Step_1055(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var comparison = emulator.A & 0x01 << 0x07;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.A & 0b00101000; // Copy X and Y from A.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x80: RES 0x00, B [0]
    private static void Step_1056(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B = (byte)(emulator.B & ~0x01);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x81: RES 0x00, C [0]
    private static void Step_1057(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C = (byte)(emulator.C & ~0x01);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x82: RES 0x00, D [0]
    private static void Step_1058(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D = (byte)(emulator.D & ~0x01);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x83: RES 0x00, E [0]
    private static void Step_1059(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E = (byte)(emulator.E & ~0x01);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x84: RES 0x00, H [0]
    private static void Step_1060(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.H = (byte)(emulator.H & ~0x01);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x85: RES 0x00, L [0]
    private static void Step_1061(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.L = (byte)(emulator.L & ~0x01);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x86: RES 0x00, (HL) [0]
    private static void Step_1062(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0x86: RES 0x00, (HL) [4]
    private static void Step_1066(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~0x01);
    }

    // 0xCB 0x86: RES 0x00, (HL) [6]
    private static void Step_1068(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0x87: RES 0x00, A [0]
    private static void Step_1069(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = (byte)(emulator.A & ~0x01);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x88: RES 0x01, B [0]
    private static void Step_1070(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B = (byte)(emulator.B & ~(0x01 << 0x01));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x89: RES 0x01, C [0]
    private static void Step_1071(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C = (byte)(emulator.C & ~(0x01 << 0x01));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x8A: RES 0x01, D [0]
    private static void Step_1072(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D = (byte)(emulator.D & ~(0x01 << 0x01));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x8B: RES 0x01, E [0]
    private static void Step_1073(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E = (byte)(emulator.E & ~(0x01 << 0x01));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x8C: RES 0x01, H [0]
    private static void Step_1074(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.H = (byte)(emulator.H & ~(0x01 << 0x01));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x8D: RES 0x01, L [0]
    private static void Step_1075(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.L = (byte)(emulator.L & ~(0x01 << 0x01));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x8E: RES 0x01, (HL) [0]
    private static void Step_1076(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0x8E: RES 0x01, (HL) [4]
    private static void Step_1080(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x01));
    }

    // 0xCB 0x8E: RES 0x01, (HL) [6]
    private static void Step_1082(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0x8F: RES 0x01, A [0]
    private static void Step_1083(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = (byte)(emulator.A & ~(0x01 << 0x01));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x90: RES 0x02, B [0]
    private static void Step_1084(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B = (byte)(emulator.B & ~(0x01 << 0x02));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x91: RES 0x02, C [0]
    private static void Step_1085(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C = (byte)(emulator.C & ~(0x01 << 0x02));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x92: RES 0x02, D [0]
    private static void Step_1086(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D = (byte)(emulator.D & ~(0x01 << 0x02));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x93: RES 0x02, E [0]
    private static void Step_1087(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E = (byte)(emulator.E & ~(0x01 << 0x02));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x94: RES 0x02, H [0]
    private static void Step_1088(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.H = (byte)(emulator.H & ~(0x01 << 0x02));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x95: RES 0x02, L [0]
    private static void Step_1089(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.L = (byte)(emulator.L & ~(0x01 << 0x02));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x96: RES 0x02, (HL) [0]
    private static void Step_1090(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0x96: RES 0x02, (HL) [4]
    private static void Step_1094(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x02));
    }

    // 0xCB 0x96: RES 0x02, (HL) [6]
    private static void Step_1096(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0x97: RES 0x02, A [0]
    private static void Step_1097(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = (byte)(emulator.A & ~(0x01 << 0x02));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x98: RES 0x03, B [0]
    private static void Step_1098(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B = (byte)(emulator.B & ~(0x01 << 0x03));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x99: RES 0x03, C [0]
    private static void Step_1099(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C = (byte)(emulator.C & ~(0x01 << 0x03));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x9A: RES 0x03, D [0]
    private static void Step_1100(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D = (byte)(emulator.D & ~(0x01 << 0x03));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x9B: RES 0x03, E [0]
    private static void Step_1101(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E = (byte)(emulator.E & ~(0x01 << 0x03));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x9C: RES 0x03, H [0]
    private static void Step_1102(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.H = (byte)(emulator.H & ~(0x01 << 0x03));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x9D: RES 0x03, L [0]
    private static void Step_1103(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.L = (byte)(emulator.L & ~(0x01 << 0x03));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0x9E: RES 0x03, (HL) [0]
    private static void Step_1104(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0x9E: RES 0x03, (HL) [4]
    private static void Step_1108(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x03));
    }

    // 0xCB 0x9E: RES 0x03, (HL) [6]
    private static void Step_1110(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0x9F: RES 0x03, A [0]
    private static void Step_1111(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = (byte)(emulator.A & ~(0x01 << 0x03));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xA0: RES 0x04, B [0]
    private static void Step_1112(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B = (byte)(emulator.B & ~(0x01 << 0x04));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xA1: RES 0x04, C [0]
    private static void Step_1113(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C = (byte)(emulator.C & ~(0x01 << 0x04));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xA2: RES 0x04, D [0]
    private static void Step_1114(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D = (byte)(emulator.D & ~(0x01 << 0x04));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xA3: RES 0x04, E [0]
    private static void Step_1115(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E = (byte)(emulator.E & ~(0x01 << 0x04));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xA4: RES 0x04, H [0]
    private static void Step_1116(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.H = (byte)(emulator.H & ~(0x01 << 0x04));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xA5: RES 0x04, L [0]
    private static void Step_1117(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.L = (byte)(emulator.L & ~(0x01 << 0x04));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xA6: RES 0x04, (HL) [0]
    private static void Step_1118(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0xA6: RES 0x04, (HL) [4]
    private static void Step_1122(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x04));
    }

    // 0xCB 0xA6: RES 0x04, (HL) [6]
    private static void Step_1124(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0xA7: RES 0x04, A [0]
    private static void Step_1125(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = (byte)(emulator.A & ~(0x01 << 0x04));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xA8: RES 0x05, B [0]
    private static void Step_1126(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B = (byte)(emulator.B & ~(0x01 << 0x05));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xA9: RES 0x05, C [0]
    private static void Step_1127(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C = (byte)(emulator.C & ~(0x01 << 0x05));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xAA: RES 0x05, D [0]
    private static void Step_1128(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D = (byte)(emulator.D & ~(0x01 << 0x05));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xAB: RES 0x05, E [0]
    private static void Step_1129(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E = (byte)(emulator.E & ~(0x01 << 0x05));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xAC: RES 0x05, H [0]
    private static void Step_1130(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.H = (byte)(emulator.H & ~(0x01 << 0x05));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xAD: RES 0x05, L [0]
    private static void Step_1131(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.L = (byte)(emulator.L & ~(0x01 << 0x05));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xAE: RES 0x05, (HL) [0]
    private static void Step_1132(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0xAE: RES 0x05, (HL) [4]
    private static void Step_1136(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x05));
    }

    // 0xCB 0xAE: RES 0x05, (HL) [6]
    private static void Step_1138(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0xAF: RES 0x05, A [0]
    private static void Step_1139(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = (byte)(emulator.A & ~(0x01 << 0x05));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xB0: RES 0x06, B [0]
    private static void Step_1140(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B = (byte)(emulator.B & ~(0x01 << 0x06));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xB1: RES 0x06, C [0]
    private static void Step_1141(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C = (byte)(emulator.C & ~(0x01 << 0x06));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xB2: RES 0x06, D [0]
    private static void Step_1142(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D = (byte)(emulator.D & ~(0x01 << 0x06));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xB3: RES 0x06, E [0]
    private static void Step_1143(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E = (byte)(emulator.E & ~(0x01 << 0x06));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xB4: RES 0x06, H [0]
    private static void Step_1144(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.H = (byte)(emulator.H & ~(0x01 << 0x06));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xB5: RES 0x06, L [0]
    private static void Step_1145(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.L = (byte)(emulator.L & ~(0x01 << 0x06));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xB6: RES 0x06, (HL) [0]
    private static void Step_1146(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0xB6: RES 0x06, (HL) [4]
    private static void Step_1150(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x06));
    }

    // 0xCB 0xB6: RES 0x06, (HL) [6]
    private static void Step_1152(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0xB7: RES 0x06, A [0]
    private static void Step_1153(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = (byte)(emulator.A & ~(0x01 << 0x06));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xB8: RES 0x07, B [0]
    private static void Step_1154(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B = (byte)(emulator.B & ~(0x01 << 0x07));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xB9: RES 0x07, C [0]
    private static void Step_1155(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C = (byte)(emulator.C & ~(0x01 << 0x07));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xBA: RES 0x07, D [0]
    private static void Step_1156(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D = (byte)(emulator.D & ~(0x01 << 0x07));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xBB: RES 0x07, E [0]
    private static void Step_1157(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E = (byte)(emulator.E & ~(0x01 << 0x07));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xBC: RES 0x07, H [0]
    private static void Step_1158(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.H = (byte)(emulator.H & ~(0x01 << 0x07));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xBD: RES 0x07, L [0]
    private static void Step_1159(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.L = (byte)(emulator.L & ~(0x01 << 0x07));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xBE: RES 0x07, (HL) [0]
    private static void Step_1160(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0xBE: RES 0x07, (HL) [4]
    private static void Step_1164(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x07));
    }

    // 0xCB 0xBE: RES 0x07, (HL) [6]
    private static void Step_1166(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0xBF: RES 0x07, A [0]
    private static void Step_1167(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = (byte)(emulator.A & ~(0x01 << 0x07));
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xC0: SET 0x00, B [0]
    private static void Step_1168(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B |= 0x01;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xC1: SET 0x00, C [0]
    private static void Step_1169(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C |= 0x01;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xC2: SET 0x00, D [0]
    private static void Step_1170(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D |= 0x01;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xC3: SET 0x00, E [0]
    private static void Step_1171(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E |= 0x01;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xC4: SET 0x00, H [0]
    private static void Step_1172(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.H |= 0x01;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xC5: SET 0x00, L [0]
    private static void Step_1173(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.L |= 0x01;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xC6: SET 0x00, (HL) [0]
    private static void Step_1174(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0xC6: SET 0x00, (HL) [3]
    private static void Step_1177(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data |= 0x01;
    }

    // 0xCB 0xC6: SET 0x00, (HL) [6]
    private static void Step_1180(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0xC7: SET 0x00, A [0]
    private static void Step_1181(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A |= 0x01;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xC8: SET 0x01, B [0]
    private static void Step_1182(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B = (byte)(emulator.B | 0x01 << 0x01);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xC9: SET 0x01, C [0]
    private static void Step_1183(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C = (byte)(emulator.C | 0x01 << 0x01);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xCA: SET 0x01, D [0]
    private static void Step_1184(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D = (byte)(emulator.D | 0x01 << 0x01);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xCB: SET 0x01, E [0]
    private static void Step_1185(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E = (byte)(emulator.E | 0x01 << 0x01);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xCC: SET 0x01, H [0]
    private static void Step_1186(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.H = (byte)(emulator.H | 0x01 << 0x01);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xCD: SET 0x01, L [0]
    private static void Step_1187(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.L = (byte)(emulator.L | 0x01 << 0x01);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xCE: SET 0x01, (HL) [0]
    private static void Step_1188(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0xCE: SET 0x01, (HL) [3]
    private static void Step_1191(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x01);
    }

    // 0xCB 0xCE: SET 0x01, (HL) [6]
    private static void Step_1194(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0xCF: SET 0x01, A [0]
    private static void Step_1195(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = (byte)(emulator.A | 0x01 << 0x01);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xD0: SET 0x02, B [0]
    private static void Step_1196(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B = (byte)(emulator.B | 0x01 << 0x02);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xD1: SET 0x02, C [0]
    private static void Step_1197(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C = (byte)(emulator.C | 0x01 << 0x02);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xD2: SET 0x02, D [0]
    private static void Step_1198(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D = (byte)(emulator.D | 0x01 << 0x02);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xD3: SET 0x02, E [0]
    private static void Step_1199(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E = (byte)(emulator.E | 0x01 << 0x02);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xD4: SET 0x02, H [0]
    private static void Step_1200(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.H = (byte)(emulator.H | 0x01 << 0x02);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xD5: SET 0x02, L [0]
    private static void Step_1201(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.L = (byte)(emulator.L | 0x01 << 0x02);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xD6: SET 0x02, (HL) [0]
    private static void Step_1202(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0xD6: SET 0x02, (HL) [3]
    private static void Step_1205(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x02);
    }

    // 0xCB 0xD6: SET 0x02, (HL) [6]
    private static void Step_1208(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0xD7: SET 0x02, A [0]
    private static void Step_1209(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = (byte)(emulator.A | 0x01 << 0x02);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xD8: SET 0x03, B [0]
    private static void Step_1210(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B = (byte)(emulator.B | 0x01 << 0x03);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xD9: SET 0x03, C [0]
    private static void Step_1211(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C = (byte)(emulator.C | 0x01 << 0x03);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xDA: SET 0x03, D [0]
    private static void Step_1212(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D = (byte)(emulator.D | 0x01 << 0x03);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xDB: SET 0x03, E [0]
    private static void Step_1213(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E = (byte)(emulator.E | 0x01 << 0x03);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xDC: SET 0x03, H [0]
    private static void Step_1214(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.H = (byte)(emulator.H | 0x01 << 0x03);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xDD: SET 0x03, L [0]
    private static void Step_1215(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.L = (byte)(emulator.L | 0x01 << 0x03);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xDE: SET 0x03, (HL) [0]
    private static void Step_1216(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0xDE: SET 0x03, (HL) [3]
    private static void Step_1219(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x03);
    }

    // 0xCB 0xDE: SET 0x03, (HL) [6]
    private static void Step_1222(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0xDF: SET 0x03, A [0]
    private static void Step_1223(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = (byte)(emulator.A | 0x01 << 0x03);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xE0: SET 0x04, B [0]
    private static void Step_1224(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B = (byte)(emulator.B | 0x01 << 0x04);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xE1: SET 0x04, C [0]
    private static void Step_1225(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C = (byte)(emulator.C | 0x01 << 0x04);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xE2: SET 0x04, D [0]
    private static void Step_1226(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D = (byte)(emulator.D | 0x01 << 0x04);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xE3: SET 0x04, E [0]
    private static void Step_1227(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E = (byte)(emulator.E | 0x01 << 0x04);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xE4: SET 0x04, H [0]
    private static void Step_1228(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.H = (byte)(emulator.H | 0x01 << 0x04);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xE5: SET 0x04, L [0]
    private static void Step_1229(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.L = (byte)(emulator.L | 0x01 << 0x04);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xE6: SET 0x04, (HL) [0]
    private static void Step_1230(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0xE6: SET 0x04, (HL) [3]
    private static void Step_1233(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x04);
    }

    // 0xCB 0xE6: SET 0x04, (HL) [6]
    private static void Step_1236(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0xE7: SET 0x04, A [0]
    private static void Step_1237(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = (byte)(emulator.A | 0x01 << 0x04);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xE8: SET 0x05, B [0]
    private static void Step_1238(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B = (byte)(emulator.B | 0x01 << 0x05);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xE9: SET 0x05, C [0]
    private static void Step_1239(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C = (byte)(emulator.C | 0x01 << 0x05);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xEA: SET 0x05, D [0]
    private static void Step_1240(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D = (byte)(emulator.D | 0x01 << 0x05);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xEB: SET 0x05, E [0]
    private static void Step_1241(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E = (byte)(emulator.E | 0x01 << 0x05);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xEC: SET 0x05, H [0]
    private static void Step_1242(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.H = (byte)(emulator.H | 0x01 << 0x05);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xED: SET 0x05, L [0]
    private static void Step_1243(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.L = (byte)(emulator.L | 0x01 << 0x05);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xEE: SET 0x05, (HL) [0]
    private static void Step_1244(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0xEE: SET 0x05, (HL) [3]
    private static void Step_1247(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x05);
    }

    // 0xCB 0xEE: SET 0x05, (HL) [6]
    private static void Step_1250(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0xEF: SET 0x05, A [0]
    private static void Step_1251(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = (byte)(emulator.A | 0x01 << 0x05);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xF0: SET 0x06, B [0]
    private static void Step_1252(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B = (byte)(emulator.B | 0x01 << 0x06);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xF1: SET 0x06, C [0]
    private static void Step_1253(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C = (byte)(emulator.C | 0x01 << 0x06);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xF2: SET 0x06, D [0]
    private static void Step_1254(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D = (byte)(emulator.D | 0x01 << 0x06);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xF3: SET 0x06, E [0]
    private static void Step_1255(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E = (byte)(emulator.E | 0x01 << 0x06);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xF4: SET 0x06, H [0]
    private static void Step_1256(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.H = (byte)(emulator.H | 0x01 << 0x06);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xF5: SET 0x06, L [0]
    private static void Step_1257(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.L = (byte)(emulator.L | 0x01 << 0x06);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xF6: SET 0x06, (HL) [0]
    private static void Step_1258(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0xF6: SET 0x06, (HL) [3]
    private static void Step_1261(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x06);
    }

    // 0xCB 0xF6: SET 0x06, (HL) [6]
    private static void Step_1264(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0xF7: SET 0x06, A [0]
    private static void Step_1265(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = (byte)(emulator.A | 0x01 << 0x06);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xF8: SET 0x07, B [0]
    private static void Step_1266(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B = (byte)(emulator.B | 0x01 << 0x07);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xF9: SET 0x07, C [0]
    private static void Step_1267(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C = (byte)(emulator.C | 0x01 << 0x07);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xFA: SET 0x07, D [0]
    private static void Step_1268(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D = (byte)(emulator.D | 0x01 << 0x07);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xFB: SET 0x07, E [0]
    private static void Step_1269(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E = (byte)(emulator.E | 0x01 << 0x07);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xFC: SET 0x07, H [0]
    private static void Step_1270(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.H = (byte)(emulator.H | 0x01 << 0x07);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xFD: SET 0x07, L [0]
    private static void Step_1271(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.L = (byte)(emulator.L | 0x01 << 0x07);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xCB 0xFE: SET 0x07, (HL) [0]
    private static void Step_1272(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xCB 0xFE: SET 0x07, (HL) [3]
    private static void Step_1275(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x07);
    }

    // 0xCB 0xFE: SET 0x07, (HL) [6]
    private static void Step_1278(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xCB 0xFF: SET 0x07, A [0]
    private static void Step_1279(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = (byte)(emulator.A | 0x01 << 0x07);
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x00: NOP [0]
    private static void Step_1280(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x01: LD BC, nn [0]
    private static void Step_1281(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x01: LD BC, nn [1]
    private static void Step_1282(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.C = emulator.data;
    }

    // 0xDD 0x01: LD BC, nn [3]
    private static void Step_1284(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x01: LD BC, nn [4]
    private static void Step_1285(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B = emulator.data;
    }

    // 0xDD 0x01: LD BC, nn [5]
    private static void Step_1286(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x02: LD (BC), A [0]
    private static void Step_1287(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.BC;
        emulator.data = emulator.A;
        emulator.Z = (byte)(emulator.C + 0x01);
        emulator.W = emulator.A;
    }

    // 0xDD 0x02: LD (BC), A [2]
    private static void Step_1289(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x03: INC BC [0]
    private static void Step_1290(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.BC += 0x01;
    }

    // 0xDD 0x03: INC BC [1]
    private static void Step_1291(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x04: INC B [0]
    private static void Step_1292(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B += 0x01;

        // Update flags.
        // Reset N.
        int flags = emulator.B & 0b00101000; // Copy X and Y from B.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0x80)) << 2; // Set PV if B == 0x80 is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.B & 0x0F) == 0x00)) << 4; // Set H if (B & 0x0F) == 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0)) << 6; // Set Z if is_zero(B) is true.
        flags |= emulator.B & 0x80; // Set S if is_negative(B) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x05: DEC B [0]
    private static void Step_1293(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B -= 0x01;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.B & 0b00101000; // Copy X and Y from B.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0x7F)) << 2; // Set PV if B == 0x7F is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.B & 0x0F) == 0x0F)) << 4; // Set H if (B & 0x0F) == 0x0F is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0)) << 6; // Set Z if is_zero(B) is true.
        flags |= emulator.B & 0x80; // Set S if is_negative(B) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x06: LD B, n [0]
    private static void Step_1294(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x06: LD B, n [1]
    private static void Step_1295(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B = emulator.data;
    }

    // 0xDD 0x06: LD B, n [2]
    private static void Step_1296(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x07: RLCA [0]
    private static void Step_1297(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.A;
        emulator.A = (byte)(emulator.A << 0x01 | emulator.A >> 0x07);

        // Update flags.
        // Reset N and H.
        int flags = emulator.A & 0b00101000; // Copy X and Y from A.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x08: EX AF; AF' [0]
    private static void Step_1298(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.Shadow_AF;
        emulator.Shadow_AF = emulator.AF;
        emulator.AF = temp;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x09: ADD IX, BC [0]
    private static void Step_1299(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.IX;
        var right = emulator.BC;
        var result = left + right;
        emulator.IX = (ushort)result;
        emulator.WZ = (ushort)(left + 0x01);

        // Update flags.
        // Reset N.
        int flags = emulator.IXH & 0b00101000; // Copy X and Y from IXH.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        flags |= Unsafe.BitCast<bool, byte>((result & 65536) == 65536); // Set C if (result & 65536) == 65536 is true.
        flags |= ((left ^ result ^ right) & 0x1000) >> 8; // Set H if did_16_bit_addition_half_carry(left, right, result) is true.
        emulator.F = (byte)flags;
    }

    // 0xDD 0x09: ADD IX, BC [6]
    private static void Step_1305(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x0A: LD A, (BC) [0]
    private static void Step_1306(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.BC;
    }

    // 0xDD 0x0A: LD A, (BC) [1]
    private static void Step_1307(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A = emulator.data;
        emulator.WZ = (ushort)(emulator.BC + 0x01);
    }

    // 0xDD 0x0A: LD A, (BC) [2]
    private static void Step_1308(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x0B: DEC BC [0]
    private static void Step_1309(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.BC -= 0x01;
    }

    // 0xDD 0x0B: DEC BC [1]
    private static void Step_1310(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x0C: INC C [0]
    private static void Step_1311(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C += 0x01;

        // Update flags.
        // Reset N.
        int flags = emulator.C & 0b00101000; // Copy X and Y from C.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.C == 0x80)) << 2; // Set PV if C == 0x80 is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.C & 0x0F) == 0x00)) << 4; // Set H if (C & 0x0F) == 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.C == 0)) << 6; // Set Z if is_zero(C) is true.
        flags |= emulator.C & 0x80; // Set S if is_negative(C) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x0D: DEC C [0]
    private static void Step_1312(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C -= 0x01;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.C & 0b00101000; // Copy X and Y from C.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.C == 0x7F)) << 2; // Set PV if C == 0x7F is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.C & 0x0F) == 0x0F)) << 4; // Set H if (C & 0x0F) == 0x0F is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.C == 0)) << 6; // Set Z if is_zero(C) is true.
        flags |= emulator.C & 0x80; // Set S if is_negative(C) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x0E: LD C, n [0]
    private static void Step_1313(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x0E: LD C, n [1]
    private static void Step_1314(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.C = emulator.data;
    }

    // 0xDD 0x0E: LD C, n [2]
    private static void Step_1315(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x0F: RRCA [0]
    private static void Step_1316(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.A;
        emulator.A = (byte)(emulator.A >> 0x01 | emulator.A << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.A & 0b00101000; // Copy X and Y from A.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x10: DJNZ d [0]
    private static void Step_1317(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xDD 0x10: DJNZ d [1]
    private static void Step_1318(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x10: DJNZ d [3]
    private static void Step_1320(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B -= 0x01;
        if (emulator.B == 0x00)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
        else
        {
            emulator.PC = (ushort)(emulator.PC + (sbyte)emulator.data);
            emulator.WZ = emulator.PC;
        }
    }

    // 0xDD 0x10: DJNZ d [8]
    private static void Step_1325(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x11: LD DE, nn [0]
    private static void Step_1326(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x11: LD DE, nn [1]
    private static void Step_1327(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.E = emulator.data;
    }

    // 0xDD 0x11: LD DE, nn [3]
    private static void Step_1329(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x11: LD DE, nn [4]
    private static void Step_1330(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.D = emulator.data;
    }

    // 0xDD 0x11: LD DE, nn [5]
    private static void Step_1331(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x12: LD (DE), A [0]
    private static void Step_1332(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.DE;
        emulator.data = emulator.A;
        emulator.Z = (byte)(emulator.E + 0x01);
        emulator.W = emulator.A;
    }

    // 0xDD 0x12: LD (DE), A [2]
    private static void Step_1334(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x13: INC DE [0]
    private static void Step_1335(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.DE += 0x01;
    }

    // 0xDD 0x13: INC DE [1]
    private static void Step_1336(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x14: INC D [0]
    private static void Step_1337(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D += 0x01;

        // Update flags.
        // Reset N.
        int flags = emulator.D & 0b00101000; // Copy X and Y from D.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.D == 0x80)) << 2; // Set PV if D == 0x80 is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.D & 0x0F) == 0x00)) << 4; // Set H if (D & 0x0F) == 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.D == 0)) << 6; // Set Z if is_zero(D) is true.
        flags |= emulator.D & 0x80; // Set S if is_negative(D) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x15: DEC D [0]
    private static void Step_1338(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D -= 0x01;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.D & 0b00101000; // Copy X and Y from D.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.D == 0x7F)) << 2; // Set PV if D == 0x7F is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.D & 0x0F) == 0x0F)) << 4; // Set H if (D & 0x0F) == 0x0F is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.D == 0)) << 6; // Set Z if is_zero(D) is true.
        flags |= emulator.D & 0x80; // Set S if is_negative(D) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x16: LD D, n [0]
    private static void Step_1339(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x16: LD D, n [1]
    private static void Step_1340(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.D = emulator.data;
    }

    // 0xDD 0x16: LD D, n [2]
    private static void Step_1341(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x17: RLA [0]
    private static void Step_1342(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.A;
        emulator.A = (byte)(emulator.A << 0x01 | (emulator.F & 0b00000001 /* flag.C */));

        // Update flags.
        // Reset N and H.
        int flags = emulator.A & 0b00101000; // Copy X and Y from A.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x18: JR d [0]
    private static void Step_1343(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x18: JR d [3]
    private static void Step_1346(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.PC = (ushort)(emulator.PC + (sbyte)emulator.data);
        emulator.WZ = emulator.PC;
    }

    // 0xDD 0x18: JR d [7]
    private static void Step_1350(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x19: ADD IX, DE [0]
    private static void Step_1351(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.IX;
        var right = emulator.DE;
        var result = left + right;
        emulator.IX = (ushort)result;
        emulator.WZ = (ushort)(left + 0x01);

        // Update flags.
        // Reset N.
        int flags = emulator.IXH & 0b00101000; // Copy X and Y from IXH.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        flags |= Unsafe.BitCast<bool, byte>((result & 65536) == 65536); // Set C if (result & 65536) == 65536 is true.
        flags |= ((left ^ result ^ right) & 0x1000) >> 8; // Set H if did_16_bit_addition_half_carry(left, right, result) is true.
        emulator.F = (byte)flags;
    }

    // 0xDD 0x19: ADD IX, DE [6]
    private static void Step_1357(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x1A: LD A, (DE) [0]
    private static void Step_1358(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.DE;
    }

    // 0xDD 0x1A: LD A, (DE) [1]
    private static void Step_1359(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A = emulator.data;
        emulator.WZ = (ushort)(emulator.DE + 0x01);
    }

    // 0xDD 0x1A: LD A, (DE) [2]
    private static void Step_1360(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x1B: DEC DE [0]
    private static void Step_1361(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.DE -= 0x01;
    }

    // 0xDD 0x1B: DEC DE [1]
    private static void Step_1362(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x1C: INC E [0]
    private static void Step_1363(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E += 0x01;

        // Update flags.
        // Reset N.
        int flags = emulator.E & 0b00101000; // Copy X and Y from E.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.E == 0x80)) << 2; // Set PV if E == 0x80 is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.E & 0x0F) == 0x00)) << 4; // Set H if (E & 0x0F) == 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.E == 0)) << 6; // Set Z if is_zero(E) is true.
        flags |= emulator.E & 0x80; // Set S if is_negative(E) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x1D: DEC E [0]
    private static void Step_1364(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E -= 0x01;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.E & 0b00101000; // Copy X and Y from E.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.E == 0x7F)) << 2; // Set PV if E == 0x7F is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.E & 0x0F) == 0x0F)) << 4; // Set H if (E & 0x0F) == 0x0F is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.E == 0)) << 6; // Set Z if is_zero(E) is true.
        flags |= emulator.E & 0x80; // Set S if is_negative(E) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x1E: LD E, n [0]
    private static void Step_1365(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x1E: LD E, n [1]
    private static void Step_1366(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.E = emulator.data;
    }

    // 0xDD 0x1E: LD E, n [2]
    private static void Step_1367(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x1F: RRA [0]
    private static void Step_1368(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.A;
        emulator.A = (byte)(emulator.A >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = emulator.A & 0b00101000; // Copy X and Y from A.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x01) == 0x01); // Set C if (temp & 0x01) == 0x01 is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x20: JR NZ, d [0]
    private static void Step_1369(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x20: JR NZ, d [2]
    private static void Step_1371(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b01000000) == 0b01000000 /* !condition.NZ */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xDD 0x20: JR NZ, d [3]
    private static void Step_1372(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.PC = (ushort)(emulator.PC + (sbyte)emulator.data);
        emulator.WZ = emulator.PC;
    }

    // 0xDD 0x20: JR NZ, d [7]
    private static void Step_1376(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x21: LD IX, nn [0]
    private static void Step_1377(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x21: LD IX, nn [1]
    private static void Step_1378(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.IXL = emulator.data;
    }

    // 0xDD 0x21: LD IX, nn [3]
    private static void Step_1380(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x21: LD IX, nn [4]
    private static void Step_1381(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.IXH = emulator.data;
    }

    // 0xDD 0x21: LD IX, nn [5]
    private static void Step_1382(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x22: LD (nn), IX [0]
    private static void Step_1383(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x22: LD (nn), IX [1]
    private static void Step_1384(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0x22: LD (nn), IX [3]
    private static void Step_1386(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x22: LD (nn), IX [4]
    private static void Step_1387(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDD 0x22: LD (nn), IX [6]
    private static void Step_1389(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.data = emulator.IXL;
        emulator.WZ += 0x01;
    }

    // 0xDD 0x22: LD (nn), IX [9]
    private static void Step_1392(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.data = emulator.IXH;
    }

    // 0xDD 0x22: LD (nn), IX [11]
    private static void Step_1394(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x23: INC IX [0]
    private static void Step_1395(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IX += 0x01;
    }

    // 0xDD 0x23: INC IX [1]
    private static void Step_1396(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x24: INC IXH [0]
    private static void Step_1397(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IXH += 0x01;

        // Update flags.
        // Reset N.
        int flags = emulator.IXH & 0b00101000; // Copy X and Y from IXH.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.IXH == 0x80)) << 2; // Set PV if IXH == 0x80 is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.IXH & 0x0F) == 0x00)) << 4; // Set H if (IXH & 0x0F) == 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.IXH == 0)) << 6; // Set Z if is_zero(IXH) is true.
        flags |= emulator.IXH & 0x80; // Set S if is_negative(IXH) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x25: DEC IXH [0]
    private static void Step_1398(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IXH -= 0x01;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.IXH & 0b00101000; // Copy X and Y from IXH.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.IXH == 0x7F)) << 2; // Set PV if IXH == 0x7F is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.IXH & 0x0F) == 0x0F)) << 4; // Set H if (IXH & 0x0F) == 0x0F is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.IXH == 0)) << 6; // Set Z if is_zero(IXH) is true.
        flags |= emulator.IXH & 0x80; // Set S if is_negative(IXH) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x26: LD IXH, n [0]
    private static void Step_1399(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x26: LD IXH, n [1]
    private static void Step_1400(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.IXH = emulator.data;
    }

    // 0xDD 0x26: LD IXH, n [2]
    private static void Step_1401(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x27: DAA [0]
    private static void Step_1402(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var low_a = emulator.A & 0x0F;
        var modifier = 0x00;
        if (((emulator.F & 0b00010000) == 0b00010000 /* flag.H */) || low_a > 0x09)
        {
            modifier += 0x06;
        }

        var carry = 0x00;
        if (((emulator.F & 0b00000001) == 0b00000001 /* flag.C */) || emulator.A > 0x99)
        {
            modifier += 0x60;
            carry = 0xFF;
        }

        if (((emulator.F & 0b00000010) == 0b00000010 /* flag.N */))
        {
            emulator.A = (byte)(emulator.A - modifier);
        }
        else
        {
            emulator.A = (byte)(emulator.A + modifier);
        }

        var half_carry = 0x00;
        if (((emulator.F & 0b00000010) == 0b00000010 /* flag.N */))
        {
            if (((emulator.F & 0b00010000) == 0b00010000 /* flag.H */) && low_a < 0x06)
            {
                half_carry = 0xFF;
            }
        }
        else
        {
            if (low_a >= 0x0A)
            {
                half_carry = 0xFF;
            }
        }

        // Update flags.
        int flags = carry & 0b00000001; // Copy C from carry.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= half_carry & 0b00010000; // Copy H from half_carry.
        flags |= emulator.F & 0b00000010; // Copy N from F.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x28: JR Z, d [0]
    private static void Step_1403(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x28: JR Z, d [2]
    private static void Step_1405(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b01000000) == 0b00000000 /* !condition.Z */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xDD 0x28: JR Z, d [3]
    private static void Step_1406(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.PC = (ushort)(emulator.PC + (sbyte)emulator.data);
        emulator.WZ = emulator.PC;
    }

    // 0xDD 0x28: JR Z, d [7]
    private static void Step_1410(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x29: ADD IX, IX [0]
    private static void Step_1411(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.IX;
        var right = emulator.IX;
        var result = left + right;
        emulator.IX = (ushort)result;
        emulator.WZ = (ushort)(left + 0x01);

        // Update flags.
        // Reset N.
        int flags = emulator.IXH & 0b00101000; // Copy X and Y from IXH.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        flags |= Unsafe.BitCast<bool, byte>((result & 65536) == 65536); // Set C if (result & 65536) == 65536 is true.
        flags |= ((left ^ result ^ right) & 0x1000) >> 8; // Set H if did_16_bit_addition_half_carry(left, right, result) is true.
        emulator.F = (byte)flags;
    }

    // 0xDD 0x29: ADD IX, IX [6]
    private static void Step_1417(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x2A: LD IX, (nn) [0]
    private static void Step_1418(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x2A: LD IX, (nn) [1]
    private static void Step_1419(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0x2A: LD IX, (nn) [3]
    private static void Step_1421(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x2A: LD IX, (nn) [4]
    private static void Step_1422(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDD 0x2A: LD IX, (nn) [6]
    private static void Step_1424(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.WZ += 0x01;
    }

    // 0xDD 0x2A: LD IX, (nn) [7]
    private static void Step_1425(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.IXL = emulator.data;
    }

    // 0xDD 0x2A: LD IX, (nn) [9]
    private static void Step_1427(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
    }

    // 0xDD 0x2A: LD IX, (nn) [10]
    private static void Step_1428(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.IXH = emulator.data;
    }

    // 0xDD 0x2A: LD IX, (nn) [11]
    private static void Step_1429(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x2B: DEC IX [0]
    private static void Step_1430(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IX -= 0x01;
    }

    // 0xDD 0x2B: DEC IX [1]
    private static void Step_1431(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x2C: INC IXL [0]
    private static void Step_1432(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IXL += 0x01;

        // Update flags.
        // Reset N.
        int flags = emulator.IXL & 0b00101000; // Copy X and Y from IXL.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.IXL == 0x80)) << 2; // Set PV if IXL == 0x80 is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.IXL & 0x0F) == 0x00)) << 4; // Set H if (IXL & 0x0F) == 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.IXL == 0)) << 6; // Set Z if is_zero(IXL) is true.
        flags |= emulator.IXL & 0x80; // Set S if is_negative(IXL) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x2D: DEC IXL [0]
    private static void Step_1433(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IXL -= 0x01;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.IXL & 0b00101000; // Copy X and Y from IXL.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.IXL == 0x7F)) << 2; // Set PV if IXL == 0x7F is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.IXL & 0x0F) == 0x0F)) << 4; // Set H if (IXL & 0x0F) == 0x0F is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.IXL == 0)) << 6; // Set Z if is_zero(IXL) is true.
        flags |= emulator.IXL & 0x80; // Set S if is_negative(IXL) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x2E: LD IXL, n [0]
    private static void Step_1434(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x2E: LD IXL, n [1]
    private static void Step_1435(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.IXL = emulator.data;
    }

    // 0xDD 0x2E: LD IXL, n [2]
    private static void Step_1436(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x2F: CPL [0]
    private static void Step_1437(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = (byte)~emulator.A;

        // Update flags.
        int flags = 0b00010010; // Set N and H.
        flags |= emulator.A & 0b00101000; // Copy X and Y from A.
        flags |= emulator.F & 0b11000101; // Copy C, PV, Z and S from F.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x30: JR NC, d [0]
    private static void Step_1438(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x30: JR NC, d [2]
    private static void Step_1440(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000001) == 0b00000001 /* !condition.NC */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xDD 0x30: JR NC, d [3]
    private static void Step_1441(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.PC = (ushort)(emulator.PC + (sbyte)emulator.data);
        emulator.WZ = emulator.PC;
    }

    // 0xDD 0x30: JR NC, d [7]
    private static void Step_1445(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x31: LD SP, nn [0]
    private static void Step_1446(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x31: LD SP, nn [1]
    private static void Step_1447(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SPL = emulator.data;
    }

    // 0xDD 0x31: LD SP, nn [3]
    private static void Step_1449(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x31: LD SP, nn [4]
    private static void Step_1450(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SPH = emulator.data;
    }

    // 0xDD 0x31: LD SP, nn [5]
    private static void Step_1451(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x32: LD (nn), A [0]
    private static void Step_1452(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x32: LD (nn), A [1]
    private static void Step_1453(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0x32: LD (nn), A [3]
    private static void Step_1455(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x32: LD (nn), A [4]
    private static void Step_1456(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDD 0x32: LD (nn), A [6]
    private static void Step_1458(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.data = emulator.A;
        emulator.Z += 0x01;
        emulator.W = emulator.A;
    }

    // 0xDD 0x32: LD (nn), A [8]
    private static void Step_1460(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x33: INC SP [0]
    private static void Step_1461(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.SP += 0x01;
    }

    // 0xDD 0x33: INC SP [1]
    private static void Step_1462(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x34: INC (IX + d) [0]
    private static void Step_1463(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x34: INC (IX + d) [1]
    private static void Step_1464(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.latch = emulator.data;
    }

    // 0xDD 0x34: INC (IX + d) [8]
    private static void Step_1471(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xDD 0x34: INC (IX + d) [10]
    private static void Step_1473(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data += 0x01;

        // Update flags.
        // Reset N.
        int flags = emulator.data & 0b00101000; // Copy X and Y from data.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0x80)) << 2; // Set PV if data == 0x80 is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.data & 0x0F) == 0x00)) << 4; // Set H if (data & 0x0F) == 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        flags |= emulator.data & 0x80; // Set S if is_negative(data) is true.
        emulator.F = (byte)flags;
    }

    // 0xDD 0x34: INC (IX + d) [14]
    private static void Step_1477(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x35: DEC (IX + d) [0]
    private static void Step_1478(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x35: DEC (IX + d) [1]
    private static void Step_1479(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.latch = emulator.data;
    }

    // 0xDD 0x35: DEC (IX + d) [8]
    private static void Step_1486(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xDD 0x35: DEC (IX + d) [10]
    private static void Step_1488(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data -= 0x01;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.data & 0b00101000; // Copy X and Y from data.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0x7F)) << 2; // Set PV if data == 0x7F is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.data & 0x0F) == 0x0F)) << 4; // Set H if (data & 0x0F) == 0x0F is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        flags |= emulator.data & 0x80; // Set S if is_negative(data) is true.
        emulator.F = (byte)flags;
    }

    // 0xDD 0x35: DEC (IX + d) [14]
    private static void Step_1492(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x36: LD (IX + d), n [0]
    private static void Step_1493(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x36: LD (IX + d), n [1]
    private static void Step_1494(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.latch = emulator.data;
    }

    // 0xDD 0x36: LD (IX + d), n [3]
    private static void Step_1496(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x36: LD (IX + d), n [8]
    private static void Step_1501(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // 0xDD 0x36: LD (IX + d), n [10]
    private static void Step_1503(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x37: SCF [0]
    private static void Step_1504(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var xy = emulator.Q ^ emulator.F | emulator.A;

        // Update flags.
        int flags = 0b00000001; // Set C. Reset N and H.
        flags |= xy & 0b00101000; // Copy X and Y from xy.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x38: JR C, d [0]
    private static void Step_1505(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x38: JR C, d [2]
    private static void Step_1507(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000001) == 0b00000000 /* !condition.C */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xDD 0x38: JR C, d [3]
    private static void Step_1508(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.PC = (ushort)(emulator.PC + (sbyte)emulator.data);
        emulator.WZ = emulator.PC;
    }

    // 0xDD 0x38: JR C, d [7]
    private static void Step_1512(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x39: ADD IX, SP [0]
    private static void Step_1513(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.IX;
        var right = emulator.SP;
        var result = left + right;
        emulator.IX = (ushort)result;
        emulator.WZ = (ushort)(left + 0x01);

        // Update flags.
        // Reset N.
        int flags = emulator.IXH & 0b00101000; // Copy X and Y from IXH.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        flags |= Unsafe.BitCast<bool, byte>((result & 65536) == 65536); // Set C if (result & 65536) == 65536 is true.
        flags |= ((left ^ result ^ right) & 0x1000) >> 8; // Set H if did_16_bit_addition_half_carry(left, right, result) is true.
        emulator.F = (byte)flags;
    }

    // 0xDD 0x39: ADD IX, SP [6]
    private static void Step_1519(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x3A: LD A, (nn) [0]
    private static void Step_1520(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x3A: LD A, (nn) [1]
    private static void Step_1521(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0x3A: LD A, (nn) [3]
    private static void Step_1523(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x3A: LD A, (nn) [4]
    private static void Step_1524(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDD 0x3A: LD A, (nn) [6]
    private static void Step_1526(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.WZ += 0x01;
    }

    // 0xDD 0x3A: LD A, (nn) [7]
    private static void Step_1527(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A = emulator.data;
    }

    // 0xDD 0x3A: LD A, (nn) [8]
    private static void Step_1528(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x3B: DEC SP [0]
    private static void Step_1529(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.SP -= 0x01;
    }

    // 0xDD 0x3B: DEC SP [1]
    private static void Step_1530(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x3C: INC A [0]
    private static void Step_1531(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A += 0x01;

        // Update flags.
        // Reset N.
        int flags = emulator.A & 0b00101000; // Copy X and Y from A.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0x80)) << 2; // Set PV if A == 0x80 is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.A & 0x0F) == 0x00)) << 4; // Set H if (A & 0x0F) == 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        flags |= emulator.A & 0x80; // Set S if is_negative(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x3D: DEC A [0]
    private static void Step_1532(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A -= 0x01;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.A & 0b00101000; // Copy X and Y from A.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0x7F)) << 2; // Set PV if A == 0x7F is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.A & 0x0F) == 0x0F)) << 4; // Set H if (A & 0x0F) == 0x0F is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        flags |= emulator.A & 0x80; // Set S if is_negative(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x3E: LD A, n [0]
    private static void Step_1533(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x3E: LD A, n [1]
    private static void Step_1534(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A = emulator.data;
    }

    // 0xDD 0x3E: LD A, n [2]
    private static void Step_1535(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x3F: CCF [0]
    private static void Step_1536(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var xy = emulator.Q ^ emulator.F | emulator.A;

        // Update flags.
        // Reset N.
        int flags = xy & 0b00101000; // Copy X and Y from xy.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        flags |= Unsafe.BitCast<bool, byte>((emulator.F & 0b00000001 /* flag.C */) == 0x00); // Set C if C == 0x00 is true.
        flags |= ((emulator.F & 0b00000001 /* flag.C */)) << 4; // Set H if C is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x40: LD B, B [0]
    private static void Step_1537(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x41: LD B, C [0]
    private static void Step_1538(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B = emulator.C;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x42: LD B, D [0]
    private static void Step_1539(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B = emulator.D;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x43: LD B, E [0]
    private static void Step_1540(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B = emulator.E;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x44: LD B, IXH [0]
    private static void Step_1541(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B = emulator.IXH;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x45: LD B, IXL [0]
    private static void Step_1542(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B = emulator.IXL;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x46: LD B, (IX + d) [0]
    private static void Step_1543(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x46: LD B, (IX + d) [8]
    private static void Step_1551(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xDD 0x46: LD B, (IX + d) [9]
    private static void Step_1552(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B = emulator.data;
    }

    // 0xDD 0x46: LD B, (IX + d) [10]
    private static void Step_1553(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x47: LD B, A [0]
    private static void Step_1554(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B = emulator.A;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x48: LD C, B [0]
    private static void Step_1555(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C = emulator.B;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x49: LD C, C [0]
    private static void Step_1556(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x4A: LD C, D [0]
    private static void Step_1557(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C = emulator.D;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x4B: LD C, E [0]
    private static void Step_1558(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C = emulator.E;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x4C: LD C, IXH [0]
    private static void Step_1559(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C = emulator.IXH;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x4D: LD C, IXL [0]
    private static void Step_1560(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C = emulator.IXL;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x4E: LD C, (IX + d) [0]
    private static void Step_1561(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x4E: LD C, (IX + d) [8]
    private static void Step_1569(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xDD 0x4E: LD C, (IX + d) [9]
    private static void Step_1570(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.C = emulator.data;
    }

    // 0xDD 0x4E: LD C, (IX + d) [10]
    private static void Step_1571(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x4F: LD C, A [0]
    private static void Step_1572(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C = emulator.A;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x50: LD D, B [0]
    private static void Step_1573(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D = emulator.B;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x51: LD D, C [0]
    private static void Step_1574(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D = emulator.C;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x52: LD D, D [0]
    private static void Step_1575(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x53: LD D, E [0]
    private static void Step_1576(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D = emulator.E;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x54: LD D, IXH [0]
    private static void Step_1577(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D = emulator.IXH;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x55: LD D, IXL [0]
    private static void Step_1578(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D = emulator.IXL;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x56: LD D, (IX + d) [0]
    private static void Step_1579(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x56: LD D, (IX + d) [8]
    private static void Step_1587(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xDD 0x56: LD D, (IX + d) [9]
    private static void Step_1588(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.D = emulator.data;
    }

    // 0xDD 0x56: LD D, (IX + d) [10]
    private static void Step_1589(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x57: LD D, A [0]
    private static void Step_1590(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D = emulator.A;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x58: LD E, B [0]
    private static void Step_1591(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E = emulator.B;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x59: LD E, C [0]
    private static void Step_1592(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E = emulator.C;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x5A: LD E, D [0]
    private static void Step_1593(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E = emulator.D;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x5B: LD E, E [0]
    private static void Step_1594(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x5C: LD E, IXH [0]
    private static void Step_1595(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E = emulator.IXH;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x5D: LD E, IXL [0]
    private static void Step_1596(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E = emulator.IXL;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x5E: LD E, (IX + d) [0]
    private static void Step_1597(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x5E: LD E, (IX + d) [8]
    private static void Step_1605(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xDD 0x5E: LD E, (IX + d) [9]
    private static void Step_1606(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.E = emulator.data;
    }

    // 0xDD 0x5E: LD E, (IX + d) [10]
    private static void Step_1607(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x5F: LD E, A [0]
    private static void Step_1608(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E = emulator.A;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x60: LD IXH, B [0]
    private static void Step_1609(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IXH = emulator.B;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x61: LD IXH, C [0]
    private static void Step_1610(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IXH = emulator.C;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x62: LD IXH, D [0]
    private static void Step_1611(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IXH = emulator.D;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x63: LD IXH, E [0]
    private static void Step_1612(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IXH = emulator.E;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x64: LD IXH, IXH [0]
    private static void Step_1613(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x65: LD IXH, IXL [0]
    private static void Step_1614(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IXH = emulator.IXL;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x66: LD H, (IX + d) [0]
    private static void Step_1615(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x66: LD H, (IX + d) [8]
    private static void Step_1623(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xDD 0x66: LD H, (IX + d) [9]
    private static void Step_1624(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.H = emulator.data;
    }

    // 0xDD 0x66: LD H, (IX + d) [10]
    private static void Step_1625(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x67: LD IXH, A [0]
    private static void Step_1626(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IXH = emulator.A;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x68: LD IXL, B [0]
    private static void Step_1627(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IXL = emulator.B;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x69: LD IXL, C [0]
    private static void Step_1628(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IXL = emulator.C;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x6A: LD IXL, D [0]
    private static void Step_1629(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IXL = emulator.D;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x6B: LD IXL, E [0]
    private static void Step_1630(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IXL = emulator.E;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x6C: LD IXL, IXH [0]
    private static void Step_1631(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IXL = emulator.IXH;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x6D: LD IXL, IXL [0]
    private static void Step_1632(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x6E: LD L, (IX + d) [0]
    private static void Step_1633(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x6E: LD L, (IX + d) [8]
    private static void Step_1641(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xDD 0x6E: LD L, (IX + d) [9]
    private static void Step_1642(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.L = emulator.data;
    }

    // 0xDD 0x6E: LD L, (IX + d) [10]
    private static void Step_1643(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x6F: LD IXL, A [0]
    private static void Step_1644(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IXL = emulator.A;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x70: LD (IX + d), B [0]
    private static void Step_1645(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x70: LD (IX + d), B [8]
    private static void Step_1653(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
        emulator.data = emulator.B;
    }

    // 0xDD 0x70: LD (IX + d), B [10]
    private static void Step_1655(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x71: LD (IX + d), C [0]
    private static void Step_1656(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x71: LD (IX + d), C [8]
    private static void Step_1664(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
        emulator.data = emulator.C;
    }

    // 0xDD 0x71: LD (IX + d), C [10]
    private static void Step_1666(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x72: LD (IX + d), D [0]
    private static void Step_1667(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x72: LD (IX + d), D [8]
    private static void Step_1675(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
        emulator.data = emulator.D;
    }

    // 0xDD 0x72: LD (IX + d), D [10]
    private static void Step_1677(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x73: LD (IX + d), E [0]
    private static void Step_1678(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x73: LD (IX + d), E [8]
    private static void Step_1686(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
        emulator.data = emulator.E;
    }

    // 0xDD 0x73: LD (IX + d), E [10]
    private static void Step_1688(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x74: LD (IX + d), H [0]
    private static void Step_1689(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x74: LD (IX + d), H [8]
    private static void Step_1697(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
        emulator.data = emulator.H;
    }

    // 0xDD 0x74: LD (IX + d), H [10]
    private static void Step_1699(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x75: LD (IX + d), L [0]
    private static void Step_1700(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x75: LD (IX + d), L [8]
    private static void Step_1708(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
        emulator.data = emulator.L;
    }

    // 0xDD 0x75: LD (IX + d), L [10]
    private static void Step_1710(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x76: HALT [0]
    private static void Step_1711(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        emulator.halted = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Move to halted cycle.
        emulator.currentStep = 9;
        Step_8(emulator, ref actionRequired);
    }

    // 0xDD 0x77: LD (IX + d), A [0]
    private static void Step_1712(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x77: LD (IX + d), A [8]
    private static void Step_1720(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
        emulator.data = emulator.A;
    }

    // 0xDD 0x77: LD (IX + d), A [10]
    private static void Step_1722(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x78: LD A, B [0]
    private static void Step_1723(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = emulator.B;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x79: LD A, C [0]
    private static void Step_1724(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = emulator.C;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x7A: LD A, D [0]
    private static void Step_1725(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = emulator.D;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x7B: LD A, E [0]
    private static void Step_1726(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = emulator.E;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x7C: LD A, IXH [0]
    private static void Step_1727(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = emulator.IXH;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x7D: LD A, IXL [0]
    private static void Step_1728(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = emulator.IXL;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x7E: LD A, (IX + d) [0]
    private static void Step_1729(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x7E: LD A, (IX + d) [8]
    private static void Step_1737(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xDD 0x7E: LD A, (IX + d) [9]
    private static void Step_1738(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A = emulator.data;
    }

    // 0xDD 0x7E: LD A, (IX + d) [10]
    private static void Step_1739(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x7F: LD A, A [0]
    private static void Step_1740(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x80: ADD A, B [0]
    private static void Step_1741(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.B;
        var result = left + right;
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x81: ADD A, C [0]
    private static void Step_1742(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.C;
        var result = left + right;
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x82: ADD A, D [0]
    private static void Step_1743(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.D;
        var result = left + right;
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x83: ADD A, E [0]
    private static void Step_1744(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.E;
        var result = left + right;
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x84: ADD A, IXH [0]
    private static void Step_1745(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.IXH;
        var result = left + right;
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x85: ADD A, IXL [0]
    private static void Step_1746(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.IXL;
        var result = left + right;
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x86: ADD A, (IX + n) [0]
    private static void Step_1747(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x86: ADD A, (IX + n) [8]
    private static void Step_1755(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xDD 0x86: ADD A, (IX + n) [9]
    private static void Step_1756(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left + right;
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xDD 0x86: ADD A, (IX + n) [10]
    private static void Step_1757(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x87: ADD A, A [0]
    private static void Step_1758(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.A;
        var result = left + right;
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x88: ADC A, B [0]
    private static void Step_1759(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.B;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x89: ADC A, C [0]
    private static void Step_1760(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.C;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x8A: ADC A, D [0]
    private static void Step_1761(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.D;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x8B: ADC A, E [0]
    private static void Step_1762(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.E;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x8C: ADC A, IXH [0]
    private static void Step_1763(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.IXH;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x8D: ADC A, IXL [0]
    private static void Step_1764(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.IXL;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x8E: ADC A, (IX + n) [0]
    private static void Step_1765(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x8E: ADC A, (IX + n) [8]
    private static void Step_1773(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xDD 0x8E: ADC A, (IX + n) [9]
    private static void Step_1774(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xDD 0x8E: ADC A, (IX + n) [10]
    private static void Step_1775(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x8F: ADC A, A [0]
    private static void Step_1776(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.A;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x90: SUB B [0]
    private static void Step_1777(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.B;
        var result = left - right;
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x91: SUB C [0]
    private static void Step_1778(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.C;
        var result = left - right;
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x92: SUB D [0]
    private static void Step_1779(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.D;
        var result = left - right;
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x93: SUB E [0]
    private static void Step_1780(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.E;
        var result = left - right;
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x94: SUB IXH [0]
    private static void Step_1781(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.IXH;
        var result = left - right;
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x95: SUB IXL [0]
    private static void Step_1782(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.IXL;
        var result = left - right;
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x96: SUB (IX + n) [0]
    private static void Step_1783(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x96: SUB (IX + n) [8]
    private static void Step_1791(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xDD 0x96: SUB (IX + n) [9]
    private static void Step_1792(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left - right;
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xDD 0x96: SUB (IX + n) [10]
    private static void Step_1793(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x97: SUB A [0]
    private static void Step_1794(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.A;
        var result = left - right;
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x98: SBC B [0]
    private static void Step_1795(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.B;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x99: SBC C [0]
    private static void Step_1796(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.C;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x9A: SBC D [0]
    private static void Step_1797(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.D;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x9B: SBC E [0]
    private static void Step_1798(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.E;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x9C: SBC IXH [0]
    private static void Step_1799(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.IXH;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x9D: SBC IXL [0]
    private static void Step_1800(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.IXL;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0x9E: SBC (IX + n) [0]
    private static void Step_1801(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0x9E: SBC (IX + n) [8]
    private static void Step_1809(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xDD 0x9E: SBC (IX + n) [9]
    private static void Step_1810(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xDD 0x9E: SBC (IX + n) [10]
    private static void Step_1811(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0x9F: SBC A [0]
    private static void Step_1812(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.A;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xA0: AND B [0]
    private static void Step_1813(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A &= emulator.B;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset C and N.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xA1: AND C [0]
    private static void Step_1814(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A &= emulator.C;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset C and N.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xA2: AND D [0]
    private static void Step_1815(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A &= emulator.D;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset C and N.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xA3: AND E [0]
    private static void Step_1816(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A &= emulator.E;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset C and N.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xA4: AND IXH [0]
    private static void Step_1817(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A &= emulator.IXH;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset C and N.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xA5: AND IXL [0]
    private static void Step_1818(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A &= emulator.IXL;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset C and N.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xA6: AND (IX + n) [0]
    private static void Step_1819(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xA6: AND (IX + n) [8]
    private static void Step_1827(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xDD 0xA6: AND (IX + n) [9]
    private static void Step_1828(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A &= emulator.data;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset C and N.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xDD 0xA6: AND (IX + n) [10]
    private static void Step_1829(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xA7: AND A [0]
    private static void Step_1830(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A &= emulator.A;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset C and N.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xA8: XOR B [0]
    private static void Step_1831(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A ^= emulator.B;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xA9: XOR C [0]
    private static void Step_1832(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A ^= emulator.C;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xAA: XOR D [0]
    private static void Step_1833(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A ^= emulator.D;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xAB: XOR E [0]
    private static void Step_1834(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A ^= emulator.E;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xAC: XOR IXH [0]
    private static void Step_1835(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A ^= emulator.IXH;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xAD: XOR IXL [0]
    private static void Step_1836(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A ^= emulator.IXL;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xAE: XOR (IX + n) [0]
    private static void Step_1837(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xAE: XOR (IX + n) [8]
    private static void Step_1845(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xDD 0xAE: XOR (IX + n) [9]
    private static void Step_1846(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A ^= emulator.data;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xDD 0xAE: XOR (IX + n) [10]
    private static void Step_1847(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xAF: XOR A [0]
    private static void Step_1848(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A ^= emulator.A;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xB0: OR B [0]
    private static void Step_1849(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A |= emulator.B;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xB1: OR C [0]
    private static void Step_1850(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A |= emulator.C;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xB2: OR D [0]
    private static void Step_1851(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A |= emulator.D;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xB3: OR E [0]
    private static void Step_1852(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A |= emulator.E;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xB4: OR IXH [0]
    private static void Step_1853(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A |= emulator.IXH;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xB5: OR IXL [0]
    private static void Step_1854(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A |= emulator.IXL;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xB6: OR (IX + n) [0]
    private static void Step_1855(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xB6: OR (IX + n) [8]
    private static void Step_1863(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xDD 0xB6: OR (IX + n) [9]
    private static void Step_1864(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A |= emulator.data;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xDD 0xB6: OR (IX + n) [10]
    private static void Step_1865(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xB7: OR A [0]
    private static void Step_1866(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A |= emulator.A;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xB8: CP B [0]
    private static void Step_1867(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.B;
        var result = left - right;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= right & 0b00101000; // Copy X and Y from right.
        flags |= result & 0b10000000; // Copy S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 6; // Set Z if is_zero(result) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xB9: CP C [0]
    private static void Step_1868(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.C;
        var result = left - right;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= right & 0b00101000; // Copy X and Y from right.
        flags |= result & 0b10000000; // Copy S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 6; // Set Z if is_zero(result) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xBA: CP D [0]
    private static void Step_1869(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.D;
        var result = left - right;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= right & 0b00101000; // Copy X and Y from right.
        flags |= result & 0b10000000; // Copy S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 6; // Set Z if is_zero(result) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xBB: CP E [0]
    private static void Step_1870(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.E;
        var result = left - right;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= right & 0b00101000; // Copy X and Y from right.
        flags |= result & 0b10000000; // Copy S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 6; // Set Z if is_zero(result) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xBC: CP IXH [0]
    private static void Step_1871(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.IXH;
        var result = left - right;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= right & 0b00101000; // Copy X and Y from right.
        flags |= result & 0b10000000; // Copy S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 6; // Set Z if is_zero(result) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xBD: CP IXL [0]
    private static void Step_1872(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.IXL;
        var result = left - right;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= right & 0b00101000; // Copy X and Y from right.
        flags |= result & 0b10000000; // Copy S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 6; // Set Z if is_zero(result) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xBE: CP (IX + n) [0]
    private static void Step_1873(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xBE: CP (IX + n) [8]
    private static void Step_1881(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xDD 0xBE: CP (IX + n) [9]
    private static void Step_1882(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left - right;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= right & 0b00101000; // Copy X and Y from right.
        flags |= result & 0b10000000; // Copy S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 6; // Set Z if is_zero(result) is true.
        emulator.F = (byte)flags;
    }

    // 0xDD 0xBE: CP (IX + n) [10]
    private static void Step_1883(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xBF: CP A [0]
    private static void Step_1884(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.A;
        var result = left - right;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= right & 0b00101000; // Copy X and Y from right.
        flags |= result & 0b10000000; // Copy S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 6; // Set Z if is_zero(result) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xC0: RET NZ [0]
    private static void Step_1885(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        if ((emulator.F & 0b01000000) == 0b01000000 /* !condition.NZ */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xDD 0xC0: RET NZ [1]
    private static void Step_1886(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xC0: RET NZ [2]
    private static void Step_1887(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xC0: RET NZ [4]
    private static void Step_1889(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xC0: RET NZ [5]
    private static void Step_1890(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
        emulator.PC = emulator.WZ;
    }

    // 0xDD 0xC0: RET NZ [6]
    private static void Step_1891(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xC1: POP BC [0]
    private static void Step_1892(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xC1: POP BC [1]
    private static void Step_1893(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.C = emulator.data;
    }

    // 0xDD 0xC1: POP BC [3]
    private static void Step_1895(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xC1: POP BC [4]
    private static void Step_1896(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B = emulator.data;
    }

    // 0xDD 0xC1: POP BC [5]
    private static void Step_1897(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xC2: JP NZ [0]
    private static void Step_1898(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xC2: JP NZ [1]
    private static void Step_1899(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xC2: JP NZ [3]
    private static void Step_1901(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xC2: JP NZ [4]
    private static void Step_1902(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDD 0xC2: JP NZ [5]
    private static void Step_1903(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b01000000) == 0b00000000 /* condition.NZ */)
        {
            emulator.PC = emulator.WZ;
        }

        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xC3: JP [0]
    private static void Step_1904(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xC3: JP [1]
    private static void Step_1905(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xC3: JP [3]
    private static void Step_1907(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xC3: JP [4]
    private static void Step_1908(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
        emulator.PC = emulator.WZ;
    }

    // 0xDD 0xC3: JP [5]
    private static void Step_1909(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xC4: CALL NZ, nn [0]
    private static void Step_1910(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xC4: CALL NZ, nn [1]
    private static void Step_1911(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xC4: CALL NZ, nn [3]
    private static void Step_1913(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xC4: CALL NZ, nn [4]
    private static void Step_1914(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDD 0xC4: CALL NZ, nn [5]
    private static void Step_1915(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b01000000) == 0b01000000 /* !condition.NZ */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xDD 0xC4: CALL NZ, nn [7]
    private static void Step_1917(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xDD 0xC4: CALL NZ, nn [10]
    private static void Step_1920(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = emulator.WZ;
    }

    // 0xDD 0xC4: CALL NZ, nn [12]
    private static void Step_1922(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xC5: PUSH BC [0]
    private static void Step_1923(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xDD 0xC5: PUSH BC [1]
    private static void Step_1924(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.B;
    }

    // 0xDD 0xC5: PUSH BC [4]
    private static void Step_1927(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.C;
    }

    // 0xDD 0xC5: PUSH BC [6]
    private static void Step_1929(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xC6: ADD A, n [0]
    private static void Step_1930(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xC6: ADD A, n [2]
    private static void Step_1932(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left + right;
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xC7: RST 0x00 [0]
    private static void Step_1933(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xDD 0xC7: RST 0x00 [1]
    private static void Step_1934(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xDD 0xC7: RST 0x00 [4]
    private static void Step_1937(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = 0x00;
        emulator.WZ = 0x00;
    }

    // 0xDD 0xC7: RST 0x00 [6]
    private static void Step_1939(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xC8: RET Z [0]
    private static void Step_1940(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        if ((emulator.F & 0b01000000) == 0b00000000 /* !condition.Z */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xDD 0xC8: RET Z [1]
    private static void Step_1941(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xC8: RET Z [2]
    private static void Step_1942(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xC8: RET Z [4]
    private static void Step_1944(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xC8: RET Z [5]
    private static void Step_1945(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
        emulator.PC = emulator.WZ;
    }

    // 0xDD 0xC8: RET Z [6]
    private static void Step_1946(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xC9: RET [0]
    private static void Step_1947(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xC9: RET [1]
    private static void Step_1948(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xC9: RET [3]
    private static void Step_1950(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xC9: RET [4]
    private static void Step_1951(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
        emulator.PC = emulator.WZ;
    }

    // 0xDD 0xC9: RET [5]
    private static void Step_1952(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xCA: JP Z [0]
    private static void Step_1953(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xCA: JP Z [1]
    private static void Step_1954(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xCA: JP Z [3]
    private static void Step_1956(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xCA: JP Z [4]
    private static void Step_1957(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDD 0xCA: JP Z [5]
    private static void Step_1958(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b01000000) == 0b01000000 /* condition.Z */)
        {
            emulator.PC = emulator.WZ;
        }

        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xCB: DDCB Redirect [0]
    private static void Step_1959(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.opcodeStepTable = OpcodeStepTableDDCB;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xCB: DDCB Redirect [1]
    private static void Step_1960(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.latch = emulator.data;
    }

    // 0xDD 0xCB: DDCB Redirect [3]
    private static void Step_1962(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xCB: DDCB Redirect [5]
    private static void Step_1964(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.currentStep = Unsafe.Add(ref MemoryMarshal.GetArrayDataReference(emulator.opcodeStepTable), emulator.data);
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xDD 0xCC: CALL Z, nn [0]
    private static void Step_1965(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xCC: CALL Z, nn [1]
    private static void Step_1966(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xCC: CALL Z, nn [3]
    private static void Step_1968(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xCC: CALL Z, nn [4]
    private static void Step_1969(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDD 0xCC: CALL Z, nn [5]
    private static void Step_1970(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b01000000) == 0b00000000 /* !condition.Z */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xDD 0xCC: CALL Z, nn [7]
    private static void Step_1972(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xDD 0xCC: CALL Z, nn [10]
    private static void Step_1975(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = emulator.WZ;
    }

    // 0xDD 0xCC: CALL Z, nn [12]
    private static void Step_1977(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xCD: CALL nn [0]
    private static void Step_1978(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xCD: CALL nn [1]
    private static void Step_1979(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xCD: CALL nn [3]
    private static void Step_1981(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xCD: CALL nn [4]
    private static void Step_1982(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDD 0xCD: CALL nn [7]
    private static void Step_1985(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xDD 0xCD: CALL nn [10]
    private static void Step_1988(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = emulator.WZ;
    }

    // 0xDD 0xCD: CALL nn [12]
    private static void Step_1990(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xCE: ADC A, n [0]
    private static void Step_1991(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xCE: ADC A, n [2]
    private static void Step_1993(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xCF: RST 0x08 [0]
    private static void Step_1994(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xDD 0xCF: RST 0x08 [1]
    private static void Step_1995(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xDD 0xCF: RST 0x08 [4]
    private static void Step_1998(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = 0x08;
        emulator.WZ = 0x08;
    }

    // 0xDD 0xCF: RST 0x08 [6]
    private static void Step_2000(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xD0: RET NC [0]
    private static void Step_2001(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        if ((emulator.F & 0b00000001) == 0b00000001 /* !condition.NC */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xDD 0xD0: RET NC [1]
    private static void Step_2002(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xD0: RET NC [2]
    private static void Step_2003(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xD0: RET NC [4]
    private static void Step_2005(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xD0: RET NC [5]
    private static void Step_2006(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
        emulator.PC = emulator.WZ;
    }

    // 0xDD 0xD0: RET NC [6]
    private static void Step_2007(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xD1: POP DE [0]
    private static void Step_2008(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xD1: POP DE [1]
    private static void Step_2009(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.E = emulator.data;
    }

    // 0xDD 0xD1: POP DE [3]
    private static void Step_2011(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xD1: POP DE [4]
    private static void Step_2012(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.D = emulator.data;
    }

    // 0xDD 0xD1: POP DE [5]
    private static void Step_2013(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xD2: JP NC [0]
    private static void Step_2014(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xD2: JP NC [1]
    private static void Step_2015(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xD2: JP NC [3]
    private static void Step_2017(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xD2: JP NC [4]
    private static void Step_2018(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDD 0xD2: JP NC [5]
    private static void Step_2019(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000001) == 0b00000000 /* condition.NC */)
        {
            emulator.PC = emulator.WZ;
        }

        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xD3: OUT (n), A [0]
    private static void Step_2020(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xD3: OUT (n), A [1]
    private static void Step_2021(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
        emulator.W = emulator.A;
    }

    // 0xDD 0xD3: OUT (n), A [3]
    private static void Step_2023(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.data = emulator.A;
    }

    // 0xDD 0xD3: OUT (n), A [5]
    private static void Step_2025(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z += 0x01;
    }

    // 0xDD 0xD3: OUT (n), A [6]
    private static void Step_2026(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xD4: CALL NC, nn [0]
    private static void Step_2027(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xD4: CALL NC, nn [1]
    private static void Step_2028(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xD4: CALL NC, nn [3]
    private static void Step_2030(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xD4: CALL NC, nn [4]
    private static void Step_2031(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDD 0xD4: CALL NC, nn [5]
    private static void Step_2032(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000001) == 0b00000001 /* !condition.NC */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xDD 0xD4: CALL NC, nn [7]
    private static void Step_2034(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xDD 0xD4: CALL NC, nn [10]
    private static void Step_2037(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = emulator.WZ;
    }

    // 0xDD 0xD4: CALL NC, nn [12]
    private static void Step_2039(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xD5: PUSH DE [0]
    private static void Step_2040(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xDD 0xD5: PUSH DE [1]
    private static void Step_2041(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.D;
    }

    // 0xDD 0xD5: PUSH DE [4]
    private static void Step_2044(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.E;
    }

    // 0xDD 0xD5: PUSH DE [6]
    private static void Step_2046(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xD6: SUB n [0]
    private static void Step_2047(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xD6: SUB n [1]
    private static void Step_2048(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left - right;
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xDD 0xD6: SUB n [2]
    private static void Step_2049(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xD7: RST 0x10 [0]
    private static void Step_2050(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xDD 0xD7: RST 0x10 [1]
    private static void Step_2051(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xDD 0xD7: RST 0x10 [4]
    private static void Step_2054(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = 0x10;
        emulator.WZ = 0x10;
    }

    // 0xDD 0xD7: RST 0x10 [6]
    private static void Step_2056(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xD8: RET C [0]
    private static void Step_2057(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        if ((emulator.F & 0b00000001) == 0b00000000 /* !condition.C */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xDD 0xD8: RET C [1]
    private static void Step_2058(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xD8: RET C [2]
    private static void Step_2059(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xD8: RET C [4]
    private static void Step_2061(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xD8: RET C [5]
    private static void Step_2062(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
        emulator.PC = emulator.WZ;
    }

    // 0xDD 0xD8: RET C [6]
    private static void Step_2063(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xD9: EXX [0]
    private static void Step_2064(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.Shadow_BC;
        emulator.Shadow_BC = emulator.BC;
        emulator.BC = temp;
        temp = emulator.Shadow_DE;
        emulator.Shadow_DE = emulator.DE;
        emulator.DE = temp;
        temp = emulator.Shadow_HL;
        emulator.Shadow_HL = emulator.HL;
        emulator.HL = temp;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xDA: JP C [0]
    private static void Step_2065(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xDA: JP C [1]
    private static void Step_2066(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xDA: JP C [3]
    private static void Step_2068(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xDA: JP C [4]
    private static void Step_2069(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDD 0xDA: JP C [5]
    private static void Step_2070(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000001) == 0b00000001 /* condition.C */)
        {
            emulator.PC = emulator.WZ;
        }

        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xDB: IN A, (n) [0]
    private static void Step_2071(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xDB: IN A, (n) [1]
    private static void Step_2072(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
        emulator.W = emulator.A;
    }

    // 0xDD 0xDB: IN A, (n) [3]
    private static void Step_2074(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.data = emulator.A;
    }

    // 0xDD 0xDB: IN A, (n) [5]
    private static void Step_2076(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A = emulator.data;
        emulator.WZ += 0x01;
    }

    // 0xDD 0xDB: IN A, (n) [6]
    private static void Step_2077(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xDC: CALL C, nn [0]
    private static void Step_2078(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xDC: CALL C, nn [1]
    private static void Step_2079(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xDC: CALL C, nn [3]
    private static void Step_2081(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xDC: CALL C, nn [4]
    private static void Step_2082(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDD 0xDC: CALL C, nn [5]
    private static void Step_2083(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000001) == 0b00000000 /* !condition.C */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xDD 0xDC: CALL C, nn [7]
    private static void Step_2085(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xDD 0xDC: CALL C, nn [10]
    private static void Step_2088(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = emulator.WZ;
    }

    // 0xDD 0xDC: CALL C, nn [12]
    private static void Step_2090(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xDE: SBC n [0]
    private static void Step_2091(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xDE: SBC n [1]
    private static void Step_2092(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xDD 0xDE: SBC n [2]
    private static void Step_2093(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xDF: RST 0x18 [0]
    private static void Step_2094(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xDD 0xDF: RST 0x18 [1]
    private static void Step_2095(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xDD 0xDF: RST 0x18 [4]
    private static void Step_2098(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = 0x18;
        emulator.WZ = 0x18;
    }

    // 0xDD 0xDF: RST 0x18 [6]
    private static void Step_2100(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xE0: RET PO [0]
    private static void Step_2101(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        if ((emulator.F & 0b00000100) == 0b00000100 /* !condition.PO */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xDD 0xE0: RET PO [1]
    private static void Step_2102(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xE0: RET PO [2]
    private static void Step_2103(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xE0: RET PO [4]
    private static void Step_2105(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xE0: RET PO [5]
    private static void Step_2106(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
        emulator.PC = emulator.WZ;
    }

    // 0xDD 0xE0: RET PO [6]
    private static void Step_2107(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xE1: POP IX [0]
    private static void Step_2108(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xE1: POP IX [1]
    private static void Step_2109(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.IXL = emulator.data;
    }

    // 0xDD 0xE1: POP IX [3]
    private static void Step_2111(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xE1: POP IX [4]
    private static void Step_2112(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.IXH = emulator.data;
    }

    // 0xDD 0xE1: POP IX [5]
    private static void Step_2113(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xE2: JP PO [0]
    private static void Step_2114(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xE2: JP PO [1]
    private static void Step_2115(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xE2: JP PO [3]
    private static void Step_2117(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xE2: JP PO [4]
    private static void Step_2118(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDD 0xE2: JP PO [5]
    private static void Step_2119(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000100) == 0b00000000 /* condition.PO */)
        {
            emulator.PC = emulator.WZ;
        }

        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xE3: EX (SP); IX [0]
    private static void Step_2120(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.SP;
    }

    // 0xDD 0xE3: EX (SP); IX [1]
    private static void Step_2121(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xE3: EX (SP); IX [3]
    private static void Step_2123(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = (ushort)(emulator.SP + 0x01);
    }

    // 0xDD 0xE3: EX (SP); IX [4]
    private static void Step_2124(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDD 0xE3: EX (SP); IX [7]
    private static void Step_2127(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = emulator.IXH;
    }

    // 0xDD 0xE3: EX (SP); IX [10]
    private static void Step_2130(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.data = emulator.IXL;
    }

    // 0xDD 0xE3: EX (SP); IX [14]
    private static void Step_2134(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.IX = emulator.WZ;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xE4: CALL PO, nn [0]
    private static void Step_2135(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xE4: CALL PO, nn [1]
    private static void Step_2136(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xE4: CALL PO, nn [3]
    private static void Step_2138(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xE4: CALL PO, nn [4]
    private static void Step_2139(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDD 0xE4: CALL PO, nn [5]
    private static void Step_2140(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000100) == 0b00000100 /* !condition.PO */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xDD 0xE4: CALL PO, nn [7]
    private static void Step_2142(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xDD 0xE4: CALL PO, nn [10]
    private static void Step_2145(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = emulator.WZ;
    }

    // 0xDD 0xE4: CALL PO, nn [12]
    private static void Step_2147(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xE5: PUSH IX [0]
    private static void Step_2148(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xDD 0xE5: PUSH IX [1]
    private static void Step_2149(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.IXH;
    }

    // 0xDD 0xE5: PUSH IX [4]
    private static void Step_2152(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.IXL;
    }

    // 0xDD 0xE5: PUSH IX [6]
    private static void Step_2154(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xE6: AND n [0]
    private static void Step_2155(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xE6: AND n [1]
    private static void Step_2156(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A &= emulator.data;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset C and N.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xDD 0xE6: AND n [2]
    private static void Step_2157(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xE7: RST 0x20 [0]
    private static void Step_2158(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xDD 0xE7: RST 0x20 [1]
    private static void Step_2159(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xDD 0xE7: RST 0x20 [4]
    private static void Step_2162(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = 0x20;
        emulator.WZ = 0x20;
    }

    // 0xDD 0xE7: RST 0x20 [6]
    private static void Step_2164(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xE8: RET PE [0]
    private static void Step_2165(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        if ((emulator.F & 0b00000100) == 0b00000000 /* !condition.PE */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xDD 0xE8: RET PE [1]
    private static void Step_2166(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xE8: RET PE [2]
    private static void Step_2167(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xE8: RET PE [4]
    private static void Step_2169(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xE8: RET PE [5]
    private static void Step_2170(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
        emulator.PC = emulator.WZ;
    }

    // 0xDD 0xE8: RET PE [6]
    private static void Step_2171(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xE9: JP IX [0]
    private static void Step_2172(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.PC = emulator.IX;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xEA: JP PE [0]
    private static void Step_2173(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xEA: JP PE [1]
    private static void Step_2174(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xEA: JP PE [3]
    private static void Step_2176(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xEA: JP PE [4]
    private static void Step_2177(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDD 0xEA: JP PE [5]
    private static void Step_2178(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000100) == 0b00000100 /* condition.PE */)
        {
            emulator.PC = emulator.WZ;
        }

        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xEB: EX DE; HL [0]
    private static void Step_2179(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var temp = emulator.DE;
        emulator.DE = emulator.HL;
        emulator.HL = temp;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xEC: CALL PE, nn [0]
    private static void Step_2180(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xEC: CALL PE, nn [1]
    private static void Step_2181(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xEC: CALL PE, nn [3]
    private static void Step_2183(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xEC: CALL PE, nn [4]
    private static void Step_2184(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDD 0xEC: CALL PE, nn [5]
    private static void Step_2185(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b00000100) == 0b00000000 /* !condition.PE */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xDD 0xEC: CALL PE, nn [7]
    private static void Step_2187(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xDD 0xEC: CALL PE, nn [10]
    private static void Step_2190(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = emulator.WZ;
    }

    // 0xDD 0xEC: CALL PE, nn [12]
    private static void Step_2192(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xED: DDED Redirect [0]
    private static void Step_2193(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.opcodeStepTable = OpcodeStepTablePrefixED;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xEE: XOR n [0]
    private static void Step_2194(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xEE: XOR n [1]
    private static void Step_2195(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A ^= emulator.data;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xDD 0xEE: XOR n [2]
    private static void Step_2196(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xEF: RST 0x28 [0]
    private static void Step_2197(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xDD 0xEF: RST 0x28 [1]
    private static void Step_2198(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xDD 0xEF: RST 0x28 [4]
    private static void Step_2201(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = 0x28;
        emulator.WZ = 0x28;
    }

    // 0xDD 0xEF: RST 0x28 [6]
    private static void Step_2203(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xF0: RET P [0]
    private static void Step_2204(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        if ((emulator.F & 0b10000000) == 0b10000000 /* !condition.P */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xDD 0xF0: RET P [1]
    private static void Step_2205(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xF0: RET P [2]
    private static void Step_2206(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xF0: RET P [4]
    private static void Step_2208(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xF0: RET P [5]
    private static void Step_2209(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
        emulator.PC = emulator.WZ;
    }

    // 0xDD 0xF0: RET P [6]
    private static void Step_2210(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xF1: POP AF [0]
    private static void Step_2211(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xF1: POP AF [1]
    private static void Step_2212(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.F = emulator.data;
    }

    // 0xDD 0xF1: POP AF [3]
    private static void Step_2214(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xF1: POP AF [4]
    private static void Step_2215(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A = emulator.data;
    }

    // 0xDD 0xF1: POP AF [5]
    private static void Step_2216(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xF2: JP P [0]
    private static void Step_2217(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xF2: JP P [1]
    private static void Step_2218(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xF2: JP P [3]
    private static void Step_2220(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xF2: JP P [4]
    private static void Step_2221(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDD 0xF2: JP P [5]
    private static void Step_2222(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b10000000) == 0b00000000 /* condition.P */)
        {
            emulator.PC = emulator.WZ;
        }

        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xF3: DI [0]
    private static void Step_2223(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.iff1 = false;
        emulator.iff2 = false;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xF4: CALL P, nn [0]
    private static void Step_2224(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xF4: CALL P, nn [1]
    private static void Step_2225(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xF4: CALL P, nn [3]
    private static void Step_2227(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xF4: CALL P, nn [4]
    private static void Step_2228(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDD 0xF4: CALL P, nn [5]
    private static void Step_2229(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b10000000) == 0b10000000 /* !condition.P */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xDD 0xF4: CALL P, nn [7]
    private static void Step_2231(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xDD 0xF4: CALL P, nn [10]
    private static void Step_2234(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = emulator.WZ;
    }

    // 0xDD 0xF4: CALL P, nn [12]
    private static void Step_2236(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xF5: PUSH AF [0]
    private static void Step_2237(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xDD 0xF5: PUSH AF [1]
    private static void Step_2238(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.A;
    }

    // 0xDD 0xF5: PUSH AF [4]
    private static void Step_2241(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.F;
    }

    // 0xDD 0xF5: PUSH AF [6]
    private static void Step_2243(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xF6: OR n [0]
    private static void Step_2244(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xF6: OR n [1]
    private static void Step_2245(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A |= emulator.data;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xDD 0xF6: OR n [2]
    private static void Step_2246(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xF7: RST 0x30 [0]
    private static void Step_2247(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xDD 0xF7: RST 0x30 [1]
    private static void Step_2248(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xDD 0xF7: RST 0x30 [4]
    private static void Step_2251(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = 0x30;
        emulator.WZ = 0x30;
    }

    // 0xDD 0xF7: RST 0x30 [6]
    private static void Step_2253(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xF8: RET M [0]
    private static void Step_2254(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        if ((emulator.F & 0b10000000) == 0b00000000 /* !condition.M */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xDD 0xF8: RET M [1]
    private static void Step_2255(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xF8: RET M [2]
    private static void Step_2256(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xF8: RET M [4]
    private static void Step_2258(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xDD 0xF8: RET M [5]
    private static void Step_2259(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
        emulator.PC = emulator.WZ;
    }

    // 0xDD 0xF8: RET M [6]
    private static void Step_2260(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xF9: LD SP, IX [0]
    private static void Step_2261(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.SP = emulator.IX;
    }

    // 0xDD 0xF9: LD SP, IX [1]
    private static void Step_2262(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xFA: JP M [0]
    private static void Step_2263(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xFA: JP M [1]
    private static void Step_2264(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xFA: JP M [3]
    private static void Step_2266(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xFA: JP M [4]
    private static void Step_2267(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDD 0xFA: JP M [5]
    private static void Step_2268(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b10000000) == 0b10000000 /* condition.M */)
        {
            emulator.PC = emulator.WZ;
        }

        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xFB: EI [0]
    private static void Step_2269(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.iff1 = true;
        emulator.iff2 = true;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        emulator.address = emulator.PC;
        emulator.currentStep = 0x01;
    }

    // 0xDD 0xFC: CALL M, nn [0]
    private static void Step_2270(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xFC: CALL M, nn [1]
    private static void Step_2271(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xDD 0xFC: CALL M, nn [3]
    private static void Step_2273(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xFC: CALL M, nn [4]
    private static void Step_2274(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xDD 0xFC: CALL M, nn [5]
    private static void Step_2275(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if ((emulator.F & 0b10000000) == 0b00000000 /* !condition.M */)
        {
            emulator.Q = 0x00;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xDD 0xFC: CALL M, nn [7]
    private static void Step_2277(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xDD 0xFC: CALL M, nn [10]
    private static void Step_2280(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = emulator.WZ;
    }

    // 0xDD 0xFC: CALL M, nn [12]
    private static void Step_2282(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xFD: DDFD Redirect [0]
    private static void Step_2283(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.opcodeStepTable = OpcodeStepTablePrefixFD;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xDD 0xFE: CP n [0]
    private static void Step_2284(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xDD 0xFE: CP n [1]
    private static void Step_2285(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left - right;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= right & 0b00101000; // Copy X and Y from right.
        flags |= result & 0b10000000; // Copy S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 6; // Set Z if is_zero(result) is true.
        emulator.F = (byte)flags;
    }

    // 0xDD 0xFE: CP n [2]
    private static void Step_2286(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xDD 0xFF: RST 0x38 [0]
    private static void Step_2287(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xDD 0xFF: RST 0x38 [1]
    private static void Step_2288(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCH;
    }

    // 0xDD 0xFF: RST 0x38 [4]
    private static void Step_2291(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.PCL;
        emulator.PC = 0x38;
        emulator.WZ = 0x38;
    }

    // 0xDD 0xFF: RST 0x38 [6]
    private static void Step_2293(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x40: IN B, (C) [0]
    private static void Step_2294(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.BC;
    }

    // 0xED 0x40: IN B, (C) [2]
    private static void Step_2296(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC + 0x01);
    }

    // 0xED 0x40: IN B, (C) [3]
    private static void Step_2297(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x41: OUT B, (C) [0]
    private static void Step_2298(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.BC;
        emulator.data = emulator.B;
    }

    // 0xED 0x41: OUT B, (C) [2]
    private static void Step_2300(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC + 0x01);
    }

    // 0xED 0x41: OUT B, (C) [3]
    private static void Step_2301(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x42: SBC HL, BC [0]
    private static void Step_2302(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.HL;
        var right = emulator.BC;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.HL = (ushort)result;
        emulator.WZ = (ushort)(left + 0x01);

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.H & 0b10101000; // Copy X, Y and S from H.
        flags |= Unsafe.BitCast<bool, byte>((result & 65536) == 65536); // Set C if (result & 65536) == 65536 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x8000) >> 13; // Set PV if did_16_bit_subtraction_overflow(left, right, result) is true.
        flags |= ((left ^ right ^ result) & 0x1000) >> 8; // Set H if did_16_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.HL == 0)) << 6; // Set Z if is_zero(HL) is true.
        emulator.F = (byte)flags;
    }

    // 0xED 0x42: SBC HL, BC [6]
    private static void Step_2308(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x43: LD (nn), BC [0]
    private static void Step_2309(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xED 0x43: LD (nn), BC [1]
    private static void Step_2310(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xED 0x43: LD (nn), BC [3]
    private static void Step_2312(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xED 0x43: LD (nn), BC [4]
    private static void Step_2313(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xED 0x43: LD (nn), BC [6]
    private static void Step_2315(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.data = emulator.C;
        emulator.WZ += 0x01;
    }

    // 0xED 0x43: LD (nn), BC [9]
    private static void Step_2318(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.data = emulator.B;
    }

    // 0xED 0x43: LD (nn), BC [11]
    private static void Step_2320(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x44: NEG [0]
    private static void Step_2321(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var value = emulator.A;
        emulator.A = (byte)(0x00 - value);

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= Unsafe.BitCast<bool, byte>(emulator.A != 0x00); // Set C if A != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0x80)) << 2; // Set PV if A == 0x80 is true.
        flags |= (Unsafe.BitCast<bool, byte>(((value ^ emulator.A) & 0x10) != 0x00)) << 4; // Set H if ((value ^ A) & 0x10) != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xED 0x45: RETN [0]
    private static void Step_2322(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xED 0x45: RETN [1]
    private static void Step_2323(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xED 0x45: RETN [3]
    private static void Step_2325(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xED 0x45: RETN [4]
    private static void Step_2326(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
        emulator.PC = emulator.WZ;
    }

    // 0xED 0x45: RETN [5]
    private static void Step_2327(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.iff1 = emulator.iff2;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x46: IM 0 [0]
    private static void Step_2328(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.im = 0x00;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xED 0x47: LD I, A [0]
    private static void Step_2329(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.I = emulator.A;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x48: IN C, (C) [0]
    private static void Step_2330(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.BC;
    }

    // 0xED 0x48: IN C, (C) [2]
    private static void Step_2332(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC + 0x01);
    }

    // 0xED 0x48: IN C, (C) [3]
    private static void Step_2333(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.C = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x49: OUT C, (C) [0]
    private static void Step_2334(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.BC;
        emulator.data = emulator.C;
    }

    // 0xED 0x49: OUT C, (C) [2]
    private static void Step_2336(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC + 0x01);
    }

    // 0xED 0x49: OUT C, (C) [3]
    private static void Step_2337(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x4A: ADC HL, BC [0]
    private static void Step_2338(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.HL;
        var right = emulator.BC;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.HL = (ushort)result;
        emulator.WZ = (ushort)(left + 0x01);

        // Update flags.
        // Reset N.
        int flags = emulator.H & 0b10101000; // Copy X, Y and S from H.
        flags |= Unsafe.BitCast<bool, byte>((result & 65536) == 65536); // Set C if (result & 65536) == 65536 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x8000) >> 13; // Set PV if did_16_bit_addition_overflow(left, right, result) is true.
        flags |= ((left ^ result ^ right) & 0x1000) >> 8; // Set H if did_16_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.HL == 0)) << 6; // Set Z if is_zero(HL) is true.
        emulator.F = (byte)flags;
    }

    // 0xED 0x4A: ADC HL, BC [6]
    private static void Step_2344(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x4B: LD BC, (nn) [0]
    private static void Step_2345(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xED 0x4B: LD BC, (nn) [1]
    private static void Step_2346(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xED 0x4B: LD BC, (nn) [3]
    private static void Step_2348(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xED 0x4B: LD BC, (nn) [4]
    private static void Step_2349(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xED 0x4B: LD BC, (nn) [6]
    private static void Step_2351(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.WZ += 0x01;
    }

    // 0xED 0x4B: LD BC, (nn) [7]
    private static void Step_2352(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.C = emulator.data;
    }

    // 0xED 0x4B: LD BC, (nn) [9]
    private static void Step_2354(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
    }

    // 0xED 0x4B: LD BC, (nn) [10]
    private static void Step_2355(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B = emulator.data;
    }

    // 0xED 0x4B: LD BC, (nn) [11]
    private static void Step_2356(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x4D: RETI [0]
    private static void Step_2357(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xED 0x4D: RETI [1]
    private static void Step_2358(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xED 0x4D: RETI [3]
    private static void Step_2360(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xED 0x4D: RETI [4]
    private static void Step_2361(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
        emulator.PC = emulator.WZ;
    }

    // 0xED 0x4D: RETI [5]
    private static void Step_2362(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.iff1 = emulator.iff2;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x4F: LD R, A [0]
    private static void Step_2363(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.R = emulator.A;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x50: IN D, (C) [0]
    private static void Step_2364(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.BC;
    }

    // 0xED 0x50: IN D, (C) [2]
    private static void Step_2366(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC + 0x01);
    }

    // 0xED 0x50: IN D, (C) [3]
    private static void Step_2367(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.D = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x51: OUT D, (C) [0]
    private static void Step_2368(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.BC;
        emulator.data = emulator.D;
    }

    // 0xED 0x51: OUT D, (C) [2]
    private static void Step_2370(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC + 0x01);
    }

    // 0xED 0x51: OUT D, (C) [3]
    private static void Step_2371(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x52: SBC HL, DE [0]
    private static void Step_2372(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.HL;
        var right = emulator.DE;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.HL = (ushort)result;
        emulator.WZ = (ushort)(left + 0x01);

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.H & 0b10101000; // Copy X, Y and S from H.
        flags |= Unsafe.BitCast<bool, byte>((result & 65536) == 65536); // Set C if (result & 65536) == 65536 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x8000) >> 13; // Set PV if did_16_bit_subtraction_overflow(left, right, result) is true.
        flags |= ((left ^ right ^ result) & 0x1000) >> 8; // Set H if did_16_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.HL == 0)) << 6; // Set Z if is_zero(HL) is true.
        emulator.F = (byte)flags;
    }

    // 0xED 0x52: SBC HL, DE [6]
    private static void Step_2378(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x53: LD (nn), DE [0]
    private static void Step_2379(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xED 0x53: LD (nn), DE [1]
    private static void Step_2380(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xED 0x53: LD (nn), DE [3]
    private static void Step_2382(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xED 0x53: LD (nn), DE [4]
    private static void Step_2383(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xED 0x53: LD (nn), DE [6]
    private static void Step_2385(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.data = emulator.E;
        emulator.WZ += 0x01;
    }

    // 0xED 0x53: LD (nn), DE [9]
    private static void Step_2388(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.data = emulator.D;
    }

    // 0xED 0x53: LD (nn), DE [11]
    private static void Step_2390(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x56: IM 1 [0]
    private static void Step_2391(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.im = 0x01;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xED 0x57: LD A, I [0]
    private static void Step_2392(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = emulator.I;

        // Update flags.
        // Reset N and H.
        int flags = emulator.I & 0b10101000; // Copy X, Y and S from I.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.iff2)) << 2; // Set PV if iff2 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.I == 0)) << 6; // Set Z if is_zero(I) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x58: IN E, (C) [0]
    private static void Step_2393(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.BC;
    }

    // 0xED 0x58: IN E, (C) [2]
    private static void Step_2395(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC + 0x01);
    }

    // 0xED 0x58: IN E, (C) [3]
    private static void Step_2396(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.E = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x59: OUT E, (C) [0]
    private static void Step_2397(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.BC;
        emulator.data = emulator.E;
    }

    // 0xED 0x59: OUT E, (C) [2]
    private static void Step_2399(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC + 0x01);
    }

    // 0xED 0x59: OUT E, (C) [3]
    private static void Step_2400(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x5A: ADC HL, DE [0]
    private static void Step_2401(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.HL;
        var right = emulator.DE;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.HL = (ushort)result;
        emulator.WZ = (ushort)(left + 0x01);

        // Update flags.
        // Reset N.
        int flags = emulator.H & 0b10101000; // Copy X, Y and S from H.
        flags |= Unsafe.BitCast<bool, byte>((result & 65536) == 65536); // Set C if (result & 65536) == 65536 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x8000) >> 13; // Set PV if did_16_bit_addition_overflow(left, right, result) is true.
        flags |= ((left ^ result ^ right) & 0x1000) >> 8; // Set H if did_16_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.HL == 0)) << 6; // Set Z if is_zero(HL) is true.
        emulator.F = (byte)flags;
    }

    // 0xED 0x5A: ADC HL, DE [6]
    private static void Step_2407(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x5B: LD DE, (nn) [0]
    private static void Step_2408(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xED 0x5B: LD DE, (nn) [1]
    private static void Step_2409(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xED 0x5B: LD DE, (nn) [3]
    private static void Step_2411(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xED 0x5B: LD DE, (nn) [4]
    private static void Step_2412(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xED 0x5B: LD DE, (nn) [6]
    private static void Step_2414(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.WZ += 0x01;
    }

    // 0xED 0x5B: LD DE, (nn) [7]
    private static void Step_2415(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.E = emulator.data;
    }

    // 0xED 0x5B: LD DE, (nn) [9]
    private static void Step_2417(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
    }

    // 0xED 0x5B: LD DE, (nn) [10]
    private static void Step_2418(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.D = emulator.data;
    }

    // 0xED 0x5B: LD DE, (nn) [11]
    private static void Step_2419(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x5E: IM 2 [0]
    private static void Step_2420(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.im = 0x02;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xED 0x5F: LD A, R [0]
    private static void Step_2421(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = emulator.R;

        // Update flags.
        // Reset N and H.
        int flags = emulator.R & 0b10101000; // Copy X, Y and S from R.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.iff2)) << 2; // Set PV if iff2 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.R == 0)) << 6; // Set Z if is_zero(R) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x60: IN H, (C) [0]
    private static void Step_2422(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.BC;
    }

    // 0xED 0x60: IN H, (C) [2]
    private static void Step_2424(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC + 0x01);
    }

    // 0xED 0x60: IN H, (C) [3]
    private static void Step_2425(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.H = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x61: OUT H, (C) [0]
    private static void Step_2426(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.BC;
        emulator.data = emulator.H;
    }

    // 0xED 0x61: OUT H, (C) [2]
    private static void Step_2428(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC + 0x01);
    }

    // 0xED 0x61: OUT H, (C) [3]
    private static void Step_2429(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x62: SBC HL, HL [0]
    private static void Step_2430(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.HL;
        var right = emulator.HL;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.HL = (ushort)result;
        emulator.WZ = (ushort)(left + 0x01);

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.H & 0b10101000; // Copy X, Y and S from H.
        flags |= Unsafe.BitCast<bool, byte>((result & 65536) == 65536); // Set C if (result & 65536) == 65536 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x8000) >> 13; // Set PV if did_16_bit_subtraction_overflow(left, right, result) is true.
        flags |= ((left ^ right ^ result) & 0x1000) >> 8; // Set H if did_16_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.HL == 0)) << 6; // Set Z if is_zero(HL) is true.
        emulator.F = (byte)flags;
    }

    // 0xED 0x62: SBC HL, HL [6]
    private static void Step_2436(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x63: LD (nn), HL [0]
    private static void Step_2437(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xED 0x63: LD (nn), HL [1]
    private static void Step_2438(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xED 0x63: LD (nn), HL [3]
    private static void Step_2440(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xED 0x63: LD (nn), HL [4]
    private static void Step_2441(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xED 0x63: LD (nn), HL [6]
    private static void Step_2443(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.data = emulator.L;
        emulator.WZ += 0x01;
    }

    // 0xED 0x63: LD (nn), HL [9]
    private static void Step_2446(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.data = emulator.H;
    }

    // 0xED 0x63: LD (nn), HL [11]
    private static void Step_2448(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x67: RRD [0]
    private static void Step_2449(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xED 0x67: RRD [7]
    private static void Step_2456(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var lowA = emulator.A & 0x0F;
        emulator.A = (byte)(emulator.A & 0xF0 | emulator.data & 0x0F);
        emulator.data = (byte)(emulator.data >> 0x04 | lowA << 0x04);

        // Update flags.
        // Reset N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.WZ = (ushort)(emulator.HL + 0x01);
        emulator.address = emulator.HL;
    }

    // 0xED 0x67: RRD [9]
    private static void Step_2458(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x68: IN L, (C) [0]
    private static void Step_2459(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.BC;
    }

    // 0xED 0x68: IN L, (C) [2]
    private static void Step_2461(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC + 0x01);
    }

    // 0xED 0x68: IN L, (C) [3]
    private static void Step_2462(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.L = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x69: OUT L, (C) [0]
    private static void Step_2463(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.BC;
        emulator.data = emulator.L;
    }

    // 0xED 0x69: OUT L, (C) [2]
    private static void Step_2465(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC + 0x01);
    }

    // 0xED 0x69: OUT L, (C) [3]
    private static void Step_2466(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x6A: ADC HL, HL [0]
    private static void Step_2467(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.HL;
        var right = emulator.HL;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.HL = (ushort)result;
        emulator.WZ = (ushort)(left + 0x01);

        // Update flags.
        // Reset N.
        int flags = emulator.H & 0b10101000; // Copy X, Y and S from H.
        flags |= Unsafe.BitCast<bool, byte>((result & 65536) == 65536); // Set C if (result & 65536) == 65536 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x8000) >> 13; // Set PV if did_16_bit_addition_overflow(left, right, result) is true.
        flags |= ((left ^ result ^ right) & 0x1000) >> 8; // Set H if did_16_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.HL == 0)) << 6; // Set Z if is_zero(HL) is true.
        emulator.F = (byte)flags;
    }

    // 0xED 0x6A: ADC HL, HL [6]
    private static void Step_2473(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x6B: LD HL, (nn) [0]
    private static void Step_2474(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xED 0x6B: LD HL, (nn) [1]
    private static void Step_2475(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xED 0x6B: LD HL, (nn) [3]
    private static void Step_2477(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xED 0x6B: LD HL, (nn) [4]
    private static void Step_2478(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xED 0x6B: LD HL, (nn) [6]
    private static void Step_2480(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.WZ += 0x01;
    }

    // 0xED 0x6B: LD HL, (nn) [7]
    private static void Step_2481(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.L = emulator.data;
    }

    // 0xED 0x6B: LD HL, (nn) [9]
    private static void Step_2483(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
    }

    // 0xED 0x6B: LD HL, (nn) [10]
    private static void Step_2484(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.H = emulator.data;
    }

    // 0xED 0x6B: LD HL, (nn) [11]
    private static void Step_2485(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x6F: RLD [0]
    private static void Step_2486(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
    }

    // 0xED 0x6F: RLD [7]
    private static void Step_2493(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var lowA = emulator.A & 0x0F;
        emulator.A = (byte)(emulator.A & 0xF0 | emulator.data >> 0x04);
        emulator.data = (byte)(emulator.data << 0x04 | lowA);

        // Update flags.
        // Reset N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.WZ = (ushort)(emulator.HL + 0x01);
        emulator.address = emulator.HL;
    }

    // 0xED 0x6F: RLD [9]
    private static void Step_2495(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x70: IN (C) [0]
    private static void Step_2496(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.BC;
    }

    // 0xED 0x70: IN (C) [2]
    private static void Step_2498(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC + 0x01);
    }

    // 0xED 0x70: IN (C) [3]
    private static void Step_2499(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x71: OUT (C) [0]
    private static void Step_2500(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.BC;
        emulator.data = 0x00;
    }

    // 0xED 0x71: OUT (C) [2]
    private static void Step_2502(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC + 0x01);
    }

    // 0xED 0x71: OUT (C) [3]
    private static void Step_2503(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x72: SBC HL, SP [0]
    private static void Step_2504(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.HL;
        var right = emulator.SP;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.HL = (ushort)result;
        emulator.WZ = (ushort)(left + 0x01);

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.H & 0b10101000; // Copy X, Y and S from H.
        flags |= Unsafe.BitCast<bool, byte>((result & 65536) == 65536); // Set C if (result & 65536) == 65536 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x8000) >> 13; // Set PV if did_16_bit_subtraction_overflow(left, right, result) is true.
        flags |= ((left ^ right ^ result) & 0x1000) >> 8; // Set H if did_16_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.HL == 0)) << 6; // Set Z if is_zero(HL) is true.
        emulator.F = (byte)flags;
    }

    // 0xED 0x72: SBC HL, SP [6]
    private static void Step_2510(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x73: LD (nn), SP [0]
    private static void Step_2511(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xED 0x73: LD (nn), SP [1]
    private static void Step_2512(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xED 0x73: LD (nn), SP [3]
    private static void Step_2514(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xED 0x73: LD (nn), SP [4]
    private static void Step_2515(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xED 0x73: LD (nn), SP [6]
    private static void Step_2517(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.data = emulator.SPL;
        emulator.WZ += 0x01;
    }

    // 0xED 0x73: LD (nn), SP [9]
    private static void Step_2520(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.data = emulator.SPH;
    }

    // 0xED 0x73: LD (nn), SP [11]
    private static void Step_2522(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x78: IN A, (C) [0]
    private static void Step_2523(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.BC;
    }

    // 0xED 0x78: IN A, (C) [2]
    private static void Step_2525(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC + 0x01);
    }

    // 0xED 0x78: IN A, (C) [3]
    private static void Step_2526(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x79: OUT A, (C) [0]
    private static void Step_2527(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.BC;
        emulator.data = emulator.A;
    }

    // 0xED 0x79: OUT A, (C) [2]
    private static void Step_2529(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC + 0x01);
    }

    // 0xED 0x79: OUT A, (C) [3]
    private static void Step_2530(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x7A: ADC HL, SP [0]
    private static void Step_2531(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.HL;
        var right = emulator.SP;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.HL = (ushort)result;
        emulator.WZ = (ushort)(left + 0x01);

        // Update flags.
        // Reset N.
        int flags = emulator.H & 0b10101000; // Copy X, Y and S from H.
        flags |= Unsafe.BitCast<bool, byte>((result & 65536) == 65536); // Set C if (result & 65536) == 65536 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x8000) >> 13; // Set PV if did_16_bit_addition_overflow(left, right, result) is true.
        flags |= ((left ^ result ^ right) & 0x1000) >> 8; // Set H if did_16_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.HL == 0)) << 6; // Set Z if is_zero(HL) is true.
        emulator.F = (byte)flags;
    }

    // 0xED 0x7A: ADC HL, SP [6]
    private static void Step_2537(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0x7B: LD SP, (nn) [0]
    private static void Step_2538(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xED 0x7B: LD SP, (nn) [1]
    private static void Step_2539(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xED 0x7B: LD SP, (nn) [3]
    private static void Step_2541(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xED 0x7B: LD SP, (nn) [4]
    private static void Step_2542(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xED 0x7B: LD SP, (nn) [6]
    private static void Step_2544(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.WZ += 0x01;
    }

    // 0xED 0x7B: LD SP, (nn) [7]
    private static void Step_2545(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SPL = emulator.data;
    }

    // 0xED 0x7B: LD SP, (nn) [9]
    private static void Step_2547(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
    }

    // 0xED 0x7B: LD SP, (nn) [10]
    private static void Step_2548(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SPH = emulator.data;
    }

    // 0xED 0x7B: LD SP, (nn) [11]
    private static void Step_2549(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0xA0: LDI [0]
    private static void Step_2550(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
        emulator.HL += 0x01;
    }

    // 0xED 0xA0: LDI [3]
    private static void Step_2553(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.DE;
        emulator.DE += 0x01;
    }

    // 0xED 0xA0: LDI [7]
    private static void Step_2557(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var result = emulator.A + emulator.data;
        emulator.BC -= 0x01;

        // Update flags.
        // Reset N and H.
        int flags = emulator.F & 0b11000001; // Copy C, Z and S from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.BC != 0x00)) << 2; // Set PV if BC != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>((result & 0x08) != 0x00)) << 3; // Set X if (result & 0x08) != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>((result & 0x02) != 0x00)) << 5; // Set Y if (result & 0x02) != 0x00 is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0xA1: CPI [0]
    private static void Step_2558(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
        emulator.HL += 0x01;
    }

    // 0xED 0xA1: CPI [7]
    private static void Step_2565(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.BC -= 0x01;
        emulator.WZ += 0x01;
        var left = emulator.A;
        var right = emulator.data;
        var result = left - right;
        var h = (((left ^ right ^ result) & 0x10) >> 0x04);
        var xy = result - h;
        var x = xy & 0x08;
        var y = xy & 0x02;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10000000; // Copy S from result.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.BC != 0x00)) << 2; // Set PV if BC != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(x != 0x00)) << 3; // Set X if x != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(h != 0x00)) << 4; // Set H if h != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(y != 0x00)) << 5; // Set Y if y != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 6; // Set Z if is_zero(result) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0xA2: INI [0]
    private static void Step_2566(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xED 0xA2: INI [1]
    private static void Step_2567(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.BC;
    }

    // 0xED 0xA2: INI [3]
    private static void Step_2569(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC + 0x01);
        emulator.B -= 0x01;
    }

    // 0xED 0xA2: INI [5]
    private static void Step_2571(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
        emulator.HL += 0x01;
        var carry_modifier = emulator.C + 0x01;
        var temp = emulator.data + (carry_modifier & 0xFF);
        var pv = temp & 0x07 ^ emulator.B;

        // Update flags.
        int flags = emulator.B & 0b00101000; // Copy X and Y from B.
        flags |= Unsafe.BitCast<bool, byte>(temp > 0xFF); // Set C if temp > 0xFF is true.
        flags |= (emulator.data & 0x80) >> 6; // Set N if is_negative(data) is true.
        flags |= ((~System.Numerics.BitOperations.PopCount((byte)(pv)) & 0x01)) << 2; // Set PV if even_parity(pv) is true.
        flags |= (Unsafe.BitCast<bool, byte>(temp > 0xFF)) << 4; // Set H if temp > 0xFF is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0x00)) << 6; // Set Z if B == 0x00 is true.
        flags |= emulator.B & 0x80; // Set S if is_negative(B) is true.
        emulator.F = (byte)flags;
    }

    // 0xED 0xA2: INI [7]
    private static void Step_2573(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0xA3: OUTI [0]
    private static void Step_2574(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xED 0xA3: OUTI [1]
    private static void Step_2575(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
        emulator.HL += 0x01;
    }

    // 0xED 0xA3: OUTI [2]
    private static void Step_2576(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B -= 0x01;
    }

    // 0xED 0xA3: OUTI [4]
    private static void Step_2578(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.BC;
    }

    // 0xED 0xA3: OUTI [6]
    private static void Step_2580(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC + 0x01);
    }

    // 0xED 0xA3: OUTI [7]
    private static void Step_2581(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data + (emulator.L & 0xFF);
        var pv = temp & 0x07 ^ emulator.B;

        // Update flags.
        int flags = emulator.B & 0b00101000; // Copy X and Y from B.
        flags |= Unsafe.BitCast<bool, byte>(temp > 0xFF); // Set C if temp > 0xFF is true.
        flags |= (emulator.data & 0x80) >> 6; // Set N if is_negative(data) is true.
        flags |= ((~System.Numerics.BitOperations.PopCount((byte)(pv)) & 0x01)) << 2; // Set PV if even_parity(pv) is true.
        flags |= (Unsafe.BitCast<bool, byte>(temp > 0xFF)) << 4; // Set H if temp > 0xFF is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0x00)) << 6; // Set Z if B == 0x00 is true.
        flags |= emulator.B & 0x80; // Set S if is_negative(B) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0xA8: LDD [0]
    private static void Step_2582(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
        emulator.HL -= 0x01;
    }

    // 0xED 0xA8: LDD [3]
    private static void Step_2585(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.DE;
        emulator.DE -= 0x01;
    }

    // 0xED 0xA8: LDD [7]
    private static void Step_2589(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var result = emulator.A + emulator.data;
        emulator.BC -= 0x01;

        // Update flags.
        // Reset N and H.
        int flags = emulator.F & 0b11000001; // Copy C, Z and S from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.BC != 0x00)) << 2; // Set PV if BC != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>((result & 0x08) != 0x00)) << 3; // Set X if (result & 0x08) != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>((result & 0x02) != 0x00)) << 5; // Set Y if (result & 0x02) != 0x00 is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0xA9: CPD [0]
    private static void Step_2590(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
        emulator.HL -= 0x01;
    }

    // 0xED 0xA9: CPD [7]
    private static void Step_2597(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.BC -= 0x01;
        emulator.WZ -= 0x01;
        var left = emulator.A;
        var right = emulator.data;
        var result = left - right;
        var h = (((left ^ right ^ result) & 0x10) >> 0x04);
        var xy = result - h;
        var x = xy & 0x08;
        var y = xy & 0x02;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10000000; // Copy S from result.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.BC != 0x00)) << 2; // Set PV if BC != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(x != 0x00)) << 3; // Set X if x != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(h != 0x00)) << 4; // Set H if h != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(y != 0x00)) << 5; // Set Y if y != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 6; // Set Z if is_zero(result) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0xAA: IND [0]
    private static void Step_2598(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xED 0xAA: IND [1]
    private static void Step_2599(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.BC;
    }

    // 0xED 0xAA: IND [3]
    private static void Step_2601(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC - 0x01);
        emulator.B -= 0x01;
    }

    // 0xED 0xAA: IND [5]
    private static void Step_2603(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
        emulator.HL -= 0x01;
        var carry_modifier = emulator.C - 0x01;
        var temp = emulator.data + (carry_modifier & 0xFF);
        var pv = temp & 0x07 ^ emulator.B;

        // Update flags.
        int flags = emulator.B & 0b00101000; // Copy X and Y from B.
        flags |= Unsafe.BitCast<bool, byte>(temp > 0xFF); // Set C if temp > 0xFF is true.
        flags |= (emulator.data & 0x80) >> 6; // Set N if is_negative(data) is true.
        flags |= ((~System.Numerics.BitOperations.PopCount((byte)(pv)) & 0x01)) << 2; // Set PV if even_parity(pv) is true.
        flags |= (Unsafe.BitCast<bool, byte>(temp > 0xFF)) << 4; // Set H if temp > 0xFF is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0x00)) << 6; // Set Z if B == 0x00 is true.
        flags |= emulator.B & 0x80; // Set S if is_negative(B) is true.
        emulator.F = (byte)flags;
    }

    // 0xED 0xAA: IND [7]
    private static void Step_2605(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0xAB: OUTD [0]
    private static void Step_2606(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xED 0xAB: OUTD [1]
    private static void Step_2607(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
        emulator.HL -= 0x01;
    }

    // 0xED 0xAB: OUTD [2]
    private static void Step_2608(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B -= 0x01;
    }

    // 0xED 0xAB: OUTD [4]
    private static void Step_2610(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.BC;
    }

    // 0xED 0xAB: OUTD [6]
    private static void Step_2612(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC - 0x01);
    }

    // 0xED 0xAB: OUTD [7]
    private static void Step_2613(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data + (emulator.L & 0xFF);
        var pv = temp & 0x07 ^ emulator.B;

        // Update flags.
        int flags = emulator.B & 0b00101000; // Copy X and Y from B.
        flags |= Unsafe.BitCast<bool, byte>(temp > 0xFF); // Set C if temp > 0xFF is true.
        flags |= (emulator.data & 0x80) >> 6; // Set N if is_negative(data) is true.
        flags |= ((~System.Numerics.BitOperations.PopCount((byte)(pv)) & 0x01)) << 2; // Set PV if even_parity(pv) is true.
        flags |= (Unsafe.BitCast<bool, byte>(temp > 0xFF)) << 4; // Set H if temp > 0xFF is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0x00)) << 6; // Set Z if B == 0x00 is true.
        flags |= emulator.B & 0x80; // Set S if is_negative(B) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0xB0: LDIR [0]
    private static void Step_2614(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
        emulator.HL += 0x01;
    }

    // 0xED 0xB0: LDIR [3]
    private static void Step_2617(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.DE;
        emulator.DE += 0x01;
    }

    // 0xED 0xB0: LDIR [7]
    private static void Step_2621(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var result = emulator.A + emulator.data;
        emulator.BC -= 0x01;
        if (emulator.BC == 0x00)
        {
            var x = result & 0x08;
            var y = result & 0x02;

            // Update flags.
            // Reset N and H.
            int flags = emulator.F & 0b11000001; // Copy C, Z and S from F.
            flags |= (Unsafe.BitCast<bool, byte>(emulator.BC != 0x00)) << 2; // Set PV if BC != 0x00 is true.
            flags |= (Unsafe.BitCast<bool, byte>(x != 0x00)) << 3; // Set X if x != 0x00 is true.
            flags |= (Unsafe.BitCast<bool, byte>(y != 0x00)) << 5; // Set Y if y != 0x00 is true.
            emulator.F = (byte)flags;
            emulator.Q = emulator.F;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xED 0xB0: LDIR [9]
    private static void Step_2623(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.PC -= 0x01;
        emulator.WZ = emulator.PC;
        emulator.PC -= 0x01;
        var x = emulator.PCH & 0x08;
        var y = emulator.PCH & 0x20;

        // Update flags.
        // Reset N and H.
        int flags = emulator.F & 0b11000001; // Copy C, Z and S from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.BC != 0x00)) << 2; // Set PV if BC != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(x != 0x00)) << 3; // Set X if x != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(y != 0x00)) << 5; // Set Y if y != 0x00 is true.
        emulator.F = (byte)flags;
    }

    // 0xED 0xB0: LDIR [12]
    private static void Step_2626(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0xB1: CPIR [0]
    private static void Step_2627(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
        emulator.HL += 0x01;
    }

    // 0xED 0xB1: CPIR [7]
    private static void Step_2634(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.BC -= 0x01;
        emulator.WZ += 0x01;
        emulator.latch = (byte)(emulator.A - emulator.data);
        if (emulator.latch == 0x00 || emulator.BC == 0x00)
        {
            var xy = emulator.latch - (((emulator.A ^ emulator.data ^ emulator.latch) & 0x10) >> 0x04);
            var x = xy & 0x08;
            var y = xy & 0x02;

            // Update flags.
            int flags = 0b00000010; // Set N.
            flags |= emulator.latch & 0b10000000; // Copy S from latch.
            flags |= emulator.F & 0b00000001; // Copy C from F.
            flags |= (Unsafe.BitCast<bool, byte>(emulator.BC != 0x00)) << 2; // Set PV if BC != 0x00 is true.
            flags |= (Unsafe.BitCast<bool, byte>(x != 0x00)) << 3; // Set X if x != 0x00 is true.
            flags |= (emulator.A ^ emulator.data ^ emulator.latch) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(A, data, latch) is true.
            flags |= (Unsafe.BitCast<bool, byte>(y != 0x00)) << 5; // Set Y if y != 0x00 is true.
            flags |= (Unsafe.BitCast<bool, byte>(emulator.latch == 0)) << 6; // Set Z if is_zero(latch) is true.
            emulator.F = (byte)flags;
            emulator.Q = emulator.F;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xED 0xB1: CPIR [9]
    private static void Step_2636(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.PC -= 0x01;
        emulator.WZ = emulator.PC;
        emulator.PC -= 0x01;
        var x = emulator.PCH & 0x08;
        var y = emulator.PCH & 0x20;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.latch & 0b10000000; // Copy S from latch.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.BC != 0x00)) << 2; // Set PV if BC != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(x != 0x00)) << 3; // Set X if x != 0x00 is true.
        flags |= (emulator.A ^ emulator.data ^ emulator.latch) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(A, data, latch) is true.
        flags |= (Unsafe.BitCast<bool, byte>(y != 0x00)) << 5; // Set Y if y != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.latch == 0)) << 6; // Set Z if is_zero(latch) is true.
        emulator.F = (byte)flags;
    }

    // 0xED 0xB1: CPIR [12]
    private static void Step_2639(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0xB2: INIR [0]
    private static void Step_2640(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xED 0xB2: INIR [1]
    private static void Step_2641(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.BC;
    }

    // 0xED 0xB2: INIR [3]
    private static void Step_2643(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC + 0x01);
        emulator.B -= 0x01;
    }

    // 0xED 0xB2: INIR [5]
    private static void Step_2645(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
        emulator.HL += 0x01;
    }

    // 0xED 0xB2: INIR [7]
    private static void Step_2647(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if (emulator.B == 0x00)
        {
            var carry_modifier = emulator.C + 0x01;
            var temp = emulator.data + (carry_modifier & 0xFF);
            var x = (emulator.B & 0x08) != 0x00;
            var y = (emulator.B & 0x02) != 0x00;
            var h = temp > 0xFF;
            var pv = (~System.Numerics.BitOperations.PopCount((byte)(temp & 0x07 ^ emulator.B)) & 0x01) == 0x01;

            // Update flags.
            int flags = Unsafe.BitCast<bool, byte>(temp > 0xFF); // Set C if temp > 0xFF is true.
            flags |= (emulator.data & 0x80) >> 6; // Set N if is_negative(data) is true.
            flags |= (Unsafe.BitCast<bool, byte>(pv)) << 2; // Set PV if pv is true.
            flags |= (Unsafe.BitCast<bool, byte>(x)) << 3; // Set X if x is true.
            flags |= (Unsafe.BitCast<bool, byte>(h)) << 4; // Set H if h is true.
            flags |= (Unsafe.BitCast<bool, byte>(y)) << 5; // Set Y if y is true.
            flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0x00)) << 6; // Set Z if B == 0x00 is true.
            flags |= emulator.B & 0x80; // Set S if is_negative(B) is true.
            emulator.F = (byte)flags;
            emulator.Q = emulator.F;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xED 0xB2: INIR [9]
    private static void Step_2649(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.PC -= 0x02;
        emulator.WZ = (ushort)(emulator.PC + 0x01);
    }

    // 0xED 0xB2: INIR [12]
    private static void Step_2652(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var carry_modifier = emulator.C + 0x01;
        var temp = emulator.data + (carry_modifier & 0xFF);
        var x = (emulator.PCH & 0x08) != 0x00;
        var y = (emulator.PCH & 0x20) != 0x00;
        bool h;
        bool pv;
        if (temp > 0xFF)
        {
            if ((emulator.data & 0x80) != 0x00)
            {
                h = (emulator.B & 0x0F) == 0x00;
                pv = ((~System.Numerics.BitOperations.PopCount((byte)(temp & 0x07 ^ emulator.B)) & 0x01) ^ (~System.Numerics.BitOperations.PopCount((byte)(emulator.B - 0x01 & 0x07)) & 0x01) ^ 0x01) == 0x01;
            }
            else
            {
                h = (emulator.B & 0x0F) == 0x0F;
                pv = ((~System.Numerics.BitOperations.PopCount((byte)(temp & 0x07 ^ emulator.B)) & 0x01) ^ (~System.Numerics.BitOperations.PopCount((byte)(emulator.B + 0x01 & 0x07)) & 0x01) ^ 0x01) == 0x01;
            }
        }
        else
        {
            h = false;
            pv = ((~System.Numerics.BitOperations.PopCount((byte)(temp & 0x07 ^ emulator.B)) & 0x01) ^ (~System.Numerics.BitOperations.PopCount((byte)(emulator.B & 0x07)) & 0x01) ^ 0x01) == 0x01;
        }

        // Update flags.
        int flags = Unsafe.BitCast<bool, byte>(temp > 0xFF); // Set C if temp > 0xFF is true.
        flags |= (emulator.data & 0x80) >> 6; // Set N if is_negative(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(pv)) << 2; // Set PV if pv is true.
        flags |= (Unsafe.BitCast<bool, byte>(x)) << 3; // Set X if x is true.
        flags |= (Unsafe.BitCast<bool, byte>(h)) << 4; // Set H if h is true.
        flags |= (Unsafe.BitCast<bool, byte>(y)) << 5; // Set Y if y is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0x00)) << 6; // Set Z if B == 0x00 is true.
        flags |= emulator.B & 0x80; // Set S if is_negative(B) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0xB3: OTIR [0]
    private static void Step_2653(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xED 0xB3: OTIR [1]
    private static void Step_2654(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
        emulator.HL += 0x01;
    }

    // 0xED 0xB3: OTIR [2]
    private static void Step_2655(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B -= 0x01;
    }

    // 0xED 0xB3: OTIR [4]
    private static void Step_2657(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.BC;
    }

    // 0xED 0xB3: OTIR [6]
    private static void Step_2659(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC + 0x01);
    }

    // 0xED 0xB3: OTIR [7]
    private static void Step_2660(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if (emulator.B == 0x00)
        {
            var temp = emulator.data + (emulator.L & 0xFF);
            var x = (emulator.B & 0x08) != 0x00;
            var y = (emulator.B & 0x02) != 0x00;
            var h = temp > 0xFF;
            var pv = (~System.Numerics.BitOperations.PopCount((byte)(temp & 0x07 ^ emulator.B)) & 0x01) == 0x01;

            // Update flags.
            int flags = Unsafe.BitCast<bool, byte>(temp > 0xFF); // Set C if temp > 0xFF is true.
            flags |= (emulator.data & 0x80) >> 6; // Set N if is_negative(data) is true.
            flags |= (Unsafe.BitCast<bool, byte>(pv)) << 2; // Set PV if pv is true.
            flags |= (Unsafe.BitCast<bool, byte>(x)) << 3; // Set X if x is true.
            flags |= (Unsafe.BitCast<bool, byte>(h)) << 4; // Set H if h is true.
            flags |= (Unsafe.BitCast<bool, byte>(y)) << 5; // Set Y if y is true.
            flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0x00)) << 6; // Set Z if B == 0x00 is true.
            flags |= emulator.B & 0x80; // Set S if is_negative(B) is true.
            emulator.F = (byte)flags;
            emulator.Q = emulator.F;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xED 0xB3: OTIR [9]
    private static void Step_2662(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.PC -= 0x02;
        emulator.WZ = (ushort)(emulator.PC + 0x01);
    }

    // 0xED 0xB3: OTIR [12]
    private static void Step_2665(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data + (emulator.L & 0xFF);
        var x = (emulator.PCH & 0x08) != 0x00;
        var y = (emulator.PCH & 0x20) != 0x00;
        bool h;
        bool pv;
        if (temp > 0xFF)
        {
            if ((emulator.data & 0x80) != 0x00)
            {
                h = (emulator.B & 0x0F) == 0x00;
                pv = ((~System.Numerics.BitOperations.PopCount((byte)(temp & 0x07 ^ emulator.B)) & 0x01) ^ (~System.Numerics.BitOperations.PopCount((byte)(emulator.B - 0x01 & 0x07)) & 0x01) ^ 0x01) == 0x01;
            }
            else
            {
                h = (emulator.B & 0x0F) == 0x0F;
                pv = ((~System.Numerics.BitOperations.PopCount((byte)(temp & 0x07 ^ emulator.B)) & 0x01) ^ (~System.Numerics.BitOperations.PopCount((byte)(emulator.B + 0x01 & 0x07)) & 0x01) ^ 0x01) == 0x01;
            }
        }
        else
        {
            h = false;
            pv = ((~System.Numerics.BitOperations.PopCount((byte)(temp & 0x07 ^ emulator.B)) & 0x01) ^ (~System.Numerics.BitOperations.PopCount((byte)(emulator.B & 0x07)) & 0x01) ^ 0x01) == 0x01;
        }

        // Update flags.
        int flags = Unsafe.BitCast<bool, byte>(temp > 0xFF); // Set C if temp > 0xFF is true.
        flags |= (emulator.data & 0x80) >> 6; // Set N if is_negative(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(pv)) << 2; // Set PV if pv is true.
        flags |= (Unsafe.BitCast<bool, byte>(x)) << 3; // Set X if x is true.
        flags |= (Unsafe.BitCast<bool, byte>(h)) << 4; // Set H if h is true.
        flags |= (Unsafe.BitCast<bool, byte>(y)) << 5; // Set Y if y is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0x00)) << 6; // Set Z if B == 0x00 is true.
        flags |= emulator.B & 0x80; // Set S if is_negative(B) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0xB8: LDDR [0]
    private static void Step_2666(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
        emulator.HL -= 0x01;
    }

    // 0xED 0xB8: LDDR [3]
    private static void Step_2669(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.DE;
        emulator.DE -= 0x01;
    }

    // 0xED 0xB8: LDDR [7]
    private static void Step_2673(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var result = emulator.A + emulator.data;
        emulator.BC -= 0x01;
        if (emulator.BC == 0x00)
        {
            var x = result & 0x08;
            var y = result & 0x02;

            // Update flags.
            // Reset N and H.
            int flags = emulator.F & 0b11000001; // Copy C, Z and S from F.
            flags |= (Unsafe.BitCast<bool, byte>(emulator.BC != 0x00)) << 2; // Set PV if BC != 0x00 is true.
            flags |= (Unsafe.BitCast<bool, byte>(x != 0x00)) << 3; // Set X if x != 0x00 is true.
            flags |= (Unsafe.BitCast<bool, byte>(y != 0x00)) << 5; // Set Y if y != 0x00 is true.
            emulator.F = (byte)flags;
            emulator.Q = emulator.F;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xED 0xB8: LDDR [9]
    private static void Step_2675(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.PC -= 0x01;
        emulator.WZ = emulator.PC;
        emulator.PC -= 0x01;
        var x = emulator.PCH & 0x08;
        var y = emulator.PCH & 0x20;

        // Update flags.
        // Reset N and H.
        int flags = emulator.F & 0b11000001; // Copy C, Z and S from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.BC != 0x00)) << 2; // Set PV if BC != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(x != 0x00)) << 3; // Set X if x != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(y != 0x00)) << 5; // Set Y if y != 0x00 is true.
        emulator.F = (byte)flags;
    }

    // 0xED 0xB8: LDDR [12]
    private static void Step_2678(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0xB9: CPDR [0]
    private static void Step_2679(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.HL;
        emulator.HL -= 0x01;
    }

    // 0xED 0xB9: CPDR [7]
    private static void Step_2686(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.BC -= 0x01;
        emulator.WZ -= 0x01;
        emulator.latch = (byte)(emulator.A - emulator.data);
        if (emulator.latch == 0x00 || emulator.BC == 0x00)
        {
            var xy = emulator.latch - (((emulator.A ^ emulator.data ^ emulator.latch) & 0x10) >> 0x04);
            var x = xy & 0x08;
            var y = xy & 0x02;

            // Update flags.
            int flags = 0b00000010; // Set N.
            flags |= emulator.latch & 0b10000000; // Copy S from latch.
            flags |= emulator.F & 0b00000001; // Copy C from F.
            flags |= (Unsafe.BitCast<bool, byte>(emulator.BC != 0x00)) << 2; // Set PV if BC != 0x00 is true.
            flags |= (Unsafe.BitCast<bool, byte>(x != 0x00)) << 3; // Set X if x != 0x00 is true.
            flags |= (emulator.A ^ emulator.data ^ emulator.latch) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(A, data, latch) is true.
            flags |= (Unsafe.BitCast<bool, byte>(y != 0x00)) << 5; // Set Y if y != 0x00 is true.
            flags |= (Unsafe.BitCast<bool, byte>(emulator.latch == 0)) << 6; // Set Z if is_zero(latch) is true.
            emulator.F = (byte)flags;
            emulator.Q = emulator.F;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xED 0xB9: CPDR [9]
    private static void Step_2688(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.PC -= 0x01;
        emulator.WZ = emulator.PC;
        emulator.PC -= 0x01;
        var x = emulator.PCH & 0x08;
        var y = emulator.PCH & 0x20;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.latch & 0b10000000; // Copy S from latch.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.BC != 0x00)) << 2; // Set PV if BC != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(x != 0x00)) << 3; // Set X if x != 0x00 is true.
        flags |= (emulator.A ^ emulator.data ^ emulator.latch) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(A, data, latch) is true.
        flags |= (Unsafe.BitCast<bool, byte>(y != 0x00)) << 5; // Set Y if y != 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.latch == 0)) << 6; // Set Z if is_zero(latch) is true.
        emulator.F = (byte)flags;
    }

    // 0xED 0xB9: CPDR [12]
    private static void Step_2691(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0xBA: INDR [0]
    private static void Step_2692(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xED 0xBA: INDR [1]
    private static void Step_2693(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.BC;
    }

    // 0xED 0xBA: INDR [3]
    private static void Step_2695(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC - 0x01);
        emulator.B -= 0x01;
    }

    // 0xED 0xBA: INDR [5]
    private static void Step_2697(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
        emulator.HL -= 0x01;
    }

    // 0xED 0xBA: INDR [7]
    private static void Step_2699(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if (emulator.B == 0x00)
        {
            var carry_modifier = emulator.C - 0x01;
            var temp = emulator.data + (carry_modifier & 0xFF);
            var x = (emulator.B & 0x08) != 0x00;
            var y = (emulator.B & 0x02) != 0x00;
            var h = temp > 0xFF;
            var pv = (~System.Numerics.BitOperations.PopCount((byte)(temp & 0x07 ^ emulator.B)) & 0x01) == 0x01;

            // Update flags.
            int flags = Unsafe.BitCast<bool, byte>(temp > 0xFF); // Set C if temp > 0xFF is true.
            flags |= (emulator.data & 0x80) >> 6; // Set N if is_negative(data) is true.
            flags |= (Unsafe.BitCast<bool, byte>(pv)) << 2; // Set PV if pv is true.
            flags |= (Unsafe.BitCast<bool, byte>(x)) << 3; // Set X if x is true.
            flags |= (Unsafe.BitCast<bool, byte>(h)) << 4; // Set H if h is true.
            flags |= (Unsafe.BitCast<bool, byte>(y)) << 5; // Set Y if y is true.
            flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0x00)) << 6; // Set Z if B == 0x00 is true.
            flags |= emulator.B & 0x80; // Set S if is_negative(B) is true.
            emulator.F = (byte)flags;
            emulator.Q = emulator.F;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xED 0xBA: INDR [9]
    private static void Step_2701(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.PC -= 0x02;
        emulator.WZ = (ushort)(emulator.PC + 0x01);
    }

    // 0xED 0xBA: INDR [12]
    private static void Step_2704(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var carry_modifier = emulator.C - 0x01;
        var temp = emulator.data + (carry_modifier & 0xFF);
        var x = (emulator.PCH & 0x08) != 0x00;
        var y = (emulator.PCH & 0x20) != 0x00;
        bool h;
        bool pv;
        if (temp > 0xFF)
        {
            if ((emulator.data & 0x80) != 0x00)
            {
                h = (emulator.B & 0x0F) == 0x00;
                pv = ((~System.Numerics.BitOperations.PopCount((byte)(temp & 0x07 ^ emulator.B)) & 0x01) ^ (~System.Numerics.BitOperations.PopCount((byte)(emulator.B - 0x01 & 0x07)) & 0x01) ^ 0x01) == 0x01;
            }
            else
            {
                h = (emulator.B & 0x0F) == 0x0F;
                pv = ((~System.Numerics.BitOperations.PopCount((byte)(temp & 0x07 ^ emulator.B)) & 0x01) ^ (~System.Numerics.BitOperations.PopCount((byte)(emulator.B + 0x01 & 0x07)) & 0x01) ^ 0x01) == 0x01;
            }
        }
        else
        {
            h = false;
            pv = ((~System.Numerics.BitOperations.PopCount((byte)(temp & 0x07 ^ emulator.B)) & 0x01) ^ (~System.Numerics.BitOperations.PopCount((byte)(emulator.B & 0x07)) & 0x01) ^ 0x01) == 0x01;
        }

        // Update flags.
        int flags = Unsafe.BitCast<bool, byte>(temp > 0xFF); // Set C if temp > 0xFF is true.
        flags |= (emulator.data & 0x80) >> 6; // Set N if is_negative(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(pv)) << 2; // Set PV if pv is true.
        flags |= (Unsafe.BitCast<bool, byte>(x)) << 3; // Set X if x is true.
        flags |= (Unsafe.BitCast<bool, byte>(h)) << 4; // Set H if h is true.
        flags |= (Unsafe.BitCast<bool, byte>(y)) << 5; // Set Y if y is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0x00)) << 6; // Set Z if B == 0x00 is true.
        flags |= emulator.B & 0x80; // Set S if is_negative(B) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xED 0xBB: OTDR [0]
    private static void Step_2705(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xED 0xBB: OTDR [1]
    private static void Step_2706(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.HL;
        emulator.HL -= 0x01;
    }

    // 0xED 0xBB: OTDR [2]
    private static void Step_2707(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B -= 0x01;
    }

    // 0xED 0xBB: OTDR [4]
    private static void Step_2709(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.BC;
    }

    // 0xED 0xBB: OTDR [6]
    private static void Step_2711(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.BC - 0x01);
    }

    // 0xED 0xBB: OTDR [7]
    private static void Step_2712(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        if (emulator.B == 0x00)
        {
            var temp = emulator.data + (emulator.L & 0xFF);
            var x = (emulator.B & 0x08) != 0x00;
            var y = (emulator.B & 0x02) != 0x00;
            var h = temp > 0xFF;
            var pv = (~System.Numerics.BitOperations.PopCount((byte)(temp & 0x07 ^ emulator.B)) & 0x01) == 0x01;

            // Update flags.
            int flags = Unsafe.BitCast<bool, byte>(temp > 0xFF); // Set C if temp > 0xFF is true.
            flags |= (emulator.data & 0x80) >> 6; // Set N if is_negative(data) is true.
            flags |= (Unsafe.BitCast<bool, byte>(pv)) << 2; // Set PV if pv is true.
            flags |= (Unsafe.BitCast<bool, byte>(x)) << 3; // Set X if x is true.
            flags |= (Unsafe.BitCast<bool, byte>(h)) << 4; // Set H if h is true.
            flags |= (Unsafe.BitCast<bool, byte>(y)) << 5; // Set Y if y is true.
            flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0x00)) << 6; // Set Z if B == 0x00 is true.
            flags |= emulator.B & 0x80; // Set S if is_negative(B) is true.
            emulator.F = (byte)flags;
            emulator.Q = emulator.F;
            emulator.instructionComplete = true;
            if (HandleInterrupts(emulator, ref actionRequired))
            {
                return;
            }

            // Finish instruction.
            emulator.currentStep = 0;
        }
    }

    // 0xED 0xBB: OTDR [9]
    private static void Step_2714(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.PC -= 0x02;
        emulator.WZ = (ushort)(emulator.PC + 0x01);
    }

    // 0xED 0xBB: OTDR [12]
    private static void Step_2717(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data + (emulator.L & 0xFF);
        var x = (emulator.PCH & 0x08) != 0x00;
        var y = (emulator.PCH & 0x20) != 0x00;
        bool h;
        bool pv;
        if (temp > 0xFF)
        {
            if ((emulator.data & 0x80) != 0x00)
            {
                h = (emulator.B & 0x0F) == 0x00;
                pv = ((~System.Numerics.BitOperations.PopCount((byte)(temp & 0x07 ^ emulator.B)) & 0x01) ^ (~System.Numerics.BitOperations.PopCount((byte)(emulator.B - 0x01 & 0x07)) & 0x01) ^ 0x01) == 0x01;
            }
            else
            {
                h = (emulator.B & 0x0F) == 0x0F;
                pv = ((~System.Numerics.BitOperations.PopCount((byte)(temp & 0x07 ^ emulator.B)) & 0x01) ^ (~System.Numerics.BitOperations.PopCount((byte)(emulator.B + 0x01 & 0x07)) & 0x01) ^ 0x01) == 0x01;
            }
        }
        else
        {
            h = false;
            pv = ((~System.Numerics.BitOperations.PopCount((byte)(temp & 0x07 ^ emulator.B)) & 0x01) ^ (~System.Numerics.BitOperations.PopCount((byte)(emulator.B & 0x07)) & 0x01) ^ 0x01) == 0x01;
        }

        // Update flags.
        int flags = Unsafe.BitCast<bool, byte>(temp > 0xFF); // Set C if temp > 0xFF is true.
        flags |= (emulator.data & 0x80) >> 6; // Set N if is_negative(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(pv)) << 2; // Set PV if pv is true.
        flags |= (Unsafe.BitCast<bool, byte>(x)) << 3; // Set X if x is true.
        flags |= (Unsafe.BitCast<bool, byte>(h)) << 4; // Set H if h is true.
        flags |= (Unsafe.BitCast<bool, byte>(y)) << 5; // Set Y if y is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.B == 0x00)) << 6; // Set Z if B == 0x00 is true.
        flags |= emulator.B & 0x80; // Set S if is_negative(B) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x09: ADD IY, BC [0]
    private static void Step_2718(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.IY;
        var right = emulator.BC;
        var result = left + right;
        emulator.IY = (ushort)result;
        emulator.WZ = (ushort)(left + 0x01);

        // Update flags.
        // Reset N.
        int flags = emulator.IYH & 0b00101000; // Copy X and Y from IYH.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        flags |= Unsafe.BitCast<bool, byte>((result & 65536) == 65536); // Set C if (result & 65536) == 65536 is true.
        flags |= ((left ^ result ^ right) & 0x1000) >> 8; // Set H if did_16_bit_addition_half_carry(left, right, result) is true.
        emulator.F = (byte)flags;
    }

    // 0xFD 0x09: ADD IY, BC [6]
    private static void Step_2724(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x19: ADD IY, DE [0]
    private static void Step_2725(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.IY;
        var right = emulator.DE;
        var result = left + right;
        emulator.IY = (ushort)result;
        emulator.WZ = (ushort)(left + 0x01);

        // Update flags.
        // Reset N.
        int flags = emulator.IYH & 0b00101000; // Copy X and Y from IYH.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        flags |= Unsafe.BitCast<bool, byte>((result & 65536) == 65536); // Set C if (result & 65536) == 65536 is true.
        flags |= ((left ^ result ^ right) & 0x1000) >> 8; // Set H if did_16_bit_addition_half_carry(left, right, result) is true.
        emulator.F = (byte)flags;
    }

    // 0xFD 0x19: ADD IY, DE [6]
    private static void Step_2731(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x21: LD IY, nn [0]
    private static void Step_2732(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x21: LD IY, nn [1]
    private static void Step_2733(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.IYL = emulator.data;
    }

    // 0xFD 0x21: LD IY, nn [3]
    private static void Step_2735(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x21: LD IY, nn [4]
    private static void Step_2736(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.IYH = emulator.data;
    }

    // 0xFD 0x21: LD IY, nn [5]
    private static void Step_2737(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x22: LD (nn), IY [0]
    private static void Step_2738(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x22: LD (nn), IY [1]
    private static void Step_2739(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xFD 0x22: LD (nn), IY [3]
    private static void Step_2741(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x22: LD (nn), IY [4]
    private static void Step_2742(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xFD 0x22: LD (nn), IY [6]
    private static void Step_2744(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.data = emulator.IYL;
        emulator.WZ += 0x01;
    }

    // 0xFD 0x22: LD (nn), IY [9]
    private static void Step_2747(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.data = emulator.IYH;
    }

    // 0xFD 0x22: LD (nn), IY [11]
    private static void Step_2749(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x23: INC IY [0]
    private static void Step_2750(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IY += 0x01;
    }

    // 0xFD 0x23: INC IY [1]
    private static void Step_2751(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x24: INC IYH [0]
    private static void Step_2752(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IYH += 0x01;

        // Update flags.
        // Reset N.
        int flags = emulator.IYH & 0b00101000; // Copy X and Y from IYH.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.IYH == 0x80)) << 2; // Set PV if IYH == 0x80 is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.IYH & 0x0F) == 0x00)) << 4; // Set H if (IYH & 0x0F) == 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.IYH == 0)) << 6; // Set Z if is_zero(IYH) is true.
        flags |= emulator.IYH & 0x80; // Set S if is_negative(IYH) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x25: DEC IYH [0]
    private static void Step_2753(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IYH -= 0x01;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.IYH & 0b00101000; // Copy X and Y from IYH.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.IYH == 0x7F)) << 2; // Set PV if IYH == 0x7F is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.IYH & 0x0F) == 0x0F)) << 4; // Set H if (IYH & 0x0F) == 0x0F is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.IYH == 0)) << 6; // Set Z if is_zero(IYH) is true.
        flags |= emulator.IYH & 0x80; // Set S if is_negative(IYH) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x26: LD IYH, n [0]
    private static void Step_2754(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x26: LD IYH, n [1]
    private static void Step_2755(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.IYH = emulator.data;
    }

    // 0xFD 0x26: LD IYH, n [2]
    private static void Step_2756(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x29: ADD IY, IY [0]
    private static void Step_2757(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.IY;
        var right = emulator.IY;
        var result = left + right;
        emulator.IY = (ushort)result;
        emulator.WZ = (ushort)(left + 0x01);

        // Update flags.
        // Reset N.
        int flags = emulator.IYH & 0b00101000; // Copy X and Y from IYH.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        flags |= Unsafe.BitCast<bool, byte>((result & 65536) == 65536); // Set C if (result & 65536) == 65536 is true.
        flags |= ((left ^ result ^ right) & 0x1000) >> 8; // Set H if did_16_bit_addition_half_carry(left, right, result) is true.
        emulator.F = (byte)flags;
    }

    // 0xFD 0x29: ADD IY, IY [6]
    private static void Step_2763(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x2A: LD IY, (nn) [0]
    private static void Step_2764(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x2A: LD IY, (nn) [1]
    private static void Step_2765(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xFD 0x2A: LD IY, (nn) [3]
    private static void Step_2767(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x2A: LD IY, (nn) [4]
    private static void Step_2768(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xFD 0x2A: LD IY, (nn) [6]
    private static void Step_2770(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
        emulator.WZ += 0x01;
    }

    // 0xFD 0x2A: LD IY, (nn) [7]
    private static void Step_2771(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.IYL = emulator.data;
    }

    // 0xFD 0x2A: LD IY, (nn) [9]
    private static void Step_2773(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.WZ;
    }

    // 0xFD 0x2A: LD IY, (nn) [10]
    private static void Step_2774(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.IYH = emulator.data;
    }

    // 0xFD 0x2A: LD IY, (nn) [11]
    private static void Step_2775(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x2B: DEC IY [0]
    private static void Step_2776(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IY -= 0x01;
    }

    // 0xFD 0x2B: DEC IY [1]
    private static void Step_2777(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x2C: INC IYL [0]
    private static void Step_2778(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IYL += 0x01;

        // Update flags.
        // Reset N.
        int flags = emulator.IYL & 0b00101000; // Copy X and Y from IYL.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.IYL == 0x80)) << 2; // Set PV if IYL == 0x80 is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.IYL & 0x0F) == 0x00)) << 4; // Set H if (IYL & 0x0F) == 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.IYL == 0)) << 6; // Set Z if is_zero(IYL) is true.
        flags |= emulator.IYL & 0x80; // Set S if is_negative(IYL) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x2D: DEC IYL [0]
    private static void Step_2779(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IYL -= 0x01;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.IYL & 0b00101000; // Copy X and Y from IYL.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.IYL == 0x7F)) << 2; // Set PV if IYL == 0x7F is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.IYL & 0x0F) == 0x0F)) << 4; // Set H if (IYL & 0x0F) == 0x0F is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.IYL == 0)) << 6; // Set Z if is_zero(IYL) is true.
        flags |= emulator.IYL & 0x80; // Set S if is_negative(IYL) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x2E: LD IYL, n [0]
    private static void Step_2780(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x2E: LD IYL, n [1]
    private static void Step_2781(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.IYL = emulator.data;
    }

    // 0xFD 0x2E: LD IYL, n [2]
    private static void Step_2782(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x34: INC (IY + d) [0]
    private static void Step_2783(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x34: INC (IY + d) [1]
    private static void Step_2784(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.latch = emulator.data;
    }

    // 0xFD 0x34: INC (IY + d) [8]
    private static void Step_2791(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xFD 0x34: INC (IY + d) [10]
    private static void Step_2793(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data += 0x01;

        // Update flags.
        // Reset N.
        int flags = emulator.data & 0b00101000; // Copy X and Y from data.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0x80)) << 2; // Set PV if data == 0x80 is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.data & 0x0F) == 0x00)) << 4; // Set H if (data & 0x0F) == 0x00 is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        flags |= emulator.data & 0x80; // Set S if is_negative(data) is true.
        emulator.F = (byte)flags;
    }

    // 0xFD 0x34: INC (IY + d) [14]
    private static void Step_2797(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x35: DEC (IY + d) [0]
    private static void Step_2798(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x35: DEC (IY + d) [1]
    private static void Step_2799(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.latch = emulator.data;
    }

    // 0xFD 0x35: DEC (IY + d) [8]
    private static void Step_2806(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xFD 0x35: DEC (IY + d) [10]
    private static void Step_2808(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data -= 0x01;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= emulator.data & 0b00101000; // Copy X and Y from data.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0x7F)) << 2; // Set PV if data == 0x7F is true.
        flags |= (Unsafe.BitCast<bool, byte>((emulator.data & 0x0F) == 0x0F)) << 4; // Set H if (data & 0x0F) == 0x0F is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        flags |= emulator.data & 0x80; // Set S if is_negative(data) is true.
        emulator.F = (byte)flags;
    }

    // 0xFD 0x35: DEC (IY + d) [14]
    private static void Step_2812(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x36: LD (IY + d), n [0]
    private static void Step_2813(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x36: LD (IY + d), n [1]
    private static void Step_2814(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.latch = emulator.data;
    }

    // 0xFD 0x36: LD (IY + d), n [3]
    private static void Step_2816(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x36: LD (IY + d), n [8]
    private static void Step_2821(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // 0xFD 0x36: LD (IY + d), n [10]
    private static void Step_2823(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x39: ADD IY, SP [0]
    private static void Step_2824(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.IY;
        var right = emulator.SP;
        var result = left + right;
        emulator.IY = (ushort)result;
        emulator.WZ = (ushort)(left + 0x01);

        // Update flags.
        // Reset N.
        int flags = emulator.IYH & 0b00101000; // Copy X and Y from IYH.
        flags |= emulator.F & 0b11000100; // Copy PV, Z and S from F.
        flags |= Unsafe.BitCast<bool, byte>((result & 65536) == 65536); // Set C if (result & 65536) == 65536 is true.
        flags |= ((left ^ result ^ right) & 0x1000) >> 8; // Set H if did_16_bit_addition_half_carry(left, right, result) is true.
        emulator.F = (byte)flags;
    }

    // 0xFD 0x39: ADD IY, SP [6]
    private static void Step_2830(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x44: LD B, IYH [0]
    private static void Step_2831(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B = emulator.IYH;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x45: LD B, IYL [0]
    private static void Step_2832(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.B = emulator.IYL;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x46: LD B, (IY + d) [0]
    private static void Step_2833(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x46: LD B, (IY + d) [8]
    private static void Step_2841(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xFD 0x46: LD B, (IY + d) [9]
    private static void Step_2842(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.B = emulator.data;
    }

    // 0xFD 0x46: LD B, (IY + d) [10]
    private static void Step_2843(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x4C: LD C, IYH [0]
    private static void Step_2844(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C = emulator.IYH;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x4D: LD C, IYL [0]
    private static void Step_2845(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.C = emulator.IYL;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x4E: LD C, (IY + d) [0]
    private static void Step_2846(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x4E: LD C, (IY + d) [8]
    private static void Step_2854(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xFD 0x4E: LD C, (IY + d) [9]
    private static void Step_2855(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.C = emulator.data;
    }

    // 0xFD 0x4E: LD C, (IY + d) [10]
    private static void Step_2856(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x54: LD D, IYH [0]
    private static void Step_2857(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D = emulator.IYH;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x55: LD D, IYL [0]
    private static void Step_2858(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.D = emulator.IYL;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x56: LD D, (IY + d) [0]
    private static void Step_2859(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x56: LD D, (IY + d) [8]
    private static void Step_2867(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xFD 0x56: LD D, (IY + d) [9]
    private static void Step_2868(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.D = emulator.data;
    }

    // 0xFD 0x56: LD D, (IY + d) [10]
    private static void Step_2869(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x5C: LD E, IYH [0]
    private static void Step_2870(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E = emulator.IYH;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x5D: LD E, IYL [0]
    private static void Step_2871(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.E = emulator.IYL;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x5E: LD E, (IY + d) [0]
    private static void Step_2872(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x5E: LD E, (IY + d) [8]
    private static void Step_2880(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xFD 0x5E: LD E, (IY + d) [9]
    private static void Step_2881(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.E = emulator.data;
    }

    // 0xFD 0x5E: LD E, (IY + d) [10]
    private static void Step_2882(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x60: LD IYH, B [0]
    private static void Step_2883(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IYH = emulator.B;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x61: LD IYH, C [0]
    private static void Step_2884(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IYH = emulator.C;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x62: LD IYH, D [0]
    private static void Step_2885(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IYH = emulator.D;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x63: LD IYH, E [0]
    private static void Step_2886(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IYH = emulator.E;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x64: LD IYH, IYH [0]
    private static void Step_2887(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x65: LD IYH, IYL [0]
    private static void Step_2888(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IYH = emulator.IYL;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x66: LD H, (IY + d) [0]
    private static void Step_2889(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x66: LD H, (IY + d) [8]
    private static void Step_2897(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xFD 0x66: LD H, (IY + d) [9]
    private static void Step_2898(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.H = emulator.data;
    }

    // 0xFD 0x66: LD H, (IY + d) [10]
    private static void Step_2899(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x67: LD IYH, A [0]
    private static void Step_2900(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IYH = emulator.A;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x68: LD IYL, B [0]
    private static void Step_2901(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IYL = emulator.B;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x69: LD IYL, C [0]
    private static void Step_2902(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IYL = emulator.C;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x6A: LD IYL, D [0]
    private static void Step_2903(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IYL = emulator.D;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x6B: LD IYL, E [0]
    private static void Step_2904(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IYL = emulator.E;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x6C: LD IYL, IYH [0]
    private static void Step_2905(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IYL = emulator.IYH;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x6D: LD IYL, IYL [0]
    private static void Step_2906(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x6E: LD L, (IY + d) [0]
    private static void Step_2907(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x6E: LD L, (IY + d) [8]
    private static void Step_2915(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xFD 0x6E: LD L, (IY + d) [9]
    private static void Step_2916(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.L = emulator.data;
    }

    // 0xFD 0x6E: LD L, (IY + d) [10]
    private static void Step_2917(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x6F: LD IYL, A [0]
    private static void Step_2918(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.IYL = emulator.A;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x70: LD (IY + d), B [0]
    private static void Step_2919(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x70: LD (IY + d), B [8]
    private static void Step_2927(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
        emulator.data = emulator.B;
    }

    // 0xFD 0x70: LD (IY + d), B [10]
    private static void Step_2929(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x71: LD (IY + d), C [0]
    private static void Step_2930(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x71: LD (IY + d), C [8]
    private static void Step_2938(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
        emulator.data = emulator.C;
    }

    // 0xFD 0x71: LD (IY + d), C [10]
    private static void Step_2940(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x72: LD (IY + d), D [0]
    private static void Step_2941(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x72: LD (IY + d), D [8]
    private static void Step_2949(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
        emulator.data = emulator.D;
    }

    // 0xFD 0x72: LD (IY + d), D [10]
    private static void Step_2951(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x73: LD (IY + d), E [0]
    private static void Step_2952(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x73: LD (IY + d), E [8]
    private static void Step_2960(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
        emulator.data = emulator.E;
    }

    // 0xFD 0x73: LD (IY + d), E [10]
    private static void Step_2962(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x74: LD (IY + d), H [0]
    private static void Step_2963(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x74: LD (IY + d), H [8]
    private static void Step_2971(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
        emulator.data = emulator.H;
    }

    // 0xFD 0x74: LD (IY + d), H [10]
    private static void Step_2973(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x75: LD (IY + d), L [0]
    private static void Step_2974(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x75: LD (IY + d), L [8]
    private static void Step_2982(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
        emulator.data = emulator.L;
    }

    // 0xFD 0x75: LD (IY + d), L [10]
    private static void Step_2984(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x77: LD (IY + d), A [0]
    private static void Step_2985(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x77: LD (IY + d), A [8]
    private static void Step_2993(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
        emulator.data = emulator.A;
    }

    // 0xFD 0x77: LD (IY + d), A [10]
    private static void Step_2995(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x7C: LD A, IYH [0]
    private static void Step_2996(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = emulator.IYH;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x7D: LD A, IYL [0]
    private static void Step_2997(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A = emulator.IYL;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x7E: LD A, (IY + d) [0]
    private static void Step_2998(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x7E: LD A, (IY + d) [8]
    private static void Step_3006(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xFD 0x7E: LD A, (IY + d) [9]
    private static void Step_3007(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A = emulator.data;
    }

    // 0xFD 0x7E: LD A, (IY + d) [10]
    private static void Step_3008(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x84: ADD A, IYH [0]
    private static void Step_3009(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.IYH;
        var result = left + right;
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x85: ADD A, IYL [0]
    private static void Step_3010(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.IYL;
        var result = left + right;
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x86: ADD A, (IY + n) [0]
    private static void Step_3011(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x86: ADD A, (IY + n) [8]
    private static void Step_3019(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xFD 0x86: ADD A, (IY + n) [9]
    private static void Step_3020(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left + right;
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xFD 0x86: ADD A, (IY + n) [10]
    private static void Step_3021(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x8C: ADC A, IYH [0]
    private static void Step_3022(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.IYH;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x8D: ADC A, IYL [0]
    private static void Step_3023(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.IYL;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x8E: ADC A, (IY + n) [0]
    private static void Step_3024(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x8E: ADC A, (IY + n) [8]
    private static void Step_3032(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xFD 0x8E: ADC A, (IY + n) [9]
    private static void Step_3033(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left + right + (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        // Reset N.
        int flags = result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((result ^ left) & (result ^ right) & 0x80) >> 5; // Set PV if did_8_bit_addition_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_addition_half_carry(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xFD 0x8E: ADC A, (IY + n) [10]
    private static void Step_3034(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x94: SUB IYH [0]
    private static void Step_3035(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.IYH;
        var result = left - right;
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x95: SUB IYL [0]
    private static void Step_3036(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.IYL;
        var result = left - right;
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x96: SUB (IY + n) [0]
    private static void Step_3037(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x96: SUB (IY + n) [8]
    private static void Step_3045(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xFD 0x96: SUB (IY + n) [9]
    private static void Step_3046(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left - right;
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xFD 0x96: SUB (IY + n) [10]
    private static void Step_3047(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0x9C: SBC IYH [0]
    private static void Step_3048(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.IYH;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x9D: SBC IYL [0]
    private static void Step_3049(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.IYL;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0x9E: SBC (IY + n) [0]
    private static void Step_3050(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0x9E: SBC (IY + n) [8]
    private static void Step_3058(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xFD 0x9E: SBC (IY + n) [9]
    private static void Step_3059(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left - right - (emulator.F & 0b00000001 /* flag.C */);
        emulator.A = (byte)result;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= result & 0b10101000; // Copy X, Y and S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xFD 0x9E: SBC (IY + n) [10]
    private static void Step_3060(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0xA4: AND IYH [0]
    private static void Step_3061(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A &= emulator.IYH;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset C and N.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0xA5: AND IYL [0]
    private static void Step_3062(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A &= emulator.IYL;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset C and N.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0xA6: AND (IY + n) [0]
    private static void Step_3063(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0xA6: AND (IY + n) [8]
    private static void Step_3071(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xFD 0xA6: AND (IY + n) [9]
    private static void Step_3072(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A &= emulator.data;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset C and N.
        flags |= emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xFD 0xA6: AND (IY + n) [10]
    private static void Step_3073(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0xAC: XOR IYH [0]
    private static void Step_3074(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A ^= emulator.IYH;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0xAD: XOR IYL [0]
    private static void Step_3075(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A ^= emulator.IYL;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0xAE: XOR (IY + n) [0]
    private static void Step_3076(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0xAE: XOR (IY + n) [8]
    private static void Step_3084(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xFD 0xAE: XOR (IY + n) [9]
    private static void Step_3085(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A ^= emulator.data;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xFD 0xAE: XOR (IY + n) [10]
    private static void Step_3086(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0xB4: OR IYH [0]
    private static void Step_3087(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A |= emulator.IYH;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0xB5: OR IYL [0]
    private static void Step_3088(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.A |= emulator.IYL;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0xB6: OR (IY + n) [0]
    private static void Step_3089(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0xB6: OR (IY + n) [8]
    private static void Step_3097(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xFD 0xB6: OR (IY + n) [9]
    private static void Step_3098(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.A |= emulator.data;

        // Update flags.
        // Reset C, N and H.
        int flags = emulator.A & 0b10101000; // Copy X, Y and S from A.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.A) & 0x01)) << 2; // Set PV if even_parity(A) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.A == 0)) << 6; // Set Z if is_zero(A) is true.
        emulator.F = (byte)flags;
    }

    // 0xFD 0xB6: OR (IY + n) [10]
    private static void Step_3099(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0xBC: CP IYH [0]
    private static void Step_3100(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.IYH;
        var result = left - right;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= right & 0b00101000; // Copy X and Y from right.
        flags |= result & 0b10000000; // Copy S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 6; // Set Z if is_zero(result) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0xBD: CP IYL [0]
    private static void Step_3101(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        var left = emulator.A;
        var right = emulator.IYL;
        var result = left - right;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= right & 0b00101000; // Copy X and Y from right.
        flags |= result & 0b10000000; // Copy S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 6; // Set Z if is_zero(result) is true.
        emulator.F = (byte)flags;
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0xBE: CP (IY + n) [0]
    private static void Step_3102(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0xBE: CP (IY + n) [8]
    private static void Step_3110(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.data);
        emulator.address = emulator.WZ;
    }

    // 0xFD 0xBE: CP (IY + n) [9]
    private static void Step_3111(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var left = emulator.A;
        var right = emulator.data;
        var result = left - right;

        // Update flags.
        int flags = 0b00000010; // Set N.
        flags |= right & 0b00101000; // Copy X and Y from right.
        flags |= result & 0b10000000; // Copy S from result.
        flags |= Unsafe.BitCast<bool, byte>((result & 0x0100) == 0x0100); // Set C if (result & 0x0100) == 0x0100 is true.
        flags |= ((left ^ right) & (result ^ left) & 0x80) >> 5; // Set PV if did_8_bit_subtraction_overflow(left, right, result) is true.
        flags |= (left ^ right ^ result) & 0x10; // Set H if did_8_bit_subtraction_half_borrow(left, right, result) is true.
        flags |= (Unsafe.BitCast<bool, byte>(result == 0)) << 6; // Set Z if is_zero(result) is true.
        emulator.F = (byte)flags;
    }

    // 0xFD 0xBE: CP (IY + n) [10]
    private static void Step_3112(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0xCB: FDCB Redirect [0]
    private static void Step_3113(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.opcodeStepTable = OpcodeStepTableFDCB;
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0xCB: FDCB Redirect [1]
    private static void Step_3114(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.latch = emulator.data;
    }

    // 0xFD 0xCB: FDCB Redirect [3]
    private static void Step_3116(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.PC;
        emulator.PC += 0x01;
    }

    // 0xFD 0xCB: FDCB Redirect [5]
    private static void Step_3118(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.currentStep = Unsafe.Add(ref MemoryMarshal.GetArrayDataReference(emulator.opcodeStepTable), emulator.data);
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xFD 0xDD: FDDD Redirect [0]
    private static void Step_3119(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.opcodeStepTable = OpcodeStepTablePrefixDD;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0xE1: POP IY [0]
    private static void Step_3120(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xFD 0xE1: POP IY [1]
    private static void Step_3121(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.IYL = emulator.data;
    }

    // 0xFD 0xE1: POP IY [3]
    private static void Step_3123(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.SP += 0x01;
    }

    // 0xFD 0xE1: POP IY [4]
    private static void Step_3124(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.IYH = emulator.data;
    }

    // 0xFD 0xE1: POP IY [5]
    private static void Step_3125(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0xE3: EX (SP); IY [0]
    private static void Step_3126(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.address = emulator.SP;
    }

    // 0xFD 0xE3: EX (SP); IY [1]
    private static void Step_3127(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Z = emulator.data;
    }

    // 0xFD 0xE3: EX (SP); IY [3]
    private static void Step_3129(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = (ushort)(emulator.SP + 0x01);
    }

    // 0xFD 0xE3: EX (SP); IY [4]
    private static void Step_3130(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.W = emulator.data;
    }

    // 0xFD 0xE3: EX (SP); IY [7]
    private static void Step_3133(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = emulator.IYH;
    }

    // 0xFD 0xE3: EX (SP); IY [10]
    private static void Step_3136(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.address = emulator.SP;
        emulator.data = emulator.IYL;
    }

    // 0xFD 0xE3: EX (SP); IY [14]
    private static void Step_3140(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.IY = emulator.WZ;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0xE5: PUSH IY [0]
    private static void Step_3141(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
    }

    // 0xFD 0xE5: PUSH IY [1]
    private static void Step_3142(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.IYH;
    }

    // 0xFD 0xE5: PUSH IY [4]
    private static void Step_3145(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.SP -= 0x01;
        emulator.address = emulator.SP;
        emulator.data = emulator.IYL;
    }

    // 0xFD 0xE5: PUSH IY [6]
    private static void Step_3147(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // 0xFD 0xE9: JP IY [0]
    private static void Step_3148(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.PC = emulator.IY;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0xED: FDED Redirect [0]
    private static void Step_3149(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.opcodeStepTable = OpcodeStepTablePrefixED;
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        // Overlapped opcode read.
        Step_0(emulator, ref actionRequired);
    }

    // 0xFD 0xF9: LD SP, IY [0]
    private static void Step_3150(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.opcodeStepTable = OpcodeStepTableNoPrefix;
        emulator.SP = emulator.IY;
    }

    // 0xFD 0xF9: LD SP, IY [1]
    private static void Step_3151(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x00: RLC (IX + d), B [2]
    private static void Step_3154(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x00: RLC (IX + d), B [5]
    private static void Step_3157(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | emulator.data >> 0x07);
        emulator.B = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x00: RLC (IX + d), B [8]
    private static void Step_3160(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x01: RLC (IX + d), C [2]
    private static void Step_3163(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x01: RLC (IX + d), C [5]
    private static void Step_3166(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | emulator.data >> 0x07);
        emulator.C = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x01: RLC (IX + d), C [8]
    private static void Step_3169(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x02: RLC (IX + d), D [2]
    private static void Step_3172(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x02: RLC (IX + d), D [5]
    private static void Step_3175(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | emulator.data >> 0x07);
        emulator.D = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x02: RLC (IX + d), D [8]
    private static void Step_3178(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x03: RLC (IX + d), E [2]
    private static void Step_3181(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x03: RLC (IX + d), E [5]
    private static void Step_3184(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | emulator.data >> 0x07);
        emulator.E = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x03: RLC (IX + d), E [8]
    private static void Step_3187(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x04: RLC (IX + d), H [2]
    private static void Step_3190(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x04: RLC (IX + d), H [5]
    private static void Step_3193(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | emulator.data >> 0x07);
        emulator.H = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x04: RLC (IX + d), H [8]
    private static void Step_3196(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x05: RLC (IX + d), L [2]
    private static void Step_3199(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x05: RLC (IX + d), L [5]
    private static void Step_3202(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | emulator.data >> 0x07);
        emulator.L = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x05: RLC (IX + d), L [8]
    private static void Step_3205(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x06: RLC (IX + d) [2]
    private static void Step_3208(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x06: RLC (IX + d) [5]
    private static void Step_3211(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | emulator.data >> 0x07);

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x06: RLC (IX + d) [8]
    private static void Step_3214(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x07: RLC (IX + d), A [2]
    private static void Step_3217(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x07: RLC (IX + d), A [5]
    private static void Step_3220(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | emulator.data >> 0x07);
        emulator.A = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x07: RLC (IX + d), A [8]
    private static void Step_3223(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x08: RRC (IX + d), B [2]
    private static void Step_3226(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x08: RRC (IX + d), B [5]
    private static void Step_3229(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data << 0x07);
        emulator.B = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x08: RRC (IX + d), B [8]
    private static void Step_3232(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x09: RRC (IX + d), C [2]
    private static void Step_3235(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x09: RRC (IX + d), C [5]
    private static void Step_3238(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data << 0x07);
        emulator.C = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x09: RRC (IX + d), C [8]
    private static void Step_3241(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x0A: RRC (IX + d), D [2]
    private static void Step_3244(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x0A: RRC (IX + d), D [5]
    private static void Step_3247(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data << 0x07);
        emulator.D = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x0A: RRC (IX + d), D [8]
    private static void Step_3250(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x0B: RRC (IX + d), E [2]
    private static void Step_3253(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x0B: RRC (IX + d), E [5]
    private static void Step_3256(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data << 0x07);
        emulator.E = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x0B: RRC (IX + d), E [8]
    private static void Step_3259(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x0C: RRC (IX + d), H [2]
    private static void Step_3262(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x0C: RRC (IX + d), H [5]
    private static void Step_3265(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data << 0x07);
        emulator.H = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x0C: RRC (IX + d), H [8]
    private static void Step_3268(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x0D: RRC (IX + d), L [2]
    private static void Step_3271(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x0D: RRC (IX + d), L [5]
    private static void Step_3274(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data << 0x07);
        emulator.L = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x0D: RRC (IX + d), L [8]
    private static void Step_3277(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x0E: RRC (IX + d) [2]
    private static void Step_3280(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x0E: RRC (IX + d) [5]
    private static void Step_3283(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x0E: RRC (IX + d) [8]
    private static void Step_3286(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x0F: RRC (IX + d), A [2]
    private static void Step_3289(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x0F: RRC (IX + d), A [5]
    private static void Step_3292(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data << 0x07);
        emulator.A = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x0F: RRC (IX + d), A [8]
    private static void Step_3295(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x10: RL (IX + d), B [2]
    private static void Step_3298(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x10: RL (IX + d), B [5]
    private static void Step_3301(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | (emulator.F & 0b00000001 /* flag.C */));
        emulator.B = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x10: RL (IX + d), B [8]
    private static void Step_3304(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x11: RL (IX + d), C [2]
    private static void Step_3307(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x11: RL (IX + d), C [5]
    private static void Step_3310(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | (emulator.F & 0b00000001 /* flag.C */));
        emulator.C = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x11: RL (IX + d), C [8]
    private static void Step_3313(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x12: RL (IX + d), D [2]
    private static void Step_3316(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x12: RL (IX + d), D [5]
    private static void Step_3319(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | (emulator.F & 0b00000001 /* flag.C */));
        emulator.D = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x12: RL (IX + d), D [8]
    private static void Step_3322(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x13: RL (IX + d), E [2]
    private static void Step_3325(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x13: RL (IX + d), E [5]
    private static void Step_3328(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | (emulator.F & 0b00000001 /* flag.C */));
        emulator.E = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x13: RL (IX + d), E [8]
    private static void Step_3331(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x14: RL (IX + d), H [2]
    private static void Step_3334(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x14: RL (IX + d), H [5]
    private static void Step_3337(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | (emulator.F & 0b00000001 /* flag.C */));
        emulator.H = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x14: RL (IX + d), H [8]
    private static void Step_3340(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x15: RL (IX + d), L [2]
    private static void Step_3343(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x15: RL (IX + d), L [5]
    private static void Step_3346(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | (emulator.F & 0b00000001 /* flag.C */));
        emulator.L = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x15: RL (IX + d), L [8]
    private static void Step_3349(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x16: RL (IX + d) [2]
    private static void Step_3352(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x16: RL (IX + d) [5]
    private static void Step_3355(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | (emulator.F & 0b00000001 /* flag.C */));

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x16: RL (IX + d) [8]
    private static void Step_3358(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x17: RL (IX + d), A [2]
    private static void Step_3361(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x17: RL (IX + d), A [5]
    private static void Step_3364(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | (emulator.F & 0b00000001 /* flag.C */));
        emulator.A = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x17: RL (IX + d), A [8]
    private static void Step_3367(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x18: RR (IX + d), B [2]
    private static void Step_3370(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x18: RR (IX + d), B [5]
    private static void Step_3373(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);
        emulator.B = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x18: RR (IX + d), B [8]
    private static void Step_3376(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x19: RR (IX + d), C [2]
    private static void Step_3379(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x19: RR (IX + d), C [5]
    private static void Step_3382(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);
        emulator.C = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x19: RR (IX + d), C [8]
    private static void Step_3385(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x1A: RR (IX + d), D [2]
    private static void Step_3388(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x1A: RR (IX + d), D [5]
    private static void Step_3391(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);
        emulator.D = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x1A: RR (IX + d), D [8]
    private static void Step_3394(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x1B: RR (IX + d), E [2]
    private static void Step_3397(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x1B: RR (IX + d), E [5]
    private static void Step_3400(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);
        emulator.E = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x1B: RR (IX + d), E [8]
    private static void Step_3403(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x1C: RR (IX + d), H [2]
    private static void Step_3406(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x1C: RR (IX + d), H [5]
    private static void Step_3409(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);
        emulator.H = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x1C: RR (IX + d), H [8]
    private static void Step_3412(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x1D: RR (IX + d), L [2]
    private static void Step_3415(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x1D: RR (IX + d), L [5]
    private static void Step_3418(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);
        emulator.L = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x1D: RR (IX + d), L [8]
    private static void Step_3421(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x1E: RR (IX + d) [2]
    private static void Step_3424(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x1E: RR (IX + d) [5]
    private static void Step_3427(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x1E: RR (IX + d) [8]
    private static void Step_3430(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x1F: RR (IX + d), A [2]
    private static void Step_3433(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x1F: RR (IX + d), A [5]
    private static void Step_3436(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);
        emulator.A = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x1F: RR (IX + d), A [8]
    private static void Step_3439(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x20: SLA (IX + d), B [2]
    private static void Step_3442(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x20: SLA (IX + d), B [5]
    private static void Step_3445(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data <<= 0x01;
        emulator.B = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x20: SLA (IX + d), B [8]
    private static void Step_3448(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x21: SLA (IX + d), C [2]
    private static void Step_3451(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x21: SLA (IX + d), C [5]
    private static void Step_3454(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data <<= 0x01;
        emulator.C = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x21: SLA (IX + d), C [8]
    private static void Step_3457(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x22: SLA (IX + d), D [2]
    private static void Step_3460(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x22: SLA (IX + d), D [5]
    private static void Step_3463(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data <<= 0x01;
        emulator.D = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x22: SLA (IX + d), D [8]
    private static void Step_3466(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x23: SLA (IX + d), E [2]
    private static void Step_3469(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x23: SLA (IX + d), E [5]
    private static void Step_3472(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data <<= 0x01;
        emulator.E = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x23: SLA (IX + d), E [8]
    private static void Step_3475(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x24: SLA (IX + d), H [2]
    private static void Step_3478(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x24: SLA (IX + d), H [5]
    private static void Step_3481(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data <<= 0x01;
        emulator.H = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x24: SLA (IX + d), H [8]
    private static void Step_3484(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x25: SLA (IX + d), L [2]
    private static void Step_3487(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x25: SLA (IX + d), L [5]
    private static void Step_3490(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data <<= 0x01;
        emulator.L = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x25: SLA (IX + d), L [8]
    private static void Step_3493(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x26: SLA (IX + d) [2]
    private static void Step_3496(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x26: SLA (IX + d) [5]
    private static void Step_3499(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data <<= 0x01;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x26: SLA (IX + d) [8]
    private static void Step_3502(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x27: SLA (IX + d), A [2]
    private static void Step_3505(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x27: SLA (IX + d), A [5]
    private static void Step_3508(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data <<= 0x01;
        emulator.A = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x27: SLA (IX + d), A [8]
    private static void Step_3511(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x28: SRA (IX + d), B [2]
    private static void Step_3514(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x28: SRA (IX + d), B [5]
    private static void Step_3517(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data & 0x80);
        emulator.B = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x28: SRA (IX + d), B [8]
    private static void Step_3520(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x29: SRA (IX + d), C [2]
    private static void Step_3523(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x29: SRA (IX + d), C [5]
    private static void Step_3526(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data & 0x80);
        emulator.C = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x29: SRA (IX + d), C [8]
    private static void Step_3529(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x2A: SRA (IX + d), D [2]
    private static void Step_3532(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x2A: SRA (IX + d), D [5]
    private static void Step_3535(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data & 0x80);
        emulator.D = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x2A: SRA (IX + d), D [8]
    private static void Step_3538(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x2B: SRA (IX + d), E [2]
    private static void Step_3541(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x2B: SRA (IX + d), E [5]
    private static void Step_3544(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data & 0x80);
        emulator.E = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x2B: SRA (IX + d), E [8]
    private static void Step_3547(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x2C: SRA (IX + d), H [2]
    private static void Step_3550(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x2C: SRA (IX + d), H [5]
    private static void Step_3553(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data & 0x80);
        emulator.H = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x2C: SRA (IX + d), H [8]
    private static void Step_3556(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x2D: SRA (IX + d), L [2]
    private static void Step_3559(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x2D: SRA (IX + d), L [5]
    private static void Step_3562(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data & 0x80);
        emulator.L = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x2D: SRA (IX + d), L [8]
    private static void Step_3565(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x2E: SRA (IX + d) [2]
    private static void Step_3568(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x2E: SRA (IX + d) [5]
    private static void Step_3571(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data & 0x80);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x2E: SRA (IX + d) [8]
    private static void Step_3574(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x2F: SRA (IX + d), A [2]
    private static void Step_3577(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x2F: SRA (IX + d), A [5]
    private static void Step_3580(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data & 0x80);
        emulator.A = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x2F: SRA (IX + d), A [8]
    private static void Step_3583(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x30: SLL (IX + d), B [2]
    private static void Step_3586(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x30: SLL (IX + d), B [5]
    private static void Step_3589(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | 0x01);
        emulator.B = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x30: SLL (IX + d), B [8]
    private static void Step_3592(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x31: SLL (IX + d), C [2]
    private static void Step_3595(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x31: SLL (IX + d), C [5]
    private static void Step_3598(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | 0x01);
        emulator.C = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x31: SLL (IX + d), C [8]
    private static void Step_3601(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x32: SLL (IX + d), D [2]
    private static void Step_3604(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x32: SLL (IX + d), D [5]
    private static void Step_3607(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | 0x01);
        emulator.D = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x32: SLL (IX + d), D [8]
    private static void Step_3610(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x33: SLL (IX + d), E [2]
    private static void Step_3613(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x33: SLL (IX + d), E [5]
    private static void Step_3616(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | 0x01);
        emulator.E = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x33: SLL (IX + d), E [8]
    private static void Step_3619(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x34: SLL (IX + d), H [2]
    private static void Step_3622(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x34: SLL (IX + d), H [5]
    private static void Step_3625(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | 0x01);
        emulator.H = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x34: SLL (IX + d), H [8]
    private static void Step_3628(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x35: SLL (IX + d), L [2]
    private static void Step_3631(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x35: SLL (IX + d), L [5]
    private static void Step_3634(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | 0x01);
        emulator.L = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x35: SLL (IX + d), L [8]
    private static void Step_3637(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x36: SLL (IX + d) [2]
    private static void Step_3640(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x36: SLL (IX + d) [5]
    private static void Step_3643(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | 0x01);

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x36: SLL (IX + d) [8]
    private static void Step_3646(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x37: SLL (IX + d), A [2]
    private static void Step_3649(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x37: SLL (IX + d), A [5]
    private static void Step_3652(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | 0x01);
        emulator.A = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x37: SLL (IX + d), A [8]
    private static void Step_3655(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x38: SRL (IX + d), B [2]
    private static void Step_3658(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x38: SRL (IX + d), B [5]
    private static void Step_3661(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 & 0x7F);
        emulator.B = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x38: SRL (IX + d), B [8]
    private static void Step_3664(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x39: SRL (IX + d), C [2]
    private static void Step_3667(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x39: SRL (IX + d), C [5]
    private static void Step_3670(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 & 0x7F);
        emulator.C = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x39: SRL (IX + d), C [8]
    private static void Step_3673(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x3A: SRL (IX + d), D [2]
    private static void Step_3676(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x3A: SRL (IX + d), D [5]
    private static void Step_3679(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 & 0x7F);
        emulator.D = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x3A: SRL (IX + d), D [8]
    private static void Step_3682(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x3B: SRL (IX + d), E [2]
    private static void Step_3685(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x3B: SRL (IX + d), E [5]
    private static void Step_3688(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 & 0x7F);
        emulator.E = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x3B: SRL (IX + d), E [8]
    private static void Step_3691(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x3C: SRL (IX + d), H [2]
    private static void Step_3694(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x3C: SRL (IX + d), H [5]
    private static void Step_3697(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 & 0x7F);
        emulator.H = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x3C: SRL (IX + d), H [8]
    private static void Step_3700(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x3D: SRL (IX + d), L [2]
    private static void Step_3703(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x3D: SRL (IX + d), L [5]
    private static void Step_3706(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 & 0x7F);
        emulator.L = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x3D: SRL (IX + d), L [8]
    private static void Step_3709(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x3E: SRL (IX + d) [2]
    private static void Step_3712(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x3E: SRL (IX + d) [5]
    private static void Step_3715(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 & 0x7F);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x3E: SRL (IX + d) [8]
    private static void Step_3718(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x3F: SRL (IX + d), A [2]
    private static void Step_3721(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x3F: SRL (IX + d), A [5]
    private static void Step_3724(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 & 0x7F);
        emulator.A = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x3F: SRL (IX + d), A [8]
    private static void Step_3727(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x40: BIT 0x00, (IX + d) [2]
    private static void Step_3730(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x40: BIT 0x00, (IX + d) [4]
    private static void Step_3732(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x40: BIT 0x00, (IX + d) [5]
    private static void Step_3733(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x48: BIT 0x01, (IX + d) [2]
    private static void Step_3736(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x48: BIT 0x01, (IX + d) [4]
    private static void Step_3738(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01 << 0x01;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x48: BIT 0x01, (IX + d) [5]
    private static void Step_3739(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x50: BIT 0x02, (IX + d) [2]
    private static void Step_3742(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x50: BIT 0x02, (IX + d) [4]
    private static void Step_3744(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01 << 0x02;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x50: BIT 0x02, (IX + d) [5]
    private static void Step_3745(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x58: BIT 0x03, (IX + d) [2]
    private static void Step_3748(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x58: BIT 0x03, (IX + d) [4]
    private static void Step_3750(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01 << 0x03;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x58: BIT 0x03, (IX + d) [5]
    private static void Step_3751(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x60: BIT 0x04, (IX + d) [2]
    private static void Step_3754(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x60: BIT 0x04, (IX + d) [4]
    private static void Step_3756(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01 << 0x04;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x60: BIT 0x04, (IX + d) [5]
    private static void Step_3757(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x68: BIT 0x05, (IX + d) [2]
    private static void Step_3760(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x68: BIT 0x05, (IX + d) [4]
    private static void Step_3762(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01 << 0x05;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x68: BIT 0x05, (IX + d) [5]
    private static void Step_3763(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x70: BIT 0x06, (IX + d) [2]
    private static void Step_3766(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x70: BIT 0x06, (IX + d) [4]
    private static void Step_3768(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01 << 0x06;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x70: BIT 0x06, (IX + d) [5]
    private static void Step_3769(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x78: BIT 0x07, (IX + d) [2]
    private static void Step_3772(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x78: BIT 0x07, (IX + d) [4]
    private static void Step_3774(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01 << 0x07;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
    }

    // DDCB 0x78: BIT 0x07, (IX + d) [5]
    private static void Step_3775(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x80: RES 0x00, (IX + d), B [2]
    private static void Step_3778(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x80: RES 0x00, (IX + d), B [6]
    private static void Step_3782(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~0x01);
        emulator.B = emulator.data;
    }

    // DDCB 0x80: RES 0x00, (IX + d), B [8]
    private static void Step_3784(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x81: RES 0x00, (IX + d), C [2]
    private static void Step_3787(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x81: RES 0x00, (IX + d), C [6]
    private static void Step_3791(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~0x01);
        emulator.C = emulator.data;
    }

    // DDCB 0x81: RES 0x00, (IX + d), C [8]
    private static void Step_3793(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x82: RES 0x00, (IX + d), D [2]
    private static void Step_3796(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x82: RES 0x00, (IX + d), D [6]
    private static void Step_3800(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~0x01);
        emulator.D = emulator.data;
    }

    // DDCB 0x82: RES 0x00, (IX + d), D [8]
    private static void Step_3802(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x83: RES 0x00, (IX + d), E [2]
    private static void Step_3805(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x83: RES 0x00, (IX + d), E [6]
    private static void Step_3809(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~0x01);
        emulator.E = emulator.data;
    }

    // DDCB 0x83: RES 0x00, (IX + d), E [8]
    private static void Step_3811(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x84: RES 0x00, (IX + d), H [2]
    private static void Step_3814(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x84: RES 0x00, (IX + d), H [6]
    private static void Step_3818(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~0x01);
        emulator.H = emulator.data;
    }

    // DDCB 0x84: RES 0x00, (IX + d), H [8]
    private static void Step_3820(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x85: RES 0x00, (IX + d), L [2]
    private static void Step_3823(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x85: RES 0x00, (IX + d), L [6]
    private static void Step_3827(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~0x01);
        emulator.L = emulator.data;
    }

    // DDCB 0x85: RES 0x00, (IX + d), L [8]
    private static void Step_3829(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x86: RES 0x00, (IX + d) [2]
    private static void Step_3832(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x86: RES 0x00, (IX + d) [6]
    private static void Step_3836(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~0x01);
    }

    // DDCB 0x86: RES 0x00, (IX + d) [8]
    private static void Step_3838(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x87: RES 0x00, (IX + d), A [2]
    private static void Step_3841(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x87: RES 0x00, (IX + d), A [6]
    private static void Step_3845(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~0x01);
        emulator.A = emulator.data;
    }

    // DDCB 0x87: RES 0x00, (IX + d), A [8]
    private static void Step_3847(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x88: RES 0x01, (IX + d), B [2]
    private static void Step_3850(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x88: RES 0x01, (IX + d), B [6]
    private static void Step_3854(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x01));
        emulator.B = emulator.data;
    }

    // DDCB 0x88: RES 0x01, (IX + d), B [8]
    private static void Step_3856(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x89: RES 0x01, (IX + d), C [2]
    private static void Step_3859(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x89: RES 0x01, (IX + d), C [6]
    private static void Step_3863(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x01));
        emulator.C = emulator.data;
    }

    // DDCB 0x89: RES 0x01, (IX + d), C [8]
    private static void Step_3865(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x8A: RES 0x01, (IX + d), D [2]
    private static void Step_3868(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x8A: RES 0x01, (IX + d), D [6]
    private static void Step_3872(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x01));
        emulator.D = emulator.data;
    }

    // DDCB 0x8A: RES 0x01, (IX + d), D [8]
    private static void Step_3874(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x8B: RES 0x01, (IX + d), E [2]
    private static void Step_3877(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x8B: RES 0x01, (IX + d), E [6]
    private static void Step_3881(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x01));
        emulator.E = emulator.data;
    }

    // DDCB 0x8B: RES 0x01, (IX + d), E [8]
    private static void Step_3883(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x8C: RES 0x01, (IX + d), H [2]
    private static void Step_3886(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x8C: RES 0x01, (IX + d), H [6]
    private static void Step_3890(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x01));
        emulator.H = emulator.data;
    }

    // DDCB 0x8C: RES 0x01, (IX + d), H [8]
    private static void Step_3892(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x8D: RES 0x01, (IX + d), L [2]
    private static void Step_3895(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x8D: RES 0x01, (IX + d), L [6]
    private static void Step_3899(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x01));
        emulator.L = emulator.data;
    }

    // DDCB 0x8D: RES 0x01, (IX + d), L [8]
    private static void Step_3901(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x8E: RES 0x01, (IX + d) [2]
    private static void Step_3904(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x8E: RES 0x01, (IX + d) [6]
    private static void Step_3908(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x01));
    }

    // DDCB 0x8E: RES 0x01, (IX + d) [8]
    private static void Step_3910(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x8F: RES 0x01, (IX + d), A [2]
    private static void Step_3913(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x8F: RES 0x01, (IX + d), A [6]
    private static void Step_3917(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x01));
        emulator.A = emulator.data;
    }

    // DDCB 0x8F: RES 0x01, (IX + d), A [8]
    private static void Step_3919(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x90: RES 0x02, (IX + d), B [2]
    private static void Step_3922(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x90: RES 0x02, (IX + d), B [6]
    private static void Step_3926(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x02));
        emulator.B = emulator.data;
    }

    // DDCB 0x90: RES 0x02, (IX + d), B [8]
    private static void Step_3928(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x91: RES 0x02, (IX + d), C [2]
    private static void Step_3931(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x91: RES 0x02, (IX + d), C [6]
    private static void Step_3935(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x02));
        emulator.C = emulator.data;
    }

    // DDCB 0x91: RES 0x02, (IX + d), C [8]
    private static void Step_3937(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x92: RES 0x02, (IX + d), D [2]
    private static void Step_3940(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x92: RES 0x02, (IX + d), D [6]
    private static void Step_3944(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x02));
        emulator.D = emulator.data;
    }

    // DDCB 0x92: RES 0x02, (IX + d), D [8]
    private static void Step_3946(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x93: RES 0x02, (IX + d), E [2]
    private static void Step_3949(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x93: RES 0x02, (IX + d), E [6]
    private static void Step_3953(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x02));
        emulator.E = emulator.data;
    }

    // DDCB 0x93: RES 0x02, (IX + d), E [8]
    private static void Step_3955(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x94: RES 0x02, (IX + d), H [2]
    private static void Step_3958(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x94: RES 0x02, (IX + d), H [6]
    private static void Step_3962(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x02));
        emulator.H = emulator.data;
    }

    // DDCB 0x94: RES 0x02, (IX + d), H [8]
    private static void Step_3964(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x95: RES 0x02, (IX + d), L [2]
    private static void Step_3967(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x95: RES 0x02, (IX + d), L [6]
    private static void Step_3971(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x02));
        emulator.L = emulator.data;
    }

    // DDCB 0x95: RES 0x02, (IX + d), L [8]
    private static void Step_3973(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x96: RES 0x02, (IX + d) [2]
    private static void Step_3976(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x96: RES 0x02, (IX + d) [6]
    private static void Step_3980(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x02));
    }

    // DDCB 0x96: RES 0x02, (IX + d) [8]
    private static void Step_3982(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x97: RES 0x02, (IX + d), A [2]
    private static void Step_3985(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x97: RES 0x02, (IX + d), A [6]
    private static void Step_3989(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x02));
        emulator.A = emulator.data;
    }

    // DDCB 0x97: RES 0x02, (IX + d), A [8]
    private static void Step_3991(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x98: RES 0x03, (IX + d), B [2]
    private static void Step_3994(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x98: RES 0x03, (IX + d), B [6]
    private static void Step_3998(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x03));
        emulator.B = emulator.data;
    }

    // DDCB 0x98: RES 0x03, (IX + d), B [8]
    private static void Step_4000(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x99: RES 0x03, (IX + d), C [2]
    private static void Step_4003(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x99: RES 0x03, (IX + d), C [6]
    private static void Step_4007(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x03));
        emulator.C = emulator.data;
    }

    // DDCB 0x99: RES 0x03, (IX + d), C [8]
    private static void Step_4009(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x9A: RES 0x03, (IX + d), D [2]
    private static void Step_4012(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x9A: RES 0x03, (IX + d), D [6]
    private static void Step_4016(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x03));
        emulator.D = emulator.data;
    }

    // DDCB 0x9A: RES 0x03, (IX + d), D [8]
    private static void Step_4018(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x9B: RES 0x03, (IX + d), E [2]
    private static void Step_4021(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x9B: RES 0x03, (IX + d), E [6]
    private static void Step_4025(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x03));
        emulator.E = emulator.data;
    }

    // DDCB 0x9B: RES 0x03, (IX + d), E [8]
    private static void Step_4027(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x9C: RES 0x03, (IX + d), H [2]
    private static void Step_4030(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x9C: RES 0x03, (IX + d), H [6]
    private static void Step_4034(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x03));
        emulator.H = emulator.data;
    }

    // DDCB 0x9C: RES 0x03, (IX + d), H [8]
    private static void Step_4036(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x9D: RES 0x03, (IX + d), L [2]
    private static void Step_4039(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x9D: RES 0x03, (IX + d), L [6]
    private static void Step_4043(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x03));
        emulator.L = emulator.data;
    }

    // DDCB 0x9D: RES 0x03, (IX + d), L [8]
    private static void Step_4045(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x9E: RES 0x03, (IX + d) [2]
    private static void Step_4048(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x9E: RES 0x03, (IX + d) [6]
    private static void Step_4052(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x03));
    }

    // DDCB 0x9E: RES 0x03, (IX + d) [8]
    private static void Step_4054(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0x9F: RES 0x03, (IX + d), A [2]
    private static void Step_4057(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0x9F: RES 0x03, (IX + d), A [6]
    private static void Step_4061(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x03));
        emulator.A = emulator.data;
    }

    // DDCB 0x9F: RES 0x03, (IX + d), A [8]
    private static void Step_4063(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xA0: RES 0x04, (IX + d), B [2]
    private static void Step_4066(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xA0: RES 0x04, (IX + d), B [6]
    private static void Step_4070(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x04));
        emulator.B = emulator.data;
    }

    // DDCB 0xA0: RES 0x04, (IX + d), B [8]
    private static void Step_4072(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xA1: RES 0x04, (IX + d), C [2]
    private static void Step_4075(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xA1: RES 0x04, (IX + d), C [6]
    private static void Step_4079(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x04));
        emulator.C = emulator.data;
    }

    // DDCB 0xA1: RES 0x04, (IX + d), C [8]
    private static void Step_4081(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xA2: RES 0x04, (IX + d), D [2]
    private static void Step_4084(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xA2: RES 0x04, (IX + d), D [6]
    private static void Step_4088(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x04));
        emulator.D = emulator.data;
    }

    // DDCB 0xA2: RES 0x04, (IX + d), D [8]
    private static void Step_4090(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xA3: RES 0x04, (IX + d), E [2]
    private static void Step_4093(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xA3: RES 0x04, (IX + d), E [6]
    private static void Step_4097(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x04));
        emulator.E = emulator.data;
    }

    // DDCB 0xA3: RES 0x04, (IX + d), E [8]
    private static void Step_4099(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xA4: RES 0x04, (IX + d), H [2]
    private static void Step_4102(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xA4: RES 0x04, (IX + d), H [6]
    private static void Step_4106(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x04));
        emulator.H = emulator.data;
    }

    // DDCB 0xA4: RES 0x04, (IX + d), H [8]
    private static void Step_4108(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xA5: RES 0x04, (IX + d), L [2]
    private static void Step_4111(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xA5: RES 0x04, (IX + d), L [6]
    private static void Step_4115(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x04));
        emulator.L = emulator.data;
    }

    // DDCB 0xA5: RES 0x04, (IX + d), L [8]
    private static void Step_4117(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xA6: RES 0x04, (IX + d) [2]
    private static void Step_4120(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xA6: RES 0x04, (IX + d) [6]
    private static void Step_4124(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x04));
    }

    // DDCB 0xA6: RES 0x04, (IX + d) [8]
    private static void Step_4126(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xA7: RES 0x04, (IX + d), A [2]
    private static void Step_4129(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xA7: RES 0x04, (IX + d), A [6]
    private static void Step_4133(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x04));
        emulator.A = emulator.data;
    }

    // DDCB 0xA7: RES 0x04, (IX + d), A [8]
    private static void Step_4135(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xA8: RES 0x05, (IX + d), B [2]
    private static void Step_4138(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xA8: RES 0x05, (IX + d), B [6]
    private static void Step_4142(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x05));
        emulator.B = emulator.data;
    }

    // DDCB 0xA8: RES 0x05, (IX + d), B [8]
    private static void Step_4144(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xA9: RES 0x05, (IX + d), C [2]
    private static void Step_4147(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xA9: RES 0x05, (IX + d), C [6]
    private static void Step_4151(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x05));
        emulator.C = emulator.data;
    }

    // DDCB 0xA9: RES 0x05, (IX + d), C [8]
    private static void Step_4153(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xAA: RES 0x05, (IX + d), D [2]
    private static void Step_4156(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xAA: RES 0x05, (IX + d), D [6]
    private static void Step_4160(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x05));
        emulator.D = emulator.data;
    }

    // DDCB 0xAA: RES 0x05, (IX + d), D [8]
    private static void Step_4162(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xAB: RES 0x05, (IX + d), E [2]
    private static void Step_4165(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xAB: RES 0x05, (IX + d), E [6]
    private static void Step_4169(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x05));
        emulator.E = emulator.data;
    }

    // DDCB 0xAB: RES 0x05, (IX + d), E [8]
    private static void Step_4171(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xAC: RES 0x05, (IX + d), H [2]
    private static void Step_4174(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xAC: RES 0x05, (IX + d), H [6]
    private static void Step_4178(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x05));
        emulator.H = emulator.data;
    }

    // DDCB 0xAC: RES 0x05, (IX + d), H [8]
    private static void Step_4180(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xAD: RES 0x05, (IX + d), L [2]
    private static void Step_4183(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xAD: RES 0x05, (IX + d), L [6]
    private static void Step_4187(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x05));
        emulator.L = emulator.data;
    }

    // DDCB 0xAD: RES 0x05, (IX + d), L [8]
    private static void Step_4189(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xAE: RES 0x05, (IX + d) [2]
    private static void Step_4192(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xAE: RES 0x05, (IX + d) [6]
    private static void Step_4196(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x05));
    }

    // DDCB 0xAE: RES 0x05, (IX + d) [8]
    private static void Step_4198(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xAF: RES 0x05, (IX + d), A [2]
    private static void Step_4201(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xAF: RES 0x05, (IX + d), A [6]
    private static void Step_4205(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x05));
        emulator.A = emulator.data;
    }

    // DDCB 0xAF: RES 0x05, (IX + d), A [8]
    private static void Step_4207(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xB0: RES 0x06, (IX + d), B [2]
    private static void Step_4210(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xB0: RES 0x06, (IX + d), B [6]
    private static void Step_4214(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x06));
        emulator.B = emulator.data;
    }

    // DDCB 0xB0: RES 0x06, (IX + d), B [8]
    private static void Step_4216(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xB1: RES 0x06, (IX + d), C [2]
    private static void Step_4219(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xB1: RES 0x06, (IX + d), C [6]
    private static void Step_4223(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x06));
        emulator.C = emulator.data;
    }

    // DDCB 0xB1: RES 0x06, (IX + d), C [8]
    private static void Step_4225(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xB2: RES 0x06, (IX + d), D [2]
    private static void Step_4228(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xB2: RES 0x06, (IX + d), D [6]
    private static void Step_4232(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x06));
        emulator.D = emulator.data;
    }

    // DDCB 0xB2: RES 0x06, (IX + d), D [8]
    private static void Step_4234(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xB3: RES 0x06, (IX + d), E [2]
    private static void Step_4237(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xB3: RES 0x06, (IX + d), E [6]
    private static void Step_4241(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x06));
        emulator.E = emulator.data;
    }

    // DDCB 0xB3: RES 0x06, (IX + d), E [8]
    private static void Step_4243(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xB4: RES 0x06, (IX + d), H [2]
    private static void Step_4246(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xB4: RES 0x06, (IX + d), H [6]
    private static void Step_4250(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x06));
        emulator.H = emulator.data;
    }

    // DDCB 0xB4: RES 0x06, (IX + d), H [8]
    private static void Step_4252(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xB5: RES 0x06, (IX + d), L [2]
    private static void Step_4255(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xB5: RES 0x06, (IX + d), L [6]
    private static void Step_4259(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x06));
        emulator.L = emulator.data;
    }

    // DDCB 0xB5: RES 0x06, (IX + d), L [8]
    private static void Step_4261(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xB6: RES 0x06, (IX + d) [2]
    private static void Step_4264(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xB6: RES 0x06, (IX + d) [6]
    private static void Step_4268(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x06));
    }

    // DDCB 0xB6: RES 0x06, (IX + d) [8]
    private static void Step_4270(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xB7: RES 0x06, (IX + d), A [2]
    private static void Step_4273(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xB7: RES 0x06, (IX + d), A [6]
    private static void Step_4277(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x06));
        emulator.A = emulator.data;
    }

    // DDCB 0xB7: RES 0x06, (IX + d), A [8]
    private static void Step_4279(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xB8: RES 0x07, (IX + d), B [2]
    private static void Step_4282(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xB8: RES 0x07, (IX + d), B [6]
    private static void Step_4286(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x07));
        emulator.B = emulator.data;
    }

    // DDCB 0xB8: RES 0x07, (IX + d), B [8]
    private static void Step_4288(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xB9: RES 0x07, (IX + d), C [2]
    private static void Step_4291(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xB9: RES 0x07, (IX + d), C [6]
    private static void Step_4295(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x07));
        emulator.C = emulator.data;
    }

    // DDCB 0xB9: RES 0x07, (IX + d), C [8]
    private static void Step_4297(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xBA: RES 0x07, (IX + d), D [2]
    private static void Step_4300(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xBA: RES 0x07, (IX + d), D [6]
    private static void Step_4304(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x07));
        emulator.D = emulator.data;
    }

    // DDCB 0xBA: RES 0x07, (IX + d), D [8]
    private static void Step_4306(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xBB: RES 0x07, (IX + d), E [2]
    private static void Step_4309(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xBB: RES 0x07, (IX + d), E [6]
    private static void Step_4313(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x07));
        emulator.E = emulator.data;
    }

    // DDCB 0xBB: RES 0x07, (IX + d), E [8]
    private static void Step_4315(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xBC: RES 0x07, (IX + d), H [2]
    private static void Step_4318(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xBC: RES 0x07, (IX + d), H [6]
    private static void Step_4322(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x07));
        emulator.H = emulator.data;
    }

    // DDCB 0xBC: RES 0x07, (IX + d), H [8]
    private static void Step_4324(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xBD: RES 0x07, (IX + d), L [2]
    private static void Step_4327(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xBD: RES 0x07, (IX + d), L [6]
    private static void Step_4331(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x07));
        emulator.L = emulator.data;
    }

    // DDCB 0xBD: RES 0x07, (IX + d), L [8]
    private static void Step_4333(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xBE: RES 0x07, (IX + d) [2]
    private static void Step_4336(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xBE: RES 0x07, (IX + d) [6]
    private static void Step_4340(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x07));
    }

    // DDCB 0xBE: RES 0x07, (IX + d) [8]
    private static void Step_4342(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xBF: RES 0x07, (IX + d), A [2]
    private static void Step_4345(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xBF: RES 0x07, (IX + d), A [6]
    private static void Step_4349(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x07));
        emulator.A = emulator.data;
    }

    // DDCB 0xBF: RES 0x07, (IX + d), A [8]
    private static void Step_4351(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xC0: SET 0x00, (IX + d), B [2]
    private static void Step_4354(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xC0: SET 0x00, (IX + d), B [6]
    private static void Step_4358(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data |= 0x01;
        emulator.B = emulator.data;
    }

    // DDCB 0xC0: SET 0x00, (IX + d), B [8]
    private static void Step_4360(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xC1: SET 0x00, (IX + d), C [2]
    private static void Step_4363(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xC1: SET 0x00, (IX + d), C [6]
    private static void Step_4367(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data |= 0x01;
        emulator.C = emulator.data;
    }

    // DDCB 0xC1: SET 0x00, (IX + d), C [8]
    private static void Step_4369(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xC2: SET 0x00, (IX + d), D [2]
    private static void Step_4372(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xC2: SET 0x00, (IX + d), D [6]
    private static void Step_4376(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data |= 0x01;
        emulator.D = emulator.data;
    }

    // DDCB 0xC2: SET 0x00, (IX + d), D [8]
    private static void Step_4378(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xC3: SET 0x00, (IX + d), E [2]
    private static void Step_4381(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xC3: SET 0x00, (IX + d), E [6]
    private static void Step_4385(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data |= 0x01;
        emulator.E = emulator.data;
    }

    // DDCB 0xC3: SET 0x00, (IX + d), E [8]
    private static void Step_4387(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xC4: SET 0x00, (IX + d), H [2]
    private static void Step_4390(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xC4: SET 0x00, (IX + d), H [6]
    private static void Step_4394(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data |= 0x01;
        emulator.H = emulator.data;
    }

    // DDCB 0xC4: SET 0x00, (IX + d), H [8]
    private static void Step_4396(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xC5: SET 0x00, (IX + d), L [2]
    private static void Step_4399(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xC5: SET 0x00, (IX + d), L [6]
    private static void Step_4403(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data |= 0x01;
        emulator.L = emulator.data;
    }

    // DDCB 0xC5: SET 0x00, (IX + d), L [8]
    private static void Step_4405(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xC6: SET 0x00, (IX + d) [2]
    private static void Step_4408(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xC6: SET 0x00, (IX + d) [6]
    private static void Step_4412(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data |= 0x01;
    }

    // DDCB 0xC6: SET 0x00, (IX + d) [8]
    private static void Step_4414(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xC7: SET 0x00, (IX + d), A [2]
    private static void Step_4417(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xC7: SET 0x00, (IX + d), A [6]
    private static void Step_4421(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data |= 0x01;
        emulator.A = emulator.data;
    }

    // DDCB 0xC7: SET 0x00, (IX + d), A [8]
    private static void Step_4423(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xC8: SET 0x01, (IX + d), B [2]
    private static void Step_4426(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xC8: SET 0x01, (IX + d), B [6]
    private static void Step_4430(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x01);
        emulator.B = emulator.data;
    }

    // DDCB 0xC8: SET 0x01, (IX + d), B [8]
    private static void Step_4432(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xC9: SET 0x01, (IX + d), C [2]
    private static void Step_4435(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xC9: SET 0x01, (IX + d), C [6]
    private static void Step_4439(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x01);
        emulator.C = emulator.data;
    }

    // DDCB 0xC9: SET 0x01, (IX + d), C [8]
    private static void Step_4441(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xCA: SET 0x01, (IX + d), D [2]
    private static void Step_4444(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xCA: SET 0x01, (IX + d), D [6]
    private static void Step_4448(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x01);
        emulator.D = emulator.data;
    }

    // DDCB 0xCA: SET 0x01, (IX + d), D [8]
    private static void Step_4450(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xCB: SET 0x01, (IX + d), E [2]
    private static void Step_4453(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xCB: SET 0x01, (IX + d), E [6]
    private static void Step_4457(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x01);
        emulator.E = emulator.data;
    }

    // DDCB 0xCB: SET 0x01, (IX + d), E [8]
    private static void Step_4459(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xCC: SET 0x01, (IX + d), H [2]
    private static void Step_4462(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xCC: SET 0x01, (IX + d), H [6]
    private static void Step_4466(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x01);
        emulator.H = emulator.data;
    }

    // DDCB 0xCC: SET 0x01, (IX + d), H [8]
    private static void Step_4468(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xCD: SET 0x01, (IX + d), L [2]
    private static void Step_4471(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xCD: SET 0x01, (IX + d), L [6]
    private static void Step_4475(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x01);
        emulator.L = emulator.data;
    }

    // DDCB 0xCD: SET 0x01, (IX + d), L [8]
    private static void Step_4477(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xCE: SET 0x01, (IX + d) [2]
    private static void Step_4480(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xCE: SET 0x01, (IX + d) [6]
    private static void Step_4484(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x01);
    }

    // DDCB 0xCE: SET 0x01, (IX + d) [8]
    private static void Step_4486(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xCF: SET 0x01, (IX + d), A [2]
    private static void Step_4489(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xCF: SET 0x01, (IX + d), A [6]
    private static void Step_4493(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x01);
        emulator.A = emulator.data;
    }

    // DDCB 0xCF: SET 0x01, (IX + d), A [8]
    private static void Step_4495(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xD0: SET 0x02, (IX + d), B [2]
    private static void Step_4498(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xD0: SET 0x02, (IX + d), B [6]
    private static void Step_4502(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x02);
        emulator.B = emulator.data;
    }

    // DDCB 0xD0: SET 0x02, (IX + d), B [8]
    private static void Step_4504(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xD1: SET 0x02, (IX + d), C [2]
    private static void Step_4507(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xD1: SET 0x02, (IX + d), C [6]
    private static void Step_4511(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x02);
        emulator.C = emulator.data;
    }

    // DDCB 0xD1: SET 0x02, (IX + d), C [8]
    private static void Step_4513(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xD2: SET 0x02, (IX + d), D [2]
    private static void Step_4516(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xD2: SET 0x02, (IX + d), D [6]
    private static void Step_4520(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x02);
        emulator.D = emulator.data;
    }

    // DDCB 0xD2: SET 0x02, (IX + d), D [8]
    private static void Step_4522(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xD3: SET 0x02, (IX + d), E [2]
    private static void Step_4525(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xD3: SET 0x02, (IX + d), E [6]
    private static void Step_4529(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x02);
        emulator.E = emulator.data;
    }

    // DDCB 0xD3: SET 0x02, (IX + d), E [8]
    private static void Step_4531(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xD4: SET 0x02, (IX + d), H [2]
    private static void Step_4534(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xD4: SET 0x02, (IX + d), H [6]
    private static void Step_4538(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x02);
        emulator.H = emulator.data;
    }

    // DDCB 0xD4: SET 0x02, (IX + d), H [8]
    private static void Step_4540(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xD5: SET 0x02, (IX + d), L [2]
    private static void Step_4543(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xD5: SET 0x02, (IX + d), L [6]
    private static void Step_4547(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x02);
        emulator.L = emulator.data;
    }

    // DDCB 0xD5: SET 0x02, (IX + d), L [8]
    private static void Step_4549(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xD6: SET 0x02, (IX + d) [2]
    private static void Step_4552(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xD6: SET 0x02, (IX + d) [6]
    private static void Step_4556(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x02);
    }

    // DDCB 0xD6: SET 0x02, (IX + d) [8]
    private static void Step_4558(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xD7: SET 0x02, (IX + d), A [2]
    private static void Step_4561(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xD7: SET 0x02, (IX + d), A [6]
    private static void Step_4565(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x02);
        emulator.A = emulator.data;
    }

    // DDCB 0xD7: SET 0x02, (IX + d), A [8]
    private static void Step_4567(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xD8: SET 0x03, (IX + d), B [2]
    private static void Step_4570(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xD8: SET 0x03, (IX + d), B [6]
    private static void Step_4574(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x03);
        emulator.B = emulator.data;
    }

    // DDCB 0xD8: SET 0x03, (IX + d), B [8]
    private static void Step_4576(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xD9: SET 0x03, (IX + d), C [2]
    private static void Step_4579(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xD9: SET 0x03, (IX + d), C [6]
    private static void Step_4583(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x03);
        emulator.C = emulator.data;
    }

    // DDCB 0xD9: SET 0x03, (IX + d), C [8]
    private static void Step_4585(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xDA: SET 0x03, (IX + d), D [2]
    private static void Step_4588(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xDA: SET 0x03, (IX + d), D [6]
    private static void Step_4592(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x03);
        emulator.D = emulator.data;
    }

    // DDCB 0xDA: SET 0x03, (IX + d), D [8]
    private static void Step_4594(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xDB: SET 0x03, (IX + d), E [2]
    private static void Step_4597(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xDB: SET 0x03, (IX + d), E [6]
    private static void Step_4601(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x03);
        emulator.E = emulator.data;
    }

    // DDCB 0xDB: SET 0x03, (IX + d), E [8]
    private static void Step_4603(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xDC: SET 0x03, (IX + d), H [2]
    private static void Step_4606(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xDC: SET 0x03, (IX + d), H [6]
    private static void Step_4610(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x03);
        emulator.H = emulator.data;
    }

    // DDCB 0xDC: SET 0x03, (IX + d), H [8]
    private static void Step_4612(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xDD: SET 0x03, (IX + d), L [2]
    private static void Step_4615(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xDD: SET 0x03, (IX + d), L [6]
    private static void Step_4619(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x03);
        emulator.L = emulator.data;
    }

    // DDCB 0xDD: SET 0x03, (IX + d), L [8]
    private static void Step_4621(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xDE: SET 0x03, (IX + d) [2]
    private static void Step_4624(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xDE: SET 0x03, (IX + d) [6]
    private static void Step_4628(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x03);
    }

    // DDCB 0xDE: SET 0x03, (IX + d) [8]
    private static void Step_4630(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xDF: SET 0x03, (IX + d), A [2]
    private static void Step_4633(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xDF: SET 0x03, (IX + d), A [6]
    private static void Step_4637(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x03);
        emulator.A = emulator.data;
    }

    // DDCB 0xDF: SET 0x03, (IX + d), A [8]
    private static void Step_4639(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xE0: SET 0x04, (IX + d), B [2]
    private static void Step_4642(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xE0: SET 0x04, (IX + d), B [6]
    private static void Step_4646(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x04);
        emulator.B = emulator.data;
    }

    // DDCB 0xE0: SET 0x04, (IX + d), B [8]
    private static void Step_4648(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xE1: SET 0x04, (IX + d), C [2]
    private static void Step_4651(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xE1: SET 0x04, (IX + d), C [6]
    private static void Step_4655(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x04);
        emulator.C = emulator.data;
    }

    // DDCB 0xE1: SET 0x04, (IX + d), C [8]
    private static void Step_4657(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xE2: SET 0x04, (IX + d), D [2]
    private static void Step_4660(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xE2: SET 0x04, (IX + d), D [6]
    private static void Step_4664(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x04);
        emulator.D = emulator.data;
    }

    // DDCB 0xE2: SET 0x04, (IX + d), D [8]
    private static void Step_4666(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xE3: SET 0x04, (IX + d), E [2]
    private static void Step_4669(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xE3: SET 0x04, (IX + d), E [6]
    private static void Step_4673(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x04);
        emulator.E = emulator.data;
    }

    // DDCB 0xE3: SET 0x04, (IX + d), E [8]
    private static void Step_4675(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xE4: SET 0x04, (IX + d), H [2]
    private static void Step_4678(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xE4: SET 0x04, (IX + d), H [6]
    private static void Step_4682(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x04);
        emulator.H = emulator.data;
    }

    // DDCB 0xE4: SET 0x04, (IX + d), H [8]
    private static void Step_4684(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xE5: SET 0x04, (IX + d), L [2]
    private static void Step_4687(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xE5: SET 0x04, (IX + d), L [6]
    private static void Step_4691(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x04);
        emulator.L = emulator.data;
    }

    // DDCB 0xE5: SET 0x04, (IX + d), L [8]
    private static void Step_4693(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xE6: SET 0x04, (IX + d) [2]
    private static void Step_4696(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xE6: SET 0x04, (IX + d) [6]
    private static void Step_4700(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x04);
    }

    // DDCB 0xE6: SET 0x04, (IX + d) [8]
    private static void Step_4702(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xE7: SET 0x04, (IX + d), A [2]
    private static void Step_4705(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xE7: SET 0x04, (IX + d), A [6]
    private static void Step_4709(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x04);
        emulator.A = emulator.data;
    }

    // DDCB 0xE7: SET 0x04, (IX + d), A [8]
    private static void Step_4711(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xE8: SET 0x05, (IX + d), B [2]
    private static void Step_4714(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xE8: SET 0x05, (IX + d), B [6]
    private static void Step_4718(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x05);
        emulator.B = emulator.data;
    }

    // DDCB 0xE8: SET 0x05, (IX + d), B [8]
    private static void Step_4720(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xE9: SET 0x05, (IX + d), C [2]
    private static void Step_4723(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xE9: SET 0x05, (IX + d), C [6]
    private static void Step_4727(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x05);
        emulator.C = emulator.data;
    }

    // DDCB 0xE9: SET 0x05, (IX + d), C [8]
    private static void Step_4729(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xEA: SET 0x05, (IX + d), D [2]
    private static void Step_4732(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xEA: SET 0x05, (IX + d), D [6]
    private static void Step_4736(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x05);
        emulator.D = emulator.data;
    }

    // DDCB 0xEA: SET 0x05, (IX + d), D [8]
    private static void Step_4738(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xEB: SET 0x05, (IX + d), E [2]
    private static void Step_4741(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xEB: SET 0x05, (IX + d), E [6]
    private static void Step_4745(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x05);
        emulator.E = emulator.data;
    }

    // DDCB 0xEB: SET 0x05, (IX + d), E [8]
    private static void Step_4747(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xEC: SET 0x05, (IX + d), H [2]
    private static void Step_4750(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xEC: SET 0x05, (IX + d), H [6]
    private static void Step_4754(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x05);
        emulator.H = emulator.data;
    }

    // DDCB 0xEC: SET 0x05, (IX + d), H [8]
    private static void Step_4756(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xED: SET 0x05, (IX + d), L [2]
    private static void Step_4759(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xED: SET 0x05, (IX + d), L [6]
    private static void Step_4763(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x05);
        emulator.L = emulator.data;
    }

    // DDCB 0xED: SET 0x05, (IX + d), L [8]
    private static void Step_4765(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xEE: SET 0x05, (IX + d) [2]
    private static void Step_4768(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xEE: SET 0x05, (IX + d) [6]
    private static void Step_4772(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x05);
    }

    // DDCB 0xEE: SET 0x05, (IX + d) [8]
    private static void Step_4774(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xEF: SET 0x05, (IX + d), A [2]
    private static void Step_4777(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xEF: SET 0x05, (IX + d), A [6]
    private static void Step_4781(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x05);
        emulator.A = emulator.data;
    }

    // DDCB 0xEF: SET 0x05, (IX + d), A [8]
    private static void Step_4783(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xF0: SET 0x06, (IX + d), B [2]
    private static void Step_4786(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xF0: SET 0x06, (IX + d), B [6]
    private static void Step_4790(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x06);
        emulator.B = emulator.data;
    }

    // DDCB 0xF0: SET 0x06, (IX + d), B [8]
    private static void Step_4792(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xF1: SET 0x06, (IX + d), C [2]
    private static void Step_4795(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xF1: SET 0x06, (IX + d), C [6]
    private static void Step_4799(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x06);
        emulator.C = emulator.data;
    }

    // DDCB 0xF1: SET 0x06, (IX + d), C [8]
    private static void Step_4801(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xF2: SET 0x06, (IX + d), D [2]
    private static void Step_4804(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xF2: SET 0x06, (IX + d), D [6]
    private static void Step_4808(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x06);
        emulator.D = emulator.data;
    }

    // DDCB 0xF2: SET 0x06, (IX + d), D [8]
    private static void Step_4810(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xF3: SET 0x06, (IX + d), E [2]
    private static void Step_4813(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xF3: SET 0x06, (IX + d), E [6]
    private static void Step_4817(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x06);
        emulator.E = emulator.data;
    }

    // DDCB 0xF3: SET 0x06, (IX + d), E [8]
    private static void Step_4819(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xF4: SET 0x06, (IX + d), H [2]
    private static void Step_4822(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xF4: SET 0x06, (IX + d), H [6]
    private static void Step_4826(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x06);
        emulator.H = emulator.data;
    }

    // DDCB 0xF4: SET 0x06, (IX + d), H [8]
    private static void Step_4828(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xF5: SET 0x06, (IX + d), L [2]
    private static void Step_4831(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xF5: SET 0x06, (IX + d), L [6]
    private static void Step_4835(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x06);
        emulator.L = emulator.data;
    }

    // DDCB 0xF5: SET 0x06, (IX + d), L [8]
    private static void Step_4837(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xF6: SET 0x06, (IX + d) [2]
    private static void Step_4840(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xF6: SET 0x06, (IX + d) [6]
    private static void Step_4844(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x06);
    }

    // DDCB 0xF6: SET 0x06, (IX + d) [8]
    private static void Step_4846(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xF7: SET 0x06, (IX + d), A [2]
    private static void Step_4849(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xF7: SET 0x06, (IX + d), A [6]
    private static void Step_4853(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x06);
        emulator.A = emulator.data;
    }

    // DDCB 0xF7: SET 0x06, (IX + d), A [8]
    private static void Step_4855(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xF8: SET 0x07, (IX + d), B [2]
    private static void Step_4858(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xF8: SET 0x07, (IX + d), B [6]
    private static void Step_4862(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x07);
        emulator.B = emulator.data;
    }

    // DDCB 0xF8: SET 0x07, (IX + d), B [8]
    private static void Step_4864(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xF9: SET 0x07, (IX + d), C [2]
    private static void Step_4867(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xF9: SET 0x07, (IX + d), C [6]
    private static void Step_4871(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x07);
        emulator.C = emulator.data;
    }

    // DDCB 0xF9: SET 0x07, (IX + d), C [8]
    private static void Step_4873(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xFA: SET 0x07, (IX + d), D [2]
    private static void Step_4876(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xFA: SET 0x07, (IX + d), D [6]
    private static void Step_4880(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x07);
        emulator.D = emulator.data;
    }

    // DDCB 0xFA: SET 0x07, (IX + d), D [8]
    private static void Step_4882(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xFB: SET 0x07, (IX + d), E [2]
    private static void Step_4885(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xFB: SET 0x07, (IX + d), E [6]
    private static void Step_4889(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x07);
        emulator.E = emulator.data;
    }

    // DDCB 0xFB: SET 0x07, (IX + d), E [8]
    private static void Step_4891(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xFC: SET 0x07, (IX + d), H [2]
    private static void Step_4894(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xFC: SET 0x07, (IX + d), H [6]
    private static void Step_4898(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x07);
        emulator.H = emulator.data;
    }

    // DDCB 0xFC: SET 0x07, (IX + d), H [8]
    private static void Step_4900(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xFD: SET 0x07, (IX + d), L [2]
    private static void Step_4903(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xFD: SET 0x07, (IX + d), L [6]
    private static void Step_4907(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x07);
        emulator.L = emulator.data;
    }

    // DDCB 0xFD: SET 0x07, (IX + d), L [8]
    private static void Step_4909(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xFE: SET 0x07, (IX + d) [2]
    private static void Step_4912(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xFE: SET 0x07, (IX + d) [6]
    private static void Step_4916(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x07);
    }

    // DDCB 0xFE: SET 0x07, (IX + d) [8]
    private static void Step_4918(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // DDCB 0xFF: SET 0x07, (IX + d), A [2]
    private static void Step_4921(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IX + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // DDCB 0xFF: SET 0x07, (IX + d), A [6]
    private static void Step_4925(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x07);
        emulator.A = emulator.data;
    }

    // DDCB 0xFF: SET 0x07, (IX + d), A [8]
    private static void Step_4927(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x00: RLC (IY + d), B [2]
    private static void Step_4930(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x00: RLC (IY + d), B [5]
    private static void Step_4933(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | emulator.data >> 0x07);
        emulator.B = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x00: RLC (IY + d), B [8]
    private static void Step_4936(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x01: RLC (IY + d), C [2]
    private static void Step_4939(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x01: RLC (IY + d), C [5]
    private static void Step_4942(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | emulator.data >> 0x07);
        emulator.C = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x01: RLC (IY + d), C [8]
    private static void Step_4945(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x02: RLC (IY + d), D [2]
    private static void Step_4948(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x02: RLC (IY + d), D [5]
    private static void Step_4951(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | emulator.data >> 0x07);
        emulator.D = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x02: RLC (IY + d), D [8]
    private static void Step_4954(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x03: RLC (IY + d), E [2]
    private static void Step_4957(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x03: RLC (IY + d), E [5]
    private static void Step_4960(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | emulator.data >> 0x07);
        emulator.E = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x03: RLC (IY + d), E [8]
    private static void Step_4963(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x04: RLC (IY + d), H [2]
    private static void Step_4966(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x04: RLC (IY + d), H [5]
    private static void Step_4969(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | emulator.data >> 0x07);
        emulator.H = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x04: RLC (IY + d), H [8]
    private static void Step_4972(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x05: RLC (IY + d), L [2]
    private static void Step_4975(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x05: RLC (IY + d), L [5]
    private static void Step_4978(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | emulator.data >> 0x07);
        emulator.L = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x05: RLC (IY + d), L [8]
    private static void Step_4981(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x06: RLC (IY + d) [2]
    private static void Step_4984(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x06: RLC (IY + d) [5]
    private static void Step_4987(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | emulator.data >> 0x07);

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x06: RLC (IY + d) [8]
    private static void Step_4990(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x07: RLC (IY + d), A [2]
    private static void Step_4993(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x07: RLC (IY + d), A [5]
    private static void Step_4996(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | emulator.data >> 0x07);
        emulator.A = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x07: RLC (IY + d), A [8]
    private static void Step_4999(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x08: RRC (IY + d), B [2]
    private static void Step_5002(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x08: RRC (IY + d), B [5]
    private static void Step_5005(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data << 0x07);
        emulator.B = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x08: RRC (IY + d), B [8]
    private static void Step_5008(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x09: RRC (IY + d), C [2]
    private static void Step_5011(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x09: RRC (IY + d), C [5]
    private static void Step_5014(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data << 0x07);
        emulator.C = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x09: RRC (IY + d), C [8]
    private static void Step_5017(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x0A: RRC (IY + d), D [2]
    private static void Step_5020(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x0A: RRC (IY + d), D [5]
    private static void Step_5023(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data << 0x07);
        emulator.D = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x0A: RRC (IY + d), D [8]
    private static void Step_5026(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x0B: RRC (IY + d), E [2]
    private static void Step_5029(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x0B: RRC (IY + d), E [5]
    private static void Step_5032(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data << 0x07);
        emulator.E = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x0B: RRC (IY + d), E [8]
    private static void Step_5035(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x0C: RRC (IY + d), H [2]
    private static void Step_5038(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x0C: RRC (IY + d), H [5]
    private static void Step_5041(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data << 0x07);
        emulator.H = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x0C: RRC (IY + d), H [8]
    private static void Step_5044(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x0D: RRC (IY + d), L [2]
    private static void Step_5047(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x0D: RRC (IY + d), L [5]
    private static void Step_5050(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data << 0x07);
        emulator.L = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x0D: RRC (IY + d), L [8]
    private static void Step_5053(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x0E: RRC (IY + d) [2]
    private static void Step_5056(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x0E: RRC (IY + d) [5]
    private static void Step_5059(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x0E: RRC (IY + d) [8]
    private static void Step_5062(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x0F: RRC (IY + d), A [2]
    private static void Step_5065(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x0F: RRC (IY + d), A [5]
    private static void Step_5068(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data << 0x07);
        emulator.A = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x0F: RRC (IY + d), A [8]
    private static void Step_5071(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x10: RL (IY + d), B [2]
    private static void Step_5074(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x10: RL (IY + d), B [5]
    private static void Step_5077(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | (emulator.F & 0b00000001 /* flag.C */));
        emulator.B = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x10: RL (IY + d), B [8]
    private static void Step_5080(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x11: RL (IY + d), C [2]
    private static void Step_5083(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x11: RL (IY + d), C [5]
    private static void Step_5086(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | (emulator.F & 0b00000001 /* flag.C */));
        emulator.C = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x11: RL (IY + d), C [8]
    private static void Step_5089(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x12: RL (IY + d), D [2]
    private static void Step_5092(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x12: RL (IY + d), D [5]
    private static void Step_5095(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | (emulator.F & 0b00000001 /* flag.C */));
        emulator.D = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x12: RL (IY + d), D [8]
    private static void Step_5098(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x13: RL (IY + d), E [2]
    private static void Step_5101(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x13: RL (IY + d), E [5]
    private static void Step_5104(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | (emulator.F & 0b00000001 /* flag.C */));
        emulator.E = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x13: RL (IY + d), E [8]
    private static void Step_5107(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x14: RL (IY + d), H [2]
    private static void Step_5110(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x14: RL (IY + d), H [5]
    private static void Step_5113(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | (emulator.F & 0b00000001 /* flag.C */));
        emulator.H = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x14: RL (IY + d), H [8]
    private static void Step_5116(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x15: RL (IY + d), L [2]
    private static void Step_5119(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x15: RL (IY + d), L [5]
    private static void Step_5122(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | (emulator.F & 0b00000001 /* flag.C */));
        emulator.L = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x15: RL (IY + d), L [8]
    private static void Step_5125(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x16: RL (IY + d) [2]
    private static void Step_5128(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x16: RL (IY + d) [5]
    private static void Step_5131(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | (emulator.F & 0b00000001 /* flag.C */));

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x16: RL (IY + d) [8]
    private static void Step_5134(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x17: RL (IY + d), A [2]
    private static void Step_5137(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x17: RL (IY + d), A [5]
    private static void Step_5140(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | (emulator.F & 0b00000001 /* flag.C */));
        emulator.A = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x17: RL (IY + d), A [8]
    private static void Step_5143(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x18: RR (IY + d), B [2]
    private static void Step_5146(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x18: RR (IY + d), B [5]
    private static void Step_5149(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);
        emulator.B = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x18: RR (IY + d), B [8]
    private static void Step_5152(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x19: RR (IY + d), C [2]
    private static void Step_5155(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x19: RR (IY + d), C [5]
    private static void Step_5158(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);
        emulator.C = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x19: RR (IY + d), C [8]
    private static void Step_5161(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x1A: RR (IY + d), D [2]
    private static void Step_5164(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x1A: RR (IY + d), D [5]
    private static void Step_5167(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);
        emulator.D = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x1A: RR (IY + d), D [8]
    private static void Step_5170(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x1B: RR (IY + d), E [2]
    private static void Step_5173(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x1B: RR (IY + d), E [5]
    private static void Step_5176(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);
        emulator.E = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x1B: RR (IY + d), E [8]
    private static void Step_5179(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x1C: RR (IY + d), H [2]
    private static void Step_5182(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x1C: RR (IY + d), H [5]
    private static void Step_5185(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);
        emulator.H = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x1C: RR (IY + d), H [8]
    private static void Step_5188(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x1D: RR (IY + d), L [2]
    private static void Step_5191(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x1D: RR (IY + d), L [5]
    private static void Step_5194(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);
        emulator.L = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x1D: RR (IY + d), L [8]
    private static void Step_5197(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x1E: RR (IY + d) [2]
    private static void Step_5200(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x1E: RR (IY + d) [5]
    private static void Step_5203(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x1E: RR (IY + d) [8]
    private static void Step_5206(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x1F: RR (IY + d), A [2]
    private static void Step_5209(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x1F: RR (IY + d), A [5]
    private static void Step_5212(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | (emulator.F & 0b00000001 /* flag.C */) << 0x07);
        emulator.A = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x1F: RR (IY + d), A [8]
    private static void Step_5215(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x20: SLA (IY + d), B [2]
    private static void Step_5218(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x20: SLA (IY + d), B [5]
    private static void Step_5221(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data <<= 0x01;
        emulator.B = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x20: SLA (IY + d), B [8]
    private static void Step_5224(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x21: SLA (IY + d), C [2]
    private static void Step_5227(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x21: SLA (IY + d), C [5]
    private static void Step_5230(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data <<= 0x01;
        emulator.C = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x21: SLA (IY + d), C [8]
    private static void Step_5233(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x22: SLA (IY + d), D [2]
    private static void Step_5236(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x22: SLA (IY + d), D [5]
    private static void Step_5239(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data <<= 0x01;
        emulator.D = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x22: SLA (IY + d), D [8]
    private static void Step_5242(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x23: SLA (IY + d), E [2]
    private static void Step_5245(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x23: SLA (IY + d), E [5]
    private static void Step_5248(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data <<= 0x01;
        emulator.E = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x23: SLA (IY + d), E [8]
    private static void Step_5251(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x24: SLA (IY + d), H [2]
    private static void Step_5254(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x24: SLA (IY + d), H [5]
    private static void Step_5257(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data <<= 0x01;
        emulator.H = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x24: SLA (IY + d), H [8]
    private static void Step_5260(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x25: SLA (IY + d), L [2]
    private static void Step_5263(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x25: SLA (IY + d), L [5]
    private static void Step_5266(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data <<= 0x01;
        emulator.L = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x25: SLA (IY + d), L [8]
    private static void Step_5269(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x26: SLA (IY + d) [2]
    private static void Step_5272(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x26: SLA (IY + d) [5]
    private static void Step_5275(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data <<= 0x01;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x26: SLA (IY + d) [8]
    private static void Step_5278(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x27: SLA (IY + d), A [2]
    private static void Step_5281(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x27: SLA (IY + d), A [5]
    private static void Step_5284(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data <<= 0x01;
        emulator.A = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x27: SLA (IY + d), A [8]
    private static void Step_5287(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x28: SRA (IY + d), B [2]
    private static void Step_5290(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x28: SRA (IY + d), B [5]
    private static void Step_5293(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data & 0x80);
        emulator.B = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x28: SRA (IY + d), B [8]
    private static void Step_5296(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x29: SRA (IY + d), C [2]
    private static void Step_5299(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x29: SRA (IY + d), C [5]
    private static void Step_5302(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data & 0x80);
        emulator.C = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x29: SRA (IY + d), C [8]
    private static void Step_5305(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x2A: SRA (IY + d), D [2]
    private static void Step_5308(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x2A: SRA (IY + d), D [5]
    private static void Step_5311(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data & 0x80);
        emulator.D = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x2A: SRA (IY + d), D [8]
    private static void Step_5314(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x2B: SRA (IY + d), E [2]
    private static void Step_5317(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x2B: SRA (IY + d), E [5]
    private static void Step_5320(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data & 0x80);
        emulator.E = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x2B: SRA (IY + d), E [8]
    private static void Step_5323(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x2C: SRA (IY + d), H [2]
    private static void Step_5326(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x2C: SRA (IY + d), H [5]
    private static void Step_5329(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data & 0x80);
        emulator.H = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x2C: SRA (IY + d), H [8]
    private static void Step_5332(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x2D: SRA (IY + d), L [2]
    private static void Step_5335(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x2D: SRA (IY + d), L [5]
    private static void Step_5338(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data & 0x80);
        emulator.L = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x2D: SRA (IY + d), L [8]
    private static void Step_5341(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x2E: SRA (IY + d) [2]
    private static void Step_5344(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x2E: SRA (IY + d) [5]
    private static void Step_5347(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data & 0x80);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x2E: SRA (IY + d) [8]
    private static void Step_5350(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x2F: SRA (IY + d), A [2]
    private static void Step_5353(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x2F: SRA (IY + d), A [5]
    private static void Step_5356(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 | emulator.data & 0x80);
        emulator.A = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x2F: SRA (IY + d), A [8]
    private static void Step_5359(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x30: SLL (IY + d), B [2]
    private static void Step_5362(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x30: SLL (IY + d), B [5]
    private static void Step_5365(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | 0x01);
        emulator.B = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x30: SLL (IY + d), B [8]
    private static void Step_5368(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x31: SLL (IY + d), C [2]
    private static void Step_5371(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x31: SLL (IY + d), C [5]
    private static void Step_5374(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | 0x01);
        emulator.C = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x31: SLL (IY + d), C [8]
    private static void Step_5377(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x32: SLL (IY + d), D [2]
    private static void Step_5380(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x32: SLL (IY + d), D [5]
    private static void Step_5383(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | 0x01);
        emulator.D = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x32: SLL (IY + d), D [8]
    private static void Step_5386(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x33: SLL (IY + d), E [2]
    private static void Step_5389(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x33: SLL (IY + d), E [5]
    private static void Step_5392(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | 0x01);
        emulator.E = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x33: SLL (IY + d), E [8]
    private static void Step_5395(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x34: SLL (IY + d), H [2]
    private static void Step_5398(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x34: SLL (IY + d), H [5]
    private static void Step_5401(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | 0x01);
        emulator.H = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x34: SLL (IY + d), H [8]
    private static void Step_5404(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x35: SLL (IY + d), L [2]
    private static void Step_5407(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x35: SLL (IY + d), L [5]
    private static void Step_5410(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | 0x01);
        emulator.L = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x35: SLL (IY + d), L [8]
    private static void Step_5413(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x36: SLL (IY + d) [2]
    private static void Step_5416(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x36: SLL (IY + d) [5]
    private static void Step_5419(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | 0x01);

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x36: SLL (IY + d) [8]
    private static void Step_5422(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x37: SLL (IY + d), A [2]
    private static void Step_5425(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x37: SLL (IY + d), A [5]
    private static void Step_5428(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data << 0x01 | 0x01);
        emulator.A = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= Unsafe.BitCast<bool, byte>((temp & 0x80) == 0x80); // Set C if (temp & 0x80) == 0x80 is true.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x37: SLL (IY + d), A [8]
    private static void Step_5431(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x38: SRL (IY + d), B [2]
    private static void Step_5434(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x38: SRL (IY + d), B [5]
    private static void Step_5437(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 & 0x7F);
        emulator.B = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x38: SRL (IY + d), B [8]
    private static void Step_5440(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x39: SRL (IY + d), C [2]
    private static void Step_5443(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x39: SRL (IY + d), C [5]
    private static void Step_5446(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 & 0x7F);
        emulator.C = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x39: SRL (IY + d), C [8]
    private static void Step_5449(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x3A: SRL (IY + d), D [2]
    private static void Step_5452(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x3A: SRL (IY + d), D [5]
    private static void Step_5455(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 & 0x7F);
        emulator.D = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x3A: SRL (IY + d), D [8]
    private static void Step_5458(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x3B: SRL (IY + d), E [2]
    private static void Step_5461(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x3B: SRL (IY + d), E [5]
    private static void Step_5464(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 & 0x7F);
        emulator.E = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x3B: SRL (IY + d), E [8]
    private static void Step_5467(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x3C: SRL (IY + d), H [2]
    private static void Step_5470(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x3C: SRL (IY + d), H [5]
    private static void Step_5473(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 & 0x7F);
        emulator.H = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x3C: SRL (IY + d), H [8]
    private static void Step_5476(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x3D: SRL (IY + d), L [2]
    private static void Step_5479(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x3D: SRL (IY + d), L [5]
    private static void Step_5482(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 & 0x7F);
        emulator.L = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x3D: SRL (IY + d), L [8]
    private static void Step_5485(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x3E: SRL (IY + d) [2]
    private static void Step_5488(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x3E: SRL (IY + d) [5]
    private static void Step_5491(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 & 0x7F);

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x3E: SRL (IY + d) [8]
    private static void Step_5494(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x3F: SRL (IY + d), A [2]
    private static void Step_5497(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x3F: SRL (IY + d), A [5]
    private static void Step_5500(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var temp = emulator.data;
        emulator.data = (byte)(emulator.data >> 0x01 & 0x7F);
        emulator.A = emulator.data;

        // Update flags.
        // Reset N and H.
        int flags = temp & 0b00000001; // Copy C from temp.
        flags |= emulator.data & 0b10101000; // Copy X, Y and S from data.
        flags |= ((~System.Numerics.BitOperations.PopCount(emulator.data) & 0x01)) << 2; // Set PV if even_parity(data) is true.
        flags |= (Unsafe.BitCast<bool, byte>(emulator.data == 0)) << 6; // Set Z if is_zero(data) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x3F: SRL (IY + d), A [8]
    private static void Step_5503(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x40: BIT 0x00, (IY + d) [2]
    private static void Step_5506(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x40: BIT 0x00, (IY + d) [4]
    private static void Step_5508(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x40: BIT 0x00, (IY + d) [5]
    private static void Step_5509(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x48: BIT 0x01, (IY + d) [2]
    private static void Step_5512(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x48: BIT 0x01, (IY + d) [4]
    private static void Step_5514(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01 << 0x01;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x48: BIT 0x01, (IY + d) [5]
    private static void Step_5515(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x50: BIT 0x02, (IY + d) [2]
    private static void Step_5518(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x50: BIT 0x02, (IY + d) [4]
    private static void Step_5520(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01 << 0x02;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x50: BIT 0x02, (IY + d) [5]
    private static void Step_5521(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x58: BIT 0x03, (IY + d) [2]
    private static void Step_5524(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x58: BIT 0x03, (IY + d) [4]
    private static void Step_5526(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01 << 0x03;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x58: BIT 0x03, (IY + d) [5]
    private static void Step_5527(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x60: BIT 0x04, (IY + d) [2]
    private static void Step_5530(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x60: BIT 0x04, (IY + d) [4]
    private static void Step_5532(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01 << 0x04;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x60: BIT 0x04, (IY + d) [5]
    private static void Step_5533(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x68: BIT 0x05, (IY + d) [2]
    private static void Step_5536(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x68: BIT 0x05, (IY + d) [4]
    private static void Step_5538(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01 << 0x05;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x68: BIT 0x05, (IY + d) [5]
    private static void Step_5539(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x70: BIT 0x06, (IY + d) [2]
    private static void Step_5542(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x70: BIT 0x06, (IY + d) [4]
    private static void Step_5544(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01 << 0x06;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x70: BIT 0x06, (IY + d) [5]
    private static void Step_5545(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x78: BIT 0x07, (IY + d) [2]
    private static void Step_5548(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x78: BIT 0x07, (IY + d) [4]
    private static void Step_5550(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        var comparison = emulator.data & 0x01 << 0x07;

        // Update flags.
        int flags = 0b00010000; // Set H. Reset N.
        flags |= emulator.W & 0b00101000; // Copy X and Y from W.
        flags |= comparison & 0b10000000; // Copy S from comparison.
        flags |= emulator.F & 0b00000001; // Copy C from F.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 2; // Set PV if is_zero(comparison) is true.
        flags |= (Unsafe.BitCast<bool, byte>(comparison == 0)) << 6; // Set Z if is_zero(comparison) is true.
        emulator.F = (byte)flags;
    }

    // FDCB 0x78: BIT 0x07, (IY + d) [5]
    private static void Step_5551(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = emulator.F;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x80: RES 0x00, (IY + d), B [2]
    private static void Step_5554(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x80: RES 0x00, (IY + d), B [6]
    private static void Step_5558(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~0x01);
        emulator.B = emulator.data;
    }

    // FDCB 0x80: RES 0x00, (IY + d), B [8]
    private static void Step_5560(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x81: RES 0x00, (IY + d), C [2]
    private static void Step_5563(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x81: RES 0x00, (IY + d), C [6]
    private static void Step_5567(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~0x01);
        emulator.C = emulator.data;
    }

    // FDCB 0x81: RES 0x00, (IY + d), C [8]
    private static void Step_5569(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x82: RES 0x00, (IY + d), D [2]
    private static void Step_5572(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x82: RES 0x00, (IY + d), D [6]
    private static void Step_5576(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~0x01);
        emulator.D = emulator.data;
    }

    // FDCB 0x82: RES 0x00, (IY + d), D [8]
    private static void Step_5578(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x83: RES 0x00, (IY + d), E [2]
    private static void Step_5581(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x83: RES 0x00, (IY + d), E [6]
    private static void Step_5585(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~0x01);
        emulator.E = emulator.data;
    }

    // FDCB 0x83: RES 0x00, (IY + d), E [8]
    private static void Step_5587(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x84: RES 0x00, (IY + d), H [2]
    private static void Step_5590(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x84: RES 0x00, (IY + d), H [6]
    private static void Step_5594(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~0x01);
        emulator.H = emulator.data;
    }

    // FDCB 0x84: RES 0x00, (IY + d), H [8]
    private static void Step_5596(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x85: RES 0x00, (IY + d), L [2]
    private static void Step_5599(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x85: RES 0x00, (IY + d), L [6]
    private static void Step_5603(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~0x01);
        emulator.L = emulator.data;
    }

    // FDCB 0x85: RES 0x00, (IY + d), L [8]
    private static void Step_5605(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x86: RES 0x00, (IY + d) [2]
    private static void Step_5608(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x86: RES 0x00, (IY + d) [6]
    private static void Step_5612(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~0x01);
    }

    // FDCB 0x86: RES 0x00, (IY + d) [8]
    private static void Step_5614(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x87: RES 0x00, (IY + d), A [2]
    private static void Step_5617(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x87: RES 0x00, (IY + d), A [6]
    private static void Step_5621(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~0x01);
        emulator.A = emulator.data;
    }

    // FDCB 0x87: RES 0x00, (IY + d), A [8]
    private static void Step_5623(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x88: RES 0x01, (IY + d), B [2]
    private static void Step_5626(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x88: RES 0x01, (IY + d), B [6]
    private static void Step_5630(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x01));
        emulator.B = emulator.data;
    }

    // FDCB 0x88: RES 0x01, (IY + d), B [8]
    private static void Step_5632(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x89: RES 0x01, (IY + d), C [2]
    private static void Step_5635(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x89: RES 0x01, (IY + d), C [6]
    private static void Step_5639(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x01));
        emulator.C = emulator.data;
    }

    // FDCB 0x89: RES 0x01, (IY + d), C [8]
    private static void Step_5641(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x8A: RES 0x01, (IY + d), D [2]
    private static void Step_5644(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x8A: RES 0x01, (IY + d), D [6]
    private static void Step_5648(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x01));
        emulator.D = emulator.data;
    }

    // FDCB 0x8A: RES 0x01, (IY + d), D [8]
    private static void Step_5650(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x8B: RES 0x01, (IY + d), E [2]
    private static void Step_5653(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x8B: RES 0x01, (IY + d), E [6]
    private static void Step_5657(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x01));
        emulator.E = emulator.data;
    }

    // FDCB 0x8B: RES 0x01, (IY + d), E [8]
    private static void Step_5659(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x8C: RES 0x01, (IY + d), H [2]
    private static void Step_5662(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x8C: RES 0x01, (IY + d), H [6]
    private static void Step_5666(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x01));
        emulator.H = emulator.data;
    }

    // FDCB 0x8C: RES 0x01, (IY + d), H [8]
    private static void Step_5668(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x8D: RES 0x01, (IY + d), L [2]
    private static void Step_5671(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x8D: RES 0x01, (IY + d), L [6]
    private static void Step_5675(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x01));
        emulator.L = emulator.data;
    }

    // FDCB 0x8D: RES 0x01, (IY + d), L [8]
    private static void Step_5677(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x8E: RES 0x01, (IY + d) [2]
    private static void Step_5680(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x8E: RES 0x01, (IY + d) [6]
    private static void Step_5684(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x01));
    }

    // FDCB 0x8E: RES 0x01, (IY + d) [8]
    private static void Step_5686(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x8F: RES 0x01, (IY + d), A [2]
    private static void Step_5689(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x8F: RES 0x01, (IY + d), A [6]
    private static void Step_5693(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x01));
        emulator.A = emulator.data;
    }

    // FDCB 0x8F: RES 0x01, (IY + d), A [8]
    private static void Step_5695(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x90: RES 0x02, (IY + d), B [2]
    private static void Step_5698(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x90: RES 0x02, (IY + d), B [6]
    private static void Step_5702(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x02));
        emulator.B = emulator.data;
    }

    // FDCB 0x90: RES 0x02, (IY + d), B [8]
    private static void Step_5704(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x91: RES 0x02, (IY + d), C [2]
    private static void Step_5707(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x91: RES 0x02, (IY + d), C [6]
    private static void Step_5711(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x02));
        emulator.C = emulator.data;
    }

    // FDCB 0x91: RES 0x02, (IY + d), C [8]
    private static void Step_5713(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x92: RES 0x02, (IY + d), D [2]
    private static void Step_5716(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x92: RES 0x02, (IY + d), D [6]
    private static void Step_5720(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x02));
        emulator.D = emulator.data;
    }

    // FDCB 0x92: RES 0x02, (IY + d), D [8]
    private static void Step_5722(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x93: RES 0x02, (IY + d), E [2]
    private static void Step_5725(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x93: RES 0x02, (IY + d), E [6]
    private static void Step_5729(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x02));
        emulator.E = emulator.data;
    }

    // FDCB 0x93: RES 0x02, (IY + d), E [8]
    private static void Step_5731(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x94: RES 0x02, (IY + d), H [2]
    private static void Step_5734(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x94: RES 0x02, (IY + d), H [6]
    private static void Step_5738(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x02));
        emulator.H = emulator.data;
    }

    // FDCB 0x94: RES 0x02, (IY + d), H [8]
    private static void Step_5740(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x95: RES 0x02, (IY + d), L [2]
    private static void Step_5743(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x95: RES 0x02, (IY + d), L [6]
    private static void Step_5747(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x02));
        emulator.L = emulator.data;
    }

    // FDCB 0x95: RES 0x02, (IY + d), L [8]
    private static void Step_5749(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x96: RES 0x02, (IY + d) [2]
    private static void Step_5752(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x96: RES 0x02, (IY + d) [6]
    private static void Step_5756(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x02));
    }

    // FDCB 0x96: RES 0x02, (IY + d) [8]
    private static void Step_5758(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x97: RES 0x02, (IY + d), A [2]
    private static void Step_5761(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x97: RES 0x02, (IY + d), A [6]
    private static void Step_5765(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x02));
        emulator.A = emulator.data;
    }

    // FDCB 0x97: RES 0x02, (IY + d), A [8]
    private static void Step_5767(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x98: RES 0x03, (IY + d), B [2]
    private static void Step_5770(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x98: RES 0x03, (IY + d), B [6]
    private static void Step_5774(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x03));
        emulator.B = emulator.data;
    }

    // FDCB 0x98: RES 0x03, (IY + d), B [8]
    private static void Step_5776(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x99: RES 0x03, (IY + d), C [2]
    private static void Step_5779(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x99: RES 0x03, (IY + d), C [6]
    private static void Step_5783(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x03));
        emulator.C = emulator.data;
    }

    // FDCB 0x99: RES 0x03, (IY + d), C [8]
    private static void Step_5785(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x9A: RES 0x03, (IY + d), D [2]
    private static void Step_5788(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x9A: RES 0x03, (IY + d), D [6]
    private static void Step_5792(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x03));
        emulator.D = emulator.data;
    }

    // FDCB 0x9A: RES 0x03, (IY + d), D [8]
    private static void Step_5794(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x9B: RES 0x03, (IY + d), E [2]
    private static void Step_5797(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x9B: RES 0x03, (IY + d), E [6]
    private static void Step_5801(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x03));
        emulator.E = emulator.data;
    }

    // FDCB 0x9B: RES 0x03, (IY + d), E [8]
    private static void Step_5803(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x9C: RES 0x03, (IY + d), H [2]
    private static void Step_5806(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x9C: RES 0x03, (IY + d), H [6]
    private static void Step_5810(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x03));
        emulator.H = emulator.data;
    }

    // FDCB 0x9C: RES 0x03, (IY + d), H [8]
    private static void Step_5812(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x9D: RES 0x03, (IY + d), L [2]
    private static void Step_5815(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x9D: RES 0x03, (IY + d), L [6]
    private static void Step_5819(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x03));
        emulator.L = emulator.data;
    }

    // FDCB 0x9D: RES 0x03, (IY + d), L [8]
    private static void Step_5821(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x9E: RES 0x03, (IY + d) [2]
    private static void Step_5824(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x9E: RES 0x03, (IY + d) [6]
    private static void Step_5828(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x03));
    }

    // FDCB 0x9E: RES 0x03, (IY + d) [8]
    private static void Step_5830(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0x9F: RES 0x03, (IY + d), A [2]
    private static void Step_5833(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0x9F: RES 0x03, (IY + d), A [6]
    private static void Step_5837(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x03));
        emulator.A = emulator.data;
    }

    // FDCB 0x9F: RES 0x03, (IY + d), A [8]
    private static void Step_5839(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xA0: RES 0x04, (IY + d), B [2]
    private static void Step_5842(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xA0: RES 0x04, (IY + d), B [6]
    private static void Step_5846(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x04));
        emulator.B = emulator.data;
    }

    // FDCB 0xA0: RES 0x04, (IY + d), B [8]
    private static void Step_5848(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xA1: RES 0x04, (IY + d), C [2]
    private static void Step_5851(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xA1: RES 0x04, (IY + d), C [6]
    private static void Step_5855(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x04));
        emulator.C = emulator.data;
    }

    // FDCB 0xA1: RES 0x04, (IY + d), C [8]
    private static void Step_5857(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xA2: RES 0x04, (IY + d), D [2]
    private static void Step_5860(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xA2: RES 0x04, (IY + d), D [6]
    private static void Step_5864(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x04));
        emulator.D = emulator.data;
    }

    // FDCB 0xA2: RES 0x04, (IY + d), D [8]
    private static void Step_5866(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xA3: RES 0x04, (IY + d), E [2]
    private static void Step_5869(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xA3: RES 0x04, (IY + d), E [6]
    private static void Step_5873(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x04));
        emulator.E = emulator.data;
    }

    // FDCB 0xA3: RES 0x04, (IY + d), E [8]
    private static void Step_5875(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xA4: RES 0x04, (IY + d), H [2]
    private static void Step_5878(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xA4: RES 0x04, (IY + d), H [6]
    private static void Step_5882(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x04));
        emulator.H = emulator.data;
    }

    // FDCB 0xA4: RES 0x04, (IY + d), H [8]
    private static void Step_5884(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xA5: RES 0x04, (IY + d), L [2]
    private static void Step_5887(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xA5: RES 0x04, (IY + d), L [6]
    private static void Step_5891(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x04));
        emulator.L = emulator.data;
    }

    // FDCB 0xA5: RES 0x04, (IY + d), L [8]
    private static void Step_5893(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xA6: RES 0x04, (IY + d) [2]
    private static void Step_5896(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xA6: RES 0x04, (IY + d) [6]
    private static void Step_5900(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x04));
    }

    // FDCB 0xA6: RES 0x04, (IY + d) [8]
    private static void Step_5902(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xA7: RES 0x04, (IY + d), A [2]
    private static void Step_5905(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xA7: RES 0x04, (IY + d), A [6]
    private static void Step_5909(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x04));
        emulator.A = emulator.data;
    }

    // FDCB 0xA7: RES 0x04, (IY + d), A [8]
    private static void Step_5911(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xA8: RES 0x05, (IY + d), B [2]
    private static void Step_5914(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xA8: RES 0x05, (IY + d), B [6]
    private static void Step_5918(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x05));
        emulator.B = emulator.data;
    }

    // FDCB 0xA8: RES 0x05, (IY + d), B [8]
    private static void Step_5920(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xA9: RES 0x05, (IY + d), C [2]
    private static void Step_5923(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xA9: RES 0x05, (IY + d), C [6]
    private static void Step_5927(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x05));
        emulator.C = emulator.data;
    }

    // FDCB 0xA9: RES 0x05, (IY + d), C [8]
    private static void Step_5929(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xAA: RES 0x05, (IY + d), D [2]
    private static void Step_5932(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xAA: RES 0x05, (IY + d), D [6]
    private static void Step_5936(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x05));
        emulator.D = emulator.data;
    }

    // FDCB 0xAA: RES 0x05, (IY + d), D [8]
    private static void Step_5938(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xAB: RES 0x05, (IY + d), E [2]
    private static void Step_5941(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xAB: RES 0x05, (IY + d), E [6]
    private static void Step_5945(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x05));
        emulator.E = emulator.data;
    }

    // FDCB 0xAB: RES 0x05, (IY + d), E [8]
    private static void Step_5947(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xAC: RES 0x05, (IY + d), H [2]
    private static void Step_5950(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xAC: RES 0x05, (IY + d), H [6]
    private static void Step_5954(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x05));
        emulator.H = emulator.data;
    }

    // FDCB 0xAC: RES 0x05, (IY + d), H [8]
    private static void Step_5956(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xAD: RES 0x05, (IY + d), L [2]
    private static void Step_5959(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xAD: RES 0x05, (IY + d), L [6]
    private static void Step_5963(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x05));
        emulator.L = emulator.data;
    }

    // FDCB 0xAD: RES 0x05, (IY + d), L [8]
    private static void Step_5965(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xAE: RES 0x05, (IY + d) [2]
    private static void Step_5968(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xAE: RES 0x05, (IY + d) [6]
    private static void Step_5972(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x05));
    }

    // FDCB 0xAE: RES 0x05, (IY + d) [8]
    private static void Step_5974(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xAF: RES 0x05, (IY + d), A [2]
    private static void Step_5977(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xAF: RES 0x05, (IY + d), A [6]
    private static void Step_5981(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x05));
        emulator.A = emulator.data;
    }

    // FDCB 0xAF: RES 0x05, (IY + d), A [8]
    private static void Step_5983(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xB0: RES 0x06, (IY + d), B [2]
    private static void Step_5986(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xB0: RES 0x06, (IY + d), B [6]
    private static void Step_5990(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x06));
        emulator.B = emulator.data;
    }

    // FDCB 0xB0: RES 0x06, (IY + d), B [8]
    private static void Step_5992(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xB1: RES 0x06, (IY + d), C [2]
    private static void Step_5995(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xB1: RES 0x06, (IY + d), C [6]
    private static void Step_5999(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x06));
        emulator.C = emulator.data;
    }

    // FDCB 0xB1: RES 0x06, (IY + d), C [8]
    private static void Step_6001(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xB2: RES 0x06, (IY + d), D [2]
    private static void Step_6004(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xB2: RES 0x06, (IY + d), D [6]
    private static void Step_6008(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x06));
        emulator.D = emulator.data;
    }

    // FDCB 0xB2: RES 0x06, (IY + d), D [8]
    private static void Step_6010(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xB3: RES 0x06, (IY + d), E [2]
    private static void Step_6013(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xB3: RES 0x06, (IY + d), E [6]
    private static void Step_6017(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x06));
        emulator.E = emulator.data;
    }

    // FDCB 0xB3: RES 0x06, (IY + d), E [8]
    private static void Step_6019(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xB4: RES 0x06, (IY + d), H [2]
    private static void Step_6022(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xB4: RES 0x06, (IY + d), H [6]
    private static void Step_6026(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x06));
        emulator.H = emulator.data;
    }

    // FDCB 0xB4: RES 0x06, (IY + d), H [8]
    private static void Step_6028(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xB5: RES 0x06, (IY + d), L [2]
    private static void Step_6031(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xB5: RES 0x06, (IY + d), L [6]
    private static void Step_6035(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x06));
        emulator.L = emulator.data;
    }

    // FDCB 0xB5: RES 0x06, (IY + d), L [8]
    private static void Step_6037(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xB6: RES 0x06, (IY + d) [2]
    private static void Step_6040(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xB6: RES 0x06, (IY + d) [6]
    private static void Step_6044(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x06));
    }

    // FDCB 0xB6: RES 0x06, (IY + d) [8]
    private static void Step_6046(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xB7: RES 0x06, (IY + d), A [2]
    private static void Step_6049(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xB7: RES 0x06, (IY + d), A [6]
    private static void Step_6053(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x06));
        emulator.A = emulator.data;
    }

    // FDCB 0xB7: RES 0x06, (IY + d), A [8]
    private static void Step_6055(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xB8: RES 0x07, (IY + d), B [2]
    private static void Step_6058(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xB8: RES 0x07, (IY + d), B [6]
    private static void Step_6062(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x07));
        emulator.B = emulator.data;
    }

    // FDCB 0xB8: RES 0x07, (IY + d), B [8]
    private static void Step_6064(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xB9: RES 0x07, (IY + d), C [2]
    private static void Step_6067(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xB9: RES 0x07, (IY + d), C [6]
    private static void Step_6071(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x07));
        emulator.C = emulator.data;
    }

    // FDCB 0xB9: RES 0x07, (IY + d), C [8]
    private static void Step_6073(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xBA: RES 0x07, (IY + d), D [2]
    private static void Step_6076(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xBA: RES 0x07, (IY + d), D [6]
    private static void Step_6080(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x07));
        emulator.D = emulator.data;
    }

    // FDCB 0xBA: RES 0x07, (IY + d), D [8]
    private static void Step_6082(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xBB: RES 0x07, (IY + d), E [2]
    private static void Step_6085(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xBB: RES 0x07, (IY + d), E [6]
    private static void Step_6089(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x07));
        emulator.E = emulator.data;
    }

    // FDCB 0xBB: RES 0x07, (IY + d), E [8]
    private static void Step_6091(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xBC: RES 0x07, (IY + d), H [2]
    private static void Step_6094(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xBC: RES 0x07, (IY + d), H [6]
    private static void Step_6098(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x07));
        emulator.H = emulator.data;
    }

    // FDCB 0xBC: RES 0x07, (IY + d), H [8]
    private static void Step_6100(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xBD: RES 0x07, (IY + d), L [2]
    private static void Step_6103(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xBD: RES 0x07, (IY + d), L [6]
    private static void Step_6107(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x07));
        emulator.L = emulator.data;
    }

    // FDCB 0xBD: RES 0x07, (IY + d), L [8]
    private static void Step_6109(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xBE: RES 0x07, (IY + d) [2]
    private static void Step_6112(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xBE: RES 0x07, (IY + d) [6]
    private static void Step_6116(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x07));
    }

    // FDCB 0xBE: RES 0x07, (IY + d) [8]
    private static void Step_6118(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xBF: RES 0x07, (IY + d), A [2]
    private static void Step_6121(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xBF: RES 0x07, (IY + d), A [6]
    private static void Step_6125(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data & ~(0x01 << 0x07));
        emulator.A = emulator.data;
    }

    // FDCB 0xBF: RES 0x07, (IY + d), A [8]
    private static void Step_6127(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xC0: SET 0x00, (IY + d), B [2]
    private static void Step_6130(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xC0: SET 0x00, (IY + d), B [6]
    private static void Step_6134(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data |= 0x01;
        emulator.B = emulator.data;
    }

    // FDCB 0xC0: SET 0x00, (IY + d), B [8]
    private static void Step_6136(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xC1: SET 0x00, (IY + d), C [2]
    private static void Step_6139(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xC1: SET 0x00, (IY + d), C [6]
    private static void Step_6143(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data |= 0x01;
        emulator.C = emulator.data;
    }

    // FDCB 0xC1: SET 0x00, (IY + d), C [8]
    private static void Step_6145(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xC2: SET 0x00, (IY + d), D [2]
    private static void Step_6148(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xC2: SET 0x00, (IY + d), D [6]
    private static void Step_6152(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data |= 0x01;
        emulator.D = emulator.data;
    }

    // FDCB 0xC2: SET 0x00, (IY + d), D [8]
    private static void Step_6154(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xC3: SET 0x00, (IY + d), E [2]
    private static void Step_6157(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xC3: SET 0x00, (IY + d), E [6]
    private static void Step_6161(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data |= 0x01;
        emulator.E = emulator.data;
    }

    // FDCB 0xC3: SET 0x00, (IY + d), E [8]
    private static void Step_6163(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xC4: SET 0x00, (IY + d), H [2]
    private static void Step_6166(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xC4: SET 0x00, (IY + d), H [6]
    private static void Step_6170(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data |= 0x01;
        emulator.H = emulator.data;
    }

    // FDCB 0xC4: SET 0x00, (IY + d), H [8]
    private static void Step_6172(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xC5: SET 0x00, (IY + d), L [2]
    private static void Step_6175(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xC5: SET 0x00, (IY + d), L [6]
    private static void Step_6179(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data |= 0x01;
        emulator.L = emulator.data;
    }

    // FDCB 0xC5: SET 0x00, (IY + d), L [8]
    private static void Step_6181(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xC6: SET 0x00, (IY + d) [2]
    private static void Step_6184(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xC6: SET 0x00, (IY + d) [6]
    private static void Step_6188(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data |= 0x01;
    }

    // FDCB 0xC6: SET 0x00, (IY + d) [8]
    private static void Step_6190(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xC7: SET 0x00, (IY + d), A [2]
    private static void Step_6193(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xC7: SET 0x00, (IY + d), A [6]
    private static void Step_6197(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data |= 0x01;
        emulator.A = emulator.data;
    }

    // FDCB 0xC7: SET 0x00, (IY + d), A [8]
    private static void Step_6199(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xC8: SET 0x01, (IY + d), B [2]
    private static void Step_6202(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xC8: SET 0x01, (IY + d), B [6]
    private static void Step_6206(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x01);
        emulator.B = emulator.data;
    }

    // FDCB 0xC8: SET 0x01, (IY + d), B [8]
    private static void Step_6208(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xC9: SET 0x01, (IY + d), C [2]
    private static void Step_6211(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xC9: SET 0x01, (IY + d), C [6]
    private static void Step_6215(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x01);
        emulator.C = emulator.data;
    }

    // FDCB 0xC9: SET 0x01, (IY + d), C [8]
    private static void Step_6217(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xCA: SET 0x01, (IY + d), D [2]
    private static void Step_6220(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xCA: SET 0x01, (IY + d), D [6]
    private static void Step_6224(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x01);
        emulator.D = emulator.data;
    }

    // FDCB 0xCA: SET 0x01, (IY + d), D [8]
    private static void Step_6226(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xCB: SET 0x01, (IY + d), E [2]
    private static void Step_6229(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xCB: SET 0x01, (IY + d), E [6]
    private static void Step_6233(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x01);
        emulator.E = emulator.data;
    }

    // FDCB 0xCB: SET 0x01, (IY + d), E [8]
    private static void Step_6235(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xCC: SET 0x01, (IY + d), H [2]
    private static void Step_6238(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xCC: SET 0x01, (IY + d), H [6]
    private static void Step_6242(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x01);
        emulator.H = emulator.data;
    }

    // FDCB 0xCC: SET 0x01, (IY + d), H [8]
    private static void Step_6244(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xCD: SET 0x01, (IY + d), L [2]
    private static void Step_6247(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xCD: SET 0x01, (IY + d), L [6]
    private static void Step_6251(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x01);
        emulator.L = emulator.data;
    }

    // FDCB 0xCD: SET 0x01, (IY + d), L [8]
    private static void Step_6253(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xCE: SET 0x01, (IY + d) [2]
    private static void Step_6256(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xCE: SET 0x01, (IY + d) [6]
    private static void Step_6260(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x01);
    }

    // FDCB 0xCE: SET 0x01, (IY + d) [8]
    private static void Step_6262(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xCF: SET 0x01, (IY + d), A [2]
    private static void Step_6265(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xCF: SET 0x01, (IY + d), A [6]
    private static void Step_6269(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x01);
        emulator.A = emulator.data;
    }

    // FDCB 0xCF: SET 0x01, (IY + d), A [8]
    private static void Step_6271(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xD0: SET 0x02, (IY + d), B [2]
    private static void Step_6274(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xD0: SET 0x02, (IY + d), B [6]
    private static void Step_6278(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x02);
        emulator.B = emulator.data;
    }

    // FDCB 0xD0: SET 0x02, (IY + d), B [8]
    private static void Step_6280(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xD1: SET 0x02, (IY + d), C [2]
    private static void Step_6283(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xD1: SET 0x02, (IY + d), C [6]
    private static void Step_6287(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x02);
        emulator.C = emulator.data;
    }

    // FDCB 0xD1: SET 0x02, (IY + d), C [8]
    private static void Step_6289(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xD2: SET 0x02, (IY + d), D [2]
    private static void Step_6292(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xD2: SET 0x02, (IY + d), D [6]
    private static void Step_6296(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x02);
        emulator.D = emulator.data;
    }

    // FDCB 0xD2: SET 0x02, (IY + d), D [8]
    private static void Step_6298(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xD3: SET 0x02, (IY + d), E [2]
    private static void Step_6301(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xD3: SET 0x02, (IY + d), E [6]
    private static void Step_6305(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x02);
        emulator.E = emulator.data;
    }

    // FDCB 0xD3: SET 0x02, (IY + d), E [8]
    private static void Step_6307(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xD4: SET 0x02, (IY + d), H [2]
    private static void Step_6310(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xD4: SET 0x02, (IY + d), H [6]
    private static void Step_6314(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x02);
        emulator.H = emulator.data;
    }

    // FDCB 0xD4: SET 0x02, (IY + d), H [8]
    private static void Step_6316(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xD5: SET 0x02, (IY + d), L [2]
    private static void Step_6319(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xD5: SET 0x02, (IY + d), L [6]
    private static void Step_6323(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x02);
        emulator.L = emulator.data;
    }

    // FDCB 0xD5: SET 0x02, (IY + d), L [8]
    private static void Step_6325(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xD6: SET 0x02, (IY + d) [2]
    private static void Step_6328(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xD6: SET 0x02, (IY + d) [6]
    private static void Step_6332(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x02);
    }

    // FDCB 0xD6: SET 0x02, (IY + d) [8]
    private static void Step_6334(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xD7: SET 0x02, (IY + d), A [2]
    private static void Step_6337(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xD7: SET 0x02, (IY + d), A [6]
    private static void Step_6341(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x02);
        emulator.A = emulator.data;
    }

    // FDCB 0xD7: SET 0x02, (IY + d), A [8]
    private static void Step_6343(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xD8: SET 0x03, (IY + d), B [2]
    private static void Step_6346(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xD8: SET 0x03, (IY + d), B [6]
    private static void Step_6350(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x03);
        emulator.B = emulator.data;
    }

    // FDCB 0xD8: SET 0x03, (IY + d), B [8]
    private static void Step_6352(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xD9: SET 0x03, (IY + d), C [2]
    private static void Step_6355(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xD9: SET 0x03, (IY + d), C [6]
    private static void Step_6359(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x03);
        emulator.C = emulator.data;
    }

    // FDCB 0xD9: SET 0x03, (IY + d), C [8]
    private static void Step_6361(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xDA: SET 0x03, (IY + d), D [2]
    private static void Step_6364(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xDA: SET 0x03, (IY + d), D [6]
    private static void Step_6368(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x03);
        emulator.D = emulator.data;
    }

    // FDCB 0xDA: SET 0x03, (IY + d), D [8]
    private static void Step_6370(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xDB: SET 0x03, (IY + d), E [2]
    private static void Step_6373(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xDB: SET 0x03, (IY + d), E [6]
    private static void Step_6377(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x03);
        emulator.E = emulator.data;
    }

    // FDCB 0xDB: SET 0x03, (IY + d), E [8]
    private static void Step_6379(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xDC: SET 0x03, (IY + d), H [2]
    private static void Step_6382(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xDC: SET 0x03, (IY + d), H [6]
    private static void Step_6386(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x03);
        emulator.H = emulator.data;
    }

    // FDCB 0xDC: SET 0x03, (IY + d), H [8]
    private static void Step_6388(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xDD: SET 0x03, (IY + d), L [2]
    private static void Step_6391(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xDD: SET 0x03, (IY + d), L [6]
    private static void Step_6395(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x03);
        emulator.L = emulator.data;
    }

    // FDCB 0xDD: SET 0x03, (IY + d), L [8]
    private static void Step_6397(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xDE: SET 0x03, (IY + d) [2]
    private static void Step_6400(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xDE: SET 0x03, (IY + d) [6]
    private static void Step_6404(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x03);
    }

    // FDCB 0xDE: SET 0x03, (IY + d) [8]
    private static void Step_6406(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xDF: SET 0x03, (IY + d), A [2]
    private static void Step_6409(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xDF: SET 0x03, (IY + d), A [6]
    private static void Step_6413(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x03);
        emulator.A = emulator.data;
    }

    // FDCB 0xDF: SET 0x03, (IY + d), A [8]
    private static void Step_6415(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xE0: SET 0x04, (IY + d), B [2]
    private static void Step_6418(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xE0: SET 0x04, (IY + d), B [6]
    private static void Step_6422(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x04);
        emulator.B = emulator.data;
    }

    // FDCB 0xE0: SET 0x04, (IY + d), B [8]
    private static void Step_6424(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xE1: SET 0x04, (IY + d), C [2]
    private static void Step_6427(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xE1: SET 0x04, (IY + d), C [6]
    private static void Step_6431(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x04);
        emulator.C = emulator.data;
    }

    // FDCB 0xE1: SET 0x04, (IY + d), C [8]
    private static void Step_6433(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xE2: SET 0x04, (IY + d), D [2]
    private static void Step_6436(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xE2: SET 0x04, (IY + d), D [6]
    private static void Step_6440(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x04);
        emulator.D = emulator.data;
    }

    // FDCB 0xE2: SET 0x04, (IY + d), D [8]
    private static void Step_6442(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xE3: SET 0x04, (IY + d), E [2]
    private static void Step_6445(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xE3: SET 0x04, (IY + d), E [6]
    private static void Step_6449(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x04);
        emulator.E = emulator.data;
    }

    // FDCB 0xE3: SET 0x04, (IY + d), E [8]
    private static void Step_6451(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xE4: SET 0x04, (IY + d), H [2]
    private static void Step_6454(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xE4: SET 0x04, (IY + d), H [6]
    private static void Step_6458(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x04);
        emulator.H = emulator.data;
    }

    // FDCB 0xE4: SET 0x04, (IY + d), H [8]
    private static void Step_6460(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xE5: SET 0x04, (IY + d), L [2]
    private static void Step_6463(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xE5: SET 0x04, (IY + d), L [6]
    private static void Step_6467(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x04);
        emulator.L = emulator.data;
    }

    // FDCB 0xE5: SET 0x04, (IY + d), L [8]
    private static void Step_6469(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xE6: SET 0x04, (IY + d) [2]
    private static void Step_6472(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xE6: SET 0x04, (IY + d) [6]
    private static void Step_6476(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x04);
    }

    // FDCB 0xE6: SET 0x04, (IY + d) [8]
    private static void Step_6478(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xE7: SET 0x04, (IY + d), A [2]
    private static void Step_6481(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xE7: SET 0x04, (IY + d), A [6]
    private static void Step_6485(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x04);
        emulator.A = emulator.data;
    }

    // FDCB 0xE7: SET 0x04, (IY + d), A [8]
    private static void Step_6487(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xE8: SET 0x05, (IY + d), B [2]
    private static void Step_6490(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xE8: SET 0x05, (IY + d), B [6]
    private static void Step_6494(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x05);
        emulator.B = emulator.data;
    }

    // FDCB 0xE8: SET 0x05, (IY + d), B [8]
    private static void Step_6496(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xE9: SET 0x05, (IY + d), C [2]
    private static void Step_6499(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xE9: SET 0x05, (IY + d), C [6]
    private static void Step_6503(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x05);
        emulator.C = emulator.data;
    }

    // FDCB 0xE9: SET 0x05, (IY + d), C [8]
    private static void Step_6505(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xEA: SET 0x05, (IY + d), D [2]
    private static void Step_6508(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xEA: SET 0x05, (IY + d), D [6]
    private static void Step_6512(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x05);
        emulator.D = emulator.data;
    }

    // FDCB 0xEA: SET 0x05, (IY + d), D [8]
    private static void Step_6514(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xEB: SET 0x05, (IY + d), E [2]
    private static void Step_6517(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xEB: SET 0x05, (IY + d), E [6]
    private static void Step_6521(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x05);
        emulator.E = emulator.data;
    }

    // FDCB 0xEB: SET 0x05, (IY + d), E [8]
    private static void Step_6523(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xEC: SET 0x05, (IY + d), H [2]
    private static void Step_6526(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xEC: SET 0x05, (IY + d), H [6]
    private static void Step_6530(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x05);
        emulator.H = emulator.data;
    }

    // FDCB 0xEC: SET 0x05, (IY + d), H [8]
    private static void Step_6532(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xED: SET 0x05, (IY + d), L [2]
    private static void Step_6535(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xED: SET 0x05, (IY + d), L [6]
    private static void Step_6539(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x05);
        emulator.L = emulator.data;
    }

    // FDCB 0xED: SET 0x05, (IY + d), L [8]
    private static void Step_6541(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xEE: SET 0x05, (IY + d) [2]
    private static void Step_6544(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xEE: SET 0x05, (IY + d) [6]
    private static void Step_6548(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x05);
    }

    // FDCB 0xEE: SET 0x05, (IY + d) [8]
    private static void Step_6550(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xEF: SET 0x05, (IY + d), A [2]
    private static void Step_6553(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xEF: SET 0x05, (IY + d), A [6]
    private static void Step_6557(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x05);
        emulator.A = emulator.data;
    }

    // FDCB 0xEF: SET 0x05, (IY + d), A [8]
    private static void Step_6559(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xF0: SET 0x06, (IY + d), B [2]
    private static void Step_6562(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xF0: SET 0x06, (IY + d), B [6]
    private static void Step_6566(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x06);
        emulator.B = emulator.data;
    }

    // FDCB 0xF0: SET 0x06, (IY + d), B [8]
    private static void Step_6568(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xF1: SET 0x06, (IY + d), C [2]
    private static void Step_6571(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xF1: SET 0x06, (IY + d), C [6]
    private static void Step_6575(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x06);
        emulator.C = emulator.data;
    }

    // FDCB 0xF1: SET 0x06, (IY + d), C [8]
    private static void Step_6577(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xF2: SET 0x06, (IY + d), D [2]
    private static void Step_6580(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xF2: SET 0x06, (IY + d), D [6]
    private static void Step_6584(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x06);
        emulator.D = emulator.data;
    }

    // FDCB 0xF2: SET 0x06, (IY + d), D [8]
    private static void Step_6586(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xF3: SET 0x06, (IY + d), E [2]
    private static void Step_6589(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xF3: SET 0x06, (IY + d), E [6]
    private static void Step_6593(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x06);
        emulator.E = emulator.data;
    }

    // FDCB 0xF3: SET 0x06, (IY + d), E [8]
    private static void Step_6595(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xF4: SET 0x06, (IY + d), H [2]
    private static void Step_6598(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xF4: SET 0x06, (IY + d), H [6]
    private static void Step_6602(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x06);
        emulator.H = emulator.data;
    }

    // FDCB 0xF4: SET 0x06, (IY + d), H [8]
    private static void Step_6604(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xF5: SET 0x06, (IY + d), L [2]
    private static void Step_6607(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xF5: SET 0x06, (IY + d), L [6]
    private static void Step_6611(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x06);
        emulator.L = emulator.data;
    }

    // FDCB 0xF5: SET 0x06, (IY + d), L [8]
    private static void Step_6613(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xF6: SET 0x06, (IY + d) [2]
    private static void Step_6616(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xF6: SET 0x06, (IY + d) [6]
    private static void Step_6620(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x06);
    }

    // FDCB 0xF6: SET 0x06, (IY + d) [8]
    private static void Step_6622(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xF7: SET 0x06, (IY + d), A [2]
    private static void Step_6625(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xF7: SET 0x06, (IY + d), A [6]
    private static void Step_6629(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x06);
        emulator.A = emulator.data;
    }

    // FDCB 0xF7: SET 0x06, (IY + d), A [8]
    private static void Step_6631(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xF8: SET 0x07, (IY + d), B [2]
    private static void Step_6634(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xF8: SET 0x07, (IY + d), B [6]
    private static void Step_6638(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x07);
        emulator.B = emulator.data;
    }

    // FDCB 0xF8: SET 0x07, (IY + d), B [8]
    private static void Step_6640(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xF9: SET 0x07, (IY + d), C [2]
    private static void Step_6643(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xF9: SET 0x07, (IY + d), C [6]
    private static void Step_6647(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x07);
        emulator.C = emulator.data;
    }

    // FDCB 0xF9: SET 0x07, (IY + d), C [8]
    private static void Step_6649(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xFA: SET 0x07, (IY + d), D [2]
    private static void Step_6652(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xFA: SET 0x07, (IY + d), D [6]
    private static void Step_6656(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x07);
        emulator.D = emulator.data;
    }

    // FDCB 0xFA: SET 0x07, (IY + d), D [8]
    private static void Step_6658(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xFB: SET 0x07, (IY + d), E [2]
    private static void Step_6661(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xFB: SET 0x07, (IY + d), E [6]
    private static void Step_6665(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x07);
        emulator.E = emulator.data;
    }

    // FDCB 0xFB: SET 0x07, (IY + d), E [8]
    private static void Step_6667(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xFC: SET 0x07, (IY + d), H [2]
    private static void Step_6670(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xFC: SET 0x07, (IY + d), H [6]
    private static void Step_6674(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x07);
        emulator.H = emulator.data;
    }

    // FDCB 0xFC: SET 0x07, (IY + d), H [8]
    private static void Step_6676(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xFD: SET 0x07, (IY + d), L [2]
    private static void Step_6679(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xFD: SET 0x07, (IY + d), L [6]
    private static void Step_6683(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x07);
        emulator.L = emulator.data;
    }

    // FDCB 0xFD: SET 0x07, (IY + d), L [8]
    private static void Step_6685(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xFE: SET 0x07, (IY + d) [2]
    private static void Step_6688(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xFE: SET 0x07, (IY + d) [6]
    private static void Step_6692(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x07);
    }

    // FDCB 0xFE: SET 0x07, (IY + d) [8]
    private static void Step_6694(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }

    // FDCB 0xFF: SET 0x07, (IY + d), A [2]
    private static void Step_6697(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.WZ = (ushort)(emulator.IY + (sbyte)emulator.latch);
        emulator.address = emulator.WZ;
    }

    // FDCB 0xFF: SET 0x07, (IY + d), A [6]
    private static void Step_6701(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.data = (byte)(emulator.data | 0x01 << 0x07);
        emulator.A = emulator.data;
    }

    // FDCB 0xFF: SET 0x07, (IY + d), A [8]
    private static void Step_6703(Z80Emulator emulator, ref ActionRequired actionRequired)
    {
        emulator.Q = 0x00;
        emulator.instructionComplete = true;
        if (HandleInterrupts(emulator, ref actionRequired))
        {
            return;
        }

        emulator.currentStep = 0;
    }
}
